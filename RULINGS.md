# Founder rulings — the FR and FD series registry

> ⚠️ **This is an index, not law.** It is **not the normative home** of anything. Per FR13 each rule has exactly one home; this file only says which. Reading a ruling's consequences means opening the file in the *Landed at* column. Where this index and a package `SPEC.md` disagree, **the package spec governs** and this file is the thing that is stale.
>
> *It sat in `archive/` until 2026-08-08 and moved here because it never belonged there: `archive/` is history, so resolving a `FR#` meant first reasoning about whether the hit counted. This registry is current and actively edited. Its citations were always repo-root-relative, so they resolve from here unchanged.*

*Every FR and FD ruling, and where each one landed in the corpus. This file exists because six packages cite `FR#` labels whose registry lived only outside the repo — a citation that resolves to nothing is worse than no citation, because it reads as verified.*

---

## The numbering, stated plainly (it has a history, and the history bites)

Two series were in play the same day and they were briefly both called `R#`:

- **`R#` = review findings** — from `annnä-commitment-review-2026-08-06.md`, external to this repo. Commit `d07e571` uses this sense. So do `deployment/SCENARIOS.md` (R1–R11) and `security/SPEC.md` (R1–R3), whose R-labels are **scenario IDs and unrelated to any ruling**.
- **`FR#` = founder rulings** — this file. Renamed from `R#` to `FR#` precisely to end that collision.

**`FR-A` / `FR-B` / `FR-C` are lettered, and that is not a typo.** They were made *after* FR1–FR13, to close decisions those had left open. They are kept lettered rather than renumbered because the corpus already cites them by letter and silently renumbering a live citation is the failure mode this whole file exists to prevent.

**`FR-B` and `FR9` are the same ruling.** FR9 was the first statement (*"the rule is removed; I don't know when or how yet"*); FR-B is the final one, with the execution consequences attached. **The corpus cites `FR-B`.** No file cites `FR9` as a live label. Outside this paragraph the label appears in exactly two places, and both exist only to say it is `FR-B`: the registry row below, and `AGENTS.md`'s ruling vocabulary. Do not add a third.

---

## FR1–FR13

| # | Ruling | Landed at |
|---|---|---|
| **FR1** | v1 solves the Situations, with the **cross-tenant referral beat deferred** | `README.md` (v1 caveat) · `security/SPEC.md §9` |
| **FR2** | **Documentation states readiness, never schedule.** A checklist of conditions is allowed; "start after Phase 3" is not | corpus-wide discipline; no single home |
| **FR3** | Import is **owner-requested and repeatable**; the app prompts exactly once, first session | `app/NOTES.md` — **amended by FR12** |
| **FR4** | Escalation: **in-app always + email default-on**, per-user setting to disable email | `harness/SPEC.md §3.9` |
| **FR5** | **BYO API keys: yes.** The model package wins; the app's ban is struck | `model/SPEC.md §7` · custody at `security/SPEC.md §3.1` (member 2) |
| **FR6** | Auto-accept is legal when the owner set it up; recorded as a **Grant** | `harness/SPEC.md §7` |
| **FR7** | **Convex is ratified** as the serving substrate — after being checked against the criteria, not over them | `engine/BUILD.md` Step 0 |
| **FR8** | **Atomic read-check-write** transactions are a hard engine Step-0 criterion | `engine/INTERFACES.md §2.2` (row 1) |
| **FR9** | *See **FR-B*** — same ruling, final form | — |
| **FR10** | **v1 = Situations A, B, C, D. E stays held-out** | `user-stories/README.md` |
| **FR11** | **The engine handshake is v1 work.** Freelancers stay standalone accounts | `engine/SPEC.md §7.1` |
| **FR12** | **Import: connect the account once, then user-triggered "Sync now."** Never a background poll | `app/SPEC.md §9` · credential at `security/SPEC.md §3.1` (member 1) |
| **FR13** | **Propagation discipline:** one normative home per rule; every restatement cites it | this file's operating rule |

**Also ruled 2026-08-06, and easy to lose because it has no number:** *reactive push to subscribers* is a hard engine Step-0 criterion alongside FR8's atomicity — a non-reactive store must not be pickable by accident. It is row 5 of `engine/INTERFACES.md §2.2`. It travelled with FR8 and landed a step later than it should have; recorded here so the pair stays a pair.

## FR-A / FR-B / FR-C

| # | Ruling | Landed at |
|---|---|---|
| **FR-A** | **Preview is not a step.** The proposal card *is* the preview. The elicitation law stays **propose → accept-or-narrow** | `harness/SPEC.md` (unchanged — it never had the step) · `marketplace/SPEC.md §3` (the Templates bullet — the home) |
| **FR-B** | **The console-silent rule is removed. Entirely.** Not narrowed, not rewritten | `app/SPEC.md §3` |
| **FR-C** | ~~**The ChatGPT-subscription slot is v1**~~ — **cut 2026-08-22 (FD-65), which executes FR-C's own accepted risk below**. Kept in the series rather than renumbered; the letter is cited elsewhere | `model/SPEC.md §7` · `model/BUILD.md` Step 5 |

**FR-B's residue, recorded because absences are invisible.** Removing a prohibition is not granting a permission. `app/SPEC.md §3` therefore states that agent-initiated speech is **ungoverned pending design**, rather than leaving a hole where a rule used to be — a deleted prohibition and a granted permission look identical in a diff and are opposite things. Scenario **O1 was deleted** from `app/SCENARIOS.md` and struck from `app/BUILD.md` Step 2's gate list; the app O-family therefore starts at O2, and **O1 must not be recycled for a new scenario** — a future reader comparing against an older revision would silently get the wrong test.

**FR-C's accepted risk:** the slot is contingent on OpenAI's program terms at build time, so v1 contains one requirement a third party can revoke. `model/BUILD.md`'s degrade-to-app-supplied verification is what keeps that survivable. Recorded deliberately, not overlooked. *(Annotation 2026-08-22: the risk fired — the vendor programme went identity-only upstream and FD-65 cut the slot. The paragraph stands as the record that this outcome was priced in.)*

## FR14–FR18 — the second sitting, 2026-08-06

Ruled after a combined audit pass. Numbered continuing FR1–FR13; the lettered series is closed and not extended.

| # | Ruling | Landed at |
|---|---|---|
| **FR14** | **Entitlement revocation:** a withdrawn entitlement takes effect at the **next entitlement check** (activation, app open) — skin deactivates, fave slot clears, appearance falls back to the shipped floor. One rule, no special cases; the device copy is never deleted | `marketplace/SPEC.md §4` · scenarios `marketplace/SCENARIOS.md` E5–E6 |
| **FR15** | **Guest-facing language is the owner's setting, asked once at setup and stored.** No product default and no fallback list | `app/SPEC.md §5` |
| **FR16** | **`form_ref` is stored on the Commitment**, pinned at creation — the riser's anchor is resolved, never re-derived | `harness/SPEC.md §3.4` |
| **FR17** | **Publication posture:** private and personal material is removed from the tracked tree and the published history rewritten — **amended by FR21**, which bounds "rewritten" to what a force-push can actually achieve | this repo's `.gitignore` + the redactions of 2026-08-06 |
| **FR18** | **Commercial silence is repo-wide, not file-scoped.** Store terms live inside the closed service and are specified nowhere here | `marketplace/SPEC.md §5` |

**Why FR15 has no default, recorded because a future reader will want to add one.** The question "what language does a guest form render in?" has a right answer that differs between one owner and the next. Any default annnä shipped would be wrong for roughly half of all owners, so the design answer is that **the system asks its user once and stores the answer** — not that the founder picks. This is the general rule the corpus follows wherever a policy fork is really a user's fork, and FR15 is its clearest instance. *What FR15 does not reach:* annnä's own catalog surfaces (marketplace category names, bundle `domain` naming) have no owner to ask. **FR32 closed them a day later** — the catalog renders in the *viewing* owner's stored language, the same setting reused, English fallback behind it (`marketplace/SPEC.md §6`).

**FR14's non-collision, stated because it looks like one.** `marketplace/SPEC.md §3` says unpublish "removes the listing, never the entitlement or the copy." That is a *catalog* event. FR14 governs an *entitlement* event. Under FR14 the device copy still persists; the entitlement stops authorizing activation. Offline, no check can complete, so the last-known entitlement stands and nothing is yanked — which is what keeps the outage laws (`app/SCENARIOS.md` S4/S6) true.

**FR18's cost, accepted.** A builder eventually needs the store's terms and will not find them here. That is the intended state: this repo is the open half, and the terms belong to the closed service.

---

## FR19–FR22 — the third sitting, 2026-08-07

Ruled against a third audit of the corpus. Same series, continuing FR18.

| # | Ruling | Landed at |
|---|---|---|
| **FR19** | **The off-repo citations stay, and no `research/` directory is ever created.** Citations of the form `research/<name>.md:line` point into a tree on the founder's machine. They are honest provenance, every law they reach is stated in full in-repo, and a reader who greps for `research/` finds the disclaimer rather than a broken link. Do not delete the citations and do not satisfy them by creating the directory | disclaimer in `app/DESIGN.md`'s provenance note; the two `research/` citations in the same file |
| **FR20** | **Real Phuket operator names stay as written.** They are public businesses named as market-landscape context, never as actors, under the fiction notice that already governs the folder. Anonymizing them would destroy the finding — that the market's fragmentation *by tourist-language* is real, specific, and checkable | the fiction notice heading `user-stories/Situations/Situation-C/README.md` |
| **FR21** | **The published repo is never deleted or recreated.** The pre-redaction commit `26445df` is unreachable but remains addressable by SHA until GitHub garbage-collects it; that residue is accepted rather than chased. No `filter-repo`, no orphan-branch rebuild, no recreation — none of them reach a commit the host already holds, and the question is closed regardless of method | this file; **amends FR17** |
| **FR22** | **A defect is scoped to what verification supports, not to what an audit asserts.** Every remediation item carries a `file:line` checked in the working tree at the time of writing. Three of the third audit's tasks shrank on inspection, one was inverted, and five were dead on arrival | method; no single home |

**FR21 amends FR17, and the amendment is the interesting half.** FR17 committed to *"the published history rewritten."* It was — the tracked tree was redacted and force-pushed — but a force-push does not unpublish what a host has already stored. FR21 draws the line at what is actually achievable: **the tree is clean, the history is one commit, and the orphaned old commit is left alone.** The lesson generalizes past this repo: *the moment to keep private material out of a public repo is before the first push, because nothing after it is fully reversible.*

**Why FR19 reads as a non-answer and isn't.** Deleting the citations would make the corpus look self-contained while removing the reader's only means of checking where a law came from. Creating the directory would mean inventing files to satisfy a pointer. Keeping both the pointer and the disclaimer is the only option that stays true — it says plainly *this came from somewhere you can't see, and you don't need to.*

---

## FR23–FR25 — the fourth sitting, 2026-08-07

Ruled against the DR/addressing gap review. Same series, continuing FR22.

| # | Ruling | Landed at |
|---|---|---|
| **FR23** | **Restore is the owner's act.** The console detects a shortfall against the backup watermark, offers **one card** naming what is missing and when the last good copy was taken, and **writes nothing before the owner confirms**. Not an agent tool — no harness tool gains a restore verb, the same posture as *"the agent never shreds"* — and not a support ticket: annnä does not decide for an owner when their own board should be rolled forward. A restore is an appended, attributed write; it never rewinds, never un-latches, and never crosses a tenant | `security/SPEC.md §8` (the owner-scoped restore bullet); gated at **D5**; restated as a card in `app/SPEC.md §3` |
| **FR24** | **The backup cadence and the loss/recovery bounds are internal.** They are engineering targets, not a published promise: `README.md`, `security/README.md` and `PR/*` state no recovery commitment, and the numbers live in exactly one place. An unmet public RPO/RTO is a broken promise; an unmet internal target is a bug — the difference is worth keeping | `security/SPEC.md §8` (the cadence bullet, which says so in its own text) |
| **FR25** | **Backup retention `N ∈ [14, 90]`, default 30.** The floor exists because N is the **restore depth**, not only the erasure horizon — shortening N shortens recovery by exactly as much, and a build that sets N = 7 satisfies every other word of §8 while quietly making recovery worse. Carried to the Step 8 legal review as a **confirm item, not a blocker**: if the reviewer wants a shorter erasure horizon, the floor moves and nothing else does | `security/SPEC.md §8` (the erasure bullet, which now states both bounds) |

**Why FR23 overturns the obvious answer.** Ops-triggered restore was the safer-looking default and it was wrong: the owner is the only party who knows whether the missing data *should* be there. What makes handing them the button safe is not a permission check but a structural one — **a restore never un-latches.** Latched acts are forward-only, so a restore can return structure that was lost but can never contradict a promise a guest already holds. If any future edit weakens the un-latch prohibition, this ruling has to go back to ops.

**What the watermark does not do, recorded so it is not assumed.** It sees **absence, not wrongness**. Same-volume corruption passes it silently, and so does loss below the threshold. The agent itself never detects loss and never could — nothing in annnä deletes, so it never observes a deletion; a lossy store simply returns less. Detection is a property of the backup, not of the conversation. A declared partial detector is worth more than an over-trusted one.

---

## FR26–FR36 — the fifth sitting, 2026-08-07 (the wayfinder map)

Ruled through the wayfinder decision map (GitHub issues #1–#12 on this repo; each ruling's full record is its ticket's resolution comment). Same series, continuing FR25.

| # | Ruling | Where it lives |
|---|---|---|
| **FR26** | **One commitment, always — the bind handshake redrawn, not ratified.** There is exactly one commitment object regardless of how many people are involved; **whoever creates it owns it, and ownership does not transfer** (at this time). It reaches other boards and eyes only through engine-minted **grants**. The drafted paired-commitments pair-write is dead; Google Calendar's one-event-with-guests is the stated precedent, the incumbent two-records-reconciled shape explicitly rejected | `engine/SPEC.md §7.1` (issue #2) |
| **FR27** | **The hold is the creator's setting.** The share form carries a hold-duration field: **default 5 minutes, range 0–24 hours**, 0 = no reservation. While offered, the targeted window is held for that duration, then freed | `engine/SPEC.md §7.1` (issue #2) |
| **FR28** | **Three accept modes, user-chosen:** auto-accept what fits the schedule · auto-accept per person · always manual. The first two are ordinary stored Grants; silence never books anyone | `engine/SPEC.md §7.1`, `harness/SPEC.md §7` (issue #2) |
| **FR29** | **One machine.** The same code path serves own-board and cross-owner bookings — a cross-owner booking is an ordinary booking needing one more yes; no dedicated cross-tenant entry point | `engine/SPEC.md §7.1` (issue #2) |
| **FR30** | **The grant vocabulary:** three rungs (availability — the default exposure · details · edit); a grant attaches to one commitment or one board and is held by a person or a link token; no group grants, no manage-sharing rung; edit is per-grant direct-or-approval, defaulted direct | `engine/SPEC.md §7.1` (issue #12) |
| **FR31** | **BYO attended-only, ratified.** The 2026-08-06 drafted confinement stands as written | `model/SPEC.md §7` (issue #3) |
| **FR32** | **The catalog surface renders in the viewing owner's stored language**, English fallback where untranslated | `marketplace/SPEC.md §6` (issue #4) |
| **FR33** | **No-show is always a human mark;** the policy is a creator-set per-commitment field binding at booking under §1.5 | `engine/SPEC.md §1.9` (issue #5) |
| **FR34** | **Reachable = responded within the offer's hold window;** ranked walk one candidate at a time; exhaustion parks to the owner. A decline always carries a structured reason; the re-offer is the only counter | `harness/SPEC.md §3.4` clocked-offer law, `engine/SPEC.md §7.1` (issues #7, #8) |
| **FR35** | **A precondition may require a recorded human conversation; its violation parks as a human decision** — documents alone never satisfy it. Wiring a domain flag (e.g. dive medical) to it is template configuration, not platform law | `engine/SPEC.md §3` precondition row (issue #9) |
| **FR36** | **Calendar import v1:** Google, Microsoft, iCloud (app-specific password), ICS links; the connect flow is the console conversation; copy at the industry minimum, no extra disclosure — **amended by FD-37** (the iCloud/CalDAV route is dropped; iCloud owners import via ICS) | `app/SPEC.md §9` (issues #10, #11) |

**Also answered in the same sitting, recorded on their tickets rather than numbered:** the store-probe domain brief (issue #6 — agent-suggests discovery, Sofia templates-only, empty-account install) — story inputs, not spec law.

## FR37–FR38 — same day, after the map

| # | Ruling | Where it lives |
|---|---|---|
| **FR37** | **Domain content is user-built proof, not founder-ruled spec.** User stories and their domain designs — F7's courses and F20's boat minimum among them — are validated by the founder building them *as a user* inside the app, proving any user could build them; no founder ruling is owed on them, ever. The platform owes only the enabling mechanisms (template expansion `engine/SPEC.md §1.12`, clock-triggered rules `engine/SPEC.md §3`) | `engine/SPEC.md §1.12` and `§3` stamps |
| **FR38** | **We build the tools, never the templates — the law is authorship, not shipping.** The product is the application — engine, harness, app, marketplace. **First-priority acceptance: a user can author a template through their agent** and share it to the marketplace *(annotation 2026-08-22: the home, `marketplace/SPEC.md §2`, defines the acceptance as authoring — "share" reads through the admin-only publish law this same entry cites, never around it)* *(annotation 2026-08-24, FD-82: authorship stands — developers never author domain templates. "Share" now reads through owner-publish at the home, not admin-only. Production defaults in the binary still founder-built through the app. Admin-only *publish* is amended at `marketplace/SPEC.md` §0/§2; this row is not a second home of supply law)*. Every template named in the user stories and READMEs **may ship as a production default for all users** — but only after the founder, as administrator, has built it *through the application*, because each one is the proof that any user could. Claude/developers never author a domain template; the founder's prove → save → ship path is how a *shipped default* is proven, not how catalog supply is gated (`marketplace/SPEC.md §0`/`§2`, FD-82). Development carve-out: domain content may exist during development **only as declared throwaway** — named as scaffold before it is built, stripped before ship, rebuilt through the application as the proof. Undeclared domain scaffold is a violation | this registry entry; binds `marketplace/SPEC.md §2` (authoring-and-publish) + §0 (owner-publish, FD-82), `engine/SCENARIOS.md` W5 (expansion-equivalence) → `marketplace/SCENARIOS.md` I6 (authoring-equivalence), user-stories/README framing |

## FD1–FD3 — the harness-phase sitting, 2026-08-07 (the `FD#` series)

**A second series, and the reason it is separate.** `FR#` numbers the rulings made in the sittings above, against audits and the wayfinder map. `FD#` numbers rulings made *inside a build phase*, where a design question surfaced from the work rather than from a review. The two series are independent and neither renumbers the other; a citation must name which series it means. The corpus already cites **FD-2** by label in four places, which is what made the absence of a registry entry a broken citation rather than a missing one.

| # | Ruling | Where it lives |
|---|---|---|
| **FD-1** | **Save-as-bundle is a read-only shape projection, not a new seam verb.** A T2 authoring session projects into a `marketplace/SPEC.md §1.2` bundle through an internal `calculate`-class read with operands blanked; `draft` rules project too, and the same session projects an identical bundle every time. **The accepted residual:** a field's *name* is part of the shape the user authors, and no mechanical fence catches "John's medication list" — field names are a **publisher-review** item, valid exactly as long as the admin-only supply law stands *(annotation 2026-08-24, FD-82: that fence was the admin-only publish review. Templates are now owner-publish; the residual stands as the publisher's, with no mechanical fence. Future gates (OR-28) may review names; they are not this sitting)* | `engine/SPEC.md §1.7a` · gated at `harness/SCENARIOS.md` G8 |
| **FD-2** | **Zero trust on stranger text — "assume everything is a prompt injection."** Guest-returned free text and uploaded SOP documents are read first by a **quarantined, tool-less model** returning only a structured summary; the privileged tool-bearing model never sees the raw text. Structural, not label-obedience; spotlighting stays underneath as defense-in-depth | `security/SPEC.md §5` (the law) · seam contract `harness/INTERFACES.md §2.4` · gated at `harness/SCENARIOS.md` L5, L7 |
| **FD-3** | **`summarize` is never BYO** — not even attended. Stricter than FR31's attended-only confinement, and deliberately so: the attended confinement exists because an unqualified model degrades *pleasantness* while a human watches, and the quarantine read is a **security control** whose failure mode is a breach. **A control the owner can weaken is not a control**, and a human present cannot see an instruction that was laundered into a summary | `model/SPEC.md §7` (the reasoning and its stated cost) · enforced unconstructable at config load, `model/INTERFACES.md §2.2`, `model/BUILD.md` Step 4 · seam restatement `harness/INTERFACES.md §2.4` |

**Why FD-3 is registered separately from FD-2 rather than read off it.** FD-2 creates the `summarize` call; FD-3 decides who may supply the model behind it. They could have been one ruling and are not, because FR5/FR31 grant BYO broadly and a reader reconciling those against FD-2 alone would find no rule either way — the strictest confinement in the model layer would exist only as an unexplained parenthetical. The cost is stated where the reasoning lives: an owner who wants their own model everywhere cannot have it on one call type.

## FD-4–FD-6 — the deployment sitting, 2026-08-08 (the `FD#` series)

*Same series, same rules as FD-1–FD-3 above. These three were made inside the deployment phase and were cited across the corpus for two days under a letter form (`FD-a/b/c`) that this registry never carried; the letters are deleted and the citations renumbered here. **FD-5 had no repo footprint at all** before this entry.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-4** | **Deployment re-scoped 33→20 scenarios for a solo operator** | `deployment/SCENARIOS.md` (the twenty that survive) · the record of what died and why: `deployment/NOTES.md` · `deployment/SPEC.md §8` (DR-4, moot by it) |
| **FD-5** | **Model spike built but deliberately unrun** | `deployment/SPEC.md §8` (DR-7) · `model/spike/README.md` |
| **FD-6** | ~~**Push refused; the public repo stays stale on purpose**~~ — **reversed 2026-08-08 (FD-10)**. Kept in the series rather than renumbered; FD numbers are cited elsewhere | `deployment/SPEC.md §4` · FD-10 below |

## FD-7–FD-9 — the preflight sitting, 2026-08-08 (the `FD#` series)

*Same series, same rules. These three were the founder decisions standing between the corpus and `harness/BUILD.md` Step 0; they were put to him as three questions and answered in one sitting.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-7** | **A pre-landing hook, bypassable with `--no-verify`, is an acceptable gate — and the bypass is the accepted ceiling.** `.githooks/pre-commit` fires the aggregator before every commit; `--no-verify` defeats it and nothing prevents that. The ceiling is recorded rather than closed, because the hook is the only mechanism that can *refuse* a landing — the CI job (live since FD-10) reports after the fact. **Consequence:** B1 returns to `[MUST]` — `deployment/SCENARIOS.md` B1 pre-authorized exactly this form and named this as the condition | `deployment/SPEC.md §4` (the ceiling) · `§9` (ledger) · `deployment/SCENARIOS.md` B1 |
| **FD-8** | **Injection attack strings belong in the repo, under `harness/tests/fixtures/injection/`, with a provenance README** — created at `harness/BUILD.md` Step 5, the step that first needs them, and committed there rather than fetched. They are public-benchmark-sourced, so committing them publishes nothing that was not already public; the clause being satisfied was never "no attack strings in this repo" but "annnä does not *author* attack strings inside a public design document", which a provenance file answers. Fetch-at-test-time is disqualified by S3 (hermetic) and breaks replay determinism (B9). **They are permanent test data, not throwaway** — `security/SPEC.md §5` already said so and the restatements contradicting it were the error | `security/SPEC.md §5` (the law and the location) · restated at `harness/SCENARIOS.md` L5, `harness/BUILD.md` Step 5 |
| **FD-9** | **The `engine/`-is-owned-by-another-session rule is dropped.** It was a working practice, never spec law, and the handoff file that carried its open items no longer exists — leaving three known contradictions owned by nobody. Any session may edit `engine/`; §5's one-session-per-layer rule is the real constraint and it is unchanged | `deployment/SPEC.md §5` (the surviving constraint) · closed items: `engine/SPEC.md §1.9`, `§1.13`, `engine/SCENARIOS.md` K3 |

## FD-10 — the Alignment push, 2026-08-08 (the `FD#` series)

*Same series, same rules. One ruling, made at the squash that ends the deployment-and-harness phase.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-10** | **Push resumed; FD-6 is reversed.** The public repo stops being deliberately stale, and `.github/workflows/check.yml` (`on: push`) becomes a real event rather than a declaration. **Two consequences, both paid before the push rather than discovered after it:** the workflow installs every layer with a suite, not just the engine's — a missing install reads as a red gate rather than as a missing install; and `doc-count-check.mjs` **stops asserting a working-tree count**, because that walks the filesystem and counts whatever gitignored material the machine happens to hold — true on one laptop, false in every clone and runner. The tracked count is stable everywhere and remains asserted; the `AGENTS.md` word ceiling moved to its own gate (`deployment/scripts/agents-budget.mjs`) the same day. **The reasoning FD-6 supported is not deleted but re-based:** B1's mechanism is still the pre-commit hook, because CI reports *after* a landing and only the hook can refuse one | `deployment/SPEC.md §4` (B1's mechanism) · `deployment/SCENARIOS.md` B1 · `deployment/README.md`, "The one green command" · `.github/workflows/check.yml` |

## FD-11 — the app substrate, 2026-08-08 (the `FD#` series)

*Same series, same rules. One ruling: the app layer's framework and host.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-11** | **The app substrate is TanStack Start + Cloudflare, replacing Next.js + Vercel.** Vercel was never chosen — `.specs/deep-interview-app.md` recorded it as *"Assumed (BUILD-only, flagged not asked)"*, inherited as the default pairing for Next.js + Convex — so what this ruling replaces on the hosting side is an assumption, not a decision. **The check is owed and is not waived:** the printed five-criterion table is at `app/BUILD.md` Step 0, on the Convex precedent (FR7, `engine/BUILD.md` Step 0), and **if a criterion fails when it is first built against, this ruling reopens.** Until that table exists with a result in it, no document may describe the app substrate as *ratified* — it is ruled, which is a different word. **Three things this ruling does not touch, and they are why it is cheap:** no `SPEC.md` in any layer names a framework or a host, so no law moved; the Astryx pin survives (`@astryxdesign/core@0.3.0` peers on React ≥19 and `@stylexjs/stylex ^0.19.0`, and StyleX supports Vite); and the app holds no logic, so the stub-swap gate is unchanged. **Two gates moved with it:** `r9-noindex-nodebug.mjs`'s rung-config filenames became Wrangler's, and `r6-deploy-secret-scoping.mjs` gained `CLOUDFLARE` — it matched no Cloudflare token before, which would have left the gate green while catching nothing | `app/INTERFACES.md` (the substrate rows — the home) · `app/BUILD.md` Step 0 (the printed check) · `deployment/INTERFACES.md`, `deployment/BUILD.md` Step 2 (the rungs) · `deployment/substrate-swap.md` (the inventory and the research map) |

## FD-12 to FD-15 — the consistency close-out, 2026-08-08 (the `FD#` series)

*Same series, same rules. Ruled after two contradiction sweeps found ten confirmed findings in one day — the fourth hand-fixing pass on the same class, five of which had survived the previous sweep unchanged and one of which had spread to two more files in between. The founder's framing: the deliverable is not the fixes, it is that these classes do not come back.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-12** | **A spam complaint and a bounce run are two stops, not one — different scope, different threshold, different names.** They had been one blended rule (*"5% bounce-or-complaint rate"*), and the two layers restating it had already diverged: security averaged complaints into a per-owner rate while the harness suppressed permanently on the first one, and the harness cited security for a per-complaint threshold security never stated. **Channel suppression** is per **party**, trips on **one** complaint, no rate and no averaging — a complaint is a person saying *stop writing to me*, and continuing to write while it averages out is the failure the rule exists to prevent. **The send halt** is per **owner**, trips on a bounce rate, and is about addresses going stale. Both thresholds are security's; both readings are the harness's. The app gained a fourth delivery event, `complaint` — it had no way to record one, while two other files said it did | `security/SPEC.md §10` ("Two stops" — both thresholds) · `harness/SPEC.md §3.11` (the per-party reading) · `harness/SCENARIOS.md` D22–D23 · `security/SCENARIOS.md` R2 (the per-owner halt) · `app/SPEC.md §6` (the event) · `app/SCENARIOS.md` D3 |
| **FD-13** | **Write the scenario rather than record the debt.** `app/SPEC.md §5` said the no-build-time-snapshot rule was *"asserted at the guest-page gate"*; the G-family stopped at G8 and asserted no such thing, and two further files had since repeated the claim — including FD-11's substrate criterion 4, which asks that *"the guard is asserted, not inherited from a default"* and would therefore have failed on its own terms. **G9 now exists**, and the three sites are true as written | `app/SCENARIOS.md` G9 · `app/BUILD.md` Step 5 (its gate) and Step 0 criterion 4 |
| **FD-14** | **DR-5 closes: the engine scaffold is retro-covered, not waiting.** Its open question was whether the scaffold waits for deployment Steps 0–3 to close or is covered when the gates land. Steps 0, 1 and 3 are closed and every gate they built judges every engine commit from here — which is what the question was about. Step 2 cannot close: it waits on hosting accounts no code can create, and blocking a layer on those is precisely what DR-8 refused for the harness. Same reasoning, same answer. The deviation's record stays | `deployment/SPEC.md §8` DR-5 |
| **FD-15** | **Three gates, and one convention deleted; the fourth gate refused.** Of ten findings, six were the corpus mis-describing itself. Built: `ignore-claim.mjs` (a path called gitignored holds zero tracked files), `cite-form.mjs` (**no in-repo citation may name a line number** — quote the phrase, because a line number rots on the next insertion above it and nothing notices), `roster-check.mjs` (set equality between the gate roster and the folder — a count and a roster never compared to each other were both green while a real gate went undocumented). **Refused:** a gate over *"asserted at the X-family"* claims. That class — prose asserting a test that does not exist — stays caught only by sweeps, and it is the one the founder was told is most dangerous | `deployment/scripts/`, the three scripts · `package.json` check chain · `AGENTS.md` §Citation conventions (the line-number rule's home) · `deployment/README.md` §The one green command |

## FD-16 — why placement lives in the engine, 2026-08-08 (the `FD#` series)

*Same series, same rules. Ruled after the concept-level technical research found that the corpus's most load-bearing architectural decision was nowhere justified in writing — the split was described everywhere and argued for nowhere.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-16** | **The four-layer split is a reliability argument, and it is written down as one.** Agent success rates compound over steps: 95% per step is ~60% over ten steps and ~28% over twenty-five. Any job the size of a placed week is that many steps, so **placement in the model is a coin flip and placement in the engine is deterministic** — that, not tidiness about layers, is why the engine holds the math. **What binds: no placement, availability, capacity, quota, buffer, travel or ordering computation may move into the harness or the model, for any case, however small.** The model supplies inputs — intent, parameters, ranked preferences — and the engine decides. **Why this needed a ruling rather than staying a convention:** the corpus stated the boundary in a dozen places and gave the reason in none, so the boundary was defensible only by habit. A future contributor meeting a small case where the model "could just work it out" would find no argument in the way, and the exception always looks cheap at the moment it is proposed. The argument is now in the way | `engine/SPEC.md §0` (the home — the determinism bullet) · this registry entry. **Provenance, outside this repo** (the `research/<name>.md` convention at `AGENTS.md` §Citation conventions): the concept-feasibility research at `~/Desktop/RiskNeutral/Vaults/annnä/research/technical-annnä-concept-feasibility-research-2026-08-08.md`, which established the curve and its sources. It is a dated snapshot, holds no authority, and **must never be cited for law** — the ruling above is stated in full in-repo and loses nothing if that path does not resolve |

## FD-17 — the fifth capability-token class, 2026-08-08 (the `FD#` series)

*Same series, same rules. `PRD.md` RQ-1 was adopted with the credential filed as a fifth class, but `security/SPEC.md §3`'s table is **"extended only by ruling"** and `RQ-##` is a requirement series, not a ruling series (`AGENTS.md` §Rulings). The requirement could state the class; only a ruling could open the table. This is that ruling.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-17** | **The owner-issued external-client credential is a fifth capability-token class, and the table opens by this ruling to admit it.** An owner authorizes an agent acting for them to call the same tool contract the app calls, and withdraws it the same way. **It is not a §3.1 held credential, and the distinction is the whole reason §3.1 is printed separately:** annnä **mints** this one to let a caller *in*, where a held credential is a secret of the owner's at a third party that annnä **holds** to get *out*. The direction of trust is reversed, and so is the blast radius. **It adds no actor model** — `harness/SPEC.md §3.1` already separates the **owner** of a board from the **actor** on a turn, and this credential is issued by, and attributes to, whoever could have performed the act by hand, with the client identified alongside. **It is the only class attributing to a principal on the inside rather than to a counterparty**, and that is what it costs. **Every custody rule above §3's table binds it unchanged** — ≥128-bit minting, digest-only storage, constant-time miss, rate limits, the adversarial suite green before it goes live — and **attribution is never permission**: possession authorizes nothing the owner has not authorized (`PRD.md` RQ-7) | `security/SPEC.md §3` (the table row — the home) · `app/SPEC.md §9` (its "forbidden fifth row" caution, scoped to §3.1 held credentials, which is what it always meant) · `PRD.md` RQ-1 (the requirement this ruling admits) |

## FD-18 / FD-19 — the consolidation pass, 2026-08-08 (the `FD#` series)

*Same series, same rules. Two rulings made during a combined consolidation-and-contradiction pass. Both close a contradiction that a **later ruling opened in an earlier document** and nobody reconciled — FD-17 against `security/SPEC.md §2`, `PRD.md` RQ-2 against `app/SPEC.md §7`. Neither was catchable by any of the twenty-five gates, and that is the pattern: every gate here compares a document to a **countable fact**, and no gate compares a document to another document's meaning.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-18** | **Three credential models, not two — the `external-client` credential gets its own authorization entry point.** `security/SPEC.md §2` closed the models at *"owner session **or** guest token — never both, **never neither**"*, and FD-17 had admitted a fifth capability-token class three days earlier that necessarily reaches mutations (`PRD.md` RQ-2: *"every capability annnä exposes to an owner"*). A call bearing it was **neither**, so scenario **M5 would have failed the day the class was implemented** — and §2's own poka-yoke (*"hand-rolled per-mutation checks are a defect"*) forbade the obvious workaround. FD-17's hedge that *"every custody rule above §3's table binds it unchanged"* made this worse rather than better: §2 is above the table. **Why a peer and not a variant of the owner session**, which was the cheaper reading and was available: annnä **mints** this one to let a caller *in*, where a §3.1 held credential is a secret annnä **holds** to get *out* — the direction of trust is reversed and so is the blast radius (FD-17's own words), and a reversed blast radius earns an audited door of its own rather than a footnote inside a class whose other member cannot be issued to a third party. **The rule §2 actually enforces never was the count:** one path, one model. A third member does not weaken it; it stops the fifth class from being a path that accepts *none* | `security/SPEC.md §2` (the home) · `§13` ledger · `security/SCENARIOS.md` M5 (enumerates three) and **T9**, new — the fifth class's adversarial suite, which `§3` had asserted as *"the printed adversarial suite"* while the T-family stopped at the fourth class. Same defect class FD-13 closed by writing G9, and the class FD-15 recorded as the most dangerous |
| **FD-19** | **Appearance is out of scope for capability parity.** *(2026-08-08.)* `PRD.md` RQ-2 requires *"a mechanical check [that] fails the build when a capability has no tool"* and that *"no verb is reachable only from the console"*; `app/SPEC.md §7` makes appearance — active skin, boring stash, opacity, fave-four — *"the one app-owned write… **never carried on a seam call**"*, and §10 carries that as an invariant. Starring a skin is an owner capability falling in neither of RQ-2's two exclusions, so **the gate could only ever go red** — and a gate that cannot go green is worse than no gate, because a permanently-red check is one people learn to ignore and then ignore for real reasons too. **The exclusion rather than the seam**, because appearance is display-only state that never becomes engine truth: it is not a capability anything schedules against, an agent acting for the owner has no business setting their skin, and opening a seam for it would have cost `app/SPEC.md` an invariant and `marketplace/INTERFACES.md` its no-new-verbs constraint to buy a user nothing. **What this does not do:** it does not narrow RQ-2's principle. *"Nothing the app can do is out of reach of software acting for you"* still binds every verb that touches truth; SM-C1's satisfied-downward failure mode (`PRD.md §10.3`) is unaffected, since nothing is being removed from the app to make parity true | `PRD.md` RQ-2 Out of Scope (the home) · `app/SPEC.md §7`/`§10` unchanged, which is the point |

## FD-20–FD-23 — the UI/UX sitting, 2026-08-09 (the `FD#` series)

*Same series, same rules. Four rulings from a single design session run against a live comparison rig, one A/B at a time, over the four shipped skins. **Every one of them was decided by building it and looking at it**, not by argument — which is why each entry below names what was built and rejected as well as what stands. The session's working record is external and holds no authority: `_bmad-output/planning-artifacts/ux-designs/ux-annna-2026-08-08/`.*

*This sitting is the first to **overturn** design law rather than add it, and the overturned law came from a build outside this repo. FD-20 is what made the other three cheap.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-20** | **The prior build at `~/Desktop/annnä` is released as design law — it is inspiration, not authority.** Founder's words: *"forget everything that previously existed as a law from there. You are in control now, starting from scratch. Everything else is just inspiration."* `app/DESIGN.md` had described itself as *"distilled from the prior build's design system, which survives as **design law only**"*, so a rule's provenance was its warrant, and its class names came with it — which the founder discounted in the same breath (*"I don't even know if they're appropriate. I was making them up."*). **What the release did NOT touch, and the list is exhaustive:** rules ruled in *this* repo (the guest-page visual law, the accessibility baseline, the chrome-language posture, the appearance/skin model and the shipped four + Plain), anything another layer depends on (the fixed render catalog, display-projection, appearance-is-display-only), the design-system mechanics (the Astryx three-route split, the pin, the one-overlay rule), and `PR/BRAND.md`. **What it cost:** the `DESIGN:line` citations throughout `app/DESIGN.md` stop being warrants and become provenance. They were never resolvable from this repo anyway, which is exactly why the change is safe to make and was worth making | `app/DESIGN.md` header (the home) · the four sections re-ruled below, which carry no `DESIGN:line` citation deliberately |
| **FD-21** | **One glass at three densities — `panel`, `plate`, `block` — and the opaque *paper* console is dead.** The prior build ran two materials plus a rule that reading surfaces take *"solid card backs"*, and a rest state of *"absolute glass — no tint, no blur"*. Both were built and shown: the opaque console drew *"the right looks horrendous"* — it reads as a dialog box pasted onto the photograph — and absolute-glass-at-rest failed on its own terms, because a container with no body at rest is not findable and its contents read as a floating island of text with nothing underneath. **What replaced paper is simply more of the same glass**, which only works because the veil is derived per skin (FD-22's sibling finding, below). **The law that fell out and now binds everything:** *findable at rest, readable when engaged* — two requirements, only the second about reading — plus *a container may never be lighter than its contents*, and *engaging always makes a surface more frosted, never less* (the inverse was shipped once in the rig and read correctly as gratuitous). **Apple's Liquid Glass is settled in the same ruling:** the **material** is adopted, the **behaviour** is refused, and the reason is scale rather than inheritance — Apple puts it on a handful of controls, a board holds fifty blocks, and fifty continuously-alive lensing surfaces destroy the photograph they exist to reveal | `app/DESIGN.md` §Glass (the home) · §Console · §Appearance (the veil derivation) · §Accessibility baseline (AA moved to the engaged state, because a floor-value rest state cannot carry it) · `PR/BRAND.md` §Inherited hard law (the restatement) |
| **FD-22** | **The wake policy is the owner's, in four parts, and the default is 80 — closing `PRD.md` §11 Q1.** The PRD asked which *value*, and offered three shapes (a fixed window, a count of blocks, duration-aware). **The question was the wrong shape and the founder said so**: *"the solution is this is a user setting… the user gets the customizability to change this thing, along with that pin thing."* A value whose right answer plausibly differs between two owners is a setting, not a founder question — the corpus's own standing filter, applied to the corpus. **The four parts:** a **value** (0–100, default 80, fading fill and ink only — spine, urgency edge, border and micro-mark keep full strength), a **scope** (an open predicate over a block's own stored data), a per-commitment **`keep-awake`** mark, and a **keep-awake toggle**. **The scope being an *open predicate* is a deliberate departure** from this corpus's closed-menu discipline and it is not free: there is no finite scenario suite over scopes, and the island cannot be the same control as the console, so the island shows a distinct `custom` state. **`keep-awake` is what makes that survivable** — a coarse scope is fine when any one thing can be held out of it. **The name is load-bearing:** not *pinned* (the prior build's display flag, which collided with its engine's immovable `Placement.kind` — the collision class that forced the `R#`→`FR#` rename), and not *held* (already the fifth latched mark, D-B). **Why 80 and not a whisper:** the promise lives in **emptiness**, not transparency — a quiet default made a sparse week and a packed week look equally hazy, and a present default makes the difference legible *(Annotation 2026-08-22, FD-70: the scope's open predicate is cut to a closed four-shape menu for the first release — the departure this ruling took and priced is unwound by its own price argument; everything else here stands, and the predicate may return as a strict superset.)* | `app/DESIGN.md` §Board rendering, *The wake policy* (the home) · `PRD.md` §3 glossary, §4.3 RQ-10/RQ-11/RQ-12, §11 Q1 (closed), §12 (two assumptions discharged) · `app/SPEC.md` §2, §7 (the mark joins the display-only write set) |
| **FD-23** | **Tasks are blocks; the board has two gravities and it scrolls.** Three prior-build laws fell together: *"tasks are never grid blocks"* (they were urgency-coloured due chips with a separate **Plan riser**), *"blocks stack from the top"*, and *"the board never scrolls"*. Founder's ruling: *"tasks should also show up as a block, just like events… all the events will be shifted towards the top of the board in chronological order, and all the tasks will just be on the bottom"*, and then *"by default, the events will occupy 75% of the vertical space and the tasks 25% — both of them can scroll within those spaces."* **Events pack to the top of their own column in start order, ignoring clock position; tasks pack to the bottom, filling a line across the whole board before opening a second.** Scroll follows the fill law and is therefore **asymmetric** — the event band scrolls per column, the task band as one board-wide scroller moving whole lines. **`+N` overflow is struck with the chip**: internal scroll keeps *the grid never grows*, which was the actual law. **No time axis survives untouched** — and it is the reason scrolling costs nothing: scroll moves a block on screen without moving it in time, so it is not a time gesture and `app/NOTES.md`'s open finding (no direct-manipulation path for time) is exactly as open as it was. **Behaviorally nothing changed** — a task was always a commitment with different axes set, and only the *rendering* ever split them *(**amended 2026-08-24, FD-81:** “no time axis” is the packed **default**, not the only mode. Two gravities and scroll still stand for that default. Hour-grid is a second style; drag-to-move is of that mode only.)* | `app/DESIGN.md` §Board rendering (the home) · `app/SPEC.md` §1, §2 · `app/BUILD.md` Steps 1 and 3 (the Plan riser struck from the riser step) · `app/NOTES.md` (the quote refreshed, the finding unchanged) |

**Drafted in the same sitting and NOT founder-ratified** — real design calls with no ruling label, recorded here because the sitting's own provenance rule is the one at the bottom of this file: the **veil derivation** (a continuous function of a pack's `luminance` plus 18% of its `DarkMuted`, replacing the two-value `suggestedMode` switch — the *finding* behind it is measured and hard, the *formula* is mine) · the **halo law** (a halo is always the opposite of its own ink) · the **75/25 split being one division across all seven columns** rather than per-column · **the lens as a Chromium-only bonus** with no second material designed for Safari and Firefox · **`prefers-reduced-transparency` and `prefers-contrast: more` pinning surfaces to their engaged state** rather than flattening them to opaque. Each is marked at its site. Treat the reasoning as a proposal and the presence in the spec as provisional.

**Closed the same day — delegated, then founder-corrected.** Corner radius was first delegated (*"I'll go with your recommendation… a detail I don't care about"*) and answered as a size-proportional ratio; the founder then directed *"just follow whatever Apple does"*, and **Apple does not do a ratio.** It does **concentricity** (`inner = outer − inset` — a nested corner shares its parent's centre, shipped as `ConcentricRectangle`), **capsules for controls and rounded rectangles for surfaces**, and **continuous rather than circular curvature**. Adopted as stated. **It reverses two of the ratio's numbers** — a block 6px → **8px**, a plate 7px → **4px** — because Apple's inner corners run tighter relative to their containers than any size-proportional rule produces, and that tightness is much of why nested Apple surfaces read as one object. **The original 4-vs-14 question dissolves:** a block inset 8px inside a 16px panel has an 8px corner by construction, so the container had already decided it. **Build cost, and it is the second time this design has landed here:** continuous corners need CSS `corner-shape` (Chromium first) — verify other engines at build time, fall back to circular at the same values, and the concentricity survives regardless. Same posture as the lens: Chromium gets the finish, everyone gets the design. Home: `app/DESIGN.md` §Shapes.

**Also closed the same day, and it turned up a live defect:** the **colour** phase. The urgency triad (`#b5ead7` calm / `#ffdca8` attention / `#ff8a75` alarm) was real, executable and gating every pack build inside `assets/make-pack.mjs`, and **named in no specification** — so it now has a home at `app/DESIGN.md` §Colour, along with the accent derivation and the previously-unstated fact that **two of the four shipped packs carry `accent: null` and fall back to koi's orange**. **The defect:** the triad is a fixed constant, and measured against each skin's veil at the 3.0:1 non-text floor it scores **1.34 / 1.31 / 2.30 on koi** — all three fail, and koi is the default skin. Three dark skins passed, one light skin failed, and nobody checked the one that failed. **Same shape as the veil defect FD-21 fixed:** a value fixed globally when it should have been solved against the thing next to it. Ruled: hue and saturation hold, **lightness is derived per skin** until the mark clears its own ground — which leaves the three dark skins untouched. *Drafted, not founder-ratified.* **The semantic set is closed at one colour** (`bad`), not four: failures are stated in words on a card by existing law, and a second three-colour scale would collide with urgency on every skin.

**Typography landed the same day, minus the face itself.** `PR/BRAND.md` reserves the concrete face for the landing mockup, so that reservation was honoured and the *product's* half was specified instead: the ramp (9px micro / 9px label / 13px body / **16px guest-body floor**), whole-product tabular figures, and self-hosted subset delivery with `optional` inside the app and metric-matched `swap` outward. **Two criteria were added to BRAND.md's list, both derived rather than chosen:** the face must be a **superfamily with metric-matched Thai and CJK siblings**, because *chrome shipping in English does not make the product Latin-only* — the board renders owner-authored titles and party names, and FR15's own examples are a Rawai dive shop and a Taipei studio; and **the `ä` is a pass/fail gate at both ends of the ramp** (two dots must stay two dots at 9px; must not clip at hero size). **Self-hosting is a processor obligation, not a performance preference:** a guest page makes annnä a processor (`security/SPEC.md §12`) and an outside font host would take each guest's IP from a page annnä serves on its owner's behalf. Home: `app/DESIGN.md` §Typography. *Drafted, not founder-ratified.*

**The face was then picked by measurement, the founder having declined the visual round** (*"I have no eye to appreciate these things, so just pick one"*). Twenty open self-hostable candidates were built into a live rig over all four skins; the **superfamily criterion alone cut eighteen**, leaving Noto Sans and IBM Plex Sans, and **Noto Sans won all four remaining measurements** — x-height 0.536 vs 0.516 (the 9px mark is the floor), `tnum` present vs absent, weight axis 100–900 vs 100–700, both tabular by default. **Recorded, not ratified:** `PR/BRAND.md` reserves ratification to the landing mockup and is frozen, so the choice sits in `PR/NOTES.md` per BRAND's own instruction, with the law at `app/DESIGN.md` §Typography. **The deviation is stated rather than buried:** BRAND asks for *"quiet and slightly warm"* and Noto Sans is quiet but **neutral** — taken on purpose, because this design's job is to recede and a face with personality competes with the photograph. **The reversal is one decision, and it is mine to lose:** the superfamily criterion is drafted, not founder-ratified, and it is the only thing standing between the product and a warmer face (**Merriweather Sans**, highest x-height of the twenty at 0.557), at the cost of unmatched fallback for owner-authored Thai and Chinese titles on the board.

**A contradiction sweep the same day found eight, and six were one failure mode — the one this sitting spent the day naming.** A value fixed by hand where it should have been derived from the thing next to it. **Three were made inside `app/DESIGN.md` §Colour, whose entire subject is that colours must be sourced:** two hexes carried in from a draft outside the repo and attributed to `PR/BRAND.md`, which does not contain them *(named in prose rather than quoted here — quoting a dead colour verbatim is itself an unsourced colour, and `hex-source.mjs` refused this very line while it was being written, which is the second time a gate in this corpus has caught its own record of the defect it was built for)*; an accent fallback asserted as koi's when no file said so; and a rejection reason hedged (*"too grey or too rare"*) where the number was one command away — `nudi`'s Vibrant holds **0.013** of its photograph against a `0.02` floor, and rarity is the whole of it.

**What changed as a result, beyond the fixes.** Two of the eight had answers that were fixes rather than solutions, and both were re-cut to durable form: the failure colour is **deleted rather than sourced** — `alarm` already means *something is wrong*, nothing in the corpus renders a failure in colour, and `SCENARIOS.md` **S6** forbids the alternative outright — and replaced by a **policy** (a state colour is added only when a built surface needs one, derives from the active pack, and must clear ΔE2000 ≥ 12 from all three urgency marks on every skin); and boring mode's stash **cites the display-only set at `app/SPEC.md §7` instead of enumerating it**, an inventory that had already gone stale twice in one day. **`app/DESIGN.md` §Spacing was landed**, because adopting Apple's concentricity made the container's padding the input to every corner and the padding was stated nowhere — every radius in the file was underivable, and therefore uncheckable.

**Two gates came out of it, and they are the deliverable.** `deployment/scripts/hex-source.mjs` (every hex answers to a palette, the pipeline, or a per-line derived marker — it fires on the exact error above) and `deployment/scripts/design-law-coverage.mjs` (every `app/DESIGN.md` section is accounted for by `app/BUILD.md`). The second found **fourteen** ungated sections where the hand sweep had found three. Both were canaried against the live defects before the fixes landed.

**Still open, and named so it is a decision rather than a discovery:** the full **token set** · **motion** beyond dwell · **iconography** · per-node visual specs for the fixed catalog · and **how any of it maps onto Astryx + StyleX**, which is the highest-risk unknown in the package and is scheduled last.

## FD-24–FD-27 — the deep-pass sitting, 2026-08-21 (the `FD#` series)

*Same series, same rules. Four rulings from the first adversarial deep pass on the harness package — four independent review lenses (consistency, buildability, floor-attack, production practice), every finding verified against the files before anything was put to the founder. Each ruling was put as a two-option choice with a recommendation named; he took each recommendation. The pass's headline was that the floor — the thing the harness *is* — was not structurally closed: three sequences of individually-legal moves reached an unauthorized outward act. FD-24 to FD-26 close them; FD-27 closes the gap that made the suite's first scenario unbuildable. The mechanical remediation that needed no ruling landed in the paired commit.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-24** | **Creating or widening a Grant is an `authorization`-class act, and the floor gates it.** The reversibility classes covered what an act *does* — comms, value, destruction — and had no class for an act that changes **what future acts are permitted**, so a Grant write rode `commit` as an ordinary internal diff and the loop (or an injection reaching `normalize`) could mint its own basis and fire outward against it, voiding `security/SPEC.md §5`'s "a successful injection could only *propose*" bound. Ruled: a Grant is minted or widened **only on a live, content-bound owner confirmation at the console** — never on the basis of another Grant, never unattended, never from the external surface. The corpus had already fenced the smaller path (§6 bans the noticed-pattern offer from proposing Grants); this closes the larger one on the same reasoning | `harness/SPEC.md §7` (the home) · `§5` class vocabulary · `harness/SCENARIOS.md` D24–D26 · `harness/BUILD.md` Step 3 · `security/SPEC.md §5` (the strengthened bound) |
| **FD-25** | **The on-call list's setup answer mints an ordinary Grant** — `action_class: escalation-notify`, scope exactly the ranked list — and the ladder's per-rung emails fire on that basis. Found independently by two review lenses: the ladder's own notifications were outward sends with no tool, no class, and no basis, colliding with §7's fail-closed rule while D12 required an unattended advance. The auto-accept precedent (FR6, D10) applies unchanged: one authorization path, one ledger. An empty list means no grant and immediate park, as before; the in-app console entry is rendering, never an outward send | `harness/SPEC.md §3.9` (the home) · `§5.2` (the outward verb's widened purpose — `notify_and_await` at ruling time, renamed `send` 2026-08-22) · `harness/SCENARIOS.md` D12–D13 |
| **FD-26** | **RQ-2 gains a fourth parity exclusion: authorization-and-recovery capabilities are console-only.** Credential mint and withdrawal (RQ-1), the restore confirm (`security/SPEC.md §8` — "not an agent tool"), the suppressed-party re-enable (§3.11), and FD-24's grant minting all sat in none of RQ-2's three exclusions, so the mechanical parity gate was red forever — or a stolen credential could mint and revoke credentials. The stated principle: **a credential must never control who may act as the owner.** The gate is set equality in both directions over the non-excluded set | `PRD.md` RQ-2 Out of Scope (the home) · `harness/INTERFACES.md §6` · `harness/SCENARIOS.md` X1, X6 |
| **FD-27** | **The typed-value table gains `instant` and `interval` rows, and the engine owns time resolution.** Every layer was forbidden to author a time and none was assigned to resolve one — the model may not emit a resolved instant (`model/SPEC.md §5`), `typed_value` is "the sole entry" and its table had no time-point row — so "Thursday 3–4pm" had no legal path into a commitment and the suite's first scenario was unbuildable. Ruled: `typed_value` resolves the raw phrase locale- and zone-aware against the board's declared zone and a supplied reference instant, deterministically; the model passes the phrase raw, and a model-resolved instant literal is a forbidden authoring attempt. The alternative — model proposes, engine validates — was refused as FD-16's always-looks-cheap exception | `engine/SPEC.md §2` (the home) · `model/SPEC.md §2`/`§5` · `engine/SCENARIOS.md` Y1 · `engine/BUILD.md` Step 2 |

## FD-28–FD-37 — the corpus-review sitting, 2026-08-21 (the `FD#` series)

*Same series, same rules. Ten rulings from the first adversarial deep pass on the rest of the corpus — nine independent reviewers over engine, model, app, security, marketplace, deployment and the cross-corpus seams, every finding verified against the files. **Provenance differs from every earlier sitting and is stated plainly:** the founder delegated these in advance ("use your own recommendation... 99.9% of the time I'm just going to go with your recommendation") and was not shown them individually before they landed. Each was the recommended option of a real fork; each is reversible by a one-line counter-ruling, and the two most product-shaped (FD-36, FD-37) are flagged for his review by name.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-28** | **A trigger firing never calls `normalize`.** The five trigger sources are structured facts the harness already understands; a firing routes deterministically — assemble the slice, `calculate`, `commit` — and the model is reachable on a firing only through `summarize` (quarantined read of returned free text) and `narrate` (outward text under an existing basis). The 2026-08-21 `normalize(utterance \| trigger_event)` union is reversed: the closed intent vocabulary is owner-actions-only, a hold-expiry has no intent to return, and the event already rode `handoff_frame` — the union delivered it twice with no precedence rule. Interpretation is for people; consequences of engine-owned conditions are the engine's *(annotation 2026-08-22: five was the count at ruling time — the delivery report has since joined as the sixth source at the home, and the ruling binds all six identically)* | `harness/SPEC.md §4` (the home) · `harness/INTERFACES.md §2.1` · `model/SPEC.md §2` · `model/INTERFACES.md §2.1` |
| **FD-29** | **The availability-delta-over-time leak is an accepted, printed residue.** Each guest projection response contains only bookable time; a bearer polling on a schedule can difference the responses and recover occupancy *timing* with every label stripped. Ruled residue rather than closed: a publication-grain rule would coarsen every honest guest's view to blunt a patient adversary who still learns the grain. The residue is printed where the invariant lives, and the projection read joins `security/SPEC.md §10`'s named-limits list — every public door keeps a declared ceiling, so the polling rate is bounded and visible | `engine/SPEC.md §10` (the printed residue) · `security/SPEC.md §10` (the read limit) |
| **FD-30** | **No cancellation-fee primitive; the no-show mark latches the money state.** K1 (self-cancel erases `owed`) and K3 (the owner marks no-show) contradicted on one commitment with nothing ordering them. Ruled: once the no-show mark is set, `owed` derives from the mark and the bound policy; a later self-cancel changes state but never erases a marked debt — marks are attributed facts, and a derived value never un-derives a recorded human judgment. The closed rule menu does **not** grow a fee type: fee schedules are template territory (FR37's line), not platform law | `engine/SPEC.md §1.9` (the home) · `engine/SCENARIOS.md` K1 (the carve), K3 unchanged |
| **FD-31** | **A preview derived from a licensed image is licensed content.** The "palette-only" free half shipped a 24px LQIP rendition of the photograph being sold, as a data-URI the URL-scoped gate could not see. Ruled: LQIP moves to `derivatives` (licensed, entitled accounts only); the free preview renders from palette swatches alone; the entitlement law is restated content-based — no rendition of a licensed image, URL or data-URI, in any unentitled-reachable artifact. The shipped koi pack is out of compliance until the pipeline change lands (code, deferred and tracked) *(annotation 2026-08-22: landed, `c49c1d2` — the pipeline change shipped, all four packs comply, and the home's compliance note is discharged)* | `marketplace/SPEC.md §1.1` (the home) · `marketplace/SCENARIOS.md` E4 |
| **FD-32** | **FD-24 amendment: the `ShareGrant` carve.** FD-24's "a Grant is never the basis for minting a Grant" collided with FR28's stored accept modes, where the accept mints the counterparty-side `ShareGrant` unattended on a trigger firing. Ruled: the engine-minted `ShareGrant` on §7.1's stored-accept path is the one exemption — it is the *application* of a Grant the owner already minted under FD-24 discipline, scoped exactly to the share it accepts, never a widening. Everything else FD-24 says stands unchanged | FD-24's entry above (this carve) · `harness/SPEC.md §7` · `engine/SCENARIOS.md` I4 (qualified) · `harness/SCENARIOS.md` D25 (scoped to the tool-boundary path) |
| **FD-33** | **Credential suspension is a pause, not a withdrawal.** The §10 rate-trip introduced a suspended state §3's closed table didn't carry, and "re-activation" read as un-latching a latch. Ruled: `suspended` joins the credential lifecycle as its own state — auto-set by a declared rate or spend trip, blocks use while set, is **not** a latch — and is cleared only by an owner console act recorded `{who: owner, basis: console act, when}`. Revocation remains a latch, forever. The console card says "suspended — re-enable from the console," never "reissue" | `security/SPEC.md §3` (the home) · `§10` (the trip and the card) |
| **FD-34** | **The engine gains a fourth display projection: the candidate-shape ghost.** Marketplace D3 requires previewing what a bundle's shared shape *would* publish against the owner's real availability, before install — and none of the three fenced projections takes an uninstalled candidate shape, while producing a real Shared would mint token digests. Ruled: a read-only projection over (candidate shared-shape, the owner's own availability), selectable set fenced by construction exactly as §1.7/§1.7a fence theirs, minting nothing. This is the deliberate, ruled exception to the marketplace's zero-new-seam-shapes posture, and it is display-only like its three siblings | `engine/SPEC.md §0` (the enumeration, 3→4) · `marketplace/SPEC.md §6` · `marketplace/SCENARIOS.md` D3 |
| **FD-35** | **`PRD.md` is the requirements source of truth; `user-stories/` is the falsification-probe corpus.** Two SPEC-tier files each claimed to be the requirements source and neither named the other, leaving FR13's tiebreak unanswerable. Ruled: requirements live in the PRD (`RQ-##`); the Situations are what scenarios must derive from and be refuted by — probes, not a second register. A Situation beat with no covering scenario is a derivation defect; an RQ with no Situation exercising it is a probe gap. Both directions now have a name | `PRD.md` (the register) · `user-stories/README.md` (reworded to say what it is) |
| **FD-36** | **`booking-link` is reserved, not minted, at v1.** The class existed in security §3's closed table while no app surface, no minting story, and no engine digest row existed for it — an anonymous bearer door specified by exactly one layer. Ruled the conservative way: the row stays, marked *reserved — not minted at v1*; no door opens until a ruling opens it with its page flow, minting story and scenarios in the same sitting. H8's bare-bearer refusal clause becomes vacuously true and is annotated as such rather than deleted | `security/SPEC.md §3` (the row) · `app/SPEC.md §10` (its invariant, now literally true) · `harness/SCENARIOS.md` H8 |
| **FD-37** | **The iCloud/CalDAV import route is dropped from v1; the ICS secret-feed URL joins §3.1 as its third member.** FR36 admitted iCloud via app-specific password, but CalDAV under that password is read-write *by construction* — it turns FR12's outbound ban from an impossibility into a promise, the exact degradation §3.1's read-only-by-construction rule exists to prevent. An ICS feed is pull-only by format: ruled in with its own posture (the secret URL is the credential; read-only by construction holds). iCloud owners import via ICS export until a scoped route exists. Amends FR36's provider list | `security/SPEC.md §3.1` (the home) · `app/SPEC.md §9` · `security/SCENARIOS.md` T8 |

---

## FD-38–FD-43 — the visual sitting, 2026-08-21/22 (the `FD#` series)

*The bmad-ux run's resumption and close. **Provenance is mixed and stated per entry**: FD-38–FD-41 and FD-43 are **founder rulings made by eye** against live built comparisons (his words quoted); FD-42 pairs a founder principle with a delegated mechanism. Mid-sitting the founder issued a **blanket delegation** — "go with your recommendation for all UI/UX-related questions; bring me an original question I have not seen" — under which the following were resolved with delegated provenance, each reversible by one counter-look: the five FD-23 drafted calls + the superfamily criterion ratified · the corner-radius set closed (concentricity, the founder's own follow-Apple directive, wins over the sampler placeholders — `app/DESIGN.md` §Shapes stands) · the substrate spike ordered first and its verdict adopted (**StyleX**, three token classes, one mechanism for shipped and bought skins) · **nothing remains at the canvas edge at any width** · the presence/wake-policy naming (one mechanism, two registers, declared at the home) · and the full resolution of a two-lens reviewer gate, including three computed accessibility criticals (the urgency ground, the veil mode branch, the presence floor — each marked `a11y A#` where it landed). The run's working record: the workspace memlog (98 entries).*

| # | Ruling | Landed |
|---|---|---|
| **FD-38** | **A form is never see-through.** A riser and its form open engaged and stay engaged — "the purpose of the form is to fill it out… the on-hover effect should be the default and always." Findable-at-rest binds surfaces that *wait* (the console), never a surface the owner summoned | `app/DESIGN.md` §Glass (Attention) |
| **FD-39** | **Chat is the console's face; settings live behind the hamburger — and relocate, never delete.** Inline controls in the chat body rejected on sight ("this is horrible… the user isn't doing adjustments as their major purpose"); the islands' jobs (both dials, scope presets, skins, boring mode) moved intact into the settings pane; every owner control is **stepped stops, never a free slider**. The island `custom` state dissolves — the pane reads a predicate back in words | `app/DESIGN.md` §Console · §Islands & mobile · §Board rendering |
| **FD-40** | **Tasks sit flush to the bottom edge, growing upward; the console's default position is in the body of the photograph, never over the task band.** Bottom gravity is an anchor, not an area with dead space beneath it | `app/DESIGN.md` §Board rendering · §Console |
| **FD-41** | **The token set is the dense board + quiet glass pick** (sets 5+7 of a ten-way live sampler; body 12px, tighter gaps, blur 4/10, panel .12/.24, plate .78/.60), chosen by the founder's eye with nine alternates on screen. The taste axes are his; the skin axes derive; the accessibility axes are each user's | `app/DESIGN.md` §Typography (ramp) · §Glass · the workspace token set |
| **FD-42** | **Appearance is owner-customizable through the agent, and text size is the user's.** The urgency palette is a default a colorblind owner remaps; urgency-presentation (spine \| border) is a variant; a text-scale stop (never below 1) rides rem-authored sizes so platform zoom works first. Bounds: colour still means urgency only; catalog stays closed (parameters, never components); the set stays display-only. The **seam path is drafted, not founder-ratified** *(mechanism since ratified: FD-66, 2026-08-22 — the verb is `display_settings`)*: a narrow `display.settings` write class rides the ordinary seam into the app-owned store (`app/SPEC.md §7`), and FD-19's exclusion narrows to third-party clients | `app/DESIGN.md` §Colour (The owner's overrides) · `app/SPEC.md §7` · `PRD.md` RQ-2 |
| **FD-43** | **Every completed booking yields guest self-service manage access** — "of course, a guest should be able to edit their stuff." A booking begun on a bare entry link mints a **booking-bound manage token at confirmation** (shown on the page; carried in the confirmation message when an address exists), so cancel/move parity holds for every guest. The binding rule is untouched — finishing a booking now always mints the credential it requires; a visitor with no booking still manages nothing | `app/SPEC.md §5` (the home) · `security/SPEC.md §3` · `app/SCENARIOS.md` G10 · `app/DESIGN.md` §Guest pages |

**One corrective note, same sitting:** the FD-20–23 sitting note above says the semantic colour set is *"closed at one colour (`bad`)"*; `app/DESIGN.md` §Colour has since ruled the set **empty rather than small**, with the addition policy printed, and both spines follow it. The §Colour version is the law; the sentence above stands as the record of the earlier state.

## FD-44 — the typeface, 2026-08-22

*Founder ruling made by eye at a 10-face sampler rendering every real surface (blocks with 9px micro-marks and urgency spines on the photograph, the task band, a riser form with an error state, console chat and settings pane, the wordmark at every size). His words: "Typography goes to option number seven."*

| # | Ruling | Landed at |
|---|---|---|
| **FD-44** | **The face is Nunito Sans** — for everything, display and text, per the one-face law. This ratifies by eye what `../PR/BRAND.md` reserved to the landing-mockup moment, supersedes the measured working choice (Noto Sans, 2026-08-09, which was always recorded as unratified), and **reverses the superfamily criterion** (metric-matched Thai/CJK siblings — itself only delegated-ratified): warmth won. Consequence taken with it: Thai and CJK text resolves through **Noto Sans siblings as fallback**, metric mismatch accepted; the 9px micro-mark and tabular-figure duties stand and the build verifies Nunito Sans against them (`tnum` via `font-variant-numeric`, already law) | `app/DESIGN.md` §Typography (the home) · `PR/NOTES.md` (the ratification record) · `PR/mock/landing.html` |

**One corrective note, same day:** the Step-1 fact-check this ruling scheduled came back within hours — Nunito Sans carries **no `tnum` feature** (upstream binaries parsed), but its digits measured uniform-width at every variable-axis extreme, so columns align in practice. The fact went to the founder as the ruling requires, and the disposition he approved is **a build-failing digit-width gate at app BUILD Step 1** in place of the feature the parenthetical above assumed; the ruling's outcome (the face) is untouched, its verification mechanism corrected at the home. `app/DESIGN.md` §Typography carries the full record.

## FD-45–FD-49 — the epics-audit residue sitting, 2026-08-22 (the `FD#` series)

*The epics-and-stories audit (run the same day as the architecture audit; its report is workspace material, deliberately uncitable) left one CRITICAL and a tail of residue against the plan of record. The founder was shown the findings with a recommendation per fork and answered: **"Fold your recommended answers in and start the mechanical eight."** So the entries below are **delegated resolutions of recommendations he was shown in summary, not individually** — the FD-28 provenance pattern; each is reversible by one counter-ruling. The mechanical fixes (the import build home, the language and precondition scenarios, the duration ceiling, the supplier-ledger scenario, the C10 step re-cut, the probe-corpus gap notes) landed in the same sitting under the same instruction.*

| # | Ruling | Landed |
|---|---|---|
| **FD-45** | **The entry link mints at open; the minted token sees the published offering.** T7/G8 hid all offering detail until a visitor began a pull, while the Situation-B probe they derive from requires *browse → pull* ("opens the link, sees the live fleet with photos and prices"); FD-35 makes the probe the falsifier. Re-cut: opening the entry link mints the **single-visitor token** (rate-limited per IP/device, §10 — the scraping bound moves to the mint, where it always really was) and everything renders on that token: the published offering surface — fleet, photos, prices, open windows — is browsable **before** any pull. The bare URL still holds nothing and answers nothing when probed; per-token hold caps, checklist identity, and attribution are unchanged *(annotation 2026-08-22: FD-53 closes this entry's two residues — the bare URL carries an unfurl-safe static preview, and unused minted tokens age out)* | `security/SPEC.md §3` (the fourth-class rows) · `security/SCENARIOS.md` T7 · `app/SPEC.md §5` · `app/SCENARIOS.md` G8 |
| **FD-46** | **Guest→owner conversion is a verified-channel claim that adds no read power.** The second conversion engine ("his logbook is already sitting in it") had no mechanism in any layer. Ruled: a new owner account claims its guest history by **verifying the contact channel its per-recipient tokens were delivered to**; the claim mints an engine-recorded **party↔principal link** (never caller-constructable). The link moves nothing — every record keeps its owning tenant — and grants exactly the **union of what the linked party's booking-bound tokens could already read** (their own bookings, attestations, participation history), assembled as the new account's starting history. Ambiguity links nothing; the record owners are not consulted because nothing of theirs beyond the guest's own token views becomes readable | `security/SPEC.md §2` (the claim act) · `engine/SPEC.md §1.1` (the link) · `security/SCENARIOS.md` N6 · `user-stories/README.md` (the engine's mechanism note) |
| **FD-47** | **A guest standing series is the owner's published shape offering a pattern; either side ends it forward-only; enrollment composes, it is not a primitive.** Situation A's "one-time or recurring" choice renders only where the published Shared shape carries an **owner-enabled series option**; picking it writes an ordinary recurrence pattern on the owner's board with every instance attributed to the guest's token. The guest's manage token carries *end series* (future instances withdraw per §1.4; past instances keep their terms); the owner ends it as an ordinary forward-only pattern edit. A monthly-base **enrollment** is this series plus its money marks; ending it composes existing acts — end the series, retire the link, the ledger closes by derivation | `app/SPEC.md §5` · `app/SCENARIOS.md` G11 |
| **FD-48** | **Voice is deferred post-v1.** "Text and voice are the same console" was an unbuildable MUST — no scenario, no step, no transcription member on the four-call model seam. The sentence stands as design intent, scoped: the v1 console is text; voice arrives later as the *same* console behind the *same* seam, with its own ruling, scenarios, and seam member — never a bolt-on and never assumed by anything in v1 | `app/SPEC.md §3` (the scoped sentence) · `§9` (the non-goal row) |
| **FD-49** | **Import gets its seam verb and its build home.** The v1-ruled import capability (FR3/FR12/FR36, FD-37) was specified with no BUILD step, no scenario, no seam, and no egress-allowlist presence — the harness-Step-7 defect class verbatim, on the first thing both first-ring protagonists do. Ruled: the harness tool contract gains **`import_fetch`** (internal class, **reachable only within a caller-initiated firing, never a trigger firing** — FR12's no-background-poll made structural), the app executes it against the vault-resident §3.1 credential and returns `import`-tagged items for the ordinary propose→confirm walk; app BUILD gains **Step 6a** gating I1–I4; the allowlist carries the reserved provider rows. This is the one ruled amendment to the app-seam interview lock, made by the RQ-3 process (a new-verb capability is a spec change with its own ruling — this entry is that ruling). **Non-calendar bulk import** (Sofia's Flow, TingTing's spreadsheets/contacts) stays outside the ruled provider set and is named a v1 gap in the probe corpus rather than implied | `harness/SPEC.md §5.2` · `harness/INTERFACES.md §3.3` · `harness/SCENARIOS.md` B10 · `app/SPEC.md §9` · `app/INTERFACES.md §1/§2` · `app/SCENARIOS.md` I1–I4 · `app/BUILD.md` Step 6a · `deployment/egress-allowlist.md` · `user-stories/README.md` (gap 4) |

## FD-50–FD-67 — the fork sitting, 2026-08-22 (the `FD#` series)

*The strategy review's founder-fork register, resolved under a blanket delegation the founder issued in his own words the same day: he asked what actually required him, said he would be uninvolved, and instructed **"do as much as you can without me."** Every entry below lands a recommendation he was shown in a written brief (workspace material, deliberately uncitable), **not individually confirmed** — the FD-28/FD-45 provenance pattern at its widest; each is reversible by one counter-ruling. **One item was deliberately NOT folded:** the read-only ICS out-feed reopen (the old OR-39 ground) reverses an identity-level ban three SPECs and the final PRD carry ("not a two-way calendar"), and a laziness delegation is not a basis for reversing product identity — it stays open to him and blocks nothing.*

| # | Ruling | Landed |
|---|---|---|
| **FD-50** | **The first release is Situation A plus the freelancer half of Situation C.** FR10's "v1 = A, B, C, D" stands whole as *specification* scope; this names the distinct **release** scope — the founder's own two jobs, the only slice with a live first user — stated as a readiness condition, never a schedule (FR2). Consequences taken with it: the Situation-B forks (deposit/`held` trigger, license-class-vs-Auto, suppressed-stranger re-enable) defer to B's own sprint; travel-provider integration sits behind the first release (seam and stub stay); **skins-as-purchasable-goods defers with its machinery** (templates and shipped skins keep everything — the entitlement/revocation/signed-URL law survives as spec); the **external-client credential** defers to the beta cohort it was specified for (the parity disciplines RQ-2/3/4/5/7 stay — they are free). A release-tag gate over `[MUST]` scenarios is the owed mechanism, landed as its own pass so it lands green *(annotation 2026-08-22: landed — `[r1]`/`[r2]` on all 344 rows, vocabulary at `PRD.md §6.5`, gate `deployment/scripts/release-tags.mjs`; annotation 2026-08-22 — the full-corpus review's four tag corrections and nine new rows move the count to 353: 285 [r1] / 68 [r2], the classifying judgment otherwise standing; annotation 2026-08-23 — FD-74's D8 and FD-75's P9 move it to 355: 287 [r1] / 68 [r2]; **amended 2026-08-24, FD-80:** a Situation is never a ship unit; this entry misread FR10. Home is `PRD.md` §6.5 as rewritten)* | `PRD.md` §6.5 (FD-80; this row is history) · deferrals that survive do so at their own homes |
| **FD-51** | **FD-46's conversion union is bounded, and a channel is not a person.** The claim's union covers **unrevoked, unexpired** tokens only — an owner's revocation latch survives conversion (revocation is forever; a conversion that resurrects a revoked read would un-latch it), and an expired token's read does not return years later through a recycled address *(amended 2026-08-23, FD-76: "unexpired" was vacuous — these tokens never expire — and the recycled-address risk this clause names is now actually closed by the lifecycle-anchored dormancy bound)*. **One principal per verified channel**: a shared inbox (couple's email, front desk) delivering many parties' tokens converts only the party whose own flow verified; the rest need their own verification. N6 asserts both refusals | `security/SPEC.md §2` · `engine/SPEC.md §1.1` · `security/SCENARIOS.md` N6 |
| **FD-52** | **The monthly base is a materialized monthly commitment carrying the `pricing` rule** — not a new money-mark class. FD-47 closed the series and left the base underivable (`pricing` binds per booking; nothing derived a monthly entry). Ruled: an enrollment's base fee is its own recurring monthly commitment on the owner's board, attributed to the guest's token like every series instance, priced by the ordinary `pricing` bind; the ledger closes by the same derivation as any commitment. No new mechanism anywhere | `app/SPEC.md §5` (the FD-47 paragraph) · `app/SCENARIOS.md` G11 |
| **FD-53** | **FD-45's two mint-at-open residues closed.** An **unfurl-safe static preview** (name and photo, no availability, no offering data) is lawful on the bare URL — LINE/WhatsApp unfurlers hit entry links at the exact moment they are pasted to convert, and a blank redirect there defeats the link's purpose while a static preview leaks nothing T7 protects. And **unused visitor tokens age out**: a minted-never-used single-visitor token is purged on a declared clock, so crawler traffic cannot accumulate digest rows | `security/SPEC.md §3` (the fourth-class rows) |
| **FD-54** | **A guest reads their own money and quota through the manage token's slice, never through a board projection.** The story beat showed a guest seeing their own balance; the board-blind projection cannot select it and must not learn to. The manage-state projection (FD-43's surface) gains the guest's **own** marks and quota standing — already token-attributed data about themselves — fenced by construction like its parent | `app/SPEC.md §5` (manage state) · `engine/SPEC.md §1.7` (the digest-entry read scope) |
| **FD-55** | **Party-size-conditional composition enters the menu as a `dependency` predicate, not a new type.** The harness prose promised it, the menu could not store it. A `dependency`/`fallback` entry may carry `when: party_size ≥ N` — evaluated at resolve over the goal's declared party size; entry required iff the predicate holds; violation is the ordinary composition decline naming the entry. This is *within-goal* arithmetic on one declared number — not the deleted cross-board relation row, which stays dead | `engine/SPEC.md §3` (the dependency row) |
| **FD-56** | **Language authoring: the spec wins for v1.** TingTing's story shows translation appearing; the spec's mechanism is FR15's ranked set with **owner-hand-authored** per-language labels (app §4.1's label maps). No model translation call exists in v1 — a fifth model call is a spec change with its own ruling. The story beat reads as end-state; the probe corpus's chrome-language gap note now says the authoring cost out loud | `user-stories/README.md` (gap 3, extended) |
| **FD-57** | **FD-16 narrowed: string→instant transduction is propose-then-verify, not an engine grammar project.** FD-16's binding text names seven computations; transduction is none of them, and its "the model supplies inputs" clause permits this shape: the model may pass a **proposed structured instant beside the raw phrase** as an input; `typed_value` **verifies** deterministically — re-render the proposal into the phrase's locale and zone and compare components — and **fails closed** on mismatch or absence, exactly as an unresolvable phrase already errors. A verified proposal is engine output; an unverified one never reaches math, so the thin-agent law is untouched. Kills the two-grammar (th, zh-TW) parsing project on the critical path; the per-locale artifact becomes a **rendering** table, which is a solved problem (ICU) *(annotation 2026-08-22: the verification's comparison contract — per-locale semantic-component token tables and the no-week-marker rule — is now specified at the home, a delegated clarifying amendment under the standing pattern, reversible by one counter-ruling; ratified 2026-08-23 at the FD-73–76 sitting, and FD-73 states the invariance law it implies)* | `engine/SPEC.md §2` (FD-27 block) · `engine/BUILD.md` Step 2 · `engine/SCENARIOS.md` Y1 · `model/SPEC.md §2/§5` |
| **FD-58** | **FR18 narrowed to its ruled object: outward surfaces and the closed service's store terms.** Cost-to-serve now has a lawful home — **gitignored working material** (`docs/agents/`, which already holds exactly this class), never a tracked file; `PR/BRIEF.md` gains one risk row in non-commercial words (the per-interaction serving cost is real and unpriced). Outward money silence is untouched | `PR/BRIEF.md` (risk row) |
| **FD-59** | **D12–D14, D16–D17 and D21 are re-provenanced honestly: they derive from held-out E's roster, which build gates may not cite.** Every v1 protagonist is the empty-ladder case, so **D15 keeps `[MUST]`** (empty list parks immediately — the v1 behavior) and the ladder-walk set drops to `[SHOULD]` until a v1 Situation earns a ranked list. The ladder law (§3.9) stands unchanged and still gets built at Step 5 — what changed is the honest weight of its suite, and the traceability line no longer credits Situation C with a derivation it does not contain | `harness/SCENARIOS.md` (tags + the traceability line) |
| **FD-60** | **The third v1 gap is named: single-pass whole-week placement is the demonstration's ceiling, not the shipped mechanism.** Situation-C's situation-1 promises "the whole week placed in one pass"; engine §7 rules chained per-goal greedy first-fit with honest declines. The gap joins the probe corpus's named-gap list with the same weight as its siblings: v1 places per goal, narrates declines, and asks the owner — a global multi-goal pass has no ruling and no mechanism. The story stays true as written, as end-state, with a pointer at the beat | `user-stories/README.md` (gap 5) · `Situation-C/situation-1.md` (the pointer) |
| **FD-61** | **Reminders exist, as a creator-set kind property riding machinery that already exists — and the first release carries exactly one automated non-email channel: LINE.** The product scores no-shows in detail and never prevented one. Ruled: a commitment-kind may declare `reminders: [{offset_before_start, channel?}]`; each reminder is an ordinary clock-trigger firing whose outward send rides the outward seam verb (`notify_and_await` at ruling time; renamed `send` 2026-08-22) on an elicited **reminder-notify Grant** (the FD-25 shape — no list, no grant, nothing sends), under §10's caps and §3.11's suppression. Discharge-attention, not capture: reminders go to the party who committed, about their own commitment, never promotional. LINE is named on the pilots' geography (readiness-worded); its provider rows enter the egress allowlist as reserved floor acts *(annotation 2026-08-23, FD-74: the roster widens around this ruling — LINE stays the named first, WhatsApp joins, WeChat and Instagram carry recorded hard points at the §6 home)* | `harness/SPEC.md §3.3/§4` · `harness/SCENARIOS.md` D28 · `app/SPEC.md §6` · `deployment/egress-allowlist.md` |
| **FD-62** | **The pilot is promoted from never-authoritative NOTES to open ruling OR-40** — it is the only instrument the success criteria can run on, and an instrument named nowhere authoritative cannot be owed. OR-40 is defined where used (`PR/BRIEF.md`): who the pilot cohort is, is the founder's open call | `PR/BRIEF.md` (OR-40) · `AGENTS.md` (the OR count) |
| **FD-63** | **The founder-dogfood exit criterion is adopted:** four weeks of the founder's own real bookings on the system, zero fallbacks to the old tools — binary, FR2-legal, and it tests the completeness bet with zero users. Lands beside the success criteria as a readiness condition *(annotation 2026-08-26, FD-79: amended — dogfood and pilot are one run; the four-solo-weeks clause is struck at the home)* | `PR/BRIEF.md` |
| **FD-64** | **TypeScript moves `^5.9` → `^6.0`** in both suite-carrying layers, while reversal is one line. Checked against the registry at ruling time: 6.0.3 is the published bridge and 7.x is already `latest`, so staying on 5.9 was aging against **two** major versions. Suites green under the bump or it reverts | `engine/package.json` · `harness/package.json` (code half) |
| **FD-65** | **The ChatGPT-subscription slot is cut.** The capability has been identity-only upstream since 2026-08-02 — a slot for a programme that no longer exists is speculative machinery with a config enum member and a build step attached. `byo-chatgpt` leaves the provider enum; BUILD Step 5 records the cut; FR5/FR31 (`byo-key`, attended-only) and FD-3 are untouched. If the vendor programme returns, one ruling restores the slot. **Reverses FR-C** — its row above carries the strike, and its accepted-risk paragraph records the risk that fired | `model/SPEC.md §7` · `model/INTERFACES.md §2.2` · `model/BUILD.md` Step 5 |
| **FD-66** | **FD-42's mechanism is ratified: the verb is `display_settings`, on the app seam.** The drafted `display.settings` write class gets its name: `display_settings(diff)` — internal class, display-only state into the app-owned §7 store, diff-shaped like every settings write, reachable by the owner's console and the owner's own agent, **excluded from parity in both directions** exactly as RQ-2's amended text pre-wired (one name on the Step-8 printed exclusion list). No engine value, no seam payload carries appearance otherwise (FD-19 stands, narrowed as FD-42 already ruled). The two BUILD preconditions this clears are cleared in the same sitting | `app/SPEC.md §7` · `app/INTERFACES.md §1` · `harness/SPEC.md §5.1` · `harness/BUILD.md` Step 2 · `app/BUILD.md` Step 1 · `harness/src/seams.ts` (code half) |
| **FD-67** | **The model layer's load-refusal is re-worded from a vendor to a property: fallback and unattended bindings must be *app-supplied*, not *OpenRouter*.** OpenRouter was a hard single point of failure behind a control that fails closed for every tenant at once. The provider enum gains `app-direct` (an app-held direct provider key — app-supplied by definition, vault-custodied like any §3.1 credential); every rule that said "an `openrouter` fallback" now says "an app-supplied fallback"; FD-3 (`summarize` never BYO) and FR31 (BYO attended-only) are untouched — both were always about *whose* model, not which gateway | `model/SPEC.md §7` · `model/INTERFACES.md §2.2` |

## FD-68 — the founder's own life is held out, 2026-08-22 (the `FD#` series)

*Founder-originated, same day as the fork sitting and unlike it: this one is **his own answer in his own reasoning**, reversing a session recommendation. The session proposed eliciting "Situation-F" — the freelance-instructor bench — from his working life, since the probes-not-scripts law forbids inventing it.*

| # | Ruling | Landed |
|---|---|---|
| **FD-68** | **Situation-F is never elicited: the founder's freelance life is deliberately held out as the product's first live test.** His reasoning, near-verbatim: once the product is built, his personal experience is the first test of whether the app works and where its limits are — *"if I give you this actual truth now, then when I go to test the real thing, I have to invent a new lie to test."* This is the held-out-E discipline applied to the founder himself, and it composes with FD-63 (the dogfood exit criterion), which is exactly the run this ruling preserves the data for. Three consequences: **no one drafts a fictional Situation-F either** (an invented one is a script — the defect class the probe corpus's own principles name); the freelance-bench coverage stands on what exists — Situation-C's `resource-instructor.md` setup files and their scenarios — with the residue carried honestly rather than papered over (the offer-collision / structured-decline / receivables *run* has no probe until the live test is that probe); and the FD-63 run's findings enter the eval sets the ordinary way production mismatches do, which is how the held-out data pays twice | `RULINGS.md` (this entry — the ruling is a process fact; the probe corpus gains no file, by design) |

## FD-69 — the spec-sweep sitting, 2026-08-22 (the `FD#` series)

*Made under the FD-50–67 standing delegation ("do as much as you can without me"), during the strategy-review integration's spec-closable sweep. One ruling only — the sweep's other rows were mechanical spec edits touching no ruling.*

| # | Ruling | Landed |
|---|---|---|
| **FD-69** | **The injection-fixture benchmarks are named with licenses, and FD-8's never-author rule gains exactly one carve: adaptation with recorded provenance.** "Their named public benchmarks" resolved to no source and no license position — a mid-sprint sourcing stall on the largest harness step — and L5's calendar-import string was unsatisfiable as written (no public benchmark ships a connector-door calendar-entry injection). Ruled: the sources are **AgentDojo** (`ethz-spylab/agentdojo`), **BIPIA** (`microsoft/BIPIA`), and **InjecAgent** (`uiuc-kang-lab/InjecAgent`) — all MIT, verified at ruling time — and for a channel shape no benchmark covers, the fixture is an **adapted** benchmark string (payload kept, channel dressing changed) with source, adaptation, and reason recorded per-string in the provenance README. Authoring from nothing stays forbidden | `security/SPEC.md §5` (the FD-8 bullet, extended) · `harness/BUILD.md` Step 5 |

## FD-70 — the wake-scope menu sitting, 2026-08-22 (the `FD#` series)

*Made under the FD-50–67 standing delegation, executing the fork brief's third F10 deferral cut — the one the fork sitting recorded as recommended and deliberately deferred to its own careful sitting (the other two landed inside FD-50). Reversible by one counter-ruling, like every delegated entry.*

| # | Ruling | Landed |
|---|---|---|
| **FD-70** | **The wake policy's scope is a closed four-shape menu for the first release; FD-22's open predicate is annotated, not erased, and may return as a strict superset.** FD-22's own text priced the departure — "there is no finite scenario suite over scopes" — and the first release should not carry an infinite test surface for a display setting. The menu: **`everything`** (default) · **`today`** · **`this-week`** · **`kinds`** (the one parameterized shape — an owner-picked kind set, elicited like any rule operand). Membership evaluates mechanically over the block's own stored data; an off-menu scope ask gets the engine-discipline honest refusal (nearest shape offered, design request recorded), never a silent approximation; `keep-awake` is unchanged and is what makes a coarse entry survivable. Stored entries survive any future re-widening by construction — a superset keeps every menu shape valid *(Design request recorded 2026-08-22, this entry's own off-menu pattern: a bounded **event-wake** — a block whose commitment just changed renders awake for a declared interval — surfaced by the review's arrival-wake finding; on the founder's table, not ruled, not shipped; the story and PRD beats that assumed it are reworded to the menu that exists. Founder read 2026-08-23, the FD-73–76 sitting: **user-space, not a founder decision** — "as long as the harness allows that [customization], the specifics don't seem important" — a candidate menu shape whenever the live board earns it, and nobody re-asks him)* | `app/DESIGN.md` §Board rendering, *The wake policy* (the home) · `app/SCENARIOS.md` C9/C10 (re-cut) · `PRD.md` §3 glossary, §11 Q1 (annotated) |

## FD-71–FD-72 — the vision sitting, 2026-08-22 (the `FD#` series)

*Founder-originated, answered live: three questions were put to him with recommendations at the full-corpus review's alignment phase — what "agent-first" must mean here, what the end-state is, and what the marketplace becomes — and he answered each with his own pick (the FD-18/FD-19 class: a shown choice taken, not a delegation). The first answer — **agent-first = the governed agent** — re-ratifies the permission floor as identity and needs no new entry: it confirms `PRD.md §1.3` as written, and this preamble is its record. The other two are new law:*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-71** | **The end-state is category replacement.** The commitment-shaped functions of the accounting/CRM/contracts-class software a small operator runs beside their calendar eventually live in annnä — each is a view over commitments, and annnä holds the Commitment as its primitive. Direction, not scope: FD-80 (what ships is the application), FR10's bar, and every PRD §5 non-goal stand as written; the path is the rulings cadence (a capability enters when its scenarios and ruling exist), and non-calendar bulk import (`user-stories/README.md` gap 4) is the first post-release rulings program on it. The end-state absorbs a category by **replacement, never by sync** — the outbound ban stays identity | `PRD.md §1` (the end-state statement) · `PRD.md §5` (the sorted non-goals) |
| **FD-72** | **The marketplace's end-state is an extension ecosystem, reached agent-natively.** Templates carry business shapes, never code — F1/F2 and the §1.2 hard fence stand at every stage; platform capability grows by vetted rulings-cadence releases, never by third-party code annnä ships; third-party capability lives in **third-party agents calling the tool contract under the floor** (the external-client surface, opening outward behind FR1's legal review) — the floor-plus-contract is the ecosystem's trust model, the thing being sold. v1 marketplace law unchanged: admin-only supply, shapes-only *(annotation 2026-08-24, FD-82: admin-only *publish* is amended — templates are owner-publish; skins stay admin; shapes-only stands)*. OR-28/OR-29 gain the direction note — publisher identity and curation is this ecosystem's app-review question. The plugin-vs-agent-native mechanism question stays open with the recommendation on record: agent-native | `marketplace/SPEC.md §7` (the OR-28/OR-29 direction notes) · `PRD.md §1.1`/§4.2 (the consumer surface it opens onto) |

## FD-73–FD-76 — the channels-and-asks sitting, 2026-08-23 (the `FD#` series)

*Founder-originated, answered live: the review's five triage items were walked through with recommendations and he answered each in his own words (the FD-18/FD-19 class: shown choices taken, not a delegation). Two of the five he closed by applying **his own standing filter** — "would two owners answer differently?" — reclassifying them as the system's questions to its users, not his (OR-41 → FD-75; the event-wake, dispositioned at FD-70's row). Two more of the walkthrough's outcomes need no new entry and are recorded here: the **FD-57 comparison contract is ratified** (his question — "why should language change behavior?" — is answered by the contract itself, and FD-73 states the law it implies), and the **FD-61 counter-look is closed kept** (LINE stands, and FD-74 widens the roster around it). The provider-door facts inside FD-74 were verified against live provider documentation on 2026-08-23, not recalled. A follow-up sweep of every remaining founder-open marker found none further that fail the filter: OR-28/OR-29 (marketplace governance), OR-40 (the pilot cohort), and OR-42 (seat structure, held open at his word this sitting) are all legitimately his.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-73** | **The locale-invariance law: every language behaves identically, by construction.** FD-57's verification is one algorithm with per-locale **data** — a locale enters by shipping its two tables (rendering + semantic-component tokens), never by a locale-specific code path; a builder needing a special case for one language has found a table gap, not a code site. His words: "I don't expect variance. That just seems like a problem waiting to happen" — the contract is what makes the variance impossible rather than merely discouraged | `engine/SPEC.md §2` (the invariance sentence in the FD-57 block) |
| **FD-74** | **The channel roster is open-ended behind the one seam, and a skip is never silent.** Every mainstream messaging channel joins the first release behind the identical seam shape unless an **absolute hard point is recorded in writing** at its row — the review's LINE finding (a ruled channel silently absent from the bar) made law. Door states verified 2026-08-23: **WhatsApp joins** (appointment reminders are the utility-template class its business API exists for; Meta verification, template pre-approval, and opt-in are paperwork, not hard points) · **WeChat: recorded hard point** (an overseas Official Account carries no API; a mainland-China entity is required) · **Instagram: recorded hard point** (the API cannot initiate a conversation — reply-only inside a user-opened 24-hour window; a reminder structurally cannot ride it). Each hard point names its re-check condition. LINE (FD-61) unchanged as the named first | `app/SPEC.md §6` (the roster + hard-point records) · §9 (the re-cut non-goal) · `app/SCENARIOS.md` D8 · `app/BUILD.md` Step 6 · `deployment/egress-allowlist.md` |
| **FD-75** | **OR-41 is closed by the founder's own filter: the mid-series rate binding is the owner's choice, asked when it first exists.** "The harness should be asking the user how they want to take care of this problem." A rate edit on a standing series with not-yet-materialized instances is incomplete until the choice is stored — it writes the engine's `PendingDecision` naming exactly two options (keep the founding rate / re-rate from the edit forward), the harness routes it, the stored answer governs that series, and **no default ships**; until answered, the prior rate governs and nothing re-rates silently. Materialized instances stay bound (§1.5) either way | `engine/SPEC.md §3` (the pricing row's closure) · `engine/SCENARIOS.md` P9 · `engine/BUILD.md` Step 7 · `user-stories/…/Situation-A/situation-5.md` (the pointer) · `AGENTS.md` (the OR count) |
| **FD-76** | **FD-51's vacuous "unexpired" bound is replaced, for the class it protects, by a lifecycle-anchored dormancy bound.** Conversion by channel verification alone covers tokens with a linked commitment **open or ended within twelve months** — the end date *as edited*, so the clock runs from the commitment's life, never the send, and the live conversion funnel (his stated ~50% target) never meets the bound. Staler tokens join the claim only through the **board owner's confirm** (a harness ask naming the claimed party — the one narrowing of FD-46's owners-not-consulted, stale slice only, opt-in recovery). The line matches the attack: an address is recycled only after long idleness, so the bound bites exactly where a recycled inbox can exist. His anchor — "an event always has an end date while it can be edited and moved" — is the mechanism | `security/SPEC.md §2` (the bound) · `security/SCENARIOS.md` N6 (the bite) |

## FD-77–FD-79 — the ratification sitting, 2026-08-23 (the `FD#` series)

*Same series, same rules. These three were agreed in a **separate session the same day** and then sat unlanded in gitignored working material (`.sisyphus/plans/annna-readiness-remediation.md` and its rulings annex) while this registry ended at FD-76 and `AGENTS.md` still counted OR-40 open — the corpus and a live plan disagreeing about a closed ruling. Landed 2026-08-23 at the founder's word ("fix both points"), from the pre-staged text that session wrote for the purpose. **Provenance is mixed and FD-79 says so in its own row:** the answers are the founder's and quoted verbatim, but they were confirmed under **shown-summary delegation** rather than line-by-line review — stronger than FD-28's never-seen pattern, weaker than a read sitting. Each is reversible by one counter-ruling.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-77** | **OR-40 closes: the pilot cohort is the founder's own English students**, interacting as guest parties through tokenized links — never accounts. Recruitment is readiness-worded (FR2): it proceeds when the deployed first-phase slice is green and the founder's own live teaching week runs on annnä with student bookings flowing through it. *(Founder answered live 2026-08-23: "build this product and recruit students and have at least 10 users." **The date is the part FR2 bars, and no tracked file carries it** — it lives in gitignored working material only. The cohort size survives here as quoted provenance, not as a corpus commitment: it is a target, not a schedule, and nothing builds against it.)* | `PR/BRIEF.md` (the closure note at OR-40's definition) · `AGENTS.md` §Rulings (the open count, now three) |
| **FD-78** | **The first phase narrows to Situation A alone** — amending FD-50: *"Situation A plus the freelancer half of Situation C"* becomes **Situation A only**; the freelance-instructor half moves to the following phase alongside B's deferred forks. **FR10's specification scope (A/B/C/D) is untouched** — narrowing lives in this entry and in the `[r1]`→`[r2]` reclassification it triggers, nowhere else *(founder, same sitting: the entire vision stays intact, the first cut covers only the English-students situation)*. **The reclassification is owed, not done here:** the freelancer-half-C rows this narrows are identified by FD-50's own deferral classes, and `release-tags.mjs` must end green over the result *(**amended 2026-08-24, FD-80:** the reclassification is not owed. Tags as a ship-slice are retired. This entry misread FR10.)* | `PRD.md` §6.5 (FD-80; this row is history) |
| **FD-79** | **The ratification sitting, and the delegation that produced it.** Eight delegated rulings (FD-26, FD-46/51/76, FD-50 as amended by FD-78, FD-57, FD-60, FD-61/74, FD-71, FD-72) plus FD-63 were presented in plain language and confirmed under **shown-summary delegation** — the founder's words recorded verbatim: *"After I have the 10 users I don't feel like reading your other stuff… I'm just going to say okay."* **Four drafted-not-ratified markers are dispositioned by this ruling:** the buffer-gap binding **ratified** · the shared-token lifetime **ratified** · named-terms storage **ratified** (aligned to `harness/SPEC.md §7`'s mechanism) · the marketplace ranked guest-language member **conditionally struck** pending FR15 verification — strike if it conflicts, ratify in place if it governs a distinct object *(**verified at landing, 2026-08-23: ratified in place.** FR15's home now states the ask as "ranked, their own working language first" and names the catalog surface as what the ranking exists to serve, so the drafted premise has been adopted at the home rather than contradicted by it; and FR32's ownerless catalog is an object FR15 explicitly does not reach)*. **A fifth marker moved with the third:** `harness/SPEC.md §7`'s document-derived-authorization marker, because the security copy the sitting dispositioned calls itself that section's restatement, and a ratified restatement over a drafted home inverts FR13. The `held`-mark trigger deferral is **re-confirmed**, not dispositioned: writes stay refused until Situation B's sprint rules it. **And FD-63 is amended:** dogfood and pilot are the same run, not sequential | `engine/SPEC.md §3` (buffer row) · `security/SPEC.md` (shared-token lifetime; named-terms storage) · `marketplace/SPEC.md §6` (the conditional) · `PR/BRIEF.md` (FD-63's amendment) |

## FD-80 — Situations are never a ship unit, 2026-08-24 (the `FD#` series)

*Founder-originated, answered live while contemplating the retired `CLARIFY.md` §10. Each of the five package numbers was put to him in plain language; he confirmed or amended. This is the FD-18/FD-19 class: shown choices taken, not a delegation.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-80** | **What ships is the application. A Situation is never a ship unit.** The app is the commitment harness **and** a template builder. Situations exist so design already knows the types of problems it must solve: a person saying “this is my situation; I want to build the template that lets me do this work.” TDD is that the app can do all the situations (FR10: A–D specified; E held out). FD-50 and FD-78 misread FR10 when they named a use case as a release slice. His teaching as first live test (FD-68 / FD-77) is **proof order**, not a feature cut. **One list:** `[r1]` / `[r2]` as a ship-slice are retired — not kept under a new meaning. Postponements that are real stay postponed by their own ruling (FR1, recorded hard points, held-out E, closed-service money, unruled `held`), not by a tag. The stamps and `release-tags.mjs` remain until a follow-on commit removes them; they are leftover, not law. *(annotation 2026-08-26: the follow-on landed — stamps stripped from all 358 rows and the gate deleted)* The dive-center week is the strongest **proof**, not a SKU. The Situation × layer map stays working material until promoted to one tracked coverage index | `PRD.md` §6.5 (the home) · `README.md` §One week, run twice · FD-50 / FD-78 rows (annotated, not rewritten) |

## FD-81 — packed default, hour-grid toggle, 2026-08-24 (the `FD#` series)

*Founder-originated. He confirmed the retired `CLARIFY.md` §9.1 answers live: packed stays the default; a toggle offers a traditional hour-grid; that mode allows drag-to-move.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-81** | **FD-23’s “no time axis, ever” becomes the default mode, not the only mode.** A toggle offers a traditional hour-grid (Y-axis = clock) as a **board style**, same wallpaper, same skin, same glass. Packed default stays conversation-first. **Hour-grid allows drag-to-move**; packed mode does not. `board_mode` joins the `display_settings` closed set (`packed` default · `hour-grid`). **Residue, not in this ruling:** where undated, deadline-only, and all-day work sit on a clock. Do not build the grid or the gesture until that residue is ruled | `app/DESIGN.md` §Board rendering (the home) · `app/SPEC.md` §7 · `app/SCENARIOS.md` C12 · FD-23 (annotated) |

## FD-82 — owner-publish, 2026-08-24 (the `FD#` series)

*Founder-originated. He amended the retired `CLARIFY.md` §9.2 split the same sitting: anybody can publish a template; this sitting is functionality, not gatekeeping. Authorization gates may sit on top later.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-82** | **Anybody can publish a template.** A signed-in owner authors through the console (“I want to create a template; these are the things it needs to be able to do”), customizes, **saves for themselves**, iterates, and at some point **presses publish** — the listing is public, with detailed information on what it is and what it does. Save is not publish. This sitting does not gate who may publish; worry about the user being able to do this. **Future:** authorization gates (vouched “official” class, OR-28; terms, OR-29) may sit on top — do not invent them here. **Unchanged:** developers never author domain templates (FR38 authorship); a published bundle is an authored, data-free shape, never an extract of a populated board; skins stay the admin pack pipeline; no Stripe. **Amended:** blanket admin-only *publish* for templates | `marketplace/SPEC.md` §0 / §2 (the home) · `marketplace/SCENARIOS.md` P2/P3/P4 · FR38 / FD-1 / FD-72 (annotated) |

## FD-83–FD-86 — the dsh gap-analysis landing, 2026-08-28 (the `FD#` series)

*Every row below is **PROPOSED, not ratified**. They were drafted 2026-08-28 by a session landing sixteen founder-approved items from an external gap analysis of this layer against a public coding harness; the founder approved the **items**, and the rulings' wording is drafted on his behalf under the drafted-not-ratified marker convention (`AGENTS.md` §Rulings). Each spec section carrying one wears that marker until he rules. FD-84 and FD-86 are deliberately kept **separate** so the firing-log cluster and the turn-flow driver can be ratified independently — the second is the larger commitment of the two. Read each row's reasoning as a proposal; read its presence in the spec as provisional.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-83 (PROPOSED)** | **The loop core is built domain-blind.** The firing lifecycle, the budget/park machinery, the basis-spend machinery and the context-assembly mechanics are built as modules that **import no commitment vocabulary** — none of the commitment domain's own objects (`harness/SPEC.md §3.1`–`§3.8` and `§3.12`); the core works over opaque subject references the domain pack resolves. **The loop's own bookkeeping objects are explicitly not domain vocabulary** — `Escalation`/`OnCall`, `PendingAsk` and the `FiringEvent` are what the loop is about, and a rule banning them would ban the loop. The domain enters the built harness only through **the tool families and the policy tables the loop core consumes**, `§4a`'s dispatch tables included: the core walks a routed sequence, the tables carry the domain. External precedent named at the home (DeepSeek Harness, MIT, checked at source). **Scope:** code module structure at build time only — `harness/SPEC.md §1`'s four-layer table and the harness's ownership of §3/§6/§7 are untouched | `harness/BUILD.md` §Guardrails (the home) · pointers at Step 1 and Step 2 |
| **FD-84 (PROPOSED)** | **The firing log — the `FiringEvent` stream, its latched two-write persistence, its required-on-read law, the replay invariant, snapshot custody, and the streaming decision.** Exactly one `FiringEvent` per firing id, written in at most two write-once parts (**birth** inside the firing's first work commit; **seal** carrying the closed termination member, composed only once the termination is settled), each identified by its **(firing id, part)** part identity, neither ever mutated, an unsealed record read as `interrupted` and never stored as one. Persisted through the existing `commit` verb — no sixth seam verb, no seventh trigger source; neither part is a member of K1/X4's counted work-commit family. **Required-on-read, fail-closed:** a reader meeting an unknown member refuses the whole read, the deliberate reverse of the domain store's additive-only-with-upcast law, because a skipped event in an audit trail reads as "nothing happened". **Replay invariant:** a recorded firing's assembled context is re-derivable byte-for-byte from the log plus the store. **Snapshot custody:** recorded real-model firings live in-repo and committed; re-recording is a deliberate keyed act, never a side effect of a green build. **Streaming — the founder rules with both options visible.** *Drafted answer:* the v1 refusal — the console renders complete narrations and the log stores final `narrate` text only, chunk-level streaming being a later log-vocabulary change under the required-on-read law. *The alternative, stated so it is a choice:* chunk-level log members from the start (chunks logged, the final message derived). It is ruled now because required-on-read makes the log vocabulary the expensive thing to change later, and it is flagged because `app/NOTES.md` records perceived write-path latency as an **open finding** — cited here as that finding, never as a founder want | `harness/SPEC.md §3.14` (the home) · `engine/SPEC.md §1.16` / §5 item 7 · `harness/INTERFACES.md §1.1` · `security/SPEC.md §14` · `model/BUILD.md` (custody) · `harness/SCENARIOS.md` K1/X4 amended + five new rows |
| **FD-85 (PROPOSED)** | **The effective-policy dump is the fifth display projection.** A read-only view of the complete effective policy governing one board — grants, rules at all covering scopes with their authority level and version-of-record fields, stored answers, elicited thresholds, on-call configuration — **fenced by construction exactly as its four siblings are** (no address, no vault artifact, no token digest; cross-tenant fenced at the granted rung) and **deterministic** (same store state, identical payload). **Consumption is decided: app-subscription only, never a `calculate` member** — its sole consumer is an app read view with no harness logic, no firing reads it, and widening two closed enumerations for a read the loop never makes would be its own spec change. FD-34's precedent; the seam lock's "a fifth is a spec change with its own ruling" is the sentence this answers, and it stays true for a sixth | `engine/SPEC.md §1.7c` (the home) · §0's carve list · `engine/INTERFACES.md §2a` · `app/SPEC.md §7` |
| **FD-86 (PROPOSED)** | **Turn flow — harness-planned deterministic dispatch, and per-owner firing serialization.** The model produces intent **once**, through `normalize`'s closed intent vocabulary; the **harness** then routes intent → tool sequence **deterministically** (FD-28's trigger-firing law generalized to the console turn); **the model never chooses the next call**, and tool returns are consumed by the harness, reaching the model only as stored structure through `narrate`/read-back. **No fifth model-seam call and no `tool_schemas` context member** — the four-call pin stands and the model is never shown the tool surface. **Serialization:** per-owner, one inbox, FIFO claim; all six trigger sources and console turns queue identically at v1, and a firing's slice is assembled **after** claim. **This is the larger of the two carried recommendations** — the model-driven alternative would demand a fifth seam call, a `tool_schemas` member and a rewrite of the thin-agent posture, contradicting FD-28, the four-call pin and FD-16's compounding-error law, which is why it is not carried | `harness/SPEC.md §4a` (the home) · §5's corrected return leg · `harness/INTERFACES.md §2` · `harness/SCENARIOS.md` R-family · `harness/BUILD.md` Step 5 |

## FD-87 — archive/ leaves the tracked tree, 2026-08-29 (the `FD#` series)

*This row was **authored by an agent as a proposal** and carries the drafted-not-ratified marker (`AGENTS.md` §Rulings): the founder accepted that the archive retirement should land, and the number `FD-87` was assigned on that acceptance, but the wording below is drafted on his behalf and is his to ratify or strike. Striking it is not a forward fix — it reverts the commits that removed the tree.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-87 (PROPOSED)** | **`archive/` leaves the tracked tree; its citations resolve to git history.** The fourteen files under `archive/` are removed from the tracked corpus. They are not destroyed: every one is preserved in git at the commit that removed them, and a citation of the form `archive/<name>.md` resolves there. This extends, unchanged, the treatment the corpus already gives `DESIGN:line` and `research/<name>.md:line` — traceability, not required reading; every law is stated in full in-repo, and nothing is lost if a citation does not resolve (`AGENTS.md` §Citation conventions). **What changes:** `archive/` is no longer a location the authority order names, and `INDEX.md` no longer promises a reader a directory a clone does not carry. The `history` **tier** survives — it still grades the `.specs/` transcripts and the Situation-D brief — and `INDEX.md`'s Tier legend becomes its one home. **What does not change:** the design history is intact and reachable, and every live citation into `archive/` stays exactly where it is | `AGENTS.md` §Authority order and §Citation conventions · `INDEX.md` Tier legend |

## FD-88 — `THE-JOB.md` retires on readiness, 2026-08-29 (the `FD#` series)

*The **item** was accepted by the founder at the 2026-08-29 sitting; the row's wording is drafted on his behalf under the drafted-not-ratified marker convention (`AGENTS.md` §Rulings), so it is labelled **PROPOSED** until he rules on the wording. The file carrying it wears the marker too. What it states is a readiness condition, never a schedule (FR2), and it deletes nothing now.*

| # | Ruling | Where it lives |
|---|---|---|
| **FD-88 (PROPOSED)** | **`THE-JOB.md` is kept now and retires on readiness.** It is an index of the moment by its own description — *"an index of the moment, not a home of product law"* — and an index of the moment should say when its moment ends, so that a later session does not have to re-derive the answer. **The condition:** it retires when the go-word has been given and `harness/BUILD.md` Step 1 has opened, at which point it describes a moment that has passed. **It cannot simply go, because one thing in it is law:** despite the disclaimer — which speaks about *product* law — it is currently the sole normative home of the **G0-2** process gate, and `harness/BUILD.md`'s Readiness line cites this file by name for it. At retirement G0-2's normative home moves to `harness/BUILD.md`, the only file that cites it and where a builder is already standing; nothing else in `THE-JOB.md` is law. **Retirement is the founder's act, in one commit:** the file is deleted, its `INDEX.md` row removed, and the two `README.md` references — the Status paragraph's session-job sentence and the map row — re-pointed or removed, with the tracked-markdown count in `AGENTS.md` and `README.md` set to the **then-current** tracked count, never to a number written in advance, because sibling passes may land first. **The alternative, stated so it stays a choice:** keep it permanently and re-purpose it at the go-word as the *build*-phase job — the same index-of-the-moment role pointed at the next moment, costing its standing bytes and buying a front-door pointer that never dangles | `THE-JOB.md` §When this file retires (the condition, wearing the drafted-not-ratified marker) · `harness/BUILD.md` Readiness (G0-2's home **after** retirement — unmoved until then) · `INDEX.md` (the row) |

## Provenance — which decisions were the founder's, and which were drafted

*Added 2026-08-06. This section exists because a ruling label carries authority, and a reader cannot otherwise tell an answered question from a drafted one. Recording it is cheaper than reconstructing it later, and impossible to reconstruct once the working sessions are gone.*

**Founder-originated.** FR1–FR13, FR-A/FR-B/FR-C, FR14–FR18, FR19–FR22, FR23–FR25 and FR26–FR38 above are all **answers the founder gave**. Each closed a question that was put to him directly. The registry entries above record the answers, not proposals. **FD-18 and FD-19 likewise** — both were put to him as a two-option choice with the tension stated and a recommendation named, and he took the recommendation in each case. **FD-20 to FD-23 likewise, and more directly than most**: each was decided by the founder looking at a built comparison and saying which one, in his own words, quoted in each entry. The same sitting's *unratified* calls are listed under it, separately and by name. *(The `FD#` series before them is not annotated here either way; this note records what is known rather than backfilling what is not.)* **FD-24 to FD-27 were put as two-option choices and he took each recommendation. FD-28 to FD-37 were made under an explicit standing delegation, not shown to him individually first** — their sitting says so in its own preamble, and that difference is the reason it does. **FD-38–FD-41 and FD-43 are founder rulings made by eye against built comparisons; FD-42 is a founder principle with a delegated mechanism; the visual sitting's delegated resolutions are listed in its own preamble. FD-44 is a founder ruling made by eye at the 10-face sampler, his words quoted. FD-45–FD-49 were made under a same-sitting delegation of shown recommendations ("fold your recommended answers in") — their preamble says so, and each is reversible by one counter-ruling. FD-50–FD-67 were made under the widest delegation yet — the founder declared himself uninvolved and instructed "do as much as you can without me" against a written brief of recommendations he did not individually read; their preamble says so, one brief item was deliberately not folded (the export-ban reopen), and each is reversible by one counter-ruling. FD-68 is the counter-example that proves the register still works: a founder answer in his own reasoning, reversing a session recommendation the same day. FD-69 rides the FD-50–67 standing delegation — its sitting's preamble says so, and it is reversible by one counter-ruling. FD-70 rides the same delegation, executing a fork-brief recommendation the fork sitting recorded and deliberately deferred to its own sitting; it annotates a founder ruling (FD-22) without erasing it, and one counter-ruling restores the open predicate. FD-71 and FD-72 are founder answers given live at the vision sitting (2026-08-22) — three questions with recommendations, each answered in his own pick; their preamble says so. FD-73–FD-76 are founder answers given live at the channels-and-asks sitting (2026-08-23) — the review's five triage items walked through with recommendations; two he closed by applying his own standing filter (OR-41 and the event-wake became the system's questions to its users), and their preamble says so. FD-80 is a founder answer given live on 2026-08-24 while contemplating the retired `CLARIFY.md` §10 — five shown choices, each taken or amended; its preamble says so. FD-81 is a founder answer given live the same day — packed default, hour-grid toggle, drag on the clock; its preamble says so. FD-82 is a founder answer given live the same day — anybody can publish a template; this sitting is functionality, not gatekeeping; its preamble says so.**

**Drafted on the founder's behalf, and not separately ratified.** A repair pass on 2026-08-06 authored **thirteen design decisions** directly into spec text, because the instruction was to complete the plan rather than stop midway. They are real architecture calls, not wording fixes, and they carry no ruling label of their own. The four with the largest blast radius are named here; the rest are diffused through the same pass:

| Decision | Where it lives |
|---|---|
| **The cross-owner bind handshake** — the corpus's largest hole, drafted as paired commitments with an engine-minted `BindProposal`. **Superseded 2026-08-07: FR26–FR29 redrew this seam** (one commitment, grants, creator-set holds); the no-seam-verb stance survived into the ruling, the pair-write did not | `engine/SPEC.md §7.1` (now founder-ruled) |
| **F7 → `KindTemplate`** — a saved course is authoring-time vocabulary that *expands into* `order` + `depends_on`, so §1.8's "no third mechanism" survives. **Scoped 2026-08-07 (FR37): no ratification owed** — the mechanism stands; the course content is user-built proof | `engine/SPEC.md §1.12` |
| **F20 → `min-occupancy`** — the menu's only rule evaluated at a clock trigger, whose violation is an owner decision rather than a refusal. **Scoped 2026-08-07 (FR37): no ratification owed** — the mechanism stands; the boat is user-built proof | `engine/SPEC.md §3` |
| **BYO confinement to attended-only** — the load-bearing reason is *qualification*, not session mechanics: a user's own model is an ungraded path to the same seam. **Ratified as written 2026-08-07 (FR31)** | `model/SPEC.md §7` (now founder-ruled) |

**Also applied without prior ratification:** the **`FR#` rename** itself. The plan reserved it for the founder; the repair pass applied it because the collision had become concrete — `R7` meant "Convex ratified" here and "production refuses anonymous access" in `deployment/SCENARIOS.md`. Only founder-ruling citation sites were renamed; scenario IDs are file-local and were untouched. One word reverses it.

**How to read a drafted decision.** Treat its *reasoning* as a proposal and its *presence in the spec* as provisional. Where a drafted decision and a founder ruling disagree, the ruling wins. Where a drafted decision is simply wrong, it can be changed without overturning anything — that is the point of recording this.

---

## Provenance — the working documents beneath this index

Superseded working documents, all **external to this repo** and not published: three reviews → two plans → three comparisons → two combinations → a final plan of record, which is the source this index was built from. Those files are working material and deliberately stayed out; **this file plus the package specs are the corpus's record.** Nothing here depends on reading them.
