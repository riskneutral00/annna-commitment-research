# annnä Harness — SCENARIOS (acceptance suite / TDD substrate)

*Every decision in `SPEC.md`, turned into a pass/fail scenario. These are the harness's acceptance criteria: the harness is "done" when all **[MUST]** scenarios pass against the stubs in `INTERFACES.md`. Format: **Given / When / Then**, with the expected outcome. `[MUST]` = build-gating; `[SHOULD]` = built and tested — still in its BUILD step's Verify set — but not release- or derivation-gating (FD-59's demotion class); `[HELD-OUT]` = deliberately not designed-to, run as an out-of-sample generality check (record result, don't fix the atom to make it pass) — no row carries it since FD-109; `[ENGINE]` = the harness's part is only "hands the engine a representable state," the enforcement is verified later.*

*Write these as executable tests during `BUILD.md`; each build step names the scenarios it must turn green. A story ID a row cites (Matt English's scene, MT, CRUD and HR IDs) names the obligation the row derives from and is never itself a result: the row closes only when its own assertion runs green at the `BUILD.md` step that names it, and a `[HELD-OUT]` result is recorded, never tuned toward (`../user-stories/README.md` §The bar a story passes, item 5).*

---

## A. Harness 1 — board authoring

- **A1 [MUST]** — *Given* an empty board, *when* the owner says "put a dive lesson Thursday 3–4pm," *then* a Commitment is committed with an occupying start+end, `temporal_type = event`, and the app is asked to render it on the **board** surface.
- **A2 [MUST]** — *Given* A1's commitment exists, *when* the owner says "actually make it 3–5," *then* the same commitment is edited (not duplicated) and re-rendered.
- **A3 [MUST]** — *Given* a task ("email the report by Friday"), *then* `temporal_type = task` (end has role `deadline`, no occupying interval) and it completes **by action**, not by the clock.
- **A4 [MUST / poka-yoke]** — *Given* a board at capacity 1 with an active commitment 3–4pm, *when* a second commitment tries to consume it 3–4pm, *then* `commit` returns `conflict` and no double-book exists. `[ENGINE]` for the enforcement; harness must surface the conflict, not swallow it.
- **A5 [MUST / tasks reserve nothing]** — *Given* a start-only task at 19:00 (no end), *then* availability over that time is **unchanged** (a marker, not a reservation); *given* the same entry as an event 19:00–19:30 plus a 5-min buffer rule, *then* availability splits around 18:55–19:35. Same substrate, no extra machinery.
- **A6 [MUST / hand-edit transits the floor]** — *Given* the owner edits a commitment **by hand** on the board (direct manipulation, not an utterance — the app riser's *"edit by hand or by talking,"* `../app/SPEC.md §1` "Everything else is a riser"), *then* the write still transits `CRUD_Commitment` under the floor: a reversible field edit runs at full autonomy, and a hand-edit that would cross the line (e.g. changing a confirmed booking's counterparty-facing time) is gated **exactly as the utterance path is**. The app collects the edit and hands it to the harness untouched; the floor's applicability does not depend on whether the change arrived by hand or by talking. **Field/value controls (`SPEC.md §3.3`):** through both hand and console inputs, clear and restore Notes on the selected personal Event; the field, date/time/place, latches and earlier attributed clear survive. Fill then clear a booking-specific Meeting link, preserving the template blank and a sibling booking. No definition-branch question or implicit default refill occurs. A calculated total rejects direct overwrite; an accepted supported rate/duration input asks the engine for a new Handle. Unsupported conditional pricing remains a limitation. Step 2 checks typed routing payloads only, Step 3 the existing floor, Step 4 the draft/ask transitions; the composed stored behavior remains at the existing swaps. **Shared-edit sequence (`INTERFACES.md §3.1`):** edit a form field, submit console text about that same draft, then reverse the order; each sees the latest proposed value and stable target/use/slot, not the older saved row. A concurrent old revision or action for a different selected use changes neither draft. Submitted source spans survive; rendering and changing an unsent example emit no input or write. This draft sequence closes at Step 4, with L6/B11's actual assembly and reload at Step 5. **Contact edit controls (`SPEC.md §3.12`; S08b/S10, MT-11/46/63/74/79, CRUD-13/14/25):** use the same selected Kai party/LINE slot and expected revision from hand and console. Correct a typo, then clear/restore that slot and retire a different contact; assert identity, other addresses and prior attributed versions survive, with no quota reset or transferred token authority. Remove Student, retire/reactivate the directory association and remove a group member in separate acts: each changes only the named association, not bookings or independent contacts. Read actual preferred-channel/delivery/request/link/group/future-booking references, show only authorized facts and request any required user resolution; an unresolved dependency, concurrent reference or stale revision refuses without partial writes. Current roster readback follows the same slot; booked terms/history stay bound. Explicitly associate Georgina’s second-app value to the selected existing Georgina through OK; a same-name stranger remains separate and an unqualified handle asks platform. Owner input/evidence never verifies a person or creates a delivery grant: injected verified_at and foreign/wrong-value evidence cannot stamp verification, and correcting an address does not carry forward evidence for the old value. Step 2 checks the diff/read carriers, Step 4 the choice/acceptance transitions and Step 5 their stored composition; real transaction races remain engine A4 at its Step 5.
- **A7 [MUST / tool return validated]** — *Given* a tool returns a value that does **not** match its declared return schema, *then* the harness surfaces a **structured error** and does not pass the malformed return to the model. Separately, an unknown `calculate` query member (including an attempted subscription-only owner-board or effective-policy query) returns the existing `invalid` / `schema-mismatch` arm under `INTERFACES.md §1.1`. For each enumerated stored read, assert its declared snapshot is representable through the seam, with only the authorized fields and consumer access from that home; computed reads retain the opaque Handle and its display facet. A snapshot or display value cannot fill a correctness-critical write position. Assert no raw or unvalidated return is ever consumed — the return leg is validated exactly as the call leg is typed (`SPEC.md §5`), **before the harness consumes it** (`SPEC.md §4a`: tool returns are the harness's, and the model meets results only as stored structure through `narrate`) *(consumer corrected 2026-08-28; the gate and this ID are unchanged)*.

## B. Buffers, rules, elicitation basics

- **B1 [MUST]** — *Given* the owner says "leave 5 minutes between teaching sessions," *when* processed, *then* the agent **proposes a rule with a scope** ("apply to all your teaching sessions?"), and on accept a `Rule{type: buffer, operand: 5min, target: kind, scope}` is written. (The first sentence of use case B must produce a *write*, not just chat.) **Explicit Routine controls (`SPEC.md §6`; Matt S03/S05/S05a, MT-24/33/70, HR-01/02/28):** the unsent example causes no call. Send the edited Run weekdays (Monday/Tuesday/Thursday/Friday/Saturday), 06:00 for 45 minutes with before 10/after 15: propose “Is this a routine?” once with scoped OK/Edit. OK persists the accepted Pattern intent through ordinary validation; Edit/disagreement preserves the input without creating either a substitute Event or a Routine. For Tuesday/Thursday every other week, retain interval 2 and ask for an absent anchor; a supplied anchor survives. Read back Park/Driving as usual settings separately from Sarah's origin, 05:30 departure and total before-buffer 30 on Monday 12 October 2026 Asia/Taipei; saving Sarah's place does not alter Home or the scope. The final Save targets the selected occurrence/generation. An unresolved “remove this run” offers one occurrence versus stop-from-date; the accepted proposal names the exact target or checked future set, including exceptions/dependencies and stateful decisions beyond the horizon, under engine §1.4. Refused or pending resolutions do not become a completed stop. Step 4 checks scripted calls/readback; composed persistence and recovery remain Step 5. **Travel acceptance at Steps 4/5 (`SPEC.md §6`, engine §5; MT-07/22/30, HR-04):** use the scripted engine Handle for Sarah→Park/Driving/12 October 2026/arrive 06:00/estimated 30/depart 05:30. Spy that narration reads the facet and proposes 30 total replacing 10, never performs arithmetic or authors display literals. OK targets that proposal/draft revision and changes only the draft; final Save emits its reviewed scope and Handle. Change each meaningful input, source freshness or occurrence generation before OK or Save; the old acceptance cannot authorize the new draft, and unknown/unsupported results produce no fabricated estimate. Reuse unchanged scope answers without repeating them, retain the usual Park/Driving and Home/10 settings, and cancel with no occurrence write.
- **B2 [MUST / ask-once]** — *Given* B1's rule is stored, *when* the owner books another teaching session, *then* the agent does **not** re-ask about the buffer (a covering-scope answer already exists). **Routine scope reuse (MT-04/25):** a later firing reads the confirmed Routine and accepted usual/datetime scope from stored structure and does not repeat the settled type question. A lost first response retries the same write id and reads the one intended series, never re-creates it. An edit of an existing occurrence stays an edit. Paired inputs “I answered every email”, quoted tutorial text, “I don't run every Monday”, and a past-tense account of last month's runs create no recurrence merely from “every”; a genuinely new repeat request still receives B1's confirmation. A changed/ambiguous scope asks the new question instead of extending a prior answer. The notice-repetition/self-improvement setting is irrelevant to this explicitly requested recurrence. These controls run first with stored/scripted fixtures at Step 4 and again through context assembly at Step 5. **Contacts reuse (S08/S08b/S09/S10, MT-10/41/81, HR-16/29; §3.12):** start with Mark’s acknowledged Student contact; console Contacts filtered to Students selects that exact identity. Collect Georgina, Kai and Jasmine with their declared Messenger/LINE/Instagram values in resolved submissions. Each add persists automatically once, with no extra save-contact question; before acknowledgement render pending, afterwards its own saved result. Exactly four distinct party refs appear in console and direct browse, not four duplicate Student objects. Reuse Georgina for an already-agreed lesson without forging guest agreement or sending. The template definition’s participant defaults stay blank and another use stays independent. Repeat with a different relationship label/platform/person set to assert generality; settled explicit association/channel choices are reused, ambiguous matches still ask. Step 1 checks stored-shape/filter fixtures only; Steps 4/5 retain the elicitation/context split above. Group construction and full lesson/provider proof remain at their existing owners.
- **B3 [MUST / gap-4]** — *Given* an utterance the model returns with `ambiguities` non-empty, *then* the agent asks a single clarifying question and does not guess.
- **B4 [MUST / reversible → act]** — *Given* a reversible, inferable action (create a draft, move an internal block), *then* the agent acts without asking (the floor is the act/ask line).
- **B5 [MUST / quota]** — *Given* the owner says "no student can book more than 10 hours a month," *then* the agent proposes with scope and writes `Rule{type: quota, operand: 10h/month, scope: per-customer}`; *when* a booking would take a student to 11 hours, *then* the engine-stub verdict (refuse) is **surfaced at the picker**, not swallowed — the hour is refused before it can be booked. **Restriction readback control (`SPEC.md §6`):** keep the original quota case, then use the Matt S07 duration/window/buffer proposal from B9. Assert raw instruction → engine-typed operand → stored readback, never success inferred from a populated form field. A stub returning a stored rule with a dropped endpoint, step, anchor, before/after effect or different kind scope must surface a mismatch; a correctly stored rule is reused without re-asking. This verifies the harness’s handling, not the stub’s scheduling math. **Participant-quota choice arms (`SPEC.md §6`; `../engine/SPEC.md §3`'s participant-quota block; Matt S07/S14b, HR-22, MT-52/55)** *(added 2026-09-11)*: from “Monthly occurrences 1–4” and “Maximum time 5 hours”, assert the agent proposes **two** rules — one count-denominated, one time-denominated — and reads both back as independent limits, never one row and never one satisfied because the other has room. Assert the counted identity and period are confirmed explicitly and stored: the confirmed student, the named offering scope, and the month the lesson takes place in on the owner's calendar zone; a stub readback that has silently rescoped the count, dropped a denomination or substituted a link-creation period must surface a mismatch. Assert that a second link, a different contact channel and a second template use of the same scope are **not** re-asked and reset nothing, while an unlinked alias is narrated as the identity limitation. Then drive a pre-start cancellation with **no stored answer**: the agent surfaces the engine's two named options — release the count and booked time, or keep them counted — records the chosen one, and reads it back beside the limits; a second cancellation at the same stage is **not** re-asked, while the at-or-after-start and marked-`no-show` stages remain unanswered and are narrated as open. Assert the agent authors no option, ships no universal release or no-show default, and never reports an unanswered stage as settled policy. This verifies the harness's elicitation, storage and readback, not the stub's quota arithmetic.
- **B6 [MUST / the noticed-pattern offer]** — *Given* the owner's self-improvement setting is on **and its repetition threshold set to the fixture's value** (§6 — the threshold is a stored parameter, not a constant), *when* the owner hand-sets the same field to the same value enough times to cross that stored threshold on a console turn, *then* the agent proposes — **inline on that turn** — a Rule at a named scope, showing the current value and the proposed one, and on accept an **ordinary Rule** is written with no special marking. The stored result is indistinguishable from the same rule elicited by B1.
- **B7 [MUST / absence: the offer cannot escape its box]** — assert, against the tool contract and the assembled-context spy, all of the following are **absent**, not merely unused: (1) **no** noticed-pattern proposal is ever produced by a trigger firing — only by a console turn; (2) **no** proposal of type Grant or Exception is ever produced by this path; (3) the detection **never appears in the assembled context** handed to the model; (4) with a slice containing five `guest`-tagged repetitions and zero `owner`-tagged ones, **no** offer is produced; (5) with the owner's self-improvement setting absent, **nothing detects and nothing is offered**. Mirror of the D9/N2 absence style: assert the affordance does not exist.
- **B8 [MUST / a decline is remembered]** — *Given* the owner declines a noticed-pattern proposal, *then* a `PatternDecline` is stored (`SPEC.md §3.10`, persisted through the existing `commit` verb — assert **no new seam verb** appears in the tool contract) and a re-offer **proposing the same value** is not offered again — assert its absence on a subsequent qualifying turn. *Given* a later offer at the same `pattern_key` **proposing a different value** (a new `proposed_value_hash`), *then* it **is** offered. *Given* the owner chose "don't ask me again" (`permanent: true`), *then* **no** proposal on that `pattern_key` is offered for **any** value — assert absence — and the record is **revocable**, after which offers resume. *Given* a narrowed acceptance (G2's scope behaviour), *then* the offer does **not** return on a different instance — the decline, not the scope ladder, is what silences it.

- **B9 [MUST / the duration floor is enforced at the point of booking — and the ceiling]** — *Given* a stored `Rule{type: duration, operand: 30–120 min}` on a kind, *when* a 5-minute grab is attempted, *then* the sub-minimum slot is **unconstructable at the picker** (the engine projection excludes it — surfaced, not swallowed, B5's discipline) and a forced commit refuses naming the rule. *And the ceiling binds the same way (2026-08-22 — the operand's upper bound had no assertion anywhere)*: a 3-hour grab against the same rule is refused at the picker and at a forced commit, naming the rule. *(Situation-A's minimum-duration beat and its 120-minute maximum — the exact sibling of the quota pair Q1/B5, which was covered twice while this had no scenario anywhere.)* **Matt rule round-trip (`SPEC.md` §§3.5/6; `../engine/SPEC.md §3`; `../user-stories/Situations/Situation-A/story-matt.md` S07/S08a/S11/S13c):** accept the raw range “30 minutes to 2 hours”; when the increment is missing ask once, then store the selected **15-minute step with the read-back anchor at 30 minutes**. Surface engine-derived choices **30/45/60/75/90/105/120**, refuse **72** for step and **180** for ceiling, and await new input rather than looping or selecting a replacement. A **5-minute step** fixture accepts **35** but still refuses 72; an explicitly chosen range-only fixture has no invented increment. **30–125/step 15/anchor 30** and **30–120/step 15/anchor 5**, wrong units and missing/ambiguous anchoring elicit correction; no hidden rounding, dropped endpoint or start-time-grid change. Read back “All available times outside of 11 pm to 9 am” as English-kind daily **09:00–23:00**, intersected with actual commitments and **5 minutes before AND 5 minutes after**. The separate **Thursday 15 October 2026, Asia/Taipei, Personal appointment 15:00–16:00**, Home and zero own buffers, preserves the **06:00 Run**. The owner sees the engine’s **14:55** end/**16:05** start separation, without an owner student-page-preview step. Drive P6’s stale selection through scripted engine results: **15:30–16:15**, 180 and 72 refuse; selected **16:05–16:50** has no hold, the owner extends the appointment to **16:15**, and confirmation refuses with no booking/count increment. Keep permitted entered answers, surface the engine’s offered **16:30**, and require a new selection/confirmation showing **16:30–17:15, 45 minutes, TWD1125 at TWD1500/hour**. **16:20** remains the engine-derived earliest allowed post-extension start, never silently substituted for the story’s 16:30 choice. Only the owner’s context includes the private appointment; a guest refusal contains no private reason. Guest rule edits and unrelated/private queries must refuse at the executor even if model output proposes them; the guest-console/context contract is a separate prerequisite, not established by this rule fixture. On a supplied S13c compatible definition-change result, duration/step and existing one-on-one bound terms survive; do not claim conditional-price authoring is implemented. An unsupported restriction or unresolved removal dependency stays a visible limitation, not an enforced rule. MT-75/CRUD-04 controls retain the rule’s subject and quota usage across presentation/label edits; removing a classification with rule dependencies needs explicit resolution, never silent release. Step 4 proves authoring/readback/refusal handling against stubs; real engine agreement and the full guest journey remain the existing swaps and story proof. **Complete operand carrier:** B5’s stored readback and F3b’s ordinary-pair refusal retain the same single duration aggregate entry and both source refs for 30–120/15/30 minutes versus a 20-minute window (`../engine/SPEC.md` §§3/8); no range-only fallback loses a selected step/anchor. The legacy range fixture remains distinct and valid.

- **B10 [MUST / import is an attended pull, quarantined]** — *Given* the owner asks to bring their connected calendar's updates in, *then* the turn calls `import_fetch` (`SPEC.md §5.2`, FD-49) and walks the returned items as **ordinary propose→confirm writes** — with a **defensible** interpretation applied automatically (FD-103: the walk is confirm-of-the-write, never a per-item interpretation gauntlet; review routing carries confidence) — nothing auto-commits, nothing fires outward — with every free-text string tagged `import` on the wire (L4's law; layer-2 scoping per L5). *Assert structurally:* `handleTrigger` has **no path** to `import_fetch` — the seven trigger sources dispatch into a tool set that does not contain it (FR12's no-background-poll, made structural rather than promised) — and a provider `unavailable | timeout` surfaces as a gap on the attended turn, never a retry loop (`../security/SCENARIOS.md` T8's app half is `../app/SCENARIOS.md` I4).
- **B11 [MUST / the answer lands on the question]** *(added 2026-08-22 — `answer.provide` was an intent the suite never exercised, because nothing stored the question; `SPEC.md §3.13`.)* — *Given* a turn that ended by asking (a stored `PendingAsk` with a `proposed` value), *when* a later firing carries the owner's answer, *then* the assembled context's `pending_asks[]` names the open ask (stored structure — assert **no conversation** was carried, §8's law), the answer lands on that ask by `id`, the proposed write applies through the ordinary path with ordinary attribution, and `answered {by, at}` stamps. *And the negative half:* an answer arriving after `expires_at` applies **nothing** — the ask is dead, the reply surfaces as a fresh utterance, and re-raising is a new ask; an answer with no open ask in the slice is ordinary input, never a guessed match to a long-gone question. *And the firing half (2026-08-31):* the expiry itself arrives as the ask-age-out registration's `clock` firing carrying `registration_kind: ask-age-out` — registered by the `PendingAsk` persist (`SPEC.md §3.13`'s writer), firing **exactly once** on the virtual clock; the dead-ask disposition above is what that firing finds. *And the durable-ask request-release half:* persist a `PendingAsk` and its terminal record, withhold the human answer, and assert the active cloud request and claim end while the ask remains readable; a later valid answer uses the stored ask, an expired or unauthorized answer cannot supply authority. *Restart/permission control:* persist a floor ask with its complete `pending_act`, terminate the process, then answer that exact id from a fresh authenticated console firing. Assert the same typed operation/arguments are recovered and rechecked; replay the answer and apply no duplicate work. A wrong-owner, expired or changed-operation answer cannot mint a Grant or dispatch. This full restart/context assertion closes at Step 5 checkpoint (2), not the Step-3 floor fixture. **Shared-draft controls (`INTERFACES.md §3.1`):** persist the proposal snapshot, accept into the draft and reload with no transcript. Reconstruct the exact accepted values/scope and still-open questions; do not re-ask settled answers, including explicit Leave blank. Deliver an old OK after date/origin/value or definition/use revision changes, an expired Handle, another ask id and a duplicate answer: none accepts different content or applies twice. A finite Online choice is one submitted answer with no second Send. Picker Save changes the same draft; proposal OK records acceptance; final Save revalidates the accepted current revision through ordinary diffs/Handles. Reject stale Save. Abandon the unapplied parent after a separately committed place or contact fixture: parent writes nothing, existing rule/booking stays intact, and readback identifies the surviving independent effect. Pending or unknown write outcome must recover by its original id before a discard claim; do not undo an independently accepted booking move. An ephemeral discarded draft is not revived by an old accepted ask. Authoring-resume uses its retained structured candidate and revalidates before apply. **Blocked-candidate resume arm** *(added 2026-09-12; `SPEC.md §6`'s blocked-candidate paragraphs)*: leave a candidate blocked by an affected commitment, act elsewhere, then resume it by ref from a fresh context with no transcript. Assert the candidate values and the **already answered definition branch** are restored **from the retained operation record** (`../marketplace/SPEC.md §3`) and **not re-asked**; assert the resume path issues a **fresh `check_consistency`** and that no stored-verdict read exists to issue instead. Assert the stored rule is still shown effective, and the verdict is **recomputed against current records** rather than restored — an OK given against the earlier clearance is rejected as stale, and a blocker that arrived meanwhile is surfaced. Assert a resolution move recorded in between is an ordinary authorized act, that a refused one leaves the candidate blocked, and that discarding the candidate leaves both the stored rule and that accepted move untouched. Local fixtures verify only this generic composition; contact creation, full compatibility/guest behavior and actual provider outcomes keep their assigned gates.

**B5 scope-decision controls.** When a quota edit would split or merge existing draws, the harness surfaces exactly the engine-named **keep existing scope** and **apply reviewed repartition** choices with current/proposed target revisions and the complete affected refs. It carries the owner answer through one ordinary diff: keep-existing leaves retained draws unchanged, apply-reviewed uses the complete assignment, and stale, missing or changed refs refuse the whole candidate. A definition-only edit stays on `PendingAsk`/draft and does not create a `PendingDecision`. **Subject-member control** *(added 2026-09-12; `INTERFACES.md §7.2`'s PendingDecision subject notice)*: the surfaced decision's subject is read from `subject_ref`; a rule-subject decision carries no commitment and the harness supplies none, a historic commitment-backed decision still reads through its retained `commitment` member, and a record carrying both members with different refs or neither is refused whole rather than narrated or repaired. This is a typed read control against the engine stub at Steps 1–2, never engine storage proof. These controls prove carrier and elicitation behavior only; the engine's partition math and the answered outcomes' exact totals stay at their engine gate (`../engine/SCENARIOS.md` Q1's scope arms, `../engine/BUILD.md` Step 5).

## C. Status — latched acts over derived conditions

- **C1 [MUST]** — *Given* a stored 3-day OWD commitment fixture with waiver signed, deposit paid, deps intact and `confirmed_at` already set, *when* the owner authorizes its cancellation a week before through `SPEC.md §7`'s floor, *then* `cancelled_at` is set and `status = cancelled`, while the original `confirmed_at {by, at}` and provenance remain unchanged (`SPEC.md §3.4`). Passing the dates or recording later admissible evidence never derives `active` or `completed`. An attempted re-confirmation, completion transition or latch clear/rewrite refuses; a backdated completion claim cannot outrank the cancellation. This stored fixture needs no order/guest/cascade construction; H7/H8 exercise those later paths at BUILD Step 5 checkpoint (6).
- **C2 [MUST / the un-expire bug]** — *Given* a hold with `expires_at 11:00` and unmet `{signature}`, *when* 11:00 passes (→ `expired_at` set, `status = expired`, unit read free) *and then* the signature lands at 11:30, *then* the evidence is recorded and `status` stays `expired` (`SPEC.md §3.4`). Attempts to confirm, complete, re-hold, clear the expiry or backdate an accept to before 11:00 refuse without changing the original expiry/provenance or acquiring capacity. Repeat with a declined fixture and a late accept: status stays `declined` and `declined_at` remains unchanged. `[ENGINE]` enforces the terminal/latch invariant; evidence retention never authorizes revival.
- **C3 [MUST]** — *Given* preconditions and dependencies all met and start in the future, *when* the owner confirms, *then* `confirmed_at` is set and `status = confirmed` (`SPEC.md §3.4`). With a stored completable-kind fixture, advance to start: `active`; record the authorized completion act: `completed`, with confirmation and completion provenance intact. Repeat with C6's early-return fixture through the write/read path: `actual_end` is stored exactly and no scheduled-end lower bound rejects it. Later competing cancellation/decline/expiry attempts refuse, leaving the completion facts unchanged; repeat with an earlier claimed timestamp to prove input backdating cannot replace the valid terminal outcome. On a separate live future confirmed fixture, with no covering Grant and no live cancellation confirmation, attempt cancellation: the floor refuses it and leaves `confirmed_at` and `status = confirmed` unchanged (`SPEC.md §7`). The earlier booking confirmation supplies no basis for that later act; this control must not pass merely because completion already prohibits cancellation. The fixture uses the existing floor and direct commitment write/read at BUILD Step 5 checkpoint (1), not later order, guest or share machinery. At the same Step 5 write/read gate, execute C5's Matt camera-Task create/read/rename/removal controls through existing tools and §7 authority, retaining its deadline/id and Kai's lesson/reminder. Removal remains distinct from this scenario's explicit completion cases; no taxi service call follows from the linked owner Task in S13f.
- **C4 [MUST]** — *Given* a commitment with an unmet own-precondition AND an unmet dependency and no terminal outcome, *then* the condition set reports both `pending` and `blocked`, and scalar `status = blocked` (`SPEC.md §3.4`). Clearing only the own-precondition leaves `blocked`; clearing only the dependency leaves `pending`. With a terminal fixture, retain both facts but report its terminal status, never revive it from condition changes. Routine fixtures retain the Pattern/occurrence relation, generation, original date/time/zone and accepted type without introducing `routine` into `temporal_type` (`SPEC.md §3.4`); compare the timed Run Event with the due-only camera Task. State/dependency conditions remain those of the individual Commitment, never inherited wholesale from siblings.
- **C5 [MUST]** — *Given* an event whose end has passed with no `actual_end` and no competing terminal outcome, *then* `completed` derives true automatically **for a non-completable kind**, with `status = completed`, even when `confirmed_at` is present (FD-98, `SPEC.md §3.4`). *And the completable half:* a **completable** past its `end` with no tick derives **`review`**, never `completed`, with any confirmation intact, and its `consumes` draw **stays consumed** until `actual_end` is recorded. The recorded return then yields `completed` and frees that draw; elapsed end alone never does. A task remains incomplete until its tick. These are pure stored-fixture derivations at BUILD Step 1; C3 exercises the completion write/read at Step 5. Matt S13b/S13f / MT-71 / CRUD-09 controls: a Task due Thursday 15 October 2026 16:20 Asia/Taipei has a deadline, no fabricated fixed-duration reservation; rename Check camera to Check camera and microphone while retaining id/due time. Removing that selected future Task through the ordinary authorized cancellation path leaves Kai's independent lesson/reminder intact and does not mark the Task completed. The Arrange Jasmine's taxi Task records the owner's work; it cannot book a taxi. Step 1 proves stored type/completion fixtures only; C3's Step 5 write/read path owns the rename/removal composition, with completion tested separately. **No-inference arm** *(added 2026-09-12; `../engine/SPEC.md §1.9`'s **Nothing but an attributed human act establishes one of these facts**, whose harness half is `SPEC.md §3.4`'s declaration sentence)*: on stored fixtures, with a reminder recorded as delivered, the clock advanced past `end`, and the booking's joining link recorded as opened, assert **no** attendance outcome, no `actual_end`, no completion and no money mark derives from any of the three — the completable still reads `review` holding its draw, the non-completable still derives `completed` from its passed end alone, and every participant's attendance still reads `unknown`. Assert it as absence on the stored record, never as a validation message. The rental kind's early-return recovery in C6 is asserted here to be **that kind's declaration**: repeat the same three signals against a non-completable fixture and no recovery behavior appears.
- **C6 [MUST]** — *Given* a confirmed bike rental due Sunday 17:00, with a recorded return **Saturday 10:00** and no competing terminal outcome, *then* `actual_end = Sat 10:00` is representable (no `≥ scheduled end` rejection), `completed = actual_end` and `status = completed`, with the original `confirmed_at` unchanged (`SPEC.md §3.4`). The unit frees at the recorded actual return, not the scheduled one; this proves recorded state, not physical return. Step 1 uses stored fixtures; `../engine/SCENARIOS.md` A10 and `../engine/BUILD.md` Steps 4–5 own availability/commit agreement and `../engine/SPEC.md §6` item 7 still governs corrections that widen the interval.
- **C7 [MUST]** — *Given* "boat must be back by Friday 17:00" (end role `terminal-constraint`), *then* it is **not** typed as an occupy-nothing task; the terminal constraint is honored and the boat is not booked out across Friday afternoon.
- **C8 [MUST / the clocked offer]** — *Given* a job offered to an instructor on Review with `expires_at` set from the offer's creator-set hold (§3.4; `../engine/SPEC.md §7.1`), *when* the window passes unanswered, *then* `expired_at` latches, the lapse fires the trigger loop, and the offer **cascades to the next ranked name**; *when* the first instructor taps "yes" late, *then* the offer stays `expired` — the late yes does not revive it (C2's invariant, applied to a person's answer).

- **C9 [MUST / the park cannot self-clear]** — *Given* a commitment parked by an unattended firing (D4/K2), *when* the loop fires again and the blocking condition now looks satisfiable, *then* the park stays set and the act does **not** retry — `needs_human.cleared_by` is written only by a human act, and the commitment is surfaced as parked regardless of what `status` derives to. `[ENGINE]` rejects an `engine`/`llm`-authored clear; the harness must not model the park as derivable-away (C2's discipline, applied to the park).

## D. The floor (M1 clarify/permission)

- **D1 [MUST]** — *Given* a tool that crosses the line (`send`) and no matching grant and no live confirmation, *when* the agent wants to call it, *then* it **asks** instead — it does not fire.
- **D2 [MUST]** — *Given* a stored grant `{action_class: notify, scope: this customer, unexpired}`, *when* the agent needs to notify that customer, *then* it fires **without** asking, and the act is recorded with `{who, basis: grant#, when}`.
- **D3 [MUST / never infer]** — *Given* no grant, *when* the agent has only an *inference* that the user would approve, *then* it still asks (silence ≠ consent; no inferred grants).
- **D4 [MUST / unattended]** — *Given* a trigger fires with no human at the console and the action needs a basis it lacks, *when* escalation finds no reachable human within the timeout, *then* the commitment is **parked** in a needs-human state and surfaced — no ungranted across-the-line act fires.
- **D5 [MUST / guest side]** — *Given* an off-app renter returns a form with a signature/payment, *then* their consent is taken from the form (not an agent), and the **owner's** agent still honors the floor when acting on the returned data.
- **D6 [MUST / attribution]** — every across-the-line act has `{who, basis, when}`; assert none can be found without it.
- **D7 [MUST / narrated, not composed]** — *Given* a standing grant covers an outbound message, *when* the message's factual content diverges from stored structure (the narrate spy detects free-composed facts), *then* the send is **blocked** before it fires — outward prose is `narrate(structure)` + the typed payload, never free model composition.
- **D8 [MUST / document-derived basis]** — *Given* a signed T&C that **names** a late-return fee, *when* the return comes in late, *then* the fee is **recorded** under the signature basis without a fresh ask; *given* a damage charge the document does **not** name, *then* there is no basis and the agent asks. Same commitment, same signature — the document's named terms are the scope. **Agreement-basis arm** *(added 2026-09-11; the required members are `../engine/SPEC.md §1.5`'s and the recording rule `SPEC.md §3.6`'s)* — *given* a booked group lesson and a recorded outside agreement naming exactly this roster, time, rate and total, *when* the owner or an authorized coordinator applies that exact act, *then* the change is **recorded under that agreement as its basis**, attributed to the person who actually reported it and labelled as reported rather than verified; *given* the same booking and an agreement naming a different roster, time, rate or total, *then* there is no basis for the act proposed and the agent asks again. The document precedent applies unchanged: the agreement's named terms are the scope, and nothing widens it.
- **D9 [MUST / destruction class empty]** — assert that **no tool in the contract declares the `destruction` reversibility class** (mirror of N2): cancellation is a latch and writes are diff-only, so nothing in the harness can destroy the sole record. **Withdrawal-is-not-deletion arm** *(added 2026-09-12; `../engine/SPEC.md §1.9`)*: withdrawing a reported end, attendance outcome or payment record is asserted here to reach **no** destruction-classed tool, because none exists — the withdrawal is an ordinary diff appending a record, and the withdrawn one is read back afterwards. A contract in which withdrawing a report needed a deleting tool would fail this row, which is the mechanical carrier of "who said what is never erased".
- **D10 [MUST / auto-accept is a Grant]** — *Given* an owner's setup answer *"accept bookings from my regulars,"* *then* a **Grant** is stored (`SPEC.md §7`) and every act it authorizes carries `basis: grant#` — assert that **no authorization path exists other than grant-or-live-confirmation**, structurally. A second mechanism would be a second place to look for who allowed an act.
- **D11 [MUST / auto-accept does not widen]** — *Given* that Grant, *when* a booking arrives from someone outside its scope, *then* there is no basis and the agent asks. Scope is matched, never inferred toward. *And the covered direction:* *given* a booking **inside** the Grant's scope, *then* it commits on the grant basis without a live confirmation, attributed `{who, basis: the Grant, when}` — assert both directions branch on the same covering relation (`SPEC.md §7`), so the test proves the matcher, not one of its answers. Use `operation: commitment.confirm` with the owner-confirmed condition `duration < 60 minutes`: an eligible 45-minute booking is covered, 60 and 75 minutes are not; a wrong party/subject chain, expired grant and revoked grant return no basis. Exercise the real executor with these forbidden calls, including a forged shorter interval in the coverage request. The actual stored interval governs (`SPEC.md §7`).
- **D18 [MUST / confirmation is content-bound]** — *Given* the owner confirms an across-the-line act ("send Maria the 3 pm move"), *when* an argument changes before it fires (3 pm → 4 pm), *then* the changed act has **no basis** — the prior confirmation does not carry it — and the agent asks again. Assert `basis` records the confirmed tool + canonicalized args, so an approve-then-swap-args cannot read as authorized after the fact (`SPEC.md §7`, rules 2/4). With a typed reminder act supplied directly as a Step-3 fixture (no scheduler or context assembly), a reminder Grant covers that operation only: a same-party/same-risk form-send or escalation notification does not match. A caller-labelled reminder containing unrelated prose refuses at the executor; changing party, destination version, channel, offset or scope also defeats that policy basis. A valid structurally bound reminder passes this floor-only control; D28 later supplies the actual clock and policy lifecycle. For §6's draft proposals, supply an accepted typed proposal fixture directly here: changing date, origin, value, target/use or scope defeats the prior acceptance at apply even if the object id is unchanged. An unchanged validated proposal passes to ordinary floor checks; a changed/reminder destination still needs its own basis. Step 4 supplies the interactive acceptance transition and Step 5 its stored resume; this direct fixture does not claim those paths ran. **Coordinator arm (`SPEC.md §3.6`; Matt S13d/S14c/S14e, MT-83/84/88/90, HR-30/32/33):** use a directly supplied arrangement/report fixture at Step 3. Unchanged actor, supported operation, participant scope, proposal, terms/revision and validity admit the exact confirmed act; swapping any one refuses. At the validity boundary, an expired report or arrangement refuses. A time/venue/participant/price change requires a fresh covering report and act confirmation; an unchanged explicitly allowed subset arrangement can be reused for a new request but its prior request report cannot. Revoke or suspend between proposal and apply, then attempt the stale act directly: no write. Owner-cleared suspension alone does not refresh an old report; replacement coordinator or widened authority needs the D24 path and new agreement. Unshared availability stays unknown and account creation supplies no group power. Interactive report acquisition remains Steps 4–5, real account linking at existing security/engine gates, and unsupported pricing/quota policies remain at their engine homes. **Attributed term-change arm** *(added 2026-09-11; `../engine/SPEC.md §1.5`)*: supply an accepted bound-quote replacement fixture directly here — proposed roster, time, rate and total, its recorded agreement ref and revision, and the expected current quote revision — and assert the same content-binding bites on every one of them: changing the roster, the time, the rate or the total after the confirmation defeats that confirmation, and an agreement recorded against the prior quote revision is stale rather than covering, even where the commitment id is unchanged. Assert `basis` records the confirmed tool, the canonicalized act and the agreement it rests on, so the reporter stays attributed and an approve-then-swap cannot read as re-agreed.
- **D19 [MUST / fail closed on an undeclared tool]** — *Given* a tool whose reversibility class is **undeclared or unclassifiable**, *when* the agent would call it, *then* it is treated as **across-the-line** — it asks, and never runs unattended on the guess that it was reversible. Assert the floor fails **closed**, never open (`SPEC.md §7` rule 5). The type-level impossibility of a missing declaration is M2's job (BUILD Step 2); this is the runtime backstop.
- **D20 [MUST / the floor property]** — the floor's invariant as a **property-based test** (`../TDD.md §Harness`): *no outward act ever occurs without a matching basis, under any tool-call sequence.* Over arbitrary interleavings of tool calls, confirmations, and grants, assert every across-the-line effect the spies recorded carries `{who, basis, when}` whose basis actually matches — a content-bound live confirmation (D18) or an operation/scope/condition-matched grant (D11). Names the harness's one property (the analogue of the engine's property-tested invariants); the executable test is written at build time. **The generated constructor roster's single source is `SPEC.md §3.11`'s printed roster of record**: the arbitrary generates every roster constructor — §5's tools are one part of that full roster — while the outward-act predicate checks the across-the-line effects of those generated sequences against §7's basis rules. The trigger constructor set contains all seven §4 sources, including `offered`; a missing trigger source or suppression event is a defect, as is a phantom `publish` identifier. **Home: `SPEC.md §7`** — no new law.
- **D24 [MUST / a Grant is minted only on a live confirmation]** — *Given* an elicitation whose answer routes to a Grant (G1), *then* the mint fires only on a **live, content-bound owner confirmation at the console** (`SPEC.md §7`, FD-24) — the stored `action_class`, `operation`, `scope` and typed `conditions` are exactly what the confirmation displayed, and the mint is attributed to it. *Given* a proposed Grant the owner does not confirm, *then* nothing is stored. Assert the `authorization` class is declared at the tool boundary like every other class (D19's discipline covers it if undeclared). **And the class has its named carrier:** the mint/widen/narrow/revoke acts land on the `grant.give` / `grant.revoke` roster row (`SPEC.md §5.2`) — assert the class occupies that row rather than floating carrier-less. Supply the completed mint proposal directly for this Step-3 executor fixture; the elicitation flow that produces it is exercised when Step 4 exists. A purported stored grant without an operation or conditions member cannot become an unconstrained grant by decoding. **Shared authorization arm (`SPEC.md §3.6`; MT-83/CRUD-26):** confirm the named coordinator, exact group/instance and subset scope, read fields, explicit operations/conditions and validity through the owner console; persist exactly that arrangement through the existing authorization/store path. Missing scope/conditions, an unconfirmed proposal, a guest report, a reader token, a coordinator's attempted delegation, a trigger or an external-client mint cannot create/widen it. A saved group alone creates no authority. Read-only access to names/details/total is separately confirmed; it permits no mutation or private contact read. A positive owner revision/revoke retains attributed prior state. Internal report admission never fabricates a fifth Grant risk class or uses a reminder Grant as coordinator authority.
- **D25 [MUST / no unattended or external mint]** — *Given* `grant.give` normalized from a trigger firing, or arriving through the external surface (`SPEC.md §5.3`), *then* it is **refused** — not queued, not parked for later approval as if it were the owner's own proposal. Assert the refusal both ways, and assert a Grant is **never the basis for minting or widening a Grant** on the `grant.give`/tool-boundary path — the authorization class has no grant-shaped basis at all there. *(Scoped 2026-08-22: the engine-minted `ShareGrant` on §7.1's stored-accept path is FD-32's one ruled carve — the application of a Grant the owner already minted, outside this row's path and untested here.)*
- **D26 [MUST / an injection cannot mint its basis]** — *Given* assembled context containing a non-owner string that induces the model to emit `grant.give` (the L5 fixture corpus supplies the string), *then* no Grant is stored and no outward act later fires on any grant that did not exist before the firing — asserted over the whole firing, which is what makes `../security/SPEC.md §5`'s "an injection could only propose" true for authorizations. Extend D20's generated input space with `grant.give` events so the property covers the mint path, not only the spend path.
- **D27 [MUST / no signed term, no guest-facing debt]** — *Given* a closed rental whose signed checklist names **no** damage fee, *when* the owner says "charge him for the damage," *then* **the utterance is refused and no record stores** — no `owed` derives against the guest, no outward claim fires, and **no internal `priced` record stores either**: the SPEC-tier story's beat is a refusal with the ask, not a bookkeeping fallback (`../user-stories/Situations/Situation-B/situation-3.md` governs; the prior mapping let an ordinary internal mark stand in for the refusal). Assert the outward spy recorded zero sends, the guest-visible money state is unchanged, **and the store holds no money record from the utterance**. **D8's context, in one sentence:** D8 permits recording a charge the signed document **names** — a live confirmation atop a named, stored term; this scenario is the negative space where the document names nothing, and no confirmation substitutes for the missing term. *(Situation-B's must-be-refused beat, `situation-3.md` — previously mapped to D8, which permits exactly this charge on a live confirmation; the story's refusal is about the guest-facing claim, and this scenario pins that distinction.)* **No-agreement, no-repricing arm** *(added 2026-09-11; `../engine/SPEC.md §1.5`)*: the same negative space over a bound quote — *given* a booked lesson whose roster or terms nobody has re-agreed, *when* the owner reports who attended, when the lesson ended, or that a payment arrived, *then* **the bound quote is unchanged and no new quote stores**: assert the stored quote and its revision are identical before and after, that the reports themselves record as the attributed facts they are, and that no outward claim fires. A report is not a term, and no confirmation substitutes for the missing agreement.

**D27 outcome distinction:** pair the no-agreement report control with an unmarked unknown-attendance booking and a human-marked `no-show` booking after a quote replacement. The unknown case retains the bound quote and `priced` revision; the marked case alone invokes the booking-bound no-show policy, with its full-rate arm reading the current quote and its custom/deposit arms reading their own stored terms. Neither report path creates a new quote, and the two outcomes remain separately attributed.

## D′. Escalation (the on-call ladder, `SPEC.md §3.9`)

*Provenance corrected, FD-59 (2026-08-22): the ranked-list Given in D12–D14/D16–D17/D21 derives from **held-out E's** on-call roster — which build gates are forbidden to cite — and every v1 protagonist is the empty-ladder case. The traceability line at the bottom of this file credited Situation C with this family, falsely. So the ladder-walk set carries `[SHOULD]` until a v1 Situation earns a ranked list, **D15 keeps `[MUST]`** (the empty list parking immediately is the v1 behavior), and the §3.9 law itself stands unchanged — Step 5 still builds and tests the ladder; what changed is the suite's honest weight.*

- **D12 [SHOULD / ladder walks]** — *Given* a ranked on-call list and an escalation raised, *when* rung 1 does not answer within `step_timeout`, *then* rung 2 is notified; the `Escalation` object records each rung and time. Assert the ladder advances on the **virtual clock**, not on a live wait — **and assert each rung notification is an ordinary outward act carrying `{who, basis: the FD-25 escalation-notify Grant, when}`**, minted when the list was elicited (`SPEC.md §3.9`); a rung send with no such basis fails D20, which is the point. *Durable deadline control (`SPEC.md §3.13`):* persist the ladder and its rung/total registrations, end the raising request, terminate that worker, advance the injected clock past only the first rung deadline, and restart with a fresh worker over the same stored state. Exactly one rung advance occurs through `registration_kind: escalation-deadline` on the ordinary clock. Redeliver the old callback with repeated and fresh firing/write ids: no extra advance or send. Assert the total registration id and instant are unchanged. Repeat with quiet hours and in-app-only rungs; no live sleeper or email outcome drives the advance. These stateful loop assertions belong to Step 5 checkpoint (3), not Step 1’s typed fixture.
- **D13 [SHOULD / reaching a human is not a basis]** — *Given* rung 2 is successfully notified, *then* **no across-the-line act fires** until a human actually answers — the FD-25 grant authorizes the *notification* and nothing else; the stuck act still waits for the human's answer. Notification is not authorization. *Answer/advance race (`SPEC.md §3.13`):* before the total cutoff, apply a valid human answer before a due rung callback and then in the reverse transaction order. The first order prevents advance; the second permits the already-applied advance but the answer still closes the same escalation. Both withdraw all remaining deadlines and prevent any later notification. Duplicate or stale callbacks neither reopen nor park it. Missing/wrong-principal basis never answers or dispatches. Pause a worker before dispatch, close the escalation or invalidate its claim, then release it: zero stale outward sends; already-dispatched receipts remain separate evidence under §§3.14/7.
- **D14 [SHOULD / exhaustion parks]** — *Given* `total_timeout` passes with no answer, *then* the commitment **parks** and the Escalation closes `timed_out_parked` (this is D4's mechanism, now with an object behind it). Assert no act fired. Retain the no-stuck-act assertion independently of earlier authorized notifications. Under `SPEC.md §3.13`, step across the fixed total instant after multiple rung advances; the total registration still closes the escalation once. Run a rung/total exact tie and an answer/total tie in both callback admission orders: neither a late answer nor an advance postpones the cutoff, and a previously committed timely answer cannot be replaced by timeout. Repeated callbacks after closure append only attributed no-op history. A worker restart after total expiry produces the same park, never re-arms a fresh total budget.
- **D15 [MUST / empty list is not an error]** — *Given* an owner with **no** on-call list (the ordinary single-operator case), *then* exhaustion is immediate and the commitment parks. Assert this raises no error state and logs no defect — it is correct behaviour.
- **D16 [SHOULD / in-app cannot be disabled]** — assert the channel set can never be empty: with email disabled and quiet hours active, the in-app entry still exists. **An escalation nobody reads is a park, never a silent proceed** — assert the outcome equals D14's. Under `SPEC.md §3.13`, persist the in-app-only ladder, terminate the worker, step to its fixed total deadline and restart without opening the console: the durable ordinary clock still produces D14’s park; no delivered email or console listener is a Given.
- **D17 [SHOULD / the park says why]** — a park raised by ladder exhaustion is attributed to it and distinguishable from a park raised by a missing basis (the `SPEC.md §3.4` `reason`+`cause` pair is the mechanism). The owner can tell "nobody answered" from "I was never asked."
- **D21 [SHOULD / quiet hours never delay the ladder]** — *Given* rung 1 sits inside quiet hours, *when* `step_timeout` passes with no answer, *then* rung 2 is notified at the **same virtual-clock time** as the identical run with quiet hours off — the sleeping rung costs `step_timeout`, not the escalation (`SPEC.md §3.9`). Assert email suppression and advance timing are independent. Distinct from D16, which asserts the channel set is never empty and that the outcome equals D14's — neither of which measures *when* the ladder moved, and D14 is a terminal park, not an advance.

## D″. Channel suppression (the floor's reach subtraction, `SPEC.md §3.11`)

*Refiled 2026-08-21 — D22–D23 sat under the escalation heading, where neither belongs: both coverage maps (this file's and `../TDD.md`'s) always classed them under the reversibility floor, and the file's physical structure was the wrong copy.*

- **D22 [MUST / a suppressed channel comes back unsent, never silently dropped]** — *Given* a party under channel suppression (`SPEC.md §3.11`) — **one recorded complaint, the only trigger; there is no per-party bounce threshold** (corrected 2026-08-21, the FD-12 alignment) — *when* an outward act names that party, *then* the act returns **unsent** and the firing takes the hand-me-the-link path, exactly as for a party with no reachable stored address (`../app/SPEC.md §6`). Assert the outward spy recorded **zero** send attempts for that party, that the owner is told which party and why, and that the act was **not** dropped, retried, or re-routed to a different address for the same party. **And the interleaving** *(2026-08-22, §7's re-verify gained the un-suppressed member)*: a complaint committing **after** a send's live confirmation but **before** its fire stops that send — assert the fire-time re-verification refuses, because a post-complaint send is the unrecoverable direction.
- **D23 [MUST / the switch is the harness's, and it never self-clears]** — *Given* delivery events stored by the harness from the app's emitted raw results (`INTERFACES.md §3.3` — the immediate outcomes on the seam call, the out-of-band complaint through the delivery-report trigger source, the app deciding nothing, `../app/SPEC.md §6`), *then* the suppression is derived here, from those stored events against security's declared threshold — assert **no new stored object class and no seam verb beyond `INTERFACES.md §4`**. Assert **one** complaint suppresses that party — the first one, with no rate and no averaging (`../security/SPEC.md §10`, "Two stops"), and assert it does not wait on the per-owner bounce rate, which is a different wall (security R2). A complaint latches permanently; assert it is **not** cleared by elapsed time, by a later successful send to a different party, or by any automatic path — only by an attributed owner act through the ordinary write path (C9's shape). And assert **D20 holds unextended** over sequences containing suppressed parties: suppression removes reach and never creates it, so the floor property needs no new case (O4's reasoning). **Exercise `INTERFACES.md §3.3` correlation and `SPEC.md §3.14` receipt identity:** equal-time email and LINE failures for one party and distinct acts remain distinguishable; exact duplicate facts persist once; `sent` and `complaint` for one act persist separately under distinct receipt ids in either arrival order. A late success never erases the complaint or clears suppression. Unknown or mismatched act/party/channel tuples refuse without suppressing another party. AppStub injection proves payload carriage only; this scenario owes actual persistence and suppression behavior at BUILD.
- **D29 [MUST / the re-enable is an attributed owner act, and it is the only way back]** *(added 2026-08-28 — `SPEC.md §3.11` and `§5.3` both name re-enabling a suppressed party as an owner act, D23 asserts nothing automatic clears the suppression, and **no scenario in any layer ever drove the re-enable itself**: the mint-proves-revoke class gate found it on its first pass, `../deployment/SPEC.md §4`.)* — *Given* a party suppressed by D22/D23's single complaint, *when* the **owner** re-enables that party, *then* the re-enable lands as an **ordinary attributed write through the existing path** — `{who, basis, when}` recorded, no new stored object class and **no seam verb beyond `INTERFACES.md §4`** — and the next outward act naming that party is **no longer returned unsent**. Assert the walk end to end: suppressed → act comes back unsent (D22) → owner re-enables → act delivers. *And the refusals, which are the point:* assert **no non-owner path re-enables** — not the loop, not a trigger firing, not the external surface (`SPEC.md §5.3` places the suppressed-party re-enable in the console-only authorization-and-recovery class, FD-26) — and that the re-enable is **never implied** by elapsed time, a later successful send, or a fresh complaint-free interval (D23's never-self-clears half, asserted from the other side). The re-enable restores reach; it creates no basis, so the next act still needs its own (§7).

- **D28 [MUST / a reminder is an ordinary grant-based send, and no grant means no send]** *(FD-61; `SPEC.md §3.3`; engine registry home `../engine/SPEC.md §1.15`.)* — *Given* a confirmed kind reminder policy, its matching `reminder-notify` Grant and a confirmed booking, *when* the clock reaches the selected offset before start, *then* the ordinary clock source dispatches once through `send`, with narrated current booking structure and `{who, basis: the Grant, when}`. Retain the 24-hour-offset kind fixture alongside the owner fixture below. No new trigger source, per-student scheduler or permission ledger exists. A kind without reminders, a draft-only policy or no matching Grant sends nothing and does not park for missing authorization; suppression and caps retain D22/D23's stops.
  - **Activation and scope:** Matt's S07/MT-58/HR-24 choice names Matt, the exact verified account email, English-lesson scope and 15-minute offset. Confirming activates the matching policy/Grant together; rejected writes activate neither. Unsent example, decline, saved-unconfirmed configuration, copied template and abandoned draft activate no send. A merely proposed edit leaves the prior active policy intact; confirmed offset/channel/destination/scope edits require the displayed live basis, with unsupported/unverified destinations unsent. Check saved readback (S14), both owner-entered and student-booked lessons, and zero reminder for personal appointments, routines or a student recipient. A saved student Messenger contact cannot become send authority. CRUD-20 removes one booking's reminder independently of the all-English policy; MT-78's owner cancellation and policy removal suppress only affected firings, retaining history and governing constraints.
  - **Booking revision and clock:** exercise the exact `../user-stories/Situations/Situation-A/story-matt.md` S13/S13g/S14a/S14b transitions and `story-matt-verification.md` reservation/reminder table, all Asia/Taipei: Jasmine Monday 12 October 13:00→15:30 gives **12:45→15:15** only after the accepted move; waiting for agreement, failed/stale move and abandoned proposal retain **12:45**. Kai Thursday 16:30 stays **16:15**; Friday Jasmine 14:00 gives **13:45**, Kai 16:00 gives **15:45**. Jasmine Saturday 10:00→10:15 gives **09:45→10:00**; **Keep booking** keeps 10:00, confirmed cancellation suppresses it. Separate Sunday 10:00 gives **09:45**; Wednesday's refused request registers nothing, and its fresh successful 10:00 retry gives **09:45** on Wednesday, never resurrecting Saturday. The group fixture the retired S14c carried — Monday 18:00→19:00 gives **17:45→18:45** on the accepted move — stays as law, story-owed under FD-106, and so do its participant removals/rejoins; and reported end/attendance/payment retain one owner reminder and no extra sends; changed roster/meeting-link content comes from the current revision. S14a/S14d receipt/navigation assertions stay app-side, not an assumption that a scheduled event delivered.
  - **Stale and repeated work:** deliver the old 12:45, 09:45 and 17:45 callbacks after the corresponding committed changes, and duplicate the current callback under both repeated and fresh firing/write ids; no obsolete content or duplicate send reaches the app. Revoke/expire the Grant, cancel the whole booking or remove its policy before a queued fire (including a cap-deferred fire): it is inert. A cap-deferred reminder past lesson start is recorded obsolete, never sent. Re-read contact verification and suppression immediately before dispatch; a change after arming cannot redirect the email. Check known success/failure/handoff and unknown fate against the AppStub, preserving correlated receipts and spent basis on replay (`SPEC.md` §§3.14/7); no unauthorized fallback or delivery/read claim. Run these loop/clock assertions at Step 5 checkpoint (9) over stored booking-transition fixtures. Actual booking, participant/terms and quota transitions are engine-owned and remain their BUILD proofs; the full story composition additionally requires the mapped story contracts and the real engine/app swaps.

## E. M2 — governed-board classify gate

- **E1 [MUST]** — *Given* a board carrying governing rules, *when* a commitment is placed with **no** `kind` and **no** `exception`, *then* it is **refused** (unrepresentable), not silently recorded as compliant.
- **E2 [MUST]** — *Given* the same board, *when* a commitment is placed as an explicit `exception {reason, by}` (a maintenance block), *then* it is allowed and recorded **as an exception**, never as compliant.
- **E3 [MUST]** — *Given* a commitment declaring a governed `kind`, *then* that kind's rules are evaluated before it can go active.

## F. Conflict & versioning

- **F1 [MUST]** — *Given* a new rule that contradicts a **governing** rule, *then* the write is a **hard stop** (only forward move is a recorded exception). **Affected-commitment blocker arm** *(added 2026-09-12; `../engine/SPEC.md §8`)* — *Given* a candidate narrowing a teaching window across one live booking, *then* the harness surfaces the **engine's** blocker members — the blocking record, the violated candidate constraint and the engine-named resolutions — with the **stored** rule still shown as effective beside the proposed one, and applies nothing. Assert every narrated member came from the return: a harness-authored blocker, reason, extra resolution or grandfathering choice fails this arm, as does a surface that shows the impact and lets the apply through. **Combined-candidate request/result arm** *(added 2026-09-12; S13c)* — *Given* one submission the owner reviews and OKs together, changing capacity, the conditional price and the permitted format, *then* the harness sends it as **one** call carrying **one** `candidate` declaration — assert the emitted payload: one `ruleset` first argument and a declaration whose `rules` holds all three changed `rule_ref`s with their expected `revision`s under one `effective_scope` — narrates back **all three** changed rules from the return's `candidate.rules`, and — where one member is blocked — names **that member's** rule as the violated one while still showing the other two as part of the same pending candidate. A harness that splits the submission into three candidates, sends the ruleset with no declaration, re-asks the settled definition branch per member, or narrates the blocker against the candidate as a whole fails this arm. Assert too that a `compatibility` member of `null` is narrated as *not asked*, never as clear. **Incomplete-declaration arm** *(added 2026-09-12; `../engine/SPEC.md §8`)* — script the engine's `invalid`/`schema-mismatch` refusal of a present-but-defective declaration (an entry missing its expected `revision`; `effective_scope` absent) and assert the harness surfaces it as a refusal that applies nothing and keeps the pending candidate intact for correction — never narrated as *not asked*, never as clear, and never silently retried as a noncandidate call by dropping the declaration. **Write-path carrier arm** *(added 2026-09-12; `../engine/SPEC.md §6` item 1)* — applying a cleared candidate leaves as an ordinary rule diff on the **unchanged** `commit` input (`INTERFACES.md §1.2`): assert the emitted payload carries **no** candidate declaration, blocker, exemption, resolution or `effective_scope` member, that the harness authors none, and that a harness reporting the apply as clear because its `commit` call carried no `compatibility` fails this arm — the apply-time declaration is formed engine-side from the diff, and this seam is not where it travels. **Narrowing-by-addition arm** *(added 2026-09-12; `../engine/SPEC.md §8`)* — make the same narrowing as a **created** rule, declared `revision: new`, and assert the harness narrates the same pending candidate with the blocker named against that created rule; a surface that offers adding a rule as a way past the blocker, or that treats a create as outside the compatibility question because it has no stored revision, fails this arm.
- **F2 [MUST]** — *Given* a new rule that contradicts the owner's **own** rule, *then* the agent surfaces both and requires an explicit **override stored with a reason**. **Not the compatibility path** *(added 2026-09-12)* — *Given* a compatibility blocker on a candidate the same owner authored, *then* the override-with-reason affordance is **absent**, asserted as absence and not as an unused control; no reason text, authority claim or exemption clears it, and the only offered actions are the engine's own closed resolutions.
- **F3 [MUST]** — *Given* two rules that don't clash yet but could, *then* the agent **alerts, does not block** (latent inconsistency). **Alert versus gate** *(added 2026-09-12)* — *Given* one firing returning an empty `conflicts`/`latent` pair **and** a non-empty blocker set, *then* the agent **blocks** the apply; *given* the converse — a latent alert with no blocker — *then* it applies. Assert the two are carried and routed separately, never collapsed into one "conflict" surface.
- **F3b [MUST / unsatisfiable is not overridable]** — *Given* two of the owner's **own** rules that are jointly unsatisfiable (a boat minimum above its capacity; a minimum duration longer than any allowed window — `../engine/SPEC.md §8` item 4), *then* the agent **does not offer the F2 override path**: it surfaces the refusal with both operands and the remedy, and the write does not land. Assert the override affordance is **absent**, not merely unused — an owner who can override here ends up with rules that look stored and can never place anything. **Composed-domain arm:** use engine G5/G6’s three-rule empty intersection. Narrate all three source predicates and the typed `widen` alternative with no override affordance and no write before the user chooses an authorized change. A malformed return with three refs but two operands is rejected under A7; a valid ordinary pair still carries two. A chosen remedy is rechecked, never taken as a grant to alter governing rules (`../engine/SPEC.md §8`). **Aggregate ordinary-pair readback:** use engine G6’s stepped 30–120/15/30-minute duration against its 20-minute plain window. Exactly two refs/operands survive; the duration entry retains range/step/anchor and the other entry the typed window. Reject flattened leaves, dropped fields and a false aggregate `TypedValue`; repeat with the legacy range and the unchanged two-entry Latent shape. **Coordinated readback:** use G5/G6’s six distinct A/B/C/D/E/F rules and its single C+F widen alternative. Show all six original domains and both required edits as one choice; choosing only C or only F is not choosing that remedy. Before full choice/authority there is no write. After an authorized choice the harness emits one attach-point diff carrying the entire required rule set and both changes under one write id, never a commit per member. A denied F authority or stale-result refusal causes no partial retry, automatic substitute or governing override. The stub records call count and complete payload; real current-state checking, transactional rollback and replay remain engine BUILD Step 5 and the existing swap, not proven by this stub.
- **F4 [MUST]** — *Given* a completed course validated under governing version N, *when* the governing standard is revised to N+1, *then* the completed course keeps its `effective`/version-of-record stamp and is **not** flipped to non-compliant.
- **F5 [MUST / own terms forward-only]** — *Given* booked lessons at NT$100/hour, *when* the owner raises the rate to NT$120 (or reshapes a course), *then* the already-booked commitments **keep the terms they were made under** and the change applies only to bookings made from then on. No live commitment is silently re-priced by a later edit.
- **F6 [MUST / in-flight machine states keep their operands] `[ENGINE]`** — *Given* a unit mid-cooldown under a six-hour buffer and a live hold mid-clock, *when* the owner edits the buffer to three hours and the hold window shorter, *then* **the running cooldown finishes the six it entered with and the live hold keeps its clock** — the edits govern placements and holds created afterward, never a window already running (`../engine/SPEC.md §1.5` — `hold` binds at booking; a placement's gap requirement binds at its own commit). Assert both directions: a new booking after the running window closes uses the new operands. *(Situation-B's in-flight beat, stated twice in the story and previously covered nowhere; the enforcement is engine-side, the harness surfaces it.)*

## G. Elicitation store & correction

- **G1 [MUST]** — a learned answer is stored as the right object by what it governs: policy-over-kind → Rule; authorization → Grant; per-board fact → Board field; per-instance → Commitment field; deviation → Exception.
- **G2 [MUST / scope ladder]** — an answer stored at `kind-global` resolves a later gap at `instance` scope without re-asking; an answer stored at `instance` does **not** silence a different instance. **Independent saved-place scope (app §8; MT-21/31, HR-05):** supplied Home is an editable origin, skipped Home remains unknown until supplied. Picker Save does not persist Sarah for reuse. Accepting the separate reusable-place proposal emits its own ordinary principal-attribute diff/write id, and replay reads one entry. Refusing it leaves the valid one-time draft usable. Cancelling the run after accepting the place reports the retained saved-place act independently, never claiming no write occurred. This acceptance is not venue-publication permission and does not rescope the Routine. Steps 4/5 use scripted calls/readback; app C2/O3 and engine T4/V3 own their named real gates.
- **G3 [MUST / correction]** — *when* the owner revokes/edits a stored rule or grant, *then* the agent surfaces the downstream commitments/grants it invalidates **before** applying, and any outward consequence crosses the floor. **Saved-place dependencies (`../app/SPEC.md §8`):** before nickname/address edit or retirement, show current id/revision, the specific label/address effect and future referenced Patterns/occurrences including exceptions beyond the horizon. Obtain the user’s resolutions, then send one reviewed ordinary diff set; denied/stale/unresolved references apply nothing and never silently update history. Removal from reuse is distinct from personal-data erasure. Actual atomic storage proof is engine BUILD Step 5; this harness gate spies on the full reviewed request and refusal readback.
- **G4 [MUST / T2 go-live]** — a governed board goes live only when structural coverage is met OR every remaining hole is a recorded exception; a silent hole blocks go-live.
- **G5 [MUST / interview state]** — a T2 authoring session can save/resume/abandon; partial policy persists as `draft` (disabled) rules.
- **G6 [MUST / template-Lego]** — *Given* the owner describes a shareable thing ("students book my teaching hours"), *then* the agent proposes the **nearest known template pre-shaped** from a supplied suitable result (a lesson form with the right fields); a field add and a field remove both round-trip; the finalized form is **frozen** (generate-once) so every recipient gets the identical thing. With a supplied truthful empty-catalog result, author from a neutral shape instead of fabricating a known template; supplied unavailability has its distinct readback and retry/user-chosen authoring route. **Run all five outcomes of `INTERFACES.md §2.1`'s `catalog_result` from that member, not from narrative** *(2026-09-12, FD-105)*: a **suitable** result pre-shapes the actual returned candidate and nothing else; an **unsuitable** result is read back as no match rather than pre-shaped anyway; `status: empty` enters neutral authoring; `status: unavailable` reads back as unknown with retry or user-chosen authoring offered; and an owner **declining** a suitable result enters that same neutral authoring with the decline recorded (`SPEC.md §3.10`) and not re-offered in the same turn. Assert the three statuses are **distinguishable** — absent, `empty` and `unavailable` are three states, and neither of the last two may be narrated as the other — that a choose/customize selection resolves to a **supplied eligible** `item_ref`/`content_version` and a moved one is rechecked before use, that **no listing, emptiness or availability the member did not carry is narrated**, and that a saved selection **resumes** through the single §§2–3 document path with no silent publish. Add a **second disjoint catalog fixture** and assert suggestions never cross the supplied eligible id/version set. Hostile listing copy stays `document`-tagged through the quarantine read and gains no publication, owner privilege or guest catalog reach (L5/L8). Use the shared draft revision and pins throughout. Definition edits, per-use edits and intentionally named defaults follow `../marketplace/SPEC.md §2`; exercise the ordinary no-question reuse path, triggered unresolved identity scope, explicit private choice and already-answered branch without repeated questions. The **producer** of those fixtures is the app marketplace adapter (`../marketplace/SPEC.md §3`) and the **retrieval policy** is `../marketplace/SPEC.md §6`'s; this row asserts the harness's consumption of the supplied member and never performs a lookup itself. Real service behaviour is `../marketplace/BUILD.md` Steps 3/4 against the service mock, and model qualification is `../model/EVALS.md`'s. **Field lifecycle controls (`SPEC.md §3.3`):** create/read/edit/remove an independent optional Preparation note draft row and value, then add/save/reopen/retire an independent optional field definition through a new revision. Distinguish clearing its one value and changing its future-use default; neither removes the field nor changes sibling uses. Rename a field while a rule binds it and while a second field has the same label: bindings stay on the original id, and a historical form keeps the original meaning. A supported type revision validates its new defaults/inputs; incompatible existing values or dependent operands keep the candidate pending without coercion. Show current dependencies for required/referenced removal, offer only authorized compatible replacement/rebinding or dependent optional-rule revision/retirement, plus keep/abandon; choose and apply each valid arm in separate fixtures. A required title/governing constraint has no remove/override shortcut. Race a new dependency or changed revision against an accepted removal: the scripted refusal leaves the current definition and all candidate members untouched, preserves the pending candidate and invalidates stale acceptance. Resume unchanged decisions once, recover unknown writes by identity, and distinguish PendingAsk/document questions from commitment-backed PendingDecision. Map duration/window/buffer/pricing/quota rows to complete typed engine rules; an off-menu request never becomes enforced text. Repeat the field/reference operation with Task, Routine, contact, saved-place and reminder fixtures: referenced objects, booked terms, quota identity and send authority do not change as a side effect. Real contact, pricing, reminder, compatibility and full authoring execution remain at their own gates; these fixtures prove general lifecycle routing only.
- **G6b [MUST / pin survives re-proposal]** — *Given* the owner **hand-sets** a field during generative-UI authoring, *when* the agent subsequently re-proposes the form (a fresh suggestion, or an added field elsewhere), *then* the hand-set field is **pinned** — user-owned and immune to regeneration — and survives unchanged; the re-proposal cannot clobber it (SPEC §5). Freeze-at-finalization (G6) is the whole-form version; this is the mid-session, field-level version. Bind pins and accepted proposals to `SPEC.md §3.3` field ids/revisions: a row reorder or rename cannot move a pin to another field. Editing a supported calculation input invalidates its dependent result and acceptance; an unrelated optional-row edit preserves the pin and unchanged covering scope answer. An old OK/Edit after field retirement/revision refuses without restoring the row or producing stale prompts; re-proposal cannot recreate a user-removed row as accepted configuration. Exercise the finite submitted choice and intentional blank against that same binding; the full console dispatch design is not proved by a field fixture.
- **G8 [MUST / save-as-bundle]** — *Given* a T2 authoring session (complete or in progress — `draft` rules project too, `../engine/SPEC.md §1.7a`), *when* the owner saves it as a template bundle, *then* the harness invokes the bundle **shape-projection** as an **internal `calculate`-class read** (SPEC §6; `../engine/SPEC.md §1.7a`), consumes its declared projection snapshot, the emitted document validates as a `../marketplace/SPEC.md §1.2` bundle with operands **blanked or marked publisher's-choice**, the operation declares `internal` and does **not** publish, and **re-projecting the same session yields an identical bundle** (L2's determinism). No displayed or snapshot field becomes a correctness-critical write literal. Points to `../marketplace/SCENARIOS.md I6` for the authoring≡hand-authored equivalence law (not restated here). Separately exercise private save/reload/use through the existing app controller/document double (`../marketplace/SPEC.md §§2–3`; `../app/INTERFACES.md §4`): the accepted typed configuration retains the duration range and optional step/anchor, while the projection remains data-free. Assert the distinction between definition id/revision and fresh use request identity, per-use participant blanks, explicit named private scope and no incidental contact/credential/Grant copying. Persisted Save opens use directly; pending/unknown result does not claim saved and retry reuses the original write/use identity. Replace and Save as new are selected once for a definition candidate, never for ordinary per-use values; a copy leaves original links and terms bound. Wrong-account/stale revision and changed-retirement-dependency results are surfaced unchanged, not repaired by overwriting or minting another id. Both rendered and headless owner inputs retain normal validation and floor enforcement. These stub controls prove routing/readback only; real persistence/authorization/concurrency is app BUILD Steps 7/8, with catalog projection/install/retirement at marketplace Steps 0/3. **Assert the emitted document against the transferable subset** (`../marketplace/SPEC.md §1.2`) *(2026-09-12)*: every schema in it — `domain`'s typed fields and each `shared_shapes[].form` — carries structure and bindings only, with **no node `default?` value, no resolved board/account/party/contact/token reference, no per-use value and no instance-data-bearing `props` member at any depth** of a `children` chain, and **no `file-slot` node**. Probe a session whose authored schema holds a nested synthetic contact default and a prefilled address prop, in its `draft` and its finalized form: the projection emits neither, and it emits the same document both times (this row's determinism clause). The legal cases still project — blank parameters, neutral structure and publisher-choice operands carried whole. **The validator this row validates the emitted document against is one shared artifact** *(2026-09-13)*: `BUILD.md` Step 4 is its **first producer**, built against `../marketplace/SPEC.md §1.2`'s frozen bundle grammar, and `../marketplace/BUILD.md` Step 0's pack schema remains a separate marketplace artifact; Steps 0–1 reuse and complete the shared bundle validator — the ownership row is `../marketplace/BUILD.md` Step 0's. A second handwritten bundle validator fails this row rather than satisfying it twice.
- **G9 [MUST / the authoring fence] `[ENGINE]`** — *Given* the bundle projection, *then* it **cannot select** any counterparty, booking, history, ledger, or personal-data field — the authoring twin of `../marketplace/SCENARIOS.md F3` ("the fields do not exist"). The harness's part is only that it **hands the engine a representable request** and never turns a display/snapshot field into a correctness-critical write literal (`INTERFACES.md §1.1`); the projection's selectable-set enforcement is verified engine-side (`../engine/SCENARIOS.md` S3) — **including the schema-borne carriers**: a node `default?` value, a value-bearing `props` member and a reference-resolving `bind`, at every depth, are outside that selectable set under `../marketplace/SPEC.md §1.2`'s transferable subset, and no harness-side filter substitutes for the construction. `[ENGINE]`
- **G7 [MUST / SOP lifecycle]** — *Given* the owner authors or names an SOP through the admitted existing path (a bundle of kinds + rules, SPEC §3.6; no file upload, `../security/SPEC.md §4`), *then* it gains document identity through `CRUD_SOP`; its rules govern exactly as directly-authored ones (same precedence law); and *when* it is detached from a kind, commitments already made keep the terms they were made under — forward-only, like every rule edit. The former owner-upload arm is deferred with the excluded feature; authored/named and catalog text still take the document quarantine. An administrator-approved source-form identity is neither a participant verification result nor permission to upload a file. Exercise admitted content and an existing authorized content reference: both keep the authored/named lifecycle, and approving the source leaves its `document` tag unchanged under security §5. A submitted owner-upload reference refuses without creating an SOP or rule.

## H. Orders, groups, guest flow

- **H1 [MUST]** — *Given* an order of 3 course-days, *when* the owner cancels **day 3 only**, *then* day 3's slots release and days 1–2 stand (partial cancel), and any already-`completed` day is untouched.
- **H2 [MUST]** — *Given* whole-order cancel, *then* all members dissolve and all slots release.
- **H3 [MUST / group class]** — *Given* a class sold to 4 customers, *then* it is representable as one order with 4 customers consuming 4 seats (each attributable), and one customer cancelling does not kill the others.
- **H4 [MUST / guest]** — *Given* H2 publishes a board-blind form and an off-app party returns it, *then* `on_form_return` fires the loop and the owner's agent processes the data; assert the form exposed **nothing** of the owner's board. Under `INTERFACES.md` §3.3, exercise human declines with rate, distance, timing and an additional template-configured structured choice, plus a hostile guest free note; assert exact `offer_ref`, `party_ref`, `at`, choice value and tagged note survive the app event boundary, and an unconfigured or attacker-supplied `choice` value is rejected by form validation before record or privileged-context admission, never carried as a configuration-authored answer or silently reclassified as a free note. An engine `no-feasible-placement` failure is never substituted for a human answer. For the note's context admission, exercise L4/L5/L7/L8: preserve the guest tag, show raw text only to the tool-less quarantine model and only its validated summary to the privileged model; on quarantine failure assert raw text absent on the wire and an attended gap or unattended park, with no Escalation. Typed AppStub carriage proves payload preservation only; it does not execute real guest-door validation, loop routing or quarantine (`BUILD.md` Steps 2 and 5). **Shared request arm (`SPEC.md §3.6`; Matt S13d/MT-83/CRUD-26):** drive owner and coordinator reports through the actual executor with one request, four individually scoped links and the explicitly stored subset arrangement. Only the allowed actor's report is stored, naming reporter, affected participants, exact act/terms and arrangement revisions and validity; other people's agreement is reported, not authenticated. A reader's forged `on_form_return` agreement or objection, confirm, change or whole-cancel call refuses with no write. Positive objection control: the owner records one participant's outside objection; the affected agreement becomes unusable and the proposal stays pending, while a confirmed booking is not silently cancelled. Repeat through an explicitly permitted coordinator; without report permission, including after suspension/revocation, that same call refuses. A fresh authorized resolution report preserves the old objection and permits only its newly reviewed act. No external chat, payment or identity-verification call occurs. Existing guest quarantine controls above still run. For the request-bound conversation, use the same security §3 admitted rights as the form: forged scope/recipient/model output never reaches an owner config write or private context. No unrestricted guest agent is admitted; full conversation/context proof remains its existing BUILD gates. **Interactive-versus-return control (`SPEC.md §2`):** a deliberate guest message invokes summarize then normalize with `context_mode: guest-interactive`; loading the page invokes neither. Its reviewed structured confirmation emits one returned-form event and that trigger invokes no normalize. One accepted-act/submission identity applies once across both paths. Feed forbidden owner operations directly into the executor with a valid token and assert refusal, regardless of model output.
- **H5 [MUST / group half-clear]** — *Given* one order, 3 customers, per-member precondition gates, *when* one member's medical gate blocks, *then* only **their** member-commitment is pending — the other two stand complete and untouched — and the group confirms the moment the last gate lifts. Nobody re-fills anything.
- **H6 [MUST / per-recipient token]** — *Given* a publication delivered to recipients X and Y with per-recipient tokens, *when* a return arrives on X's token, *then* it attributes to X and never to Y, and its data lands with that attribution. **One-request arm (`SPEC.md §3.6`; Matt S08b/S13d/S14c, MT-45/82/83, CRUD-25/26, HR-14/17/29/30):** reuse one existing party and three independent successful contact adds in a group draft, save/reopen the stable group entry, then select four participants for one request. Four recipient links read that same proposal and later the same confirmed commitment, never four owner offers or four teacher bookings. Repeat a new two-participant use under an explicitly covering subset arrangement; it changes neither the saved four-person roster nor the first booking. Roster contact corrections dereference the same parties. Discard a group draft without deleting saved contacts, and recover an unknown independent add before claiming discard. Remove/retire a group member and the group with an active request/link dependency: unresolved refuses; a reviewed frozen binding or separately completed authorized withdrawal permits retirement, preserving contacts/booked participants/history. Insert a new reference after review and assert no partial retirement; reopen shows actual current references. A booking participant removal/rejoin is a separate accepted same-booking diff, while an attendance-only report changes no participant set. Scope all readbacks; another request, an unselected person or a foreign owner cannot read the shared details. With a current report and exact permitted whole-cancel confirmation, cancellation applies once to this booking, retaining its original confirmation/history, saved roster/contacts and unrelated bookings; a reader or a revoked coordinator cannot perform the same act. This arm preserves the original per-recipient attribution check and owes actual storage/application to the existing engine gates. **Linked-member composition arm (`SPEC.md §3.6`; Matt S14e, MT-90, HR-33):** with four members who have each linked an account through the verified claim and chosen what to share, ask the console for a common hour. Assert the proposal is composed only from the availability each member currently permits and the owner's current rules; a fifth member who shared nothing is surfaced as a **missing answer to coordinate directly**, never as free time. Revoke one member's sharing and re-ask: the new proposal recomputes from current permissions and repeats nothing the revoked sharing had shown. Assert the returned proposal is a candidate — the arrangement's agreement, the content-bound confirmation and the engine's final admission all still run, and neither the proposal nor a member's account creation confers coordinator authority or anyone's agreement. Drive one attempt with a similar name, a forwarded credential and a channel two members share: none supplies identity evidence and none links an account. Verified linking, the read union and the actual composition remain security BUILD Step 4 (N6) and engine BUILD Steps 1/4/8 (B8, I3, I8) obligations; these are the seam controls, not their execution. **Credential lifecycle arm (`../security/SPEC.md §3`; S10/S14b/MT-11/13/41/46/74/79):** prepare two uses for the same party and one for a similar display name. Issuing/copying a link does not book, send or verify; independently admitted fill/check/confirm affects only its bound use, and the same ref is read after confirmation. Caller-supplied party/ref/operations and a forwarded opener's claimed identity never change attribution. Contact add/edit/remove and new template uses leave prior credentials unchanged. Revoke one link and reissue under fresh owner-confirmed scope; pending old returns/replays refuse, sibling access/booking history remains, and the replacement cannot revive stale coordinator work. Script lost mint/confirmation acknowledgement and retry: recover the existing applied ref/binding, never a second booking. App/engine BUILD gates owe actual custody and atomic application. Repeat through the guest console with the same draft/request binding: valid reader queries succeed without proposal persistence, while valid coordinator report/fill/confirm follows the current arrangement. Replay after revocation or permission/arrangement revision change refuses before context/readback and apply; the same-person second request is not a substitute. Successor return controls (`INTERFACES.md §§3.3/7.2`, `SPEC.md §3.4`) round-trip ordinary structured fields, complete in-form consent and an operator assertion with its actual actor/basis/time. Missing or wrong-version consent refuses; a storage receipt cannot substitute. Participant attestation/verification riders, upload refs and artifact bytes refuse before any effect. The separate manage-mint/open-mint records preserve their existing attribution. Historical receipt reads retain custody/event semantics and confer no new permission.
- **H7 [MUST / owner cancel = crossing]** — *Given* a repeat counterparty's confirmed booking (their third session), with no covering cancellation Grant, *when* the **owner** cancels it, *then* the cancellation's outward consequence crosses the floor (confirm), and the agent **offers to invite a rebook** (implicit recurrence) rather than closing a dead one-off. After the authorized cancellation, assert `status = cancelled` with the original `confirmed_at` intact (`SPEC.md §3.4`); decline/absence of the floor confirmation leaves the booking confirmed and adds no cancellation latch. The rebook offer never revives the cancelled record or supplies a new booking without its own authority.
- **H8 [MUST / counterparty cancel ≠ gated]** — *Given* the same booking, *when* the **counterparty** cancels their own booking **through a token bound to that booking** (`per-recipient`, `hold-confirm`, or a live `single-visitor` token — `../security/SPEC.md §3`, the 2026-08-21 binding rule) — *then* it is **not** gated on the owner: recorded with that token's attribution, slots released, owner informed; `status = cancelled` and the earlier `confirmed_at` is unchanged (`SPEC.md §3.4`). A later manage attempt to move or re-confirm the cancelled booking refuses without renewed consumption; read back the same cancellation and confirmation history. Their time to give up, not the owner's to approve. *And* assert the refusal: a bare `booking-link` bearer — who attributes to nobody — **cannot** cancel this booking; the write-once `cancelled_at` latch never lands with an anonymous `{by}`. *(The refusal clause is vacuously true at v1 — FD-36 rules the `booking-link` class reserved, not minted; it stays as the law for the day a ruling opens that door.)* Run the positive own-cancel case for each class under `../security/SPEC.md §3`: a per-recipient credential with cancel in its operations metadata, a hold-confirm credential with its live own-booking binding, and a live single-visitor credential with its own-booking binding. Replay against a shared reader, a wrong request, a revoked credential and a valid per-recipient credential with cancel omitted from its operations metadata: no cancellation. Choosing Keep booking creates no diff. A still-authorized cancelled-record read returns only that booking's retained history; credential revocation alone never performs H8. Drive the guest-interactive request through its quarantined summary and content-bound cancellation readback. Keep booking preserves it; a later fresh confirmation cancels once under current terms, never deletes history or cancels a different request (Matt S14b/MT-76/CRUD-17).
- **H9 [MUST / counterparty move ≠ gated]** — *Given* the same booking and the same bound token, *when* the counterparty **moves their own slot** into the owner's published free space, *then* it is **not** gated on the owner either: the old slot frees and the new one places **atomically** (both or neither — a failed placement leaves the original standing), every §6.1 check runs on the new slot, and the owner is informed, never prompted to approve. *(Situation-A's beat — Jasmine moving her own Saturday lesson with Matt never asked, S14b; a move consumes a new slot, which cancel does not, so H8's rationale did not extend by construction and no scenario covered it. The engine walks the checks; the app surface is `../app/SCENARIOS.md` G10.)* Run the positive own-move case for each of H8's three classes using `../security/SPEC.md §3`'s class-specific allowance; a valid per-recipient credential with move omitted from its operations metadata cannot move. Require the current own-move allowance, exact accepted interval and binding even when the submitted diff claims another operation. A same-party token for a different request and a group reader cannot move this booking; a permitted coordinator uses §3.6's current arrangement/report checks. Lost-success recovery reads the same booking without another placement or quota draw, subject to current read authority. Drive the move through the same guest draft: a stale conflict keeps entered fields, leaves the old booking live and requires another selection plus fresh confirmation. Only current engine-proposed interval/price/terms are narrated; the private cause is absent. **Failed move keeps the original, including its reminder** *(added 2026-09-12; `../engine/SPEC.md §6` item 1 and `§1.15`)*: run the move refused — by a stale interval, a competing booking and an injected failure — and assert the original booking, its bound terms and its current reminder registration are unchanged, that the surface never presents the original as gone, and that a retry uses the same id rather than a fresh one. Run both winner orders of the move-versus-competing-write race and drop the winner's acknowledgement in each: exactly one result exists and is recoverable. Assert a stale-availability refusal leaves any live hold on its own clock and is never narrated as a revoked hold. Matt S11/MT-43/44/50/56 and S14b remain composition proof at the existing gates, with quota/pricing/reminder behavior owned by their homes.

## I. Handshake

- **I1 [MUST]** — *Given* two on-app users, *when* one books the other, *then* it resolves as two H1 instances working **the same one creator-owned commitment** through the engine share seam (no separate harness path invoked, no mirrored second record). *(The engine half is `../engine/SCENARIOS.md` I1–I9; the two must agree or the swap is a lie. Mechanism: `../engine/SPEC.md §7.1`.)* Walk read-only resolve → initiator creating commit → recipient `offered` arrival → stored recipient yes → acceptance apply; use no sale, form reply or expiry to cause the first arrival. At the real engine gate assert scoped creating-write replay yields one offer, hold and firing (`../engine/SCENARIOS.md` I1/A4); the local source fixtures assert only payload shape.
- **I2 [MUST / no new seam verb]** — assert the share path calls **only** the five pinned engine obligations (`INTERFACES.md §1`): `resolve` returns a read-only placement handle, the initiator's ordinary commitment-form `commit` creates the offer and emits `offered`, and acceptance applies through `commit(proposal_ref)` with the offer ref. A sixth verb would break the zero-harness-changes swap law — this is the harness-side guard on the one-machine ruling (#2).
- **I3 [MUST / counterparty's floor]** — *Given* an incoming share offer, *then* accepting is an **across-the-line act on the counterparty's side**: it fires only on a live confirmation or a matching accept-mode Grant (fits-schedule or per-person — D10). *Given* neither, *then* the offer's hold runs out and **nothing lands on the counterparty's board**. Silence is not acceptance.
- **I4 [MUST / offer is an ordinary trigger]** — the incoming offer fires the **existing** trigger loop; assert no share-specific harness path exists. Under `SPEC.md §4` and `INTERFACES.md §3.3`, assert an initial `offered` reaches exactly the stored recipient owner's inbox, never the initiator's; missing/wrong recipient or mismatched creation attribution refuses before recipient work. Duplicate arrival reuses the same inbox fact and cannot duplicate consent or a commitment. Exercise matching fits-schedule/per-person Grants and live manual yes, plus absent, expired, foreign and nonmatching basis; neither creation attribution nor an offer-side ShareGrant authorizes acceptance. These are BUILD Step 5 execution assertions; typed fixtures prove none of them.

## K. Loop verification (check-work)

- **K1 [MUST / bounded, attended]** — *Given* a turn whose commit produced stored structure that diverges from the normalized intent (stub returns a mismatched state on every read), *then* the agent detects the mismatch via read-back and does not terminate claiming success; it re-enters the loop **exactly once** and then surfaces the mismatch to the owner as a gap. Assert across the whole turn, **over the enumerated work-commit family** (`SPEC.md §3.14`'s two-write-families law — domain-structure writes and basis-spending commits; loop bookkeeping is excluded by name, the `FiringEvent` parts included) *(counted class named 2026-08-28)*: **at most two work commits, of which at most one spends a basis**, and **exactly one outward send** — the re-entry re-commits stored structure but never spends a second basis or re-fires an act already attributed (`SPEC.md §7`'s spend-vs-delivery-evidence split, which is also how this count composes with X4's one-commit-point constraint: the commit point is the basis-spending commit). **And assert the birth co-write is present in the generated sequences** — the record's birth rides the first work commit's payload under that commit's own write id and does **not** raise the count.
- **K2 [MUST / unattended]** — *Given* a trigger firing with no human present whose effect fails verification against the engine re-read, *then* the firing does not finish silently — it **parks** the commitment in a needs-human state (consistent with D4). Completion is never claimed unverified.
- **K3 [MUST / the loop terminates, and nothing escalates]** — *Given* a **trigger** firing whose engine re-read mismatches on every attempt, *then* the firing terminates in bounded attempts and **parks** the commitment. Assert the absences: **no act was re-fired** (the outward stub recorded exactly one call), and **no Escalation object was created** — a verification mismatch has no route to the ladder, because the ladder answers a missing basis and nothing here is missing a basis (`SPEC.md §3.9`).
- **K4 [MUST / a lost response cannot duplicate]** — *Given* a `commit` whose write applied but whose **response was lost** (the stub's `timeout` fixture: applied, no reply), *then* check-work's one retry re-commits **the same write id** (`INTERFACES.md §1.2`) and receives the original result — assert the store holds **exactly one** commitment, never two, and K1's at-most-two-commits count is unchanged. *Given* a genuine `unavailable` (nothing applied), *then* attended the turn surfaces a gap and unattended the firing **parks** — an infrastructure failure is never read as a semantic conflict, and never silently retried past the one bounded retry. **Private contact add (`SPEC.md §3.12`; S08b, MT-81/82):** drop Kai’s successful add acknowledgement and execute the one bounded same-payload/write-id retry. Assert the identical applied_ref/committed_revision and one new party/contact, four total Student identities, no duplicated group-reference add and no extra save question. Repeat after a fresh context/reload via the existing recovery path; never mint a replacement identity to recover an unknown result. A mismatched payload refuses; a second missing result remains unknown, never saved. Abandon the surrounding group draft only after resolving the add: acknowledged independent contacts remain, the group is not claimed saved, and the readback explains both outcomes. A duplicate answered input/OK cannot create a second write. **Entry-point recovery arm** *(added 2026-09-12; `../engine/SPEC.md §6` item 6)*: run each new entry point that can write — console request, form save, guest submission, resumed candidate apply — and assert its id exists before its first response byte by interrupting it after the write and recovering by that id alone. Assert an unknown outcome is resolved **before** any retry or discard is offered: a discard over an unresolved id is refused and surfaces the actual result, and `unavailable`/`timeout` are not definitive refusals, so the same id remains the only continuation and no duplicate write appears. A fresh intent is offered only after a returned or replayed failure envelope. These are Step-5 loop/recovery tests against stateful scripted storage; engine A4 owes durable apply parity.

### The firing record (`SPEC.md §3.14`) — five rows added 2026-08-28

- **K5 [MUST / a completing turn lands one record, in two parts]** — *Given* a turn with no horizon preparation that completes and verifies, *then* its `FiringEvent` **birth** lands **inside the firing's first work commit**, bundled under that commit's own caller-supplied write id, and its **seal** — carrying `termination: completed-verified` — lands **after check-work** as its own standalone bookkeeping write under the derived `seal_write_id`. Assert **exactly one record for the firing id**, both parts present, neither mutated; assert the firing's assembled context is **re-derivable from the birth's assembly reference plus the store** (L2's determinism, now against a stored reference rather than a live re-run); and assert **neither part raised K1's work-commit count**. **Preparation success control (§3.14(b)):** with assembled context and an availability request that may prepare, persist standalone birth first under `birth_write_id`; only its known durable result admits the request. Script a complete prepared window, then a successful authorized user work commit and check-work. Assert the unchanged single birth is required by that work commit, no second birth is bundled, all later calls/returns land in the final seal, and preparation's real effects are referenced through engine history, not counted as harness work or called cache writes. Repeat a no-work read/narration success and assert birth plus one standalone completed-verified seal, not a fused replacement. These full controls close at BUILD Step 5 checkpoint (2); engine M4 owns real materialization.
- **K6 [MUST / re-entry cannot mint a second birth]** — *Given* K1's mismatch fixture without horizon preparation, so the turn re-enters **exactly once** under the same firing id, *then* there is **one** record: the birth bundled with the firing's **first** work commit **only**, and the re-entered turn bundles no second birth. Assert the absence structurally, not by count alone: a wrongly re-bundled birth carrying an **identical** payload is **inert** (§6's idempotency returns the original result) and one carrying a **different** payload is **refused whole** (`{conflict, reason: write-id-reuse}`, the whole bundle rejected) under the (firing id, part) part-identity law — **a second birth is never representable**. The seal carries the turn's final outcome per the lifecycle. Repeat with a stored pre-preparation birth fixture: the first and re-entered work commits both preserve it without re-bundling or mutation; identical part replay is inert and a conflicting birth refuses the whole write. This stored-fixture control stays at BUILD Step 5 checkpoint (1), with assembled-context success in K5 at (2).
- **K7 [MUST / a post-commit park seals on the park's own write]** — *Given* a firing without horizon preparation that makes a work commit and then parks (`needs_human`, K2's shape), *then* the birth rode the work commit and the **seal rides the `needs_human` write** — co-written inside it, bundled under that write's id — carrying `termination: park`. Assert no third write appeared for the record, and that the park write is **not** counted by K1/X4 (it is loop bookkeeping, excluded by name, `SPEC.md §3.14`).
- **K8 [MUST / required-on-read fails closed, and an absent seal is not that case]** — *Given* a stored `FiringEvent` carrying a member the reader does not know, *then* the read **refuses whole** — a structured error, **never a partial read and never a skip**. Assert the absence of any partial-read path. *And the negative half:* an **absent seal** is **not** this case — a birth with no seal reads as the defined `interrupted` state, derived from the seal's absence and **never stored as a member**; assert it surfaces rather than erroring, and that no `interrupted` value is found anywhere in the store. With a stored pre-preparation birth and completed engine-chunk history fixture but no work commit, process death still reads interrupted, preserves the engine facts, and cannot claim that nothing happened. With birth alone and no chunk, the same interrupted reading must not invent preparation. The stored-fixture control stays at BUILD Step 5 checkpoint (1); real chunk durability is engine M4.
- **K9 [MUST / the residual path lands late, keyed and independently retryable]** — *Given* a firing whose engine seam is **unwritable at termination** (`unavailable`/`timeout`, `INTERFACES.md §1`'s infrastructure members) so its record is held as process-local state, *when* the engine becomes writable, *then* the record lands **late**, keyed to the **same firing id**, with **each part as its own standalone call under its own derived write id**. Assert **independent per-part retry**: a retried part repeats idempotently on its own id while the other part's presence or absence is undisturbed. Assert **both** the firing time and the engine-attributed arrival time are recorded, so lateness is visible; and assert K1's count law is **undisturbed** — the late landings are bookkeeping, not work commits. *(The printed ceiling this row cannot test: a process death before the engine returns loses the unsealed content — detectable on read as `interrupted`, K8's half.)* If the birth write before preparation is unavailable or its response is lost, hold the availability request: resolve/replay the same `birth_write_id` without a second birth before proceeding. A crash after durable birth but before preparation takes post-birth recovery, with no automatic replay of the firing; a crash before any birth uses the existing pre-birth loss path. Do not treat a missing response as proof of missing birth or permit unrecorded preparation. Direct scripted controls stay at BUILD Step 5 checkpoint (1), composed recovery at Step 8 X4/X8.
- **K10 [MUST / the co-write is a lawful seam input]** — *Given* a birth (or seal) bundled into a host work commit per `SPEC.md §3.14`, *then* the bundle rides the host commit's **closed input forms** under the host's **one** caller-supplied write id (`../engine/SPEC.md §6`'s one-id-covers-the-bundle shape) — no second input form, no second id, no widened `commit` signature (`INTERFACES.md §1.2` unchanged). Assert at the seam: the stub accepts the bundled payload as one commit call; a bundle whose part conflicts with its (firing id, part) slot is **refused whole**; and the engine-side admission sentence is the engine's law (`../engine/SPEC.md §6`), consumed here, never restated.
- **K11 [MUST / independent allowances identify the stop]** — *Given* injected routing limits and a scripted loop, *when* unfinished work reaches each independent allowance in turn, *then* the firing takes `SPEC.md §4a`'s budget stop, carrying the corresponding `SPEC.md §3.4` measure and unit-consistent ceiling/consumed values. At Step 5 checkpoint (1), build the first four positive/negative pairs as precursors without assembled-context or credential machinery; K11 closes at checkpoint (2) with the assembly/request-release pair:
  - Zero-effect routed reads reach the step limit while effect and provider-charge capacity remain: no next routed step executes. Control: an otherwise identical read below the step limit runs, with no basis spend or provider charge. Bookkeeping writes do not inflate either count (`SPEC.md §3.14`).
  - A permitted basis-spending commit consumes the existing effect allowance; a further required basis-spending act is stopped despite spare steps and provider budget. Control: zero-effect check-work can still verify the first effect when the other allowances permit it. A denied or missing basis exercises the floor's ask path instead, never a fabricated effect-budget trip (`SPEC.md §7`).
  - Direct scripted model calls with zero basis spends exhaust provider charge before the step limit. Use both successful returns and priced failures, including primary retry/fallback under `../model/INTERFACES.md §2.2`; further attempts stop, and failed output cannot erase charge. Control: a within-budget retry/fallback succeeds under `../model/SPEC.md §8` without a second effect; a further failure never grows the bounded ladder.
  - Re-entry retains consumed steps/effects/charges. Combine budget exhaustion with a previously dispatched outward act: unknown fate remains `unverified`, never resent or reminted; known-but-unpersisted and persisted receipts retain their identities and survive bookkeeping retry, with no refunded basis (`SPEC.md §§3.14/7`). A pre-dispatch stop after basis commit also preserves the spend. Preserve any existing seal and C9's no-self-clear law.
  - At checkpoint (2), complete the provider-charge case through assembled `summarize` input with L7: successful and priced-failure quarantine attempts count, and a budget stop before a valid summary admits no raw text. B11 supplies the durable-ask/request-release control with outstanding accounting; no held-open human request is needed. External-credential daily accumulation/suspension and active-request cancellation are later composition controls at Step 8 and `../model/EVALS.md §3`, not checkpoint (1) Givens.

## R. Turn flow — the step machinery (`SPEC.md §4a`)

*Added 2026-08-28 with §4a. Until that section existed, `BUILD.md` Step 5's "normalize → plan → tool calls" had no scenario for **plan**, so the one design decision the whole loop rests on — who chooses the next call — was untested in both directions. Every row composes existing machinery; if satisfying one tempts a fifth model-seam call or a `tool_schemas` context member, that is the finding, not the fix.*

- **R1 [MUST / dispatch is deterministic]** — *Given* the same `normalize` output replayed against the same store state, *then* the harness routes the **identical tool sequence** both times — same tools, same order, same arguments — from the routing tables alone (L2's determinism discipline moved down to the dispatch layer). Assert the sequence is a function of `{intent, store state}` and of nothing else; assert in particular that it does **not** vary with anything the model returned beyond the intent and its fields. **Adaptive-proposal arm** *(2026-09-12, FD-105 — `SPEC.md §4a`)*: replay the same owner-turn return when that return is a **proposed next permitted owner action** rather than a transcription, and assert the routed sequence is still identical — a proposal enters dispatch as `{intent, fields}` and nothing about its being a proposal reaches the tables. Script one proposal the tables map (dispatched), one carrying an intent outside `../model/SPEC.md §2`'s vocabulary, one naming a rule shape off the engine's closed menu, and one whose act has no covering §7 basis: each of the last three is **inert data with a recorded disposition**, never an executed call, and none of them varies the routing of the others. Assert a **structured trigger firing** is unaffected — it calls no `normalize` and admits no proposal (FD-28; R3's seven fixtures).
- **R2 [MUST / no model-chosen call]** — assert the affordance is **absent**, not merely unused (the D9/N2 absence style): **no model output can select, add, reorder or skip a tool call.** *Given* a scripted model return that attempts it — naming a tool, requesting a sequence, or claiming a next step — *then* it is **inert data**: the routed sequence is unchanged and the attempt is recorded rather than obeyed. *And assert the seam is not widened:* the model seam carries **exactly four call types** and no `tool_schemas` member reaches any assembled context — the model is never shown the `SPEC.md §5` tool surface (`INTERFACES.md §2`, the four-call pin; §4a). **What FD-105 amends here, stated as the exact line the row now holds** *(2026-09-12; the row is **narrowed**, not retired)*: the absent affordance is the model **selecting, adding, reordering or skipping an executed call** — a returned tool name, a requested raw sequence of tools, or a claimed next step is still inert data, recorded rather than obeyed, and **nothing the model returns executes without the harness validating and dispatching it**. What is no longer asserted absent is the owner-turn **proposal** of a next permitted action through the existing `{intent, fields}` return (R1's adaptive arm). Assert the distinction directly: a scripted return naming `calculate` by name changes no routed sequence, while a scripted return proposing a permitted owner intent is dispatched **through the tables** — and assert the proposal path still carries no tool name, no tool schema and no reordering power, so the four-call pin and the never-shown tool surface both hold unchanged.
- **R3 [MUST / per-owner serialization closes the read-staleness hole]** — *Given* one owner and a firing in flight, *when* a second trigger lands during it, *then* the second firing **queues** and its slice is assembled **after** it claims — so it sees the first firing's commits, never a store state the first has already moved. Assert on the virtual clock, and assert **all seven trigger sources and console turns queue identically** — no wake-priority ordering exists to test around (§4a; the engine's commit-time arbitration is a different wall, `INTERFACES.md §1.2`). **The seven-source claim runs from seven declared fixtures, one per source** (sale, hold-expiry, decline, returned-form, clock, delivery-report, offered): a source with no fixture is a hole this row turns red on, never a silent narrowing.
- **R4 [MUST / completion is proposed by the sequence, verified by check-work]** — *Given* a routed sequence in which every step has executed or been **refused with a disposition**, *then* that completion is what enters **check-work** (§4), which verifies it before the firing terminates. *And the negative half:* an **unfinished** sequence — a step neither executed nor dispositioned — **never claims done**: assert the firing does not terminate as `completed-verified`, and that no model output can make it (K2's "completion is never claimed unverified", now with the thing being claimed defined).
- **R5 [MUST / the compound utterance — independent steps proceed, dependents take `refused-unmet`]** — *Given* a compound utterance normalized to `normalize`'s ordered-sequence branch (`INTERFACES.md §2.1`) in which one step is refused at the floor, *then* every step **independent** of the refused one executes normally — **no global halt** — and every step **dependent** on it takes the internal disposition **`refused-unmet`**, stored and reported at the routing layer, deliberately **not** a seam refusal reason (`INTERFACES.md §7.1`'s closed table unchanged — assert no such reason string crossed the seam). The sequence terminates through R4's law: every step executed or dispositioned, check-work verifies, and an unfinished sequence never claims done (`SPEC.md §4a`, part of FD-86).
- **R6 [MUST / recoverable ownership, FIFO re-attempt and visible loss]** — *Given* a firing claimed from the engine-stored inbox that dies **before its record's birth** (no work commit, no birth part), *then* the claim **releases back to the inbox** and the trigger is re-attempted in FIFO order, **and an owner-visible loss record is written** — attributed, surfaced like any gap (FD-96, `SPEC.md §4a`). *Assert both arms, and the failure of each alone:* re-attempt with **no** record is a silent retry loop — assert the record exists; a record with **no** re-attempt drops a trigger the corpus promised would fire — assert the re-claimed firing runs. Use injected time and two worker fixtures sharing one inbox: race initial claims, kill or pause the winner, race and replay recovery, then revive the old worker. Assert one effective current worker, one release/loss disposition per lost attempt, a later distinct death's separate loss, and no old recovery releasing a successor. Advance across the chosen adapter's invalidation boundary without wall-clock sleeps. Stale fresh-id work writes and loop advances cannot apply; replay of an old applied write returns its original result but restores no ownership. Pause between an ownership check and actual dispatch, take over, then release the old worker: zero stale sends or publishes. Retain R3's seven-source/console FIFO and after-claim assembly assertions at their existing checkpoint.
  **Post-birth control:** kill after the atomic work/birth commit, including a lost commit response. Resolve by the same write id: no pre-birth loss/re-enqueue is invented, no second birth or new firing executes that intent. The dead claim releases ordinary queued work while its unsealed record remains `interrupted` under K8. The old worker cannot make a new seal decision. Authorized resume/discard is exercised at X4/X8; until then no automatic continuation, human park clear, basis reuse or resend/remint. **Evidence control:** a result already observed by the old worker, or returned later from its previously authorized dispatch, persists exactly once under its receipt id after takeover; duplicates are inert, altered same-id payloads refuse, and no new dispatch occurs. Preserve committed work, basis, birth and any existing seal. Engine-unwritable observation loss retains K9/§3.14(e)'s ceiling, never a claimed durable outbox. These are runtime assertions owed at BUILD Step 5, with turn controls at Step 8.
- **R7 [MUST / registrations route by their closed kind, and the firing carries it]** — *Given* two same-instant registrations carrying **declared members of the closed union** — one `offer-hold`, one `reminder` (`reminder | offer-hold | ask-age-out | escalation-deadline`; a value outside the union is unconstructable, which is the very shape this row proves) — *then* the seam's kind-routed mapping (`../engine/INTERFACES.md §2.2`) holds **at the routing assertion**: the `offer-hold` registration's firing arrives as the **`hold-expiry`** Event member carrying `registration_kind: offer-hold`, and the `reminder` registration's as the **`clock`** member carrying `registration_kind: reminder` — both payloads exactly as `INTERFACES.md §3.3` defines them — and the two are **distinguishable at the point the loop routes them**, never collapsed into one source. The **field half** (the closed `kind` on the stored registration shape) gates at Step 1; the **routing half** (the walk through the loop) gates at Step 5. Writer/firing paths per kind: `reminder` — D28; `offer-hold` — C8; `ask-age-out` — B11's firing half; `escalation-deadline` — D12–D14/D16 at Step 5 checkpoint (3), retaining SHOULD weight. R7’s Step-1 field half includes a typed escalation-kind clock carrier and negative field/kind/source controls; its routing half may inject that carrier at checkpoint (1) but cannot close the later ladder lifecycle from injection alone.

## L. Context assembly

- **L1 [MUST / self-sufficient firing]** — *Given* a trigger fires with no conversation (a hold expiry at 03:00), *then* the assembled context contains the **handoff frame** — the trigger event, the affected commitment's stored structure, and the covering rules/stored answers read through declared `calculate` snapshots — and the firing completes (or parks) without needing any conversational history or an EngineStub store escape.
- **L2 [MUST / no residue, deterministic]** — *Given* the same trigger fired twice (replay), *then* the assembled context is **identical both times** — built from declared EngineSeam read snapshots and stored structure only, carrying no prior-turn conversational residue.
- **L3 [MUST / bounded slice, deterministic degradation]** — *Given* a firing whose relevant slice would exceed the context budget (a busy governed board: a large SOP, an N-member order, six scope levels of covering rules), *then* the slice is truncated **deterministically** by the priority order (nearest scope first — the §6 ladder), the **same truncation on replay** (L2 holds), and the firing either remains **self-sufficient** or **parks** (D4) — it never proceeds on a silently-truncated context. **Self-sufficient is `SPEC.md §8`'s defined predicate, and the fixture exercises it from both sides:** one case where only non-covering material dropped (the firing proceeds), and one where a **named covering rule** would drop (the firing parks — assert the park carries `reason: slice_unfittable` and its `cause` names the deterministic covering rule, not a size — `SPEC.md §3.4`).
- **L4 [MUST / spotlight tags]** — *Given* a firing whose assembled context mixes owner text with guest/import/document text, *then* **every** assembled string carries its `owner | guest | import | document` source tag, stamped at the door that admitted it; assert a guest/import/document string is **never** tagged `owner`. Layer-1 spotlighting (`SPEC.md §8`; `INTERFACES.md §2.1`) — the labeling wall, asserted **on the wire**, not on model obedience.
- **L5 [MUST / dual-model isolation]** — *Given* any channel the layer-2 property covers (`SPEC.md §8` — guest-returned free text, **connector-sourced text such as a calendar entry a stranger wrote**, surface-arrived text, or admitted authored/named or catalog SOP text; a vault-resident artifact is **not** a channel — it never enters context at all, V1's law, corrected 2026-08-21), *then* the raw stranger text is read only by a **quarantined, tool-less model** that returns a structured summary, and the privileged tool-bearing model's context contains **only that summary — never the raw text** (spy on what the privileged model was shown). The fixture corpus carries at least one string per channel class, the calendar-import string included. Layer-2 isolation (`SPEC.md §8`, FD-2); assert an obeyed non-owner instruction fails the scenario. The quarantine suite is **seeded from a named injection-fixture corpus** — public-benchmark-sourced, **permanent test data**, committed under `tests/fixtures/injection/` with a provenance README — built at `BUILD.md` Step 5, which is where the corpus first exists (FD-8; the location's normative home is `../security/SPEC.md §5`, which is also where *permanent* is law). Add the guest-interactive channel with raw date/time/duration plus an injected owner-rule instruction. Assert the legitimate request survives summarize, all derived text remains guest-tagged, and neither normalize nor narrate receives board inventory, private blocking events/places, private contacts, other participants' standing or unauthorized reports. Test a shared reader and coordinator against the same projection; host-supplied scope alone determines differences. Authorized published copy is not laundered into an instruction channel.
- **L6 [MUST / deictic view-context]** — *Given* the app stamps a console utterance with view-context `{surface, visible_range?, selected_ref?, draft_ref?, draft_revision?, field_ref?, scope_ref?, ask_ref?}` (`SPEC.md §8`; `INTERFACES.md §2.1`), *when* the owner says a **deictic** utterance ("push this back an hour") with a card selected, *then* the referent resolves against the stamped `selected_ref` **deterministically** — no guess, no conversation. *Given* no `selected_ref` and an ambiguous "this", *then* the agent asks (gap-4), never guesses. The app-side companion — that the stamp is actually emitted — is `../app/SCENARIOS.md C5`. **Matt dated control (S04, MT-26/27, HR-02):** from September Week context, resolve Monday 12 October 2026 Asia/Taipei through engine verification and select that exact occurrence/generation in 11–17 October. Retain year and zone through the form and reload. Monday 15 October 2026 requires clarification with zero target writes; ambiguous year/zone, two matching runs, or a stale generation never becomes a guessed next Monday. Navigation changes the visible range, not the schedule or accepted scope; unavailable/unloaded ranges are not evidence of absence. **Current-edit controls (`SPEC.md` §§6/8, `INTERFACES.md §3.1`):** a selected stored row alone cannot replace unsaved fields. Supply the same current draft ref/revision in form input and console context, including typed travel values; verify matching fields, source tags and bounded assembly without transcript. A missing, stale or foreign draft cannot supply acceptance. Change only the activity/title and place nicknames while preserving typed inputs: the same draft/proposal path and computed request must result. An unrelated field edit uses this same carrier; it does not require a travel object. Trigger context contains no interactive draft. These are Step 5 context/call controls, not proof of broader authoring or persistence. Repeat with a selected per-use Meeting link and personal Notes edit, retaining field_ref/scope_ref/ask_ref and document_context; an accepted explicit blank stays blank and a renamed row retains its binding. The stored template/sibling booking cannot replace the selected use's unsaved value. Read accepted proposal structure alongside pending asks; a displayed echo is never a context source. Vary story names and field labels without changing typed identities to exclude domain-specific routing.
- **L7 [MUST / quarantine fails closed]** — *Given* an untrusted stranger channel whose `summarize` call fails (timeout, malformed return, **and the fallback also failed** — the stub's failure fixture, `INTERFACES.md §5`), *then* the raw text is **never admitted to the assembled context** — assert its **absence on the wire**, not merely that the privileged model did not use it — and the firing surfaces a gap (attended) or **parks** (unattended, D4). Assert **no Escalation object is created**: a failed quarantine read is not a missing basis (K3's discipline). Layer-2's failure half (`SPEC.md §8`; `INTERFACES.md §2.4`; `../model/SPEC.md §8`; law at `../security/SPEC.md §5`). *A run that admits the raw text with a warning attached fails this scenario — that is the control degrading into no control at the moment it failed.* For guest-interactive, exercise every model error and bounded retry/fallback outcome: no valid summary means no normalize with raw input; preserve permitted draft fields and surface the attended gap. Exhausted firing/request spend instead retains the existing typed budget park and accounting; another message or reload cannot clear it. Ordinary stored page rendering requires zero model calls even while the interactive binding is unavailable.
- **L8 [MUST / tags survive derivation]** — *Given* the harness computes a value from more than one tagged input (a guest-returned field folded into an assembled line; an owner note joined with an imported string), *then* the derived value carries the **least-trusted** input tag under `owner > import > document > guest`, and carries `owner` **only** when every input was `owner`. Assert the laundering case directly: a value derived from one `guest` string and one `owner` string is **never** `owner` on the wire (L4's assertion, one derivation step later), and is **not countable** by the noticed-pattern detector (`SPEC.md §6`; B6) — a stranger must not be able to manufacture the repetition that produces a rule proposal on the owner's board. Law and ordering: `../security/SPEC.md §5`; assembly is `SPEC.md §8`; wire form is `INTERFACES.md §2.1`.

## N. Money as records

- **N1 [MUST / marks are internal]** — *Given* a customer pays at the desk (outside the app), *when* the owner marks the booking `paid` (and later `settled`), *then* the mark is an **internal** act — no floor crossing, no value moves — and it is attributed `{by, at}` where both sides can see it. **Payer, channel and one-record arm** *(added 2026-09-12; the law is `../engine/SPEC.md §1.9`)*: the owner records one payment for a four-customer order's whole total, naming a payer the order already carries and the channel they named. Assert the mark carries `{amount, currency}`, `{by, at}`, that payer and that channel as **reported**; assert exactly one mark exists for the amount, with no per-customer mark and no split; and assert the recording call still classifies **internal** with zero outward acts and no external confirmation consulted — the same absence assertion this row already makes, over the payer and channel members too. Then correct the record and withdraw it, asserting after each that the superseded record and its reporter stay readable and that no mark was cleared. A guest credential on its own booking attempting to record another party's payment is refused (`SPEC.md §7`; `INTERFACES.md §7.1`'s `refused/credential-invalid`), writing nothing.
- **N2 [MUST / the class is empty]** — assert that **no tool in the contract declares the `value-transfer` reversibility class** — the class exists (future rails would occupy it) but is unoccupied; nothing in the harness can move money.

## P. Engine-originated round-trips

*Added at engine-design time (2026-08-05): the engine's reshuffle-as-proposal (`../engine/SPEC.md §7, §1.11`) rides existing harness machinery — this scenario pins the composition so the stub-swap (`../engine/SCENARIOS.md` Z2) cannot pass vacuously on the proposal path. It names **no new tool and no new seam verb**; every step below cites the existing behavior it composes.*

- **P1 [MUST / compaction pass-through]** — *Given* a mid-day cancellation trigger fires and the owner wants the day pulled tighter, *then* the harness, using only existing machinery: **(1)** asks the **direction** as a single per-event question (elicitation, B3-style — never a stored default); **(2)** calls `resolve` with a compaction goal and receives a **Proposal handle** (the §1.5 seam, unchanged signature); **(3)** narrates the proposal from its display facet — every fact traces to the handle's projection (D7); **(4)** on the owner's yes, sends each move request via `send` — outward, floor-gated (D1), attributed (D6); **(5)** collects each moved party's answer via `on_form_return` with token attribution (H6); **(6)** applies via `commit(proposal_ref)` only when every move is confirmed — a declined move applies **nothing** (K1 read-back verifies the applied state); **(7)** routes the freed-window **keep-vs-reopen** question through store-routing (G1), the window staying blocked until answered. *Assert at the end: no tool beyond the §5 contract was called, and the harness needed no change to run the flow.*
- **P2 [MUST / the pending-decision round-trip]** — *Given* **the harness's own clock trigger** firing at `start − decide_by` (`SPEC.md §4`'s fifth source — the engine's write is what that firing *finds*, never a trigger source of its own, `SPEC.md §6`), where the engine has written a `needs_human` park and a `PendingDecision` with choices *run under minimum / cancel / extend the window* (`../engine/SPEC.md §1.14`, §3 `min-occupancy`; `../engine/SCENARIOS.md` O2), *then* the harness, using only existing machinery: **(1)** reads the park and its choices through `calculate` — **no new seam verb** (I2); **(2)** surfaces them to the owner **as the engine named them**, adding and renaming nothing; **(3)** on *cancel*, treats the customer-facing consequence as an across-the-line act — floor-gated (D1), attributed (D6); on *run under minimum*, an internal write at full autonomy; **(4)** stores the choice through the existing stored-object `diff`, including `chosen { choice, by, at }`, which is what clears the park — `cleared_by` is the human (C9); **(5)** with **no** answer, the park stands and the run is neither cancelled nor confirmed. A valid human answer persists its selected choice and attribution; engine/LLM/default answers, off-menu choices, and forged actor/tenant/scope/`ShareGrant` fields refuse; replaying the answering write id applies once. Two legal answers in separate fixtures with identical human and instant remain distinct by `choice` after a reader restart. *Assert: no tool beyond the §5 contract was called, and **no Escalation was created** — a pending decision is not a missing basis (K3, `SPEC.md §3.9`).* *And the ordering is asserted, not assumed (2026-08-31):* the engine's own clock consumer ran **first** — the park and `PendingDecision` exist **before** the harness firing assembles its slice (§4a's after-claim assembly); run the **reverse** ordering and the scenario must **fail** (the firing finds nothing and terminates empty) — the green depends on engine-first, so the dependency is pinned rather than lucky.

## O. The assisted off-app path (`SPEC.md §2` — *"assisted, recorded, and converted"*)

*The edges the network cannot reach. `SPEC.md §2` has always specified this — the agent prepares the owner's phone call, records the answer as ordinary internal writes, and offers a floor-gated invite — and until now no BUILD step claimed it, so a specified capability had no build home. These scenarios pin it. Drawn from `../user-stories/Situations/Situation-C/situation-5.md`, the minimal-adoption world: the four unavoidable calls, prepared and recorded, each becoming an invite. **Every scenario below composes existing machinery — no new tool, no new seam verb, no new trigger source, no second authorization path.** If satisfying one of these tempts a new atom, that is the finding, not the fix.*

- **O1 [MUST / the call is prepared, never placed]** — *Given* an outward need whose counterparty is off-app and unreachable by any channel the app can send on, *when* the agent handles it, *then* it assembles a **preparation for the owner** — who to call, what to ask, and which stored fields the answer will land in — and **fires nothing outward**: no `send`, no third-party contact of any kind. Assert the app spy recorded **zero** outward acts for the firing, and that the preparation's facts are `narrate(structure)` over stored data, never free-composed (D7). The owner places the call; the harness never does.
- **O2 [MUST / the answer returns as ordinary internal writes]** — *Given* the owner comes back from that call with an answer, *when* it is recorded, *then* it lands through the **existing** write path — diffs via `CRUD_*` under the floor, attributed `{who, basis, when}` where the basis is the owner's own act — and classifies **internal**: the owner reporting what a human told them moves nothing outward and crosses no line (N1's shape). Assert **no tool outside the `SPEC.md §5` contract was called**, and that the resulting store state is indistinguishable from the same fact arriving through any other door **except provenance, which stays honest** *(corrected 2026-08-21 — the prior "indistinguishable" contradicted L4)*: the *act* is the owner's and attributes to them, but the answer's **free text is a third party's words** and is never tagged `owner` — it is not countable by the noticed-pattern detector, exactly as a `guest` string is not (L8). A relayed stranger is still a stranger. **Reported-outcome arm** *(added 2026-09-12; `../engine/SPEC.md §1.9`)*: the answer the owner brings back may be an outcome — an end, who attended, a payment received. Assert it lands through this same path with the **basis** stored as the owner's own act when they witnessed it and as *another person's statement, recorded as reported* when they are relaying one, and assert the relayed case is never stored as that person's authenticated act. This is the provenance clause above applied to outcome facts; it adds no tool and no path.
- **O3 [MUST / the invite is an ordinary across-the-line act]** — *Given* a prepared call whose party is not on annnä, *when* the agent offers to invite them, *then* the invite is **across the line** and needs a live confirmation or a matching `Grant` exactly like any other outward act (D1), attributed (D6). Assert that having just spoken to someone **creates no basis to message them** — the call is not consent — and that a declined offer sends nothing and **stores the decline as a small owner-and-party record** (declined/revocable, persisted through the existing `commit` verb — the `SPEC.md §3.10` precedent: a fact about the owner-and-party pair fits none of the five routing shapes, so it gets its own stored shape, the builder's design work at `BUILD.md` Step 7) rather than re-offering on the next firing for the same party.
- **O4 [MUST / no contact without a basis, over the whole path]** — assert the assisted path adds **no new way to reach a real person**: the preparation is owner-facing (O1), the answer is owner-supplied (O2), and the invite is the single outward act and is floor-gated (O3). Therefore the floor property (D20) covers this flow **unextended** — run D20's property-based test over sequences that include assisted-call firings and assert it still holds with no new case added to it. A path needing its own floor rule would mean the floor was never a property.
- **O5 [MUST / a silent off-app party lapses, it never blocks]** — *Given* a clocked offer to a party reachable only by an assisted call, *when* no answer is recorded before the offer expires, *then* it **lapses on the virtual clock and cascades exactly as C8 specifies** for any other offer. Assert the assisted path introduces **no special waiting state, no second expiry mechanism, and no indefinite park** — an off-app counterparty's silence is ordinary silence, and Situation C's clocked offer past a silent freelancer is this scenario.

## X. The external surface (`SPEC.md §5.3`, `INTERFACES.md §6`)

*Appended 2026-08-21 — the harness half of `../PRD.md` RQ-2 through RQ-9, **plus RQ-13's gate (X7)**. The credential's own custody and adversarial suite are security's (`../security/SCENARIOS.md` T9); everything below is testable against stubs, the credential check scripted like any other neighbor (`INTERFACES.md §5`). Every scenario composes existing machinery — the share family's guard applies: if satisfying one tempts a new verb or a second authorization path, that is the finding, not the fix.*

- **X1 [MUST / same contract, no new verbs]** — *Given* the tool contract before and after the external surface exists, *then* the verb rosters are **identical** — assert set equality, the I2 discipline pointed inward. *And* assert both parity directions over the enumeration **net of RQ-2's excluded classes** (guest, closed-service admin, appearance — FD-19; authorization-and-recovery — FD-26): no non-excluded verb is reachable only from the console, and no verb is reachable only by an external client (RQ-2, RQ-3). *And* assert the exclusion holds as a refusal: an external call at an authorization-and-recovery capability is refused, whatever credential it carries.
- **X2 [MUST / the floor binds identically]** — *Given* an external client holding a valid credential and an across-the-line act with no matching basis, *then* the act is refused on exactly the console's terms (D1's assertion, new caller) — possession of the credential creates no basis, attribution is never permission — *and* the refusal is **recorded and surfaces in the owner's console as a card** — the trigger-results card law (`../app/SPEC.md §3`), no new surface; the card's shape is the builder's design work at Step 8 — not only in the caller's response. *Given* a matching Grant, *then* the act fires attributed `{who: the issuing principal, basis, when}` with the client identified alongside (RQ-7; `../security/SPEC.md §3`).
- **X3 [MUST / no session affinity]** — *Given* two identical calls against the same store, in different orders relative to unrelated calls and from different clients, *then* the answers are identical; *given* an interaction spanning more than one call, *then* its continuation is addressable **only** by an explicit handle the client holds — assert no implicit server-side session exists to test around (RQ-5).
- **X4 [MUST / resume-or-abandon]** — *Given* a turn — **one firing** (`SPEC.md §5.3`; a multi-call interaction like P1 spans firings and is not one turn) — interrupted mid-flight, *then* a client can query the turn's state **by the write id it supplied at call time** (`INTERFACES.md §1.2` — available before any response, so a response that never arrived cannot orphan the turn) and either **resume** it or **discard** it. Resume completes the turn without re-firing any already-attributed act (§7's re-entry law). Discard leaves **no half-applied domain effect**: the store holds the atomic user work commit or none of it; completed engine preparation chunks and their history survive separately under `../engine/SPEC.md §5`; abandoning after commit preserves that work, birth and spent basis without claiming verification (`SPEC.md §7`) — *(reworded 2026-08-21: the former "no effect of the turn at all" was literally unsatisfiable, because attribution records and the turn record itself survive by law — §7 requires the evidence that an act fired to outlive the turn)* — and no across-the-line act fires during the abandon. Neither path requires the client that started the turn to be the client that ends it (RQ-6) — **assert it concretely: a second credential of the same principal reads the turn by its write id and resumes; a credential of a different principal reads nothing** *(2026-08-22, `../engine/SPEC.md §6.6`'s two-key split — the apply record stays per-credential, the turn-state read is per-principal)*. *Step 8's design constraints, stated so a passing build cannot break §7 or P1: **one work-commit point per turn** (the enumerated work-commit family, `SPEC.md §3.14` — loop bookkeeping is excluded by name) *(counted class named 2026-08-28)*; outward acts ordered strictly after it; the act's `{who, basis, when}` record written inside that same commit point, with the `FiringEvent` birth atomically co-written or the already durable pre-preparation birth required and preserved under §3.14(b), so an interrupted send can never re-fire as unspent and no firing that touched domain state can lack its record's birth. The **seal** lands per §3.14's lifecycle — riding a terminal bookkeeping write or standing alone — and **never inside a second work commit**.* **Recovery control (`SPEC.md §4a`):** start from R6's retired post-birth claim. A second authorized credential of the same principal resumes or discards through the existing turn handle and acquires exclusive ownership; a different principal and a withdrawn credential cannot. Race two authorized continuations and assert only one acts as current worker, with no stale effect or second birth. No response, elapsed time or automatic recovery supplies that authorization; committed basis, receipts and any existing seal remain unchanged.
- **X5 [MUST / the door stamps]** — *Given* free text arriving through the external surface, *then* it enters tagged `import` — stamped at the door, and a caller-asserted tag is **ignored, not honored** (assert a call attempting to claim `owner` lands as `import`) — takes the layer-2 tool-less read before the privileged model sees anything (L5's assertion, new door), and fails closed on no-summary (L7's assertion, same door). *Given* structured parameters — enums, handles, IDs, dates — *then* they pass with **no** layer-2 read and no quarantine cost (RQ-8). *And* assert L8's laundering case through this door: a value derived from surface-arrived text is never `owner`-tagged and is not countable by the noticed-pattern detector.
- **X6 [MUST / the enumeration is the record]** — *Given* the capability enumeration (`INTERFACES.md §6`), *then* every enumerated owner capability outside RQ-2's excluded classes (FD-19 appearance, FD-26 authorization-and-recovery, guests, closed-service admin) is reachable by an external client under the same floor, and every tool in the contract appears against a capability — **set equality in both directions over the non-excluded set**, mechanically checked (the gate in the `roster-check` shape); a capability reachable from outside the app and absent from the enumeration is a defect wherever it appears (RQ-2, RQ-9).
- **X7 [MUST / the compatibility policy exists before the first credential]** — *Given* the surface at the moment a credential would first be issued, *then* the compatibility policy (`INTERFACES.md §7`, the RQ-13 requirement at `INTERFACES.md §6`) **exists and the enumeration references it** — issuance with no policy on record is refused; assert the refusal. The policy names what constitutes a breaking change **including the widening of a closed enum or return shape**, the notice a holder gets, and how long a superseded shape stays callable. *(What no scenario can assert — that a future breaking change actually honors it — is the deployment floor's discipline, `../deployment/SPEC.md §7`, not a test.)*

- **X8 [MUST / the owner's brake]** *(added 2026-08-28 — F4; `SPEC.md §7`'s owner's-brake law. RQ-6's machinery was specified for an external client and the owner's own stop was left inferable.)* — Exercise `send` and `publish` cancellation at each boundary in that law. **Before work commit:** no applied user draft or spent basis; no dispatch or receipt. Without preparation birth, fused birth/`discarded` seal and visible discard confirmation; with a pre-preparation birth, preserve completed engine chunks and that birth and write only the `discarded` seal (§3.14(b)/(d), engine §5). **Committed, not dispatched:** the atomic work, existing birth and spent basis survive; zero dispatches or fabricated receipts; a separate `discarded` seal, never a second birth or restored authorization. **Dispatched, result unknown:** preserve committed facts and any known receipts; `needs_human.reason: unverified` with its co-written `park` seal; zero resend/remint. **Known result observed, receipt not yet persisted:** pause the receipt write, cancel during check-work, then release storage; assert the complete known result persists exactly once under its own receipt id, distinct from the work id, and the stop seals `discarded` without claiming verified completion. Repeat with receipt-write timeout and replay of that same id: no new outward call, no lost mint set. **Receipt already persisted:** cancel during check-work; the identical receipt survives beside `discarded`; retrying persistence changes neither fact nor basis. **Already sealed:** a later stop cannot overwrite the seal. In all cases count work commits separately from receipts and birth/seal bookkeeping (`SPEC.md §3.14`); a process death before sealing reads `interrupted`. Resolve a racing/unanswered commit by its write id before classifying pre-commit absence. Assert this is X3–X4's machinery with the owner as caller — no new mechanism, verb or path. These are BUILD lifecycle assertions, not behavior established by injecting an Event into AppStub. **Active-request control (`SPEC.md §4a`):** hold a model request unresolved, enqueue ordinary console/trigger work, then issue the authenticated stop through the existing turn handle. Assert the running firing observes it and releases its active request without waiting for that request to finish or for the FIFO turns ahead of the control to run. Repeat with an app call paused before dispatch and with an already-dispatched unresolved call: zero dispatch in the first case, the unknown-outcome park in the second, never an inference of failure from request cancellation. A later known result still persists under its receipt id. Revive the stopped worker after another firing claims: no new work, terminal decision or outward effect. Repeat stop idempotently and assert ordinary queued turns retain FIFO; an unauthorized stop cannot stop another principal's work. Durable-ask request release remains B11's positive control. **Preparation stop/crash control (also run for X4's external caller at BUILD Step 8):** pause before birth, after durable birth but before preparation, between completed chunks, and during a chunk racing stop. Stop reaches the active request; no further chunk runs after ownership is fenced. A racing chunk leaves its entire occurrence/conflict/history/registration bundle or nothing, and every completed bundle survives. Assert no unsaved Routine creation, usual/date edit, skip or stop is applied, no basis spent and no outward call dispatched. Deliberate stop with birth seals discarded; kill instead and assert interrupted plus preserved actual engine history, never a fused duplicate birth or automatic post-birth re-enqueue. An authorized same-principal continuation may resume through current ownership, idempotently completing the window, or discard while retaining history; a foreign/withdrawn caller and the revived old worker cannot. Retain the original no-preparation abandon control and K5's successful preparation/work/read-only controls. Engine M4/BUILD Step 6 owes actual chunk behavior and engine Step 9 the real adapter composition; these harness controls use a scripted seam, not a claim of runtime proof here.

## J. Personal preset (`SPEC.md §9`)

*J1–J4, the held-out generality probes, retired 2026-09-17 with the Situation E folder they derived from (FD-109); git history keeps them. No row in this suite carries `[HELD-OUT]`.*

- **J5 [MUST]** — a **personal preset** (a meal/workout/cycle entry) absorbed as an ordinary commitment acquires **no commercial machinery** — asserted as absences, because the old form ("no commercial fields *required*") was true by construction (`SPEC.md §3.4` makes `title` the only universal required field) and a prediction that cannot fail measures nothing *(re-cut 2026-08-22)*: no money mark is created, no `pricing` rule binds, no guest token or Shared row is minted, and no outward act arises from the absorption. A personal entry that triggers any commercial path fails this scenario. The behavior's home is `SPEC.md §9` *(re-homed 2026-08-30; see this section's preamble)*.

---

## Coverage map (decision → scenario)

| Decision (SPEC §) | Scenarios |
|---|---|
| Status latches + the clocked offer (§3.4) | C1–C8 |
| The park — neither latch nor status; human-cleared only (§3.4) | C9, D4, K2 |
| Temporal roles — events consume, tasks don't (§3.4) | A5, C7 |
| Reversibility floor (§7) | D1–D8, D18 (confirmation content-bound), D19 (fail-closed on undeclared), D20 (the floor property), D22–D23 (channel suppression subtracts reach — D20 unextended, §3.11), D24–D26 (the authorization class — FD-24), D27 (no signed term, no guest-facing debt), D28 (reminders are grant-based sends on the existing machinery — FD-61, §3.3), D29 (the suppressed party's re-enable — the attributed owner act, §3.11) |
| Escalation ladder — advance, exhaustion, park attribution (§3.9) | D12–D17, D21 (quiet hours never delay the advance) |
| Direct-manipulation write transits the floor (§7; `../app/SPEC.md §1`) | A6 |
| Document-derived authorization (§7) | D8 |
| Cancellation asymmetry + implicit recurrence (§6–7) | H7–H9 (the counterparty's own move — H9) |
| M2 gate (§3.4, §6) | E1–E3 |
| Elicitation (§6) | B1–B8, G1–G6 |
| Import — attended pull, quarantined (§5.2, FD-49) | B10 (app companion: `../app/SCENARIOS.md` I1–I4) |
| The stored `PendingAsk` — the answer lands on the question (§3.13) | B11 |
| Quota rule (§3.5) | B5 |
| Duration floor at the picker (§3.5; `../engine/SPEC.md §3`) | B9 |
| Conflict/versioning — forward-only at every level (§3.5, §9) | F1–F6 (in-flight operands — F6) |
| Orders + group half-clear (§3.7) | H1–H3, H5 |
| Guest flow / no H3 / per-recipient token (§2, §3.6) | H4, H6 |
| Handshake (§2) | I1 |
| Loop turn+trigger + check-work (§4) | D4, H4, K1–K4 |
| The firing record — one per firing, two write-once parts, required-on-read (§3.14) | K5–K10 (the co-write's seam admission — K10) |
| Independent step/effect/provider-charge allowances — typed exhaustion (§4a, §3.4) | K11 |
| Turn flow — deterministic dispatch, no model-chosen call, per-owner serialization, completion proposed then verified (§4a) | R1–R6 (the compound sequence's `refused-unmet` law — R5; the pre-birth loss record — R6) |
| Registration kinds — the closed vocabulary stored, kind-routed at the seam, distinguishable at the loop route | R7 (writer/firing paths: D28, C8, B11) |
| The owner's brake — stopping a firing before or after the commit point (§7) | X8 |
| SOP lifecycle (§3.6, §6) | G7 |
| Context assembly (§8) | L1–L2 |
| Bounded slice — finite budget, deterministic degradation (§8) | L3 |
| Provenance quarantine — spotlight tags + dual-model isolation (§8) | L4, L5, L7, L8 (tags survive derivation) |
| View-context stamping — deictic referent (§8; `../app/SPEC.md §1`) | L6 (harness), `../app/SCENARIOS.md C5` (app companion) |
| Money as records (§3.8) | N1–N2 |
| Tool contract + reversibility class (§5) | D1–D2, D7, D9, H4, N2, A7 (return-leg validated) |
| Template-matching generative-UI (§5, §6) | G6 |
| Generative-UI pin/lock — hand-set field survives re-proposal (§5) | G6b |
| FR38 save-as-bundle projection + authoring fence (§6; `../marketplace/SPEC.md §1.2`/§2) | G8, G9 |
| Engine-originated round-trips (§4–§7 composed; `../engine/SPEC.md §7`) | P1, P2 |
| Commercial machinery is entered, never acquired (§9) | J5 |
| The noticed-pattern offer — inline, non-blocking, owner-tagged only (§6) | B6, B7 |
| PatternDecline — a decline is remembered, revocably (§3.10) | B8, `../app/SCENARIOS.md` C7 |
| The assisted off-app path — prepared, recorded, converted (§2) | O1–O5 |
| The external surface — same contract, floor parity, statelessness, the stamping door, the compatibility policy (§5.3; `INTERFACES.md §6`, the policy itself at `INTERFACES.md §7`) | X1–X7 (X8 is the owner-side reading of X3–X4's machinery, above) |

**Situation traceability** *(added 2026-08-21 — this suite's preamble derives from `SPEC.md`, and FD-35 makes the Situations the falsification probes every scenario must ultimately answer to; the anchors are printed so the derivation direction is checkable, the way `../engine/SCENARIOS.md`'s Sources line already is)*: **Situation A** (Matt, FD-106) → B1–B5, B9 (the duration floor), C1–C8, F5, H7–H9 (Jasmine's own cancel and move, S14b), G-family authoring, L-family (his private life is the L4/L5 material), J5 (the personal preset, re-homed here 2026-08-30) · **Situation B** (the rental fleet) → C2/C6 (the lapsed hold, the early return), D5/D8/D27 (consent and the unnamed damage fee), F6 (in-flight cooldowns and holds), H4/H6, K-family · **Situation C** (the dive shop) → E1–E3, F1–F4, G7 (the SOP), H1–H3/H5 (the course and the group), O1–O5 (the four calls), D15 (the empty-ladder park — the single-operator case every v1 Situation actually is) · **Situation D** (the reshuffle) → P1, X-family staleness posture. A beat with no anchor here is a derivation defect (the 2026-08-21 review found five; B9, F6, H9, D27 and the engine's Q4 close them).
