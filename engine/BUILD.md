# annnä Engine — BUILD (ordered plan, each step gated by scenarios)

*Build order for the layer. Per the parent README, the engine is built **after** the harness (its seams are the requirements) and its final gate is the stub-swap. The one recorded deviation from that order is Step 0's scaffold, which ran ahead of it — the deviation record is `../deployment/SPEC.md §8` DR-5, and this sentence is the mirror that note names *(added 2026-08-22)*. Storage-technology candidates are named here and only here (SPEC is tech-neutral): the substrate is **Convex from the start** — the isolated build and the serving build run on the same store — **ratified (FR7) after being checked against** the transaction, version-chain and reactive-push requirements of `INTERFACES.md §2.2`, not chosen by preference. The check is printed at Step 0 so it can be re-run rather than trusted; PostgreSQL remains the named alternative, at the cost printed with the check (it fails the reactive-push row and would have to build it). Travel-provider candidates, same treatment *(founder-ruled 2026-08-06)*: the scripted stub for the whole isolated build, then **Google Routes API** or **Mapbox Directions** as serving candidates (OSRM self-hosted as the no-key fallback) — decided at the serving step by the `travel(request)` seam’s needs (`INTERFACES.md §2.1`: explicit origin/destination, mode, dated arrive-by/depart-at inputs, source/freshness, result caching and fail-closed precedence), and recorded in `../deployment/INTERFACES.md §4` when bound.*

## Step 0 — Substrate & scaffold

**NOT CLOSED 2026-08-08 — the scaffold and its suite landed; the Verify line is only part-met, and the tag's scope was corrected 2026-08-21.** `convex/schema.ts`, `convex/scaffold.ts`, `tests/scaffold.test.ts` and the shared transcript reporter are in the tree and green. What is *not* done, stated fully rather than rounded to the last clause: **(a)** ~~the reactive-push clause~~ **OBSERVED PASSING 2026-08-22** — `CONVEX_URL=https://upbeat-bulldog-541.convex.cloud npm run check:reactive` returned `REACTIVE-PUSH OK: subscriber saw [0,1] — a pre-mutation zero, then the new value pushed without re-querying` against the dedicated dev deployment (`dev:upbeat-bulldog-541`, project annnä, slug `annna-engine`); the criterion that fails silently by degrading into a polling loop has now been seen not to; **(b)** boot, write/read-back and the clock run through `convexTest` in-memory with a hand-rolled `makeClock` — the three substrate criteria have not been exercised against the ratified substrate itself (no scheduled function has run); **(c)** the attribution clause is deferred to the §1 object model and closes at Step 1 (the scaffold test's own comment says so) — **and Step 1's Verify line names the items that discharge it** *(2026-08-29 — a deferred clause whose arrival point names no item never mechanically arrives; the pointer now runs both ways)*. Same shape as `../deployment/BUILD.md` Step 2 — the work that needs no account is done, and the tag names what is missing rather than rounding it up to closed. Two pin notes for Step 1 *(2026-08-22)*: `convex-test` is pinned exact at `0.0.55` in `package.json` (de-careted 2026-08-22 — bump deliberately); and `@edge-runtime/vm` (last published 2024-12) should be re-checked as the recommended test VM before the suite leans harder on it.

**Substrate ratification — settled, and settled the right way round (founder ruling FR7, 2026-08-06).** The serving substrate is **Convex**. This does not weaken the *"not by preference"* law above, because the ruling was **checked against `INTERFACES.md §2.2`'s five criteria before it was accepted**, not asserted over them. The check, printed so a later reader can re-run it rather than take it on trust:

| §2.2 criterion | How Convex answers |
|---|---|
| Atomic multi-object transactions | mutations are serializable transactions over many documents, **bounded by Convex's documented per-transaction limits** (read 2026-08-22 at docs.convex.dev/production/state/limits: 32,000 documents scanned · 16 MiB read · 16 MiB written · 16,000 documents written · 1 s user code — these values move; re-read the page, never a remembered figure) — one `commit` = one mutation, and `commit` never fetches (`SPEC.md §6.1`), which keeps a single commit far inside them. Jobs that can approach the ceilings batch across transactions by design: Step 6 names them |
| Append-only writes with attribution | a discipline the schema enforces, not a substrate feature — no delete primitive is exposed above the store layer (`SPEC.md §1.10`) |
| Per-object version chains | stored explicitly as `store_version` on each object; handle staleness reads against it (`SPEC.md §4`) |
| Clock trigger facility | scheduled functions drive holds, horizon extension, and `min-occupancy` decisions; the scenario runner substitutes a steppable virtual clock |
| **Reactive push to subscribers** | live queries push a changed display projection to its subscribers on commit — the app never polls (`SPEC.md §0` sole-client carve; `../app/INTERFACES.md`, app scenario Z3) |

**Three of the five are the substrate's; two are ours.** That distinction is the point of writing this down: append-only and version chains are **our schema discipline**, and they would have to be built the same way on any substrate. Only atomicity, the clock, and reactive push are things a substrate can hand you. A future substrate change re-runs this table — the criteria stay authoritative, the answer does not.

**The fallback is not free, and this is where that shows.** PostgreSQL passes rows 1–4 and **fails row 5** as it stands: reactive push would have to be built (`LISTEN`/`NOTIFY` plus a socket fan-out layer, with its own delivery and reconnect semantics). Naming that here is the whole reason row 5 exists — without it Postgres reads as a drop-in, and the property the ruling was protecting disappears silently rather than failing a printed check.

**If any row fails at build time, the ruling reopens.** FR7 ratified a candidate that passes this check; it did not exempt the substrate from the check. That is what keeps `not by preference` literally true.

- Stand up the scenario runner with the virtual clock, the scripted travel provider, and the **projection stub** *(2026-08-29 — `INTERFACES.md §2a` states the engine owes it to another package, "a projection stub ships with the engine's stub", and no step stood it up)*: it must exercise subscription, push, **staleness-discard** (*"discards any payload whose `store_version` is older than what it already holds"*) and the `unavailable` member, so the app suite can run without a live store.
- **Verify:** an empty store boots; a diff writes and reads back with attribution; the clock steps; **a commit that changes a published projection reaches a subscriber without the subscriber asking** — the reactive-push row is checked here, not taken on trust, because it is the one criterion that fails silently by degrading into a polling loop rather than by erroring; and **S7 (the stub/push half — the §2a effective-policy projection stub ships with this step's stubs, and this closure fails while the pushed projection is absent; its object-model and fence/determinism halves close at Steps 1 and 4)**.
- **`scripts/reactive-push-check.mjs` is a required gate, not an optional probe (I4)** — required *wherever a deployment exists to run it against*. It needs a live Convex deployment and CI has none, so `npm run check` runs it when `CONVEX_URL` names one and prints an explicit *skipped* line when nothing does; the honest state is recorded at `../deployment/SPEC.md §8` DR-6 rather than papered over. It subscribes against a real dev deployment, commits, and asserts the subscriber was **pushed** the new value — the one thing `convex-test`'s in-memory harness cannot prove (it has no socket to push over). The in-memory suite passing is **not** sufficient evidence for the reactive-push row; this script must pass against a deployment before the row is believed, and it is re-confirmed at the Step 9 stub-swap when the app subscribes for real.

## Step 1 — Object model & latches
- Store `../harness/SPEC.md §3.6` group/request/arrangement/report fields on existing principal/Commitment/Order/Shared shapes. A1/A4 fixtures prove stable refs, empty legacy authority and roster/selected/booked/attendance separation here; Step 5 owns actual atomic confirmation and reference/revocation races, not this storage half.
- Implement B6’s stored field-schema identity/revision/default/dependency and historical-read controls under `../harness/SPEC.md §3.3` and `SPEC.md §1.10`. Distinguish authored schema data from physical object-schema evolution. Use the existing keyed fixture for unreadable protected history; no live erasure proof. Lifecycle write/race assertions remain Step 5. **Also exercise B6's PendingDecision subject arm (`SPEC.md §1.14`'s additive subject member, notice at `../harness/INTERFACES.md §7.2`)**: the legacy `commitment`-only row upcasting on read with unchanged bytes, the successor commitment-subject row carrying both members, the rule-subject row with no synthesized commitment, and the conflicting-ref and absent-member refusals. No field is removed or re-typed by this change; the harness's typed `PendingDecisionRead`, stub and fixture propagation stays at `../harness/BUILD.md`'s quota scope-decision carrier obligation and is not executed here.
- Store `SPEC.md §1.4`'s Pattern/occurrence identity, generation, original zoned date/time, interval/anchor and attributed exceptions beside existing history/latches. M1/M2/M6 stored-fixture precursors verify round-trip and no latch clearing here; they do not close expansion, dated edits or stop behavior, which require Steps 5/6.
- Store the proposed escalation-deadline identity at `SPEC.md §1.15` / `../harness/SPEC.md §3.13` under the `../harness/INTERFACES.md §7.2` notice. N1/N2/N4 exercise both roles, negative fields, independent total arming and adapter restart over retained storage. These are storage/clock controls; B4/N3’s actual creation/advance/answer transactions require Step 5, and harness D12–D14/D16’s ladder/restart/total-park proofs remain harness Step 5 checkpoint (3) at their existing SHOULD weight.
- Store the full Grant/condition and PendingAsk act descriptors plus `ReminderPolicy` and reminder registration revision fields from `SPEC.md` §§1.6/1.15/1.17 and `../harness/SPEC.md §3.3`. N1/N2 here prove field round-trip and clock arming/replacement only; activation/move/cancel atomicity, replay and stale firing effects need Step 5, with typed condition evaluation at Step 3.
- Implement §1 objects (principal, board, commitment, rule, grant, shared, order, money marks, history, **OnCall/Escalation** — §1.13, the stored objects behind the harness on-call ladder; shape home `../harness/SPEC.md §3.9`; **PendingDecision** — §1.14, engine-written and human-chosen, no engine-originated authorship or selection of `chosen`, while engine persistence of an authenticated human-authored answer is required (§1.14), exercised at Step 7 by O2/O3; **TriggerRegistration** — §1.15, the engine's stored clock-arming bookkeeping — the bridge to the harness clock source is seam law at `INTERFACES.md §2.2`, exercised by N1–N3; the **party↔principal link** — §1.1, FD-46/FD-51, the conversion link `{party_refs[], principal, basis, when}`, **engine-minted only**, the mint act itself being the security layer's claim (`../security/SPEC.md §2`), exercised by B8). Write-once latches and no-delete enforced at the store layer — the escalation's terminal status (`answered`/`timed_out_parked`) latches under the same discipline.
- **The §1.17 storage set + the firing log** *(2026-08-31)*: implement storage for the five harness-owned objects — `PatternDecline`, `PartyContact`, `PendingAsk`, the delivery event store (the harness §3.11 `DeliveryEvent` shape, structural members only), and the per-owner firing inbox with its loss records — plus the `FiringEvent` shape consumption (§1.16's two write-once parts and part identities; the harness owns the shape). All persist through the existing verbs and read through §5 item 7 — no object-specific entry point.
- **The conversion link's no-discriminator arm (`SPEC.md §1.1`, aligned with `../security/SPEC.md §2`)**: B8's structural walk here additionally asserts that **no input mints a link from a presented token** and no stored member of the link discriminates a party by one; a run presenting two parties' tokens read from one verified channel mints nothing. This is the storage/entry-point half only — the read-union half stays at Step 4 and the authorized-channel refusal itself is security BUILD Step 4's N6.
- **Encrypted contact writes (F-02's engine half)**: `PartyContact` contact rows land **ciphertext at rest** — written through the keyed path so post-shred unreadability is a store construction, asserted at B10, never a policy overlay.
- **Verify:** B1–B3, **B5 (no delete entry point, asserted structurally; its behavioral half — the cancel/revoke/expire walk that proves the lifecycle needs no delete — closes at Step 5, with B4's walk)**, **B4 (the store half — persistence across firings and terminal write-once; its only-`commit`/`calculate` walk closes at Step 5, when those verbs exist to walk)**, K1–K2 (the mark-storage half — K1's cancel-path close is Step 5, and its conditional-quote arm's derivation closes at Step 4; **the quote-revision chain both K1's and K2's quote arms read back — previous quote and replaced-from source stored and readable, `SPEC.md §§1.5/1.10` — is stored here, with the replacement write itself at Step 5**; **K2 also closes Step 0's deferred attribution clause (c)** — `paid/settled` marks carrying `{by, at}` is attribution written and read back on a stored §1 object, which is exactly what Step 0 deferred), A3 (the diff-refusal half — its commit-path close is Step 5), **N1–N2 (the trigger registry: armed → kind-routed firing per the §2.2 mapping, exhaustive over the union + the internal branch; re-register replaces — N3's no-business-state-write/park/ask arm and its only-own-attributed-no-op append close at Step 5)**, **B8 (the structural half — the conversion link is engine-minted, asserted as an absence over the entry-point set; its read-union and re-homes-nothing halves close at Step 4, where `calculate` exists to assemble the history — and its engine-minted attribution closes Step 0's clause (c) beside K2)**, **K6 (§1.9's poka-yoke: a write setting `held` is refused on every path that reaches a money mark, the other four marks unaffected — a store-layer refusal of B3's class, which is why it gates here and not at Step 5)**, **B6 (§1.10's additive-only schema evolution: a historic row reads back through a widened shape with its declared default and its stored bytes unchanged; a removing or re-typing change is refused at write — gated here because this is the step that freezes the schema the law governs)**. — and the 2026-08-31 additions, each individually: **B9 (storage/shape half here; its `calculate` read closes at Step 4 and its existing `commit` write closes at Step 5)**, **B10 (keyed in-memory contact-store fixture with ciphertext-at-rest, missing-key and wrong-key controls here; local evidence only, with real-provider key-deletion proof at security Step 3)**, **B11 (storage/shape and ordinary trigger-registration half here; its `calculate` read closes at Step 4 and its existing `commit` write closes at Step 5)**, **B12**, **B13 (inbox/loss-record storage half here; its owner-readable `calculate` read closes at Step 4 and enqueue/claim/release idempotency through the existing write path closes at Step 5)**, **B14 (the storage/uniqueness half — the engine intact-return walk closes at Step 5; the harness required-on-read refusal remains at its home)**, **B15**, **N4**, **A11 (the register's engine storage round-trip — the harness X3–X4/Step-5 reader stays at its own harness home)**, **K7 (the stored-evidence/amount-source half — its derivation half closes at Step 5)**, **S7 (the object-model half — the selectable set definable over this step's stored objects; its stub/push half gated at Step 0, its fence/determinism half at Step 4)**. *(The half-annotations follow Step 5's own "Q1 (commit side)" precedent; without them the red-green loop cannot go green in order.)*

## Step 2 — Type-value system (M3)
- `typed_value` / `compare` over the full §2 vocabulary; raw literals rejected outside `typed_value`. **Consumed from the shared library, not rebuilt** *(2026-08-22)*: one implementation — `../harness/INTERFACES.md §1.4`, living in `harness/src/typed-value.ts` — imported by this step; the engine adds the §3 quota math on top of it.
- Time-phrase resolution (FD-27): `instant`/`interval` rows, resolved against the `type_spec`'s `{zone, reference_instant, locale}` context — deterministic, fail-closed on an unresolvable phrase.
- **The transduction is propose-then-verify (FD-57, 2026-08-22 — this bullet previously mandated a per-locale parsing project: a vetted parsing library plus hand-built grammar tables for `th` and `zh-TW`, on the critical path, which FD-57 dissolved by narrowing FD-16 from its own text).** The model proposes a structured instant beside the raw phrase; this step builds the **verifier**: a per-locale **rendering** table (ICU/`Intl`-class formatting — `en`, `th` with Buddhist-era years, `zh-TW`, per FR15's examples) that re-renders the proposal into the phrase's locale and zone and compares components, failing closed on mismatch or absence (SPEC §2's FD-57 block is the law). The supported-locale list stays a named constant; an unsupported locale is a `typed_value` error; the rendering binding is recorded in `../deployment/INTERFACES.md §4` when made, same as the travel provider.
- **Verify:** type-error cases + **Y2 (a literal in a handle-typed position is a type error)** + Q2's typed draws + **Y1** (instant resolution, all three printed locales, plus the duration operand’s unit, step/anchor and incompatible-endpoint controls under SPEC §3, and G6’s complete legacy-leaf/aggregate discrimination and two-entry conflict shape, rejecting dropped or flattened leaves and mixed shapes). This proves typed representation only; storage/evaluation, projection and final admission remain Steps 3–5.

## Step 3 — Rule menu & evaluation points
- Verify G8’s field/rule binding and supported-type validation controls under `../harness/SPEC.md §3.3`: complete operands, unchanged identity under rename, dependent input/default compatibility and off-menu/conditional-meaning refusal. Calculated outputs accept only their supported inputs. This is validation/classification; G8’s actual lifecycle transaction and concurrent-reference controls close at Step 5.
- Implement G9's operation-aware covering relation over the existing typed comparator/range machinery (`../harness/SPEC.md §7`), including duration boundary, party/owner scope, unknown-member, expiry and revocation controls. Validate the actual act descriptor instead of trusting caller-labelled purposes or shorter duration claims. The current harness `CoverageQuery` is an unpropagated scaffold until harness Step 2; source checks do not prove this matcher.
- The closed §3 menu: storage constraint (off-menu unstorable), per-type math, binding times (§1.5's list), precedence.
- Implement §3's **clash table** (ordinary pairs plus the narrow co-applicable attribute-predicate intersection at SPEC §3; the rejected duration/buffer/window joint row stays absent) and §8's four-way classification — including the **unsatisfiable** class, which is refused at write regardless of authority and is never offered on the override path. Each row's **minimum-case test** is the implementation, not a comment: code the smallest failing input the row names, not the general shape of the clash.
- **Verify:** **G1 (classifier half here; its write-path refusal closes at Step 5)**, G2–G4, **G8 (the rule-type half — off-menu type refused at write; the recurrence-shape half closes at Step 6)**, **the conditional `pricing` tier set's classifier half (`SPEC.md §3`'s pricing row: an overlapping or gapped tier set, or one whose currency or per-hour basis is unconfirmed, classifies unstorable; its derivation closes at Step 4 and its write-path refusal at Step 5)**, Q1 (the rule-evaluation half — its `calculate`/commit sides close at Steps 4/5), **the participant-quota classifier half (`SPEC.md §3`'s participant-quota block: a count-denominated and a time-denominated `quota` over one target store and classify as two independent rules, never one coarsened limit; the per-stage cancellation-release operand admits exactly the two named options and no default; its projection closes at Step 4 and its write-path draws at Step 5)**, P4, **G5–G6 (classifier half here, including empty three-way and six-distinct-rule domains, complete operand/contributor cardinality, the stepped-duration/20-minute-window ordinary pair, and single-subject versus jointly valid coordinated set-widen remedies; the write-path refusal and authority checks close at Step 5)**, **O4 (classifier half here; its write-path refusal closes at Step 5)**, **R1 (the conversation-kind precondition parks; documents alone never satisfy — FR35)**, and **G7(a)–(b), plus G7(c)–(d)'s storage/classifier halves — preserve the earlier positives and nonempty three-way intersection and conditional site-day controls; G7(d)'s projection/admission halves close at Steps 4/5 and its single-placement resolve control closes at Step 7 beside G7(c).** G7 is the one that fails silently if skipped: an over-broad `unsatisfiable` refuses a valid ruleset, and no refusal-side assertion catches that. Gate the positive cases with the negative ones. And **G9 (the covering-grant lookup, both directions — the D8 engine owner under FD-97's selected carrier; the shared register item's request-carrier and verdict-consumer halves — its (d)/(f) arms — are harness Steps 2/3's own, named here so the ownership is printed, never implied)**.

## Step 4 — `calculate` & the travel seam
- Implement the cache-only computation side of `SPEC.md §5`'s request boundary. M4's full beyond-horizon preparation/replay proof closes at Step 6; P10's composed determinism stays Step 7. Do not use this read adapter to weaken V6's commit prohibition.
- Closed query taxonomy, handles with display facets, the travel interface + cache + precedence + fail-closed; the **travel envelope** (≤8 fresh fetches per `resolve`, per-day batching + dedup, 30-min buckets / 30-day TTL, 2s timeout + one retry) with its named constants centralized in one place (SPEC §5). T1–T4/V2–V4 additionally exercise full request identity, typed 30-minute/05:30 arrive-by results, same-bucket timing and mode counterexamples, source expiry boundaries, changed place/provider revision and tenant isolation against the scripted provider (`SPEC.md §5`). Print actual scripted calls and cache facts; fixtures are not measured drives. Step 5 closes V6 and T4’s accepted-draft/reference-edit commit arms; Steps 3/7 of app BUILD own C2/O3’s real picker/readback, Step 5 owns G3’s guest wire, and harness BUILD Steps 4/5 own B1/B2/G2’s proposal/acceptance composition and Step 5 owns L6’s generic current-draft context controls. These proofs remain owed until executed.
- **Verify:** T1–T4, S1, **S2 (the projection half — its attribution half is a write, and `commit` arrives at Step 5, where it closes)**, S3–S6 (S4's cross-tenant arm S4b closes at Step 8, where the share seam exists; S5: two tokens, two answer sets — the party-rule × projection intersection; S6: the FD-34 ghost fence, read-on-demand and never stored), P3, **Q1 (calculate side — availability excludes over-quota slots, each of the count and time constraints excluding on its own operand, and the projected two-limit standing counted by lesson-month in the board's zone across every link, channel and template use of one confirmed party)**, **P5 (unknown is not free)**, **K1's conditional-quote arm (calculate side — the sixteen tier-by-duration totals, and the same derivation read once as a rate before a duration is chosen and once as that duration's exact total; its commit-side close is Step 5)**, **P6 (the duration range/step/window and both-buffer projection arms, including actual Matt commitments and the extension to 16:15; its commit half closes at Step 5), G7(d) (effective domain and conditional site-day projection)**, **V1–V5 (the travel envelope: V1/V5 resolve/search halves close at Step 7; V2–V4's dedup/batch/cache/decline mechanics are measured here against the named scripted Situation-D candidate/triple fixture, without a resolve implementation)**, **B9/B11/B13 (the `calculate` stored-object read halves, closing Step 1's storage splits)**, **B8 (the read-union half, closing Step 1's split — the conversion link's assembled starting history is exactly the union of what the linked party's booking-bound tokens could already read, and it re-homes nothing)** — assembled from the **single** party the verified channel maps to, never from a channel holding several parties' tokens. **The V2–V4 tuning-mechanics row is owed here:** print the measured dedup, batch and cache counts against that fixture; the binding 8-triple ceiling tuning moves to Step 7 beside the re-solve round bound. This step's Verify additionally gates **S7 (the fence/determinism half of the effective-policy projection — §1.7c's selectable-set fence and identical-payload determinism)** and **A10 (the availability half — a completable past `end` with no `actual_end` is excluded from availability; its commit half closes at Step 5)**.
- **Bound-quote calculation control:** this step's K1 arm must call the existing `calculate` `bound-quote` member with both source forms (`current-pricing` and `bound-quote`), once without `duration` for the rate-only facet and once with a valid duration for the exact total/end facet. Keep QUOTE-01–16 unchanged; the separate minor-unit controls use ISO-4217 minor-unit integers in every `amount` field: `{amount: 1501, currency: "TWD"}` (TWD 15.01/hour) × 30 minutes → `{amount: 751, currency: "TWD"}` (TWD 7.51) on the half-unit tie, and `{amount: 1001, currency: "TWD"}` (TWD 10.01/hour) × 45 minutes → `{amount: 751, currency: "TWD"}` (TWD 7.51) when rounding once at the final amount, whereas rounding each of three 15-minute intermediates to `{amount: 250, currency: "TWD"}` (TWD 2.50) before summing yields `{amount: 750, currency: "TWD"}` (TWD 7.50). These are the rounding proof. This source obligation does not execute the later commit, replacement race or full story.
- **The serving provider's criteria, fixed before either candidate is tried** *(2026-08-22 — FR7 and FD-11 both got printed tables on the re-derive-don't-inherit discipline; the travel provider was the one bound substrate left to preference)*: (1) demonstrates the actual `INTERFACES.md §2.1` request/result contract for each supported mode, dated arrive-by and depart-at semantics, zone, source/freshness and unavailable handling, using retained requests/responses and measured evidence; unsupported combinations stay explicitly unavailable, never inferred supported from a candidate name; (2) per-request cost stated and acceptable at the ≤8-triple ceiling per `resolve`; (3) p95 latency inside the 2s timeout + one-retry envelope; (4) egress fits the allowlist discipline (one declared host); (5) OSRM self-hosted parity — demonstrate the same semantics for every claimed supported input or record the unsupported combinations honestly; a shared signature alone proves no fallback capability. Results recorded here and in `../deployment/INTERFACES.md §4` when bound.

## Step 5 — `commit`
- A1/A4 additionally execute `SPEC.md §§1.7/6` shared-request controls: one teacher draw, all-participant atomic admission/edits, both paired-versus-solo race winners, fresh alternatives, lost acknowledgement and same-request fresh-id duplicate refusal, plus retirement/reference and revocation/apply serializations. Step-1 fixtures supply stored records; current declared reads use Step 4, and real token/swap/provider/full-story proof remains at existing owners. A supplied terms/policy fixture does not implement the separately owed pricing/quota contracts.
- Complete B6/G8’s field/rule lifecycle transactions under `SPEC.md §6` and `../harness/SPEC.md §3.3`: independent optional retirement, explicit value clear/restore, forward default/schema revisions, atomic valid dependent resolutions, current-reference races, denied/member-failure rollback and same-id replay. Assert prior bytes/bound terms and referenced objects unchanged; app-owned document references remain the separate `../marketplace/SPEC.md §3` adapter contract. The full engine/harness composition stays at Step 9.
- Exercise M2/M3/M8's ordinary authorized instance-diff precursors against stored fixtures: exact dated identity/generation, stale/foreign-target refusal, one cancellation and each user-selected stateful resolution. Pattern edits cannot clear latches or substitute for those acts. Full materialization/stop composition closes at Step 6, preserving this step's V6 no-fetch/no-cache-write checks. Also close T4/V3’s saved-place dependency transactions and accepted occurrence travel writes: stale estimate/draft/generation/reference/authority refuses with zero partial changes, explicit one-time scope preserves Home and siblings, bookmark retirement and its reviewed future resolutions apply together, and replay keeps the original result. V6 repeats with stale/unsupported/different-mode cache controls and asserts zero provider calls/cache writes.
- Close N2's transaction half and N3's stale/replay controls under `SPEC.md` §§1.15/6: policy/Grant activation together, successful move replaces registration, failed/stale move preserves it, cancellation/removal/revocation withdraws, content/roster refresh cannot reset a spent occurrence, and concurrent fresh-id callbacks cannot double-spend. Exercise G9's persisted grant/condition and unauthorized activation writes here. The full D28 clock table is re-run over real engine writes at Step 9; fixtures here prove store/registry state, never email delivery or HR-30 role design.
- **Participant-quota draws (`SPEC.md §3`'s participant-quota block):** every affected participant's count and booked-duration check and draw inside the one shared-booking transaction — no partial set, the teacher's placement/travel/buffer draws computed once, buffers drawing from nobody; the move, refused/stale/losing and reported-fact paths writing no draw; the cross-month move releasing and taking inside one transaction or refusing whole; the participant addition/removal/rejoin draws; the per-stage cancellation-release answer applied where stored, with an unanswered stage applying the cancellation and writing only the `PendingDecision` for the allowance; and the scope re-partition decision's two answered outcomes with their exact successor totals, its stale-revision, missing-ref, partial-assignment and coverage-illegal refusals leaving the prior scope unchanged, and the write-id replay of each (Q1's scope arms). The full October ledger is Step 9's.
- One-transaction checks (commit never fetches — §6.1), races, handle redemption, diff application, the interval-widening re-check (§6.7); the write-id idempotency index (§6.6, tenant-scoped); the apply-proposal, apply-offer and apply-Order input forms (§6.5 a/b/c — the offer form's accept-gate included).
- Build the existing standalone birth/claim transaction precursors for M4 under `SPEC.md` §§1.16/1.17/5 and harness §3.14(b): resolve a lost birth response by id, preserve that birth during later work, and use post-birth recovery even when no work committed. Full preparation chunks close at Step 6, harness lifecycle at harness Steps 5/8 and the real adapter composition at this file's Step 9; this does not relax V6.
- **B13 recovery enforcement (`SPEC.md §1.17`):** use injected time and two workers to prove transactional ownership/invalidation, stale new-write refusal, claim replay without ownership revival, and once-per-attempt pre-birth release/loss under crashes and competing retries. Resolve a lost work-commit response before the birth-dependent disposition; retain post-birth interrupted continuations and ordinary FIFO progress. Exercise the retired-worker receipt exception with identity conflicts, immutable birth/seal and no new basis spend. Step 1's stored fields alone do not prove this; actual dispatch fencing and active-request stop require Step 9's adapter composition.

- **Verify:** A1–A2, **A4 (write-id idempotency: replay, payload-mismatch refusal, failed-commit replay, foreign-tenant miss)**, **A5 (re-widening cannot double-book)**, A3 (commit-path close of Step 1's half), **G1, G5–G6, O4 (the write-path refusal halves, closing Step 3's classifier splits; the refusals preserve every contributor/typed operand and the remedy, with exactly two complete entries for ordinary pairs and no suggestion-applied write; G6’s six-rule atomic create refusal/retry and stored-source enable/edit fixture prove every-member authority, current-state revalidation, no partial rule/version/provenance writes on denial, staleness or injected member failure, and one-write-id success/replay)**, Q1 (commit side), **Q2 (commit side — the 11th and post-expiry draws refuse)**, **Q4 (both stored cancellation answers — the *release allowance* arm restores the draw, the *keep counted* arm does not and the rebook refuses; an unanswered stage writes the two-option `PendingDecision` and no default; Keep booking changes nothing; the stages stay separately elicited)**, P6 (all commit-side duration, both-buffer and stale-selection arms; zero lesson/quota writes on refusal), G7(d) (admission of the surviving domain and refusal of excluded values/site-days), B1 (lapse under the clock), B4 (the only-`commit`/`calculate` walk, closing Step 1's half), **B5 (cancel/revoke/expire behavioral half, closing the Step 1 no-delete structural half; withdrawal closes at Step 6)**, **B9/B11/B13 (the existing `commit` write and idempotency halves, closing Step 1's storage halves)**, **N3 (a withdrawn or orphaned registration makes no business-state write, park, or ask and appends only its own attributed no-op record — closing Step 1's half)**, **K1–K3 (owed derived with the FD-30 carve, marks latched, no value moved, and no engine-declared `no-show` once the start has passed under the clock)**, **K4 (the marked no-show survives a self-cancel)**, **K1's conditional-quote arm (commit side — the bound quote is what the booking carries), K2's quote-history arm, K7's unknown-outcome arm and A4's bound-quote replacement arm (`SPEC.md §1.5`'s one validated act: proposed roster, time, rate and total plus recorded agreement, current authority and expected quote revision, all matched inside item 1's transaction; the stale, unauthorized, payload-mismatched and report-only candidates each refused whole; one commitment identity and one reservation preserved across every accepted step)**, **O1 (an early booking below the minimum is never blocked at commit)**, **S2 (the attribution half, closing Step 4's split — a return through a per-recipient token attributes to exactly that recipient, which is a write)**, **T5 (FD-79: a `buffer` operand edit governs forward only — the running window neither shortens nor lengthens, the next commit takes the new operand, and the two bindings coexist across a neighbour pair)**, **B7 (a restore conflicts loudly and cannot resurrect a latch — the conflict set parks per row, and the latch arm is checked against the full version chain)**, **V6 (commit never fetches: a cache-missing commit makes zero provider calls, fails closed on the unknown gap and writes no cache fact; the calculate contrast is immediate here, and its resolve contrast closes at Step 7)**, and the 2026-08-31 additions, each individually: **A6**, **A7**, **A8 (commit-side input-shape half, run over a stored Proposal fixture; proposal construction and apply-proposal resolve half close at Step 8)**, **A9**, **K7 (the forfeit-derivation half, closing Step 1's stored-source half)**, **A10 (the commit-agreement half, closing Step 4's availability half)**, **B14 (the engine intact-return walk, closing Step 1's storage half; the harness required-on-read refusal remains at its home)**. *(The park-cause half-annotation, printed: the engine half of that register item's work — a park raised engine-side carries its closed `cause` per B15 — sits beside its harness Step-3 owner, which types and raises the causes; neither half is implied by the other.)*

- **Q4 operand control at the commit gate:** run the explicit 1-count/60-minute, 0/0, 30-minute pre-start cancellation and same-booking rebook fixture for release, keep-counted and unanswered answers, including the 2-count/60-minute keep-counted headroom pair and the retained-draw rejoin/replay totals. This executes no documentary ledger or provider proof; those remain at Step 9 and their named swaps.

## Step 6 — Recurrence & materialization
- Complete M1/M2/M3/M4/M6/M7/M8 under `SPEC.md` §§1.4/5/9: typed Routine mapping, alternate interval/anchor, exact October 12 exception and October 17 skip, future-stop scope including stateful exceptions beyond the horizon, and tenant/window-bounded preparation with zero-write identical-reference replay. Retain wrong-calendar/stale-generation refusals, interrupted/chunk/overlap controls, existing conflict registrations and quota invariance. Step 4 owns projection computation, Step 5 the authorized writes and V6, and Step 7 still owns P10; source checks execute none of these.
- M4/M7 additionally exercise both arming paths of the same horizon job: rolling clock and beyond-horizon requests (availability and direct subscription). Execute atomic occurrence/conflict/history/registry chunks, birth/current-claim admission for harness requests, stop/crash between and during chunks, rejected draft inputs, and successful preparation followed by ordinary booking. Preserve completed chunks, generation/cancellation fences and no-write replay; direct subscription/clock history must not manufacture a harness birth. Step 5 owns standalone birth/write precursors; harness K5/K6/K8/K9 at Step 5 and X4/X8 at Step 8 own firing records and recovery against stubs; this file's Step 9 owns the real composition. Documentary controls do not assert execution.
- Pattern objects, rolling-horizon job, forward-only edits (the §1.4 withdrawal disposition for superseded bare drafts), bounded on-demand materialization, the §9 materialization-capacity law (conflicted drafts, never double-books), the §9 DST law. **Bulk writers batch across transactions by construction** *(2026-08-22 — Step 0's substrate row names Convex's per-transaction ceilings; the horizon job's 8-week look-ahead and any bulk regeneration after a forward-only pattern edit are the two writers that can approach them)*: neither may assume one mutation; both chunk and stay idempotent per chunk.
- **Verify:** M1–M6, **M7 (materialization cannot double-book)**, **M8 (an `except` date over a stateful instance registers a `TriggerRegistration` at the affected instant and surfaces the instance as an ordinary parked question — the bare-draft withdrawal asserted beside it, so the two dispositions stay distinct)**, **B5 (withdrawal behavioral half for a bare draft, closing the §1.4 arm left open by Step 1's no-delete structural half; its cancel/revoke/expire walk closes at Step 5)**, **G8 (the recurrence-shape half — off-menu shape refused at write)**, Q3.

## Step 7 — `resolve`: place-only + decomposition + templates + min-occupancy clock
- Placement search with the §7 constraint classes; structured declines; contention ordering + bounded re-solve — **measured against the substrate's 1-second user-code budget, with the number printed here** *(2026-08-22: if the search cannot fit, the shape answer is an action or a chained sequence — decide by measurement at this step, cheap now and expensive after Step 8)*. **The re-solve round bound's tuning row is owed here too** *(2026-08-29 — SPEC §7 prints 3 as a starting value, the 8-triple precedent)*: run the contention behaviour against a real Situation-D candidate set and print the **measured round count and the tuned constant** beside the budget measurement, replacing 3 as the binding number. **Multi-day decomposition** as caller-chained per-day `resolve` calls composed by an Order (§7); **`KindTemplate` expansion** to ordinary objects (§1.12); the **min-occupancy clock trigger** writing the `needs_human` park (no loop logic here — that is the harness's, §3).
- **Verify:** P1–P2, **P7 (the fallback's two engagement causes stay distinguishable — capacity vs `location-window`)**, **P8 (contention: deterministic arbitration, bounded re-solve, the surviving-goal decline — moved from prose to a gate 2026-08-22)**, **P9 (the mid-series rate edit asks its owner via `PendingDecision`; the prior rate governs until answered — FD-75, added 2026-08-23)**, **Q5 (the operator's day ceiling binds above free resources — moved whole from Step 5 2026-08-22: both its assertions are `resolve`-phrased)**, **V1 and V5 completed (the search-side halves of Step 4's travel envelope)**, **G7(c)–(d) (resolve halves, preserving the pinned fallback and single-placement positive controls)**, **V6 (the resolve contrast fetches normally, closing Step 5's commit-no-fetch half; its calculate contrast is already gated at Step 5)**, **W1–W9 (multi-day decomposition + course templates: per-day calls, partial-survives, which-days-placed, all-or-nothing, expansion-equivalence, relative offsets, role-binds-at-expansion, forward-only template edit, the spanning gear consumption)**, **O2–O3 (registration subject run and its own interval as of the trigger: two-of-four parks, four-of-four stays silent on an otherwise idle board; unrelated same-board runs add nothing)**, **P10 (value-level determinism: a replay at the identical supplied reference instant returns identical values across the TTL boundary, the contended window and the horizon-materialized window — gated here, the earliest step at which all three sites exist)**, and **P11 (the `party_size` predicated fallback entry — FD-55, engaged exactly per the goal's declared size)**. **Binding tuning rows:** against the real Situation-D candidate/triple fixture, print the measured 8-triple travel ceiling and tuned ceiling beside the measured re-solve round count and tuned round bound; these are the Step 7 binding values for the two BUILD-tuned constants.

## Step 8 — `resolve`: reshuffle proposals + cross-owner share
- Proposal objects, direction, pin pre-filtering, the ≤3-move bound, apply-proposal via commit, freed-window fail-closed. **The §7.1 group composition arm rides this step** (`SPEC.md §7.1`'s per-person grant rule): compose over several members' own availability refs, prove the ungranted member returns unknown rather than free, and re-query after one revocation. I3 owns the read-power and unknown assertions and I8 the still-needed-agreement assertion; no group grant, group query member or new parameter is implemented, and the harness/app group surfaces stay at their own gates. The **§7.1 cross-owner share**: read-only `resolve` over authorized published availability → placement handle; the initiator's ordinary commitment-form `commit` creates `offered`, arms the temporary hold, mints the offer-side ShareGrant and emits the recipient Event; the counterparty's stored accept supplies separate accept-side authority; `commit(offer_ref)` applies acceptance. No new apply-proposal form or seam verb. I1/I4/I6/I8 must exercise the real writes, scoped replay after a lost response, grant minting, authorized availability, hold timing and refusal without the counterparty's own yes; source/type fixtures do not execute any of these.
- **Verify:** X1–X7, **I1–I9 (the cross-owner share: engine parity, both-boards-or-neither, no read power added, caller cannot author a crossing, silence never binds, the creator's hold, terminal states recorded both sides, the accept-gate — a live unanswered offer cannot commit — and the offer's zone, Y1's carved arm)**, **S4b (the owner-board fence's cross-tenant rung — closing Step 4's split)**, **A8 (proposal construction plus the apply-proposal resolve half, closing Step 5's commit-side input-shape split)**, **K5 (the cross-owner money record — one record's marks on both boards, and no ledger rung added)**.

## Step 9 — The stub-swap (definition of done)
- Point the built harness at the real engine. Run the **entire harness suite**. The swap also builds the **seam adapter** *(2026-08-29 — Z3 gated the artifact at this step and no step named it; `INTERFACES.md §1` maps the members to `SPEC.md §9`, which describes the *condition*, not the component)*: the component that maps an unreachable or timed-out engine call to the `unavailable`/`timeout` members. It **is not the engine** — Z3's own sentence, *"A healthy engine can never emit these members"* — and it lives on the **harness-facing side of the seam**, so no `SPEC.md §9` text changes to accommodate it.
- **Recoverable-claim adapter proof:** implement the claim detection/ownership mechanics left open by `../harness/SPEC.md §4a` using this engine's existing storage, a lease/heartbeat or proved equivalent. Re-run B13 and harness R6/X4/X8/B11 with injected time and two real adapter workers: crash/recover once, revive the stale worker at work-commit and actual-dispatch boundaries, preserve known receipts and spent basis, keep post-birth work interrupted pending authorized resolution, and exercise non-queued stop and durable-ask release with cloud calls held pending. Keep the full seven-source/console FIFO controls. Record which runtime operations reject stale ownership; a static string match, chosen timing constant or in-memory field fixture is not this proof. This gate adds no service, trigger or provider invocation to a documentary repair.

- **The full October ledger against real engine state:** run LED-01 to LED-33 in the recorded narrative order and assert every student's count and minutes after each accepted act, the refused rows' unchanged after-state, the teacher interval consumed once per group booking, and the final Arthur 4/225, Bobby 3/180, Chloe 2/120, Daniel 2/120 (`../user-stories/Situations/Situation-A/story-matt-verification.md`'s October ledger). Run MT-52's independent COUNT and TIME exhaustion variants here too — neither replaces the full-story state. Q1 and Q4's fixtures at Steps 4/5 prove the rules; only this step proves the accumulated ledger.
- **Verify:** Z1–Z2 — every harness scenario green, `git diff` on `harness/` empty — and **Z3** (fault injection: unreachable and timed-out calls return the infrastructure members and recover through the write id). If any harness change is needed, stop: that is a seam-contract bug to fix on the engine side (or to flag, per `INTERFACES.md §1`), never a harness edit.

## Guardrails
- **The bound is law:** if Step 8 tempts you toward route optimization or week-level moves, re-read SPEC §7/§11 — feasibility and improvement, ≤3 moves, one day, one owner.
- **No new seam verbs** (INTERFACES §1); no rule type without §3's math + evaluation point; no delete, ever.
- Determinism is a test: any scenario that flakes under replay is a bug in the engine, not the suite.
