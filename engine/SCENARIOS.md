# annnä Engine — SCENARIOS (deterministic acceptance suite)

*Pass/fail, deterministic, replayable — the engine is the layer where determinism lives, so unlike `../model/EVALS.md` (graded) every item here either holds or the build fails. Run against the stubs in `INTERFACES.md §4` (scripted travel provider, virtual clock). Sources: the user-stories corpus (A/B/C/D/E), the interview acceptance list (`../.specs/deep-interview-engine.md`), and the harness stub contract. Every scenario is MUST. A `[SHOULD]` tag, if one ever appears here, is **not build-gating — this sentence is the semantics the coverage gate parses**, and the gate's parse conforms to it, never the reverse.*

*(Section letters are local to this file.)*

## A — Atomic commit & races
- **A1 [race, one winner]** Two commits race for the last unit of capacity. Exactly one succeeds; the other returns `{conflict, reason, alternatives}` listing still-open times. *(Sofia's 3:00; Situation-D's last Friday slot; Situation-C's last boat seats.)*
- **A2 [all checks, one transaction]** A commit that passes capacity but violates a buffer gap against its *following* neighbor fails atomically — nothing partial is stored.
- **A3 [governing wipe rejected]** A lower-authority diff whose effect would disable a governing rule does not apply; the governing rule remains in force. *(Mirror of harness D-series.)*
- **A4 [write-id idempotency — §6.6]** A commit applies; the identical write id is re-committed: the **original result** returns and nothing applies twice. The same id arrives with a **different payload**: refused (`{conflict, reason: write-id-reuse}`), neither payload honored. A commit **fails**; replaying its id returns the same failure. A **foreign tenant** replays the id: nothing matches, nothing returns — the index is `(owner_org, credential)`-scoped. *(The engine half of harness K4.)*
- **A5 [re-widening cannot double-book — §6.7]** A rental ends early (`actual_end` 14:00), the freed 14:00–17:00 is re-let, then a diff corrects the already-recorded `actual_end` back to 16:00: the write returns `{conflict, reason, alternatives}` and **parks for a human**; refusing this expanding correction adds no overlap and preserves both existing records. The same correction with the slot still free applies as an ordinary diff. This is the expanding-correction arm of `SPEC.md §6.7`, gated at `BUILD.md` Step 5; it does not require a park for A10's clock-only overlap or first non-expanding end report.
- **A6 [the `commitment` form returns the applied-result — Q2-003]** `commit` of a commitment applies and returns `{ok, applied_ref}` with `applied_ref` naming **the committed commitment** — never a bare `{ok}`, never a ref to anything else (`../harness/INTERFACES.md §1.2`'s neutral applied-result, asserted at the seam).
- **A7 [the `diff` form returns the applied diff's object ref — Q2-003]** A Board/Rule diff applies and `applied_ref` names **the object the diff applied to** — the same neutral shape, the ref honest for this input form.
- **A8 [the `proposal_ref` form returns the proposal's ref — Q2-003]** An apply-proposal commit returns `applied_ref` naming **the proposal applied** — all-or-nothing per §6.5, the applied-result covering the third input form.
- **A9 [the co-write fourth arm — Q2-033]** A `FiringEvent` birth (or seal) **bundled inside a host work commit's payload** rides the host's **one** caller-supplied write id (§6 item 5(c)'s bundle shape), is checked against its (firing id, part) identity **in the same transaction**, and the call returns the **host's** applied-result; a bundle whose part conflicts with an occupied slot is **refused whole**. Not a fourth input form — content inside whichever form the host write takes (§6.5's admission sentence).
- **A10 [completable capacity — recorded admission, first end and correction; availability and commit agree — FD-98]** Derive capacity from `SPEC.md §1.2`, interval-edit classification from `SPEC.md §6.7`, and per-kind completion from `../harness/SPEC.md §3.4`. Use a capacity-1 board, confirmed live completable A with `start=0`, `end=24`, `consumes.quantity=1` and a bound six-hour buffer; B independently reserves `start=30`, `end=40` with quantity 1; for this fixture B is a non-completable event with no bound buffer of its own. All times use the same supplied reference clock. No separately authorized successor follow-up rule is configured in this fixture.
  - **Preaccepted successor / clock-only overlap:** at reference instant 12, admit B: A's scheduled end plus bound buffer is 30, so B fits. Advance to 24.1 without A's `actual_end`: A reads `review`, keeps its confirmation history and still draws quantity 1. Advance to 30.1 with no report: unresolved A and preaccepted B both count, giving recorded draw 2; no admission write occurred at either boundary. Availability excludes the unit, and an attempted new overlapping commitment refuses atomically, preserving A and B. Keep B's accepted reservation and both original schedules/history; neither record proves physical possession. In this fixture, the overlap alone creates no park, outward effect or successor mutation. This does not disable independently configured permitted actions.
  - **First late report:** at reference instant 34, the authorized owner first records `A.actual_end=34` with ordinary per-field owner attribution. Under §6.7 this closes A's unresolved draw without a new placement; the historical overlap during 30–34 remains recorded and alone cannot block the write. Read back A as `completed` under its kind with unchanged `confirmed_at`, both original schedules and attributed history, and B unchanged: no move, deletion or reclassification. A's bound six-hour buffer runs from the reported actual end to 40; B independently reserves 30–40. Do not declare any time free until both constraints permit it; with no other constraint in this fixture, the unit is available from 40. Replay the identical write ID: the original result returns and the end report/history applies once (§6.6/A4).
  - **Timely and no-report controls:** in a separate arm, record `A.actual_end=23.5`; the bound buffer ends at 29.5, before B starts at 30. A's early completion retains its confirmation and owner attribution. In the no-report arm, A remains unresolved even after B's end at 40, so the unit remains excluded from availability and a new overlapping commitment still refuses. Time alone is no return, attendance or payment evidence and never completes the completable. Contrast a non-completable event at its passed end: it may derive `completed` per the kind's home.
  - **Already-recorded end correction:** in a separate arm, `A.actual_end=20` is already recorded and its bound buffer ends at 26; B reserves 30–40. Correcting A's recorded end to 32 expands into re-let time (with buffer to 38), so A5's conflict/park applies and neither record is silently changed. The same correction with that slot still free applies. First-end classification does not bypass ordinary authorization, attribution, schema/precondition or latch checks: retain B1–B3's refusal controls and unchanged history; any other expansion bundled into the diff still faces §6.7's placement checks.
  These are documentary acceptance controls, not executed store proof. `BUILD.md` Step 4 owns the availability/projection arms; Step 5 owns admission/commit agreement, first-end recording/replay and the corrective-expansion controls, with A5 still gated there. The first-end arm follows §6.7's smallest recommended classification in the approved repair plan, not a new founder ratification or an unconditional overrun workflow.
- **A11 [the turn-state round-trip — §6 item 6's second index; Q2-043]** A turn-state record written under a caller-supplied write id is read back per `(owner_org, issuing principal)`: **any credential of the same principal** resumes or discards the turn by that id; **another principal's id reads nothing** — the storage round-trip of the second index, stored and readable before any loop consumes it (the harness X3–X4 reader is the harness's own scenario at its own home, never re-homed here).

## B — Latches
- **B1 [no un-expire]** A hold lapses (`expired_at` set by the virtual clock); the signature arrives late. Evidence is recorded; status stays `expired`; the unit is never re-held (`../harness/SPEC.md §3.4`, enforced by `SPEC.md §6` item 2). Confirmation/completion/re-hold attempts refuse, including a backdated accept; expiry and provenance stay unchanged. At BUILD Step 1 use store fixtures. At Step 5, this existing latch commit gate also walks B2/B3's valid and refused transitions through `commit`/read: confirm then cancel; confirm then recorded completion; cancelled/declined then attempted revival; completed then conflicting cancellation; each original fact survives. These commit-path controls close here, never from Step 1's storage-only proof. *(Situation-B's lapsed hold.)*
- **B2 [terminal precedence and confirmation history]** Store fixtures from `../harness/SPEC.md §3.4`'s table derive: confirmed future booking → `confirmed`; confirmed plus start reached → `active`; confirmed plus authorized cancellation → `cancelled`; confirmed plus valid recorded completion → `completed`. Retain the same `confirmed_at {by, at}` and provenance throughout. A cancelled-but-otherwise-complete commitment reports `cancelled`, not `completed`; a later admissible return fact cannot revive it or retroactively replace cancellation. A completed fixture refuses a later competing cancellation/decline/expiry, including a backdated input. A declined fixture refuses later acceptance; first-terminal precedence never mistakes confirmation for a terminal outcome. Pending plus blocked yields `blocked` with both conditions retained; clearing one condition leaves the other. Step 1 owns these storage/derivation controls; B1 owns their existing commit-path close at Step 5.
- **B3 [no clearing or rewriting write]** Any write attempting to null a set latch or replace its author/timestamp is rejected by the store (`SPEC.md §1.3`). For each declared latch, read back the original fact/provenance after refusal. Adding an authorized `cancelled_at` after confirmation and recording valid completion without clearing `confirmed_at` are positive controls, not conflicting writes (`../harness/SPEC.md §3.4`). Attempts to revive cancelled/expired/declined state refuse even when every precondition is now met; an unbacked completion assertion never substitutes for the kind's recorded act (`SPEC.md §3` precondition).
- **B4 [escalation stored, terminal write-once]** An `Escalation` (`../harness/SPEC.md §3.9`; stored per SPEC §1.13) is written, its `ladder_state` advanced across two virtual-clock firings (rung 1 → rung 2), then its status latched `timed_out_parked`. A later write attempting to reopen it (revert to `open`) is **rejected by the store**, and the co-occurring `needs_human` park is clearable only by a human (B3's discipline applied to the escalation). Assert the object **persists across firings** (read back from the store, not from harness memory) and that **no escalation-specific entry point** is called — only `commit`/`calculate`. *(The engine half of `../harness/SCENARIOS.md D12–D17; §3.9`.)*

- **B5 [no delete entry point — §1.10]** The engine's callable surface contains **no operation whose effect removes a stored row** — asserted structurally as an absence over the entry-point set (the K2 pattern applied to deletion), plus one behavioral half: cancelling, revoking, expiring, and withdrawing a bare draft (§1.4) each leave the row readable in history.

- **B6 [a historic shape stays readable — §1.10]** *(added 2026-08-29 — §1.10's additive-only law had no scenario, no `§10` row and no build step, in an append-only store where the rows it governs are permanent by law, so a violation is not recoverable by a later fix.)* *Given* rows written under an object shape, *when* the shape gains a field with a declared default, *then* every historic row **reads back through the new shape** with the declared default supplied **on read** — and the stored bytes of those rows are asserted **unchanged**, because *"upcast on read, never rewrite"* is falsified by a migration that quietly succeeds. And a shape change that **removes or re-types** a field is **refused at write as a storage fact** — asserted as refusal, the G8 pattern, not as a validation message and not as a migration that runs.

- **B7 [a restore conflicts loudly and cannot resurrect a latch — §9, §6.2]** *(added 2026-08-29 — `§10` carried the restore row as the one ledger entry whose construction site pointed outside the package, while no engine scenario mentioned a restore and no BUILD step named one.)* Two arms, asserted in one run. **Conflict arm:** a version restored over a slot that has since been **re-let** returns the conflict set as a **parked owner decision, per row** (`needs_human`, §1.3) — assert **per-row granularity**, not one merged park, and assert **nothing applied silently**. **Latch arm:** restoring a **pre-cancellation** version of a cancelled commitment is **refused**, asserted against the **full version chain** with the latch set on a version *other than* the one being restored — a check that reads only the restored version passes this scenario wrongly, which is the whole reason the arm is written this way. And assert the boundary §9 draws — *"A restore recovers structure; it never re-runs the past"* — as the absence of any replayed terminal event. *(The restore operation's own home is `../security/SPEC.md §8`; this is the engine law §9 states about it.)*

- **B8 [the conversion link is engine-minted and adds no read power — §1.1]** *(added 2026-08-29 — FD-46/FD-51's party↔principal link was defined in `SPEC.md §1.1` with engine-enforced semantics, absent from Step 1's object roster, and gated only by `../security/SCENARIOS.md` N6 in another package.)* **The structural half (Step 1):** **no caller-constructable entry point mints a link** — asserted as an absence over the entry-point set, the B5 pattern; and a link naming two tenants is **engine-authored or it does not exist** (I4's discipline applied to identity — the mint act itself is the security layer's claim, `../security/SPEC.md §2`). **The read-union half (Step 4):** the link's assembled starting history is **exactly** the union of what the linked party's booking-bound tokens could already read — asserted as **set equality**, never as a sample — and drawn *"over unrevoked, unexpired tokens only"*: a **revoked** token contributes nothing and an **expired** one contributes nothing, each asserted in the same run, so a build that assembles over the whole token history fails here. **And it re-homes nothing:** every carried record keeps its owning tenant after conversion, asserted on each record's stored `owner_org`. *(The engine half of `../security/SCENARIOS.md` N6 — the two must agree or conversion means different things on the two sides of the seam.)*
- **B9 [`PatternDecline` round-trips by declared shape — §1.17]** A `PatternDecline` persists through the existing `commit` verb's stored-object `diff` over its declared shape and reads back through `calculate`'s stored-object member (§5 item 7) with `pattern_key`, `proposed_value_hash`, `permanent` and the latches compared exact — no object-specific entry point exists to write or read it.
- **B10 [`PartyContact` is engine-resident and unreadable by any projection — §1.17]** A stored `PartyContact` uses that same declared-shape `diff`, is selectable by **no** display projection (the §0 fence: an address is not in any selectable set), is read back through the declared fire-time path in this carrier scenario, and its contact rows are **ciphertext at rest** — readable only through the keyed path, so the `../security/SPEC.md §4` crypto-shred posture is a store-side fact, not a policy promise (F-02's engine half). An automatic contact save creates no delivery grant and sends no network message. **This scenario's added save assertion covers the stored-object carrier and no-grant/no-send behavior.** The separate story requirements are `../user-stories/Situations/Situation-A/story-matt.md` S08b, “I make a group from one existing student and three new people”, and S13d, “Four individual links lead to one group decision.” Contact persistence and authenticated-owner private Contacts reads belong at `../harness/SPEC.md §3.12`; group identity and coordinator authority belong at `../harness/SPEC.md §3.6`. Those separate contracts remain owed; this carrier assertion does not complete them.
- **B11 [`PendingAsk` persists through `commit`; expiry rides the ordinary registry — §1.17]** A `PendingAsk` persists through the existing stored-object `diff`, reads back by declared shape, and its `expires_at` arms an ordinary `TriggerRegistration` (§1.15) — no ask-specific clock, no new entry point. Round-trip the complete `pending_act` including operation and canonicalized arguments unchanged after store restart; the answer-by-id and expiry decision remain harness B11, with the write/read halves at this layer's existing Steps 5/4.
- **B12 [`DeliveryEvent` stores the harness §3.11 shape, structural members only — §1.17]** A stored `DeliveryEvent` uses the declared-shape `diff` and carries exactly `{kind, party_ref, channel, act_ref, at}`; a write attempting a raw address or provider payload text in the record is **refused** — the shape's structural-members-only law is the store's, not a reader convention.
- **B13 [the per-owner inbox: idempotent claim mechanics and the owner-readable loss record — FD-96's engine half; §1.17]** Enqueue, FIFO claim and release are stored-object `diff` writes under §6.6's idempotency (a replayed claim returns the original result); a **pre-birth loss record** stores as an ordinary attributed object and is **owner-readable** through the declared shape — the engine stores the fact; surfacing it is the harness loop's (`../harness/SCENARIOS.md` R6).
  At Step 5, use injected time and two workers against the same store: race claims, invalidate a killed/paused claimant, race/replay recovery, then revive it. Assert one effective current owner; release plus attributed loss is atomic and occurs once per pre-birth lost attempt, with no partial disposition after injected failure, no duplicate enqueue/loss on retry, and a distinct record for a later lost attempt. Old recovery cannot release a successor; old claim replay cannot restore authority. Reject fresh-id stale work commits and loop advances inside the applying transaction, including a commit racing invalidation; an already-applied write still returns its scoped original result. For a lost response to work/birth commit, query the existing write id before recovery: preserve both, retire the claim without auto-requeueing that firing, keep its interrupted continuation, and allow ordinary FIFO work to claim. Correlated already-authorized receipt writes from the retired observer still persist once under their own ids; same-id changed payloads refuse, and neither a receipt nor recovery can mutate birth/seal, clear a human park, spend a new basis or authorize dispatch. Step 9 composes these store assertions with harness R6/X4/X8/B11: stop reaches active requests; takeover between ownership check and dispatch fences the stale effect. Storage-only success cannot close that adapter proof.
- **B14 [the `FiringEvent` store: part identity, write-once, intact return — §1.16]** At most one birth and one seal per firing id are representable — the atomic (firing id, part) uniqueness refuses a second part **whole** on every path; neither part is ever rewritten; and the engine read returns a stored record carrying an unknown vocabulary member **intact**, so the harness's K8 reader can refuse the whole read, fail-closed, without a field being dropped (§1.16; the storage/uniqueness half gates at Step 1, the intact-return walk closes at Step 5 — the B4/B5 half convention). Its co-write remains A9's host commit, not a new object entry point.
- **B15 [a stored park round-trips with its `cause` unchanged — Q2-072(a)]** A `needs_human` park written with a closed-union `cause` uses the declared-shape `diff` and reads back with the **same** `cause`, shape-checked member for member — the engine-side restatement never drops or coarsens the harness's typed detail (`../harness/SPEC.md §3.4`). Attempts to forge actor, tenant, scope, engine-minted reference or `ShareGrant` fields are refused.

## Q — Quota & balance
- **Q1 [the 11th hour]** A customer at 10h/month: `calculate` availability excludes any slot that would exceed the rate; `commit` refuses if forced. *(Sofia's cap.)*
- **Q2 [balance pack]** A 10-dive/3-month pack (`SPEC.md §1.2`'s printed class-pack): draws decrement; the 11th draw and the post-expiry draw both refuse.
- **Q3 [window edge]** A quota window spanning the materialization horizon computes identically before and after horizon extension.
- **Q4 [cancel restores the draw]** A booked half-hour drawing against a per-customer quota is cancelled: the half-hour **returns to the window's allowance** in the same window, and a rebook draws it again normally — the money credit (K1) and the restored hours ride onto the rebook together. *(Situation-A's cancellation-credit beat — K1 covered the money half by name while the quota half had nothing.)*
- **Q5 [the operator's day ceiling]** *(Situation C's "capacity — how many students the center runs in a single day"; added 2026-08-22; reworded 2026-08-22 — the row cited G7(b) for a board-targeted quota `SPEC.md §3` does not admit.)* An owner-scoped count-denominated `quota` over a day window targeting the course kind (§3's quota row: kind and audience are its targets) sits at its cap: `resolve` for one more course that day returns the structured decline naming the quota — **even though every resource it would draw on (instructor, pool, gear) is free**. The ceiling binds above the resources; cancelling one of the day's courses restores the count and the next resolve places (Q4's law).

## T — Travel & gaps
- **T1 [unreachable excluded]** Adjacent placements at two places: any candidate violating `gap ≥ travel(a,b,at)` is absent from availability and from `resolve` output. *(Situation-D: 10:00 across town after a 9:00–10:00 visit is never offered.)*
- **T2 [override beats computed]** A declared travel rule (route + time-of-day scoped) contradicts the provider's number; the declared value governs every computation touching that route/time. *(The bridge at rush hour.)*
- **T3 [fail closed]** The travel stub returns `unavailable`: the gap is unknown; the slot is not offered, the move not proposed; the decline carries the reason.
- **T4 [constant buffer]** A constant-operand `buffer` rule evaluates exactly as the classic buffer (Sofia's 10-before/15-after run) — and the harness's stored shape, `Rule{type: buffer, operand: 5min}` (harness B1), stores and evaluates **unchanged** on this menu.

- **T5 [a buffer operand edit governs forward only — FD-79]** *(added 2026-08-29 — the founder ruling landed in `SPEC.md §3`'s buffer row with no acceptance row anywhere: T4 covers constant-buffer math only, no BUILD step named it, and the coverage map omitted it. This is the class the corpus named on R1 — the FD-15 class.)* *Given* a committed placement whose `buffer` window is **currently running** off it, *when* the `buffer` rule's operand is edited — **both directions in one run, lengthened and shortened** — *then* the running window **neither shortens nor lengthens**, asserted on the gap the running placement enforces before and after the edit; and the **next** commit is governed by the **new** operand. And assert the sibling that makes the ruling non-vacuous: a placement committed *after* the edit and adjacent to the pre-edit one is checked against the **new** operand on its own side and the **old** window on the neighbour's — the two bindings coexisting without either being retroactive. *(FD-79's home is `SPEC.md §3`'s buffer row; the harness half is `../harness/SCENARIOS.md` F6.)*

## M — Recurrence & materialization
- **M1 [materialize]** A weekly pattern materializes real instances over the horizon; each has its own id and latches. **Matt S03 / MT-04/05/24/33 controls (`SPEC.md §1.4`):** materialize the accepted Run at 06:00–06:45 Asia/Taipei, Monday/Tuesday/Thursday/Friday/Saturday, with 10 minutes before and 15 after; Wednesday is absent from this Pattern, leaving the independent Wednesday lesson unchanged. Read back the same Pattern/occurrence identity and derived Event type under the Routine label. Replay creation under its original write id, including a lost response: one Pattern and one instance per intended occurrence. An alternate Tuesday/Thursday `interval: 2` retains its confirmed anchor across reload and overlapping expansion; a missing anchor is refused pending clarification, never treated as every week. Step 1 stores identity fixtures only; actual creation/expansion closes at Step 6.
- **M2 [edit one]** Cancelling one instance latches that instance only; siblings and pattern untouched. **Matt S05a / MT-69 / CRUD-07 / HR-28:** cancel Run on Saturday 17 October 2026 in Asia/Taipei, retaining its occurrence id/generation and attributed latch; release 05:50–07:00. Clear a display filter, replay the same write, extend the horizon and regenerate bare siblings after a forward-only edit: no second Run on that original date and no cleared/rewritten latch. The September history, next Monday, other weekdays and 12 October Sarah exception survive. An unauthorized or stale target changes nothing; cancellation is not completion or a Pattern stop (`SPEC.md` §§1.4/6).
- **M3 [forward-only pattern edit]** Pattern shifts Monday→Tuesday: instances carrying state (a latch, an attached party, precondition evidence) keep their day; bare drafts regenerate on Tuesday — and the superseded Monday drafts are **withdrawn** (§1.4's named disposition): capacity released, absent from every projection, rows kept in history with the `superseded_by_generation` event. **Stop-from-date control (MT-70 / CRUD-06):** use a fresh Pattern fixture with a past instance, an in-flight instance, future bare drafts, a dated exception, and future stateful instances separately carrying a latch, a party or precondition evidence; include a stored future exception beyond the horizon and a moved instance whose original date is in scope. Stop from 17 October 2026 Asia/Taipei. Assert the checked set and `ends` boundary under `SPEC.md §1.4`, withdrawal/history for eligible bare drafts only, retained state and ordinary registered parks for unresolved instances, and no further generation at/after the boundary even on replay. Resolve only each user's authorized choices through ordinary instance writes; a dependency/authority/version change refuses that proposed resolution and never substitutes bulk cancellation. No full-stop success while unresolved instances remain. Before-boundary and in-flight records retain terms/history; an exception cannot evade the boundary. Whole-series stop is an owed fixture, not the act performed in S05a.
- **M4 [beyond horizon]** An authorized tenant's availability read for week 12 beyond its eight-week horizon first prepares only the requested window under `SPEC.md §5`, then returns the projection; an ensuing booking uses ordinary commit checks. Inspect rows/history/trigger registrations before and after preparation: engine attribution, no other tenant's writes, no filling intervening weeks 9–11, no unrequested expansion. Snapshot the complete resulting store and projection values, replay the same request at the identical supplied reference instant with unchanged inputs, and assert zero further writes (including cache facts/history/registrations), stable occurrence ids and identical values. Overlapping requests/chunk retries materialize only missing authorized occurrences; an unauthorized tenant/window request makes no writes. Interrupted preparation reports unknown, not verified emptiness; resume idempotently. Exercise both availability and §2a subscription entry paths. Preserve M7's conflicted-draft/trigger behavior and quota verdicts; P10 owns the composed value-level determinism gate. Keep preparation outside the booking commit; V6 independently proves commit never fetches or writes a travel cache fact. **Stop/crash controls (`SPEC.md §5`):** for a harness request, first store the ordinary birth and claim fixture; missing birth, a lost/unresolved birth response or retired ownership permits no preparation write. Kill before the first chunk, during a chunk, and after one committed chunk: no chunk is partially stored; completed occurrence/conflict/history/registration bundles survive with engine author, tenant/window/reference instant, Pattern/generation and firing correlation. The unsealed birth reads interrupted through harness K8, not a completed user draft. Race cancellation, a Pattern generation change and an existing skip against the next chunk: stale work cannot apply or resurrect the cancelled slot. Retain a proposed new Routine and unsaved usual/date/skip/stop edits beside the fixture and assert none enters preparation. For the rolling-clock and direct-subscription paths, assert the same atomic history bundles without a fabricated harness birth. **Successful controls:** prepare the full authorized window, read the projection, then commit a separately authorized booking with its existing birth and normal atomic work/basis checks; replay preparation and the booking's write id without duplicate effects. K5/K6 and X4/X8 own harness lifecycle assertions; engine BUILD Step 6 owns these real chunk controls, Step 5 the birth/claim write precursors and V6, Step 9 their adapter composition. These are documentary requirements, not executed runtime proof.
- **M7 [materialization cannot double-book — §9]** A one-off books week 12 (beyond the horizon, via M4's on-demand path); a pattern covering that slot is then created; the rolling job extends the horizon to week 12: the pattern instance materializes as a **conflicted bare draft** — drawing no capacity, absent from availability, carrying its structured conflict record readable through `calculate`'s stored-object member — and the capacity invariant holds throughout. Nothing is silently dropped: the conflict record is exactly what the harness's next firing surfaces. **And the seen-not-silent half** *(added 2026-08-29 — §9's law has two halves and only the fence half was asserted anywhere: "**And the draft is visible on the owner's display projection** — marked as conflicted, invisible only to *availability* — so the owner sees their weekly class in trouble rather than a hole")*: the conflicted draft **is selectable on the owner board projection** (§1.7b) and **carries its conflicted marking** there — asserted as a **positive on the projection payload**, the S4 pattern — while the same draft is **absent from the availability projection** in the same run, the two asserted side by side so *"invisible only to availability"* is tested as the narrow exclusion it claims to be rather than a general one. The positive is what matters: a projection that omits the marked draft satisfies every fence in this file and still produces the exact hole §9 was written to prevent. Run the same occupied-slot fixture through §1.4's beyond-horizon request arming path as well as the rolling clock; both reach the same job and retain the same conflict/history/registration bundle, including after a stop/crash under M4. No request path bypasses this conflict behavior.
- **M5 [DST gap]** A daily pattern at a local time erased by spring-forward (zone with DST): that day's instance materializes at the gap's first existing instant; every other day is unchanged; no day yields zero instances. *(Law: SPEC §9.)*
- **M6 [DST duplicate]** The same pattern crossing fall-back: the duplicated local time materializes exactly one instance, at the earlier-UTC occurrence, and a duration spanning the transition keeps its true elapsed length. *(Law: SPEC §9.)* The stored original local date/time, zone and occurrence identity remain stable across replay; an exception selection in the duplicate hour cannot create a second occurrence (`SPEC.md §1.4`).

- **M8 [an `except` date over a stateful instance is registered, not stranded — §1.4, §1.15]** *(added 2026-08-29 — §1.4's `except` path had no scenario at all: M3 covers the day-shift path only, so the registered form ET-4 states was executed by nothing.)* *Given* a pattern with a materialized instance **carrying state** — a latch, an attached party, or precondition evidence — on date D, and a board with **no other scheduled firing before the horizon edge** (the condition §9 names as the one that made the silent drop possible), *when* `except: [D]` is added to the pattern, *then*: a **`TriggerRegistration` (§1.15) exists at D's instant**, asserted on the **stored registration** and never inferred from a later firing; stepping the virtual clock to D **fires it** and the instance surfaces as an ordinary parked question (`needs_human`, §1.3); the stateful instance is **not withdrawn** — it survives with its state; and any **bare** draft on D **is** withdrawn under §1.4's named disposition (M3's law: capacity released, absent from every projection, the row kept in history with the `superseded_by_generation` event) — the two dispositions asserted **side by side**, so they cannot be conflated. Assert the firing arrives as the **ordinary clock source** and that **no new trigger kind appears** (N1's law: the trigger vocabulary widens by nothing). **Dated edit controls (Matt S04/S05, MT-06/23/26, HR-02):** in a separate Asia/Taipei fixture select Monday 12 October 2026 by occurrence id, Pattern and generation; ordinary forward-only usual settings are Park/Driving, Home, 06:00–06:45, before 10/after 15 minutes. Only that Monday's accepted origin becomes Sarah's house, departure 05:30 and before-buffer 30 minutes total, reserving 05:30–07:00. Tuesday 13 October and another Monday retain Home and 05:50–07:00; saving Sarah's named address does not mutate Home or rescope the exception. These are supplied travel-result fixtures; provider mode/arrival computation remains separately owed. A Monday 15 October 2026 request fails calendar verification (15 October is Thursday) pending clarification; wrong year/zone, multiple matching targets and stale generation cannot silently retarget or write. Reload and materialize again without losing the accepted exception; date editing is not `except` exclusion or a governing-kind exception. Use M3's future-stop fixture to exercise the same registration/park over unresolved stateful exceptions.

## P — Place-only resolve & projections
- **P1 [placement]** `resolve` returns a placement satisfying every board and rule it touches.
- **P2 [honest decline]** No placement exists: structured decline naming the refusing constraint class — never a forced fit. *(Situation-B's "no bike"; **held-out E's** safe park — the Situation-E example, re-provenanced under FD-59 2026-08-31: the widened probe gate classifies this row under the suite's every-scenario-is-MUST preamble and admits the citation only through this marker.)*
- **P3 [site-day projection]** A `location-window` rule (Eastside only Mon/Wed): availability projections offer the day's place only. *(Debra's link; Situation-C's boat route rotation — which site the boat runs which day is the same rule.)*
- **P4 [domain refusal]** An `attribute-domain` violation (address outside service area) refuses at intake with the honest no.
- **P5 [unknown is not free — §1.2]** A proxy board with `empty_means: unknown` and no confirmations: every availability computation over it returns **nothing** — emptiness never reads as free; a confirmed window then appears, and only it is offered. *(The §1.2 law had no scenario; T3/V4 cover only the travel half of unknown-fails-closed.)*
- **P6 [the duration floor excludes at projection — and the ceiling refuses]** A kind under `duration: 30–120 min`: availability projections offer no start that could only hold a sub-minimum booking, and a forced sub-minimum commit refuses naming the rule. **And the ceiling** *(2026-08-22 — the operand's upper bound had no assertion in either suite)*: a forced over-maximum commit refuses naming the same rule, and no projection offers a span longer than the maximum as a single bookable duration — floor and ceiling are one operand, asserted at both ends. *(The engine half of harness B9 — Situation-A's minimum-duration beat and her 120-minute maximum; the commit half closes at Step 5 with the other commit-side laws.)* **Matt duration/window arms (SPEC §3; `../user-stories/Situations/Situation-A/story-matt.md` S07/S08a/S11/S13c; its verification MT-33/34/40/43/44/49/50/51/56, HR-10/13/19):** (a) store the English duration range 30–120 minutes, step 15 minutes, anchor 30 minutes. In a sufficiently open window, the offered lengths are exactly **30/45/60/75/90/105/120**; force each through intake and commit and each admits. **72** refuses for step and **180** for ceiling at all boundaries, naming the same rule. With step 5 and anchor 30, 35 admits and 72 still refuses; a range-only control admits 72 if otherwise feasible. No rule changes start-time granularity. (b) In **Asia/Taipei, Thursday 15 October 2026**, use the actual stored Personal appointment **15:00–16:00**, Home, zero own before/after buffers, plus the stored personal **06:00 Run**. English’s daily **09:00–23:00** window excludes overnight lessons, never the Run; a free time outside this window is not bookable for English. English carries **5 minutes before and 5 minutes after**: a 45-minute lesson **14:10–14:55** and **16:05–16:50** both fit; **14:15–15:00**, **16:00–16:45** and the requested **15:30–16:15** refuse. Projection and forced commit agree on the whole lesson interval, actual occupation and both gaps, not just the start. (c) Arthur’s unconfirmed **16:05–16:50** selection holds no capacity. Save the appointment extension to **16:15**, leaving its **15:00** start and zero buffers. Fresh projection admits **16:20–17:05**; the stale 16:05 confirmation refuses atomically, creates no lesson or quota increment, and discloses no private appointment reason. Arthur chooses **16:30**, receives **16:30–17:15**, **45 minutes**, **TWD1125** at **TWD1500/hour**, and confirms anew against current state; the retry uses the existing atomic/write-id rules, not automatic relocation. Preserve legitimate holds in a separate control; this fixture has none. (d) S13c’s proposed two-to-four-person price/format extension retains the stored duration range/step/anchor and existing one-on-one bound terms. The price-tier and template-replacement mechanisms need their own contracts; this arm proves only unchanged duration admission under a supplied compatible fixture, not that those mechanisms exist.

- **P7 [the fallback engages for two distinct causes — and the causes stay distinguishable]** *(Situation C's flagship beat: the house boat bumped once for seats, once for route — added 2026-08-22.)* Given a kind carrying a ranked `fallback` chain (house boat → backup) and `location-window` route rules on both boards: **(a)** a goal that exceeds the top entry's remaining capacity places on the backup; **(b)** a goal whose site the top entry's window doesn't cover that day places on the backup — two placements, two different obstructions, no forced fit. And the causes are never collapsed: with the backup *also* unable to serve, run (a)'s structured decline carries the **capacity** class and run (b)'s carries **`location-window`** — asserted as different refusing constraint classes (V4's discipline applied to the fallback chain); one merged reason fails this scenario.
- **P8 [contention is deterministic, bounded, and parks honestly]** *(added 2026-08-22 — `SPEC.md §7`'s contention law had no row, while printed gate names elsewhere referred to races this family did not contain.)* Two goals firing simultaneously over the same free window: arbitration orders them **deterministically** (arrival, then stable-id tiebreak — the identical order on replay); re-solve rounds are **bounded**; and a goal that survives no round returns a **structured decline for the harness loop** naming the contention — never a stall, never a silent drop.
- **P9 [a mid-series rate edit asks its owner, and nothing re-rates silently]** *(added 2026-08-23 — FD-75 closed OR-41 as an owner ask, not a founder ruling.)* Given a standing series with materialized instances behind it and not-yet-materialized instances ahead, when the owner edits the series' `pricing` operand: already-materialized instances are untouched (§1.5's bind-at-booking, asserted unchanged); the edit writes a **`PendingDecision`** naming exactly the two choices (keep the series' founding rate / re-rate as instances materialize, from the edit forward) — asserted engine-named, unrenamed on the surface (P2's discipline); **until the answer lands, an instance materializing takes the prior rate** — asserted no silent re-rate; and each stored answer governs that series' subsequent materializations. Answer two otherwise identical fixtures with the same human and instant but different legal choices, restart the reader, and assert the two later series materialize at their respective rates. No third choice and no default exist (asserted as absence); an off-menu choice refuses, clears no park, and replaying the answering write id applies once.

- **P10 [the same store state returns the same *values*, not merely the same verdicts — §0]** *(added 2026-08-29 — §0's foundational claim was asserted engine-side only twice and narrowly (Y1 for one typed value, P8 for arbitration order), `BUILD.md`'s "determinism is a test" guardrail named no test and no step, and the one repo-level gate is recorded at `../deployment/SPEC.md §7a` row 5 as comparing **test names and statuses, not content** — so a scenario returning a different placement on replay while still passing is invisible to it.)* One run, **replayed at the identical supplied reference instant**, over the three sites §5/§7 name as the clock-leak points: a placement crossing a **travel-cache TTL boundary**, a **contended** window (P8's Given), and a window materialized by the **horizon job**. Assert the returned **values** are identical across the two runs — the placement's instants, the proposal's `moves[]` order, and the availability set's members, each compared by value. State the assertion's own floor: **a pass/fail comparison does not satisfy this scenario**, because the repo-level determinism gate compares names and statuses only; that recorded narrowing is precisely why this assertion has to be value-level here.
- **P11 [a `party_size` predicated entry engages exactly per the declared size — FD-55/Q1-11]** A `dependency`/`fallback` entry carrying `when: party_size ≥ N` is required **iff** the predicate holds over the goal's own declared party size: a goal of size N−1 composes without the entry; a goal of size N whose entry cannot place returns the ordinary composition decline **naming the entry** — never a silent skip, never a clash with `pin`.

## X — Reshuffle proposals
- **X1 [direction honored]** Same trigger, `toward-start` vs `toward-end`: two different proposals, each improving in its stated direction only.
- **X2 [pin pre-filtered]** A pinned party's placements never appear in `moves` — they appear in `untouched` with reason `pin`. *(Harold is never even suggested.)*
- **X3 [stale proposal]** The board changes after a proposal is computed; apply-proposal re-validates and returns `{conflict}` — no partial application.
- **X4 [declined move]** One move's confirmation is `declined`: apply refuses; the board is unchanged; nothing was moved piecemeal.
- **X5 [freed fail-closed]** The proposal's freed window is unbookable while its decision is `pending`; `keep-blocked` keeps it so; `reopen` restores it to projections. *(The afternoon that stays hers.)*
- **X6 [confirmations required]** Apply-proposal with any `pending` confirmation refuses.
- **X7 [bound respected]** A compaction needing 4 moves returns the **first feasible ≤3-move proposal in §7's printed enumeration order** — or a decline — never an unbounded search *(reworded 2026-08-29: the row previously promised the "best" proposal, which `SPEC.md §7` refuses to define — "the promise is feasibility and improvement in the chosen direction, not optimality" — and a SPEC outranks a derived suite)*. And the clause that makes the reword load-bearing: the **identical trigger returns the identical proposal on replay**, which is exactly what the printed enumeration buys. *(The printed v1 bound.)*

## K — Money marks
- **K1 [owed derived]** Cancelling a visit corrects the record: the amount is no longer owed, with no mutable-balance write anywhere — **on a commitment carrying no `no-show` mark** *(carved 2026-08-21, FD-30: a marked record is K4's case, and before the carve K1 and K3 were both MUST on one commitment with nothing ordering them)*. *(The $140 never owed; Bobby's credit.)*
- **K2 [marks latched]** `paid/settled` marks carry `{by, at}`; no engine operation moves value (no such entry point exists to call).
- **K3 [no-show is never engine-declared]** A commitment whose scheduled start has passed with no check-in: the engine surfaces **the fact** and **never writes a `no-show` mark of its own** — assert the absence, on the stored record, after the clock has advanced well past the start. The mark appears only from an owner act, attributed `{by, at}`; what it then costs is the commitment's no-show policy, **bound at booking** (SPEC §1.5), so a policy edited after booking does not change this instance's owed amount. *(SPEC §1.9, founder-ruled 2026-08-07 #5 — the ruling had no acceptance criterion until now.)*

- **K4 [the mark latches the money state — FD-30]** The owner marks `no-show` on a commitment whose bound policy is deposit-forfeit; the counterparty then self-cancels on a valid token: status reflects the cancel, and `owed` **still derives the forfeit from the mark** — the record never reads $0. Marks first, then state; a derived value never un-derives a recorded human judgment.

- **K5 [cross-owner money is one record on both boards]** *(Situation C's freelancer day rate — "what Hug owes Matthew"; §1.9 × §7.1; added 2026-08-22.)* A §7.1 share-seam commitment carrying `priced` at the supplier's stated day rate: `owed` derives **identically on both boards** — one record, no mirrored copy to diff (I1's law applied to money) — and the K-family laws hold across the tenant line: marks latch `{by, at}` whichever side sets them, a `settled` set by one side is the same mark the other side reads, and no engine operation moves value. Assert the boundary too: neither side's **other** ledger entries become reachable — the shared commitment's own marks are details of the record both parties stand on, never a rung over the counterparty's ledger (I3 unchanged, N5's assertion pattern).

- **K6 [`held` is unsettable until its trigger is ruled — §1.9]** *(added 2026-08-29 — §1.9's poka-yoke was written specifically to stop the obvious wrong build, "the obvious wiring is the natural build and nothing else gated against it", and had no `§10` row, no scenario and no Verify line: a builder who wired hold-creation to `held` passed this whole suite.)* A write setting the **`held`** mark **on any path that can reach a money mark** is **refused** — asserted as a refusal, the G8 pattern, never as a validation message, and asserted over the whole set of reaching paths rather than one of them. In the same run the other four marks (`priced / owed / paid / settled`) are asserted **unaffected**, so the scenario cannot pass by refusing money marks generally. The refusal reuses the **existing** `refused` / `unruled` member — *"a write attempting a mark whose trigger is deliberately unspecified — today, `held`"* — whose home is `../harness/INTERFACES.md §7.1`: **no reason set is widened** by this scenario.
- **K7 [deposit-forfeit's amount has a stored source; `held` stays unsettable — Q2-040]** The `no-show` policy's *deposit forfeit* arm derives its amount from the deposit recorded in the `payment` precondition's **satisfaction evidence** (§1.3's evidence member — the stored-evidence/amount-source half, gated at Step 1 in the K1/K2 convention), and the derivation reads exactly that source at mark time (the forfeit-derivation half, Step 5); a write attempting `held` still refuses (§1.9's poka-yoke unchanged — this row adds the source, never the trigger).

## R — Preconditions (§3 `precondition`)
- **R1 [the conversation precondition parks; documents alone never satisfy]** *(FR35, founder-ruled 2026-08-07 #9 — the §3 row carried the ruling with no scenario until 2026-08-22, the FD-15 class.)* A kind whose precondition list includes a **conversation-kind** member: with every document requirement satisfied and **no recorded human conversation**, the commitment stays `pending` — assert the precondition reads unmet, never auto-satisfied by the document set — and forcing completion violates as an **attributed human decision park** (`needs_human`), never a silent pass. Recording the conversation `{who, basis, when}` lifts it, and the evidence lands on the record exactly as any other precondition's does. *(Wiring a domain flag to it — a dive-medical flag — stays template configuration, per the ruling.)*

## S — Shared projection
- **S1 [board-blind]** However a Shared projection is queried (weeks ahead, blacked-out days), the answer set contains only bookable time — no commitment, reason, name, or address of anything blocking. **And the fence covers money** *(sentence added 2026-08-22 — FD-54's engine half had no assertion)*: the projection cannot select a mark, a balance, or anyone's quota standing (`SPEC.md §1.7`; FD-54's manage-read is the separate token-scoped read at `../app/SPEC.md §5`, not this projection). *(Sofia's life; Debra's route.)*
- **S2 [token attribution]** A return through a per-recipient token attributes to exactly that recipient.
- **S3 [bundle-fence]** *Given* the FR38 template-bundle projection (SPEC §1.7a), *then* a query that attempts to select any counterparty, booking, history, ledger, or personal-data field is **unrepresentable** — the field is not in the projection's selectable set, asserted as schema-level unrepresentability, not a runtime strip. The engine twin of `../harness/SCENARIOS.md G9 [ENGINE]` and the authoring twin of the §1.7 leak test (S1). *(The two must agree or the harness→engine swap is a lie.)*
- **S4 [owner-board fence]** *(Split 2026-08-22, the Z1/Z2 precedent: one row held this ordinary-board fence — first-release machinery, testable at Step 4 — and a cross-tenant arm whose Given needs Step 8's share seam; the arm is S4b.)* *Given* the owner board projection (SPEC §1.7b) over an ordinary board, *then* a query that attempts to select **any** party's stored contact address is **unrepresentable**: the field is not in the projection's selectable set, asserted at the schema level exactly as S3 asserts the bundle fence. And the projection *does* select the `needs_human` park alongside status — a parked commitment that a board payload could omit would falsify `../harness/SPEC.md §3.4`'s surfacing law.
- **S4b [the cross-tenant rung]** *Given* the same projection over a board carrying a §7.1 shared commitment, *then* a query that attempts to select the counterparty tenant's fields beyond the granted rung — a blocking commitment, a refusing constraint class, a name — is **unrepresentable**, under the same schema-level assertion. *(Closes at Step 8, where the share seam exists to construct the Given.)*
- **S5 [two tokens, two answer sets — and neither can see the other's narrowing]** *(added 2026-08-22 — Mrs. Ito's link shows "Tuesday mornings at Riverside and nothing else"; the one place a `party`-targeted rule and a projection intersect, and nothing asserted it.)* *Given* one Shared with two per-recipient tokens and a party-targeted rule narrowing recipient A's shape (a `location-window` with `party: A`), *then* A's projection offers only the narrowed set (no Friday she cannot use), B's projection is the un-narrowed set — **two different answer sets from one Shared, asserted side by side** — and B's answer set is byte-identical to the same query in a world where A's rule does not exist: the narrowing is not observable from another token, in existence or in shape. Too little narrowing and too much leakage both fail this scenario.
- **S6 [the ghost is shape-blind]** *(added 2026-08-22 — FD-34's candidate-shape ghost had fence rows for its three sibling projections and none of its own.)* *Given* the FD-34 candidate-shape ghost projection over a store candidate, *then* a query that attempts to select people, bookings, history, ledger, or personal data is **unrepresentable** — the ghost renders shape alone, asserted at the schema level exactly as S3 — and the ghost is computed on request and **never stored** (`INTERFACES.md §2a`'s read-on-demand member).
- **S7 [the effective-policy projection: stub-gated, object-grounded, fenced and deterministic — FD-85 (PROPOSED)]** Three halves, half-annotated: **(Step 0)** the §2a projection stub ships with the engine stub and **Step 0's closure fails while the pushed effective-policy projection is absent** — the stub is not optional scaffolding (Q2-061/Q2-062); **(Step 1)** the projection's selectable set is definable over Step 1's stored objects — the grants, the covering-scope rules with their authority and version-of-record fields, the stored answers, the elicited thresholds and the on-call configuration §1.7c enumerates; **(Step 4)** the selectable-set fence holds by construction (no address, no vault artifact, no token digests, the cross-tenant rung) and the same store state projects the identical payload.

## G — Consistency & coverage
- **G1 [governing conflict]** A rule clashing with a governing rule: hard stop, write refused.
- **G2 [own conflict]** A rule clashing with the owner's own rule: returned for the override-with-reason path — not silently applied, not hard-stopped.
- **G3 [latent]** A ruleset hitting §3's `buffer × location-window` latent row (the buffer exceeds every gap between allowed windows): the alert returns naming the row, nothing is blocked, the ruleset stores. *(Rewritten 2026-08-21 — the old Given named "an SOP-internal latent inconsistency," an artifact the engine does not store; SOPs are harness-side.)*
- **G4 [coverage structural]** `check_coverage` flags a missing required attribute; it makes no semantic-completeness claim.
- **G5 [unsatisfiable outranks authority]** A pair marked ***unsatisfiable*** in §3's clash table — run it for a time-denominated `duration × quota` and for `duration × location-window`, both authored at **individual** authority — is **refused at write** and is **not** returned on the override-with-reason path. Assert the override path is unreachable for these, and that the refusal carries the naming message. *(The G2 own-rule case proves the opposite routing for a genuinely overridable pair; G5 is the line between them. The old second exemplar, `dependency × pin`, died with the pin disambiguation — a pin no longer touches bookability.)* **Three-way arm (SPEC §§3/8):** attempt three `attribute-domain` rules on one kind’s same weekday attribute: A={Mon,Tue}, B={Tue,Wed}, C={Mon,Wed}. Each pair has a witness but the joint domain is empty. The third write and an atomic ruleset write both refuse as `unsatisfiable`, including when all three are individual rules; no override path stores them. The conflict names A, B and C and their original typed domains. Reorder insertion and the classification stays the same, with no partial rule write. **Six-distinct-rule arm (SPEC §§3/8):** one atomic write proposes A={Mon,Tue}, B={Tue,Wed}, C={Mon,Wed}, D={Mon,Tue}, E={Tue,Wed}, F={Mon,Wed} on the same kind/weekday attribute. A–F have distinct source refs even where their domains repeat. All fifteen pairs intersect; the joint domain and every leave-one-out intersection are empty, so no single-source widening can repair it. Refuse with all six original source refs/operands, no override and no partial write; reordering does not change the verdict.
- **G6 [the refusal names the fix]** Every unsatisfiable refusal carries all contributing rule refs, their original typed operands and the remedy — never a bare "invalid ruleset." Ordinary pair refusals retain exactly two entries; G5’s three-way refusal has exactly three in `rules` and three positionally corresponding `operands`, never two chosen witnesses or one synthetic intersection. Assert `rules.length = operands.length` and reject unequal or dropped-contributor shapes. For G5’s sets, a `widen` remedy names C with typed `toward={Mon,Tue,Wed}`; it restores the Tue witness without changing A/B. A bare string, wrong-domain type or non-widening set is not that remedy. The suggested edit alone writes nothing. A user-authorized C edit is checked again and can store; neither an unapproved suggestion nor the remedy’s existence bypasses authority or governing rules (SPEC §8). *(SPEC §8; the `../model/SPEC.md §6` decline voice, enforced engine-side so it holds with no model in the loop.)* **Complete ordinary-pair encoding (SPEC §3):** use a duration operand `{range: typed 30–120 minutes, step: typed 15 minutes, anchor: typed 30 minutes}` and a plain `location-window` containing only a 20-minute contiguous window. Return exactly `[duration_ref, window_ref]` and `[the complete duration aggregate, the typed recurring-window set]`; retain source provenance and all three duration leaves through storage, refusal and harness readback. Dropped step/anchor, flattened three-leaf entries, a mixed `type_tag`/`range` object or the aggregate masquerading as a `TypedValue` fail. Repeat with the legacy typed range leaf; it still occupies one of two entries. Latent stays two complete operands with no remedy. **Coordinated six-rule remedy (SPEC §8):** for G5’s A–F return one alternative `{edits: [{subject: C, direction: widen, toward: typed {Mon,Tue,Wed}}, {subject: F, direction: widen, toward: typed {Mon,Tue,Wed}}]}`. C and F together restore Tue; either alone leaves an empty intersection. All six contributors stay in the conflict; C-only and F-only are not separate valid alternatives. Reject empty/duplicate/noncontributing/wrong-type/non-widening or jointly ineffective edits. On the originally refused atomic create, no A–F rules exist: an authorized retry carrying all six proposed rules with both widenings must store all six or none. For editing stored sources, use a lawful fixture with A/B/D/E enabled and C/F present but disabled; enabling C/F unmodified refuses, then one authorized diff enabling and widening both yields the Tue witness. Neither member can land first. Run authority denied for F (including governing/foreign-tenant controls), a stale intervening C change for which the proposed toward is no longer a widening, and an injected failure after staging the first member: the whole attempt leaves all rule versions/provenance unchanged, apart from the scoped failed write-id record. Run unchanged-current-rules success and same-write-id replay: both edits/attributions land once together. These transaction assertions belong to BUILD Step 5; a set oracle or source check alone does not execute them.
- **G7 [the class does not over-apply]** Rulesets that **look** unsatisfiable and are not, each asserted **storable** — written as positive tests because an over-broad `unsatisfiable` fails by refusing something valid, which no refusal-side assertion can catch: **(a)** a composition whose draw exceeds capacity only for *larger* bookings (`capacity × dependency` above its minimum) stores, and oversized bookings refuse at commit per A1; **(b)** a **count-denominated** `quota` ("max 3 lessons a day") beside a `duration` rule stores — the pair shares no evaluated quantity (§3's unit guard); **(c)** a ranked `fallback` chain whose top choice is `pin`ned stores and resolves normally — a pin constrains moving, never booking. *(The original (a)–(c) controls survive; (d) exercises SPEC §3’s scoped composition.)* **(d)** A={Mon,Tue}, B={Tue,Wed}, C={Tue,Thu} on one kind’s same attribute store, with effective domain {Tue}; Tue admits, Mon/Wed/Thu refuse under the same stored predicates. Repeat with conditional `location-window` rules Eastside Mon/Wed and Riverside Tue/Fri: both store, project only their own allowed site-days per P3, and a booking at Eastside Tuesday or Riverside Monday refuses. Do not flatten these branches into an empty weekday intersection. A single minimum-length booking alone in a large-enough window remains feasible even if its buffer would prevent a second booking there: the rejected duration/buffer joint row remains absent.

- **G8 [off-menu unstorable — §1.4, §1.5]** An off-menu rule `type` and an off-menu recurrence `shape` are each **refused at write as a storage fact** — asserted as refusal, not as a validation message; nothing off-menu is ever reachable at evaluation because it was never storable. *(Both laws were stated twice and tested nowhere; the rule-type half closes at Step 3, the recurrence half at Step 6.)* A natural-language restriction outside the closed menu or an unsupported typed predicate remains an explicit authoring limitation; storing its words as a form field is not a stored executable rule, and no projection or admission claim may treat it as enforced (SPEC §3; harness B9).
- **G9 [the covering-grant lookup answers both directions — FD-97]** `check_coverage`'s `covering-grant` kind returns **the exact covering grant's ref** when a stored, unexpired, unrevoked grant covers the act, and **`null`** when none does — including a grant that exists but is expired or revoked; the return is the pure lookup (§1.6) and **the decision to act stays the harness floor's**: this seam answers existence, never permission-to-proceed. Under `../harness/SPEC.md §7`, use stored typed operation/conditions and compare the actual canonical act: positive reminder and 45-minute `commitment.confirm` under a <60-minute Grant; negative form-send/escalation under that reminder Grant, 60/75-minute booking, wrong principal/party/subject, expired and revoked Grants. A forged shorter interval or operation cannot substitute for the validated act. Unknown operation, missing conditions, invalid comparator or incompatible units refuse under §2; a valid nonmatch returns `null`. Changing a referenced ordinary rule does not widen the Grant's stored operands. Step 3 runs the pure relation over validated fixtures; Step 5 exercises grant-write/activation refusal, with harness Steps 2/3 owning the carrier and executor decision.

## I — The cross-owner share (SPEC §7.1)
*Parity family: `../harness/SCENARIOS.md` I1 asserts the harness half (the path is the ordinary loop, no separate harness invoked); these assert the engine half.*
- **I1 [engine parity with harness I1]** Two on-app owners, one books the other: `resolve` (over the counterparty's authorized exposed availability) → read-only placement handle → initiator's commitment-form creating `commit` → stored counterparty accept → `commit(offer_ref)` yield **one creator-owned commitment standing on both boards in one transaction** — no mirrored second record exists to diff. **No function outside the §1 seam set is called** — asserted as a walk of the contract, which is what makes the swap law survive this feature. Before the creating commit, resolve leaves the store, grants, holds and inbox unchanged. Drop that commit's response, replay its write id in the same authenticated apply scope, and assert the same offered commitment/result, one hold and one counterparty firing; changing the payload refuses under A4, and a foreign tenant or credential reads no original result. *(The engine-side mirror of harness I1 — the two must agree or the swap is a lie; real transaction proof is BUILD Step 8.)*
- **I2 [both boards or neither]** The counterparty's board fails a check at commit time (a buffer violated by a placement made since the offer): **the commitment appears on neither board**; `{conflict, reason}` returns — reason and alternatives bounded to the exposed availability (§7.1 item 3: the initiator learns *taken*, never *why*); the **offer ends** and the terminal state is a **recorded attributed event**, not a status value (I7's record). *Assert the commitment's status reads as one of `../harness/SPEC.md §3.4`'s values — the terminal event releases the hold, so the `offered` condition no longer derives (§3.4's conflict-case rule) and no latch was set — and that **no fourth value** appears for the conflict; a run that invents one fails this scenario.*
- **I3 [no read power added]** Throughout an offer, every value the initiator's side can observe about the counterparty is derivable from the `availability` grant (the published Shared projection). No commitment, rule, party, name, or money mark of the counterparty is reachable without a higher rung they granted — asserted as a walk of what the seam returns, not a runtime sample. *(The S1 board-blind guarantee, re-asserted across the tenant line.)*
- **I4 [caller cannot author a crossing]** A caller-supplied structure naming a second tenant is a type error; a **ShareGrant** is engine-minted or it does not exist *(qualified 2026-08-21: the §1.6 authorization Grant is harness-elicited under FD-24 and is not this edge — the unqualified sentence conflated the two shapes §7.1 separates)*. The creating commit may target only an authorized published availability ref: test revoked/unauthorized refs and attempts to supply a private board, wider target data, forged recipient or caller-authored ShareGrant. No unauthorized offer, hold or firing is stored. At a valid creating commit the engine mints only the offer-side ShareGrant for this offer; the distinct accept-side grant still requires the counterparty's stored basis (`SPEC.md §7.1`). Both creating and accepting paths retain the authenticated replay scope and exposed-availability-only refusal data (I2/A4). Structural caller-unconstructability and real write refusals are separate assertions at BUILD Step 8.
- **I5 [silence never binds]** An offer that is never answered reaches `expired` on the virtual clock when its hold runs out: the commitment stands on no counterparty board, a recorded terminal event on both.
- **I6 [the hold is the creator's setting]** Resolve alone reserves nothing; the hold arms only at the initiator's creating commit. While an offer sits `offered`, the targeted window is **held for the offer's `hold` duration** as a temporary reservation, not accepted consumption (default 5 minutes; creator-set 0–24 h) and then freed. With `hold: 0` the window stays bookable and the first commit wins. A racer against a zero-hold or expired offer loses with `{conflict, reason, alternatives}`, alternatives drawn from **exposed availability**, never the board.
- **I7 [terminal states recorded both sides]** Decline (with its structured reason), expiry, and conflict each write an attributed `{who, basis, when}` event to **both** boards and place the commitment on neither. *(`../security/SPEC.md §9` N3, made testable.)*
- **I8 [a live unanswered offer cannot commit — §6.5 b]** The initiator has successfully created only offered/hold bookkeeping, including the offer-side ShareGrant, then calls `commit(offer_ref)` while the offer sits `offered` with no stored accept: the commit **refuses** — the accept-gate is checked, not vacuously satisfied through an empty `moves[]`. Neither the creating write's basis nor its ShareGrant is the counterparty's stored yes. Exercise a matching counterparty Grant/live confirmation as the positive control and absent, foreign, expired or nonmatching basis as refusals; only the counterparty-authorized accept supplies accept-side authority (`SPEC.md §7.1`). *(Before 2026-08-21 the accept requirement lived only in §7.1 prose and the moves-check was vacuously true for every offer; I5 tested silence via expiry only.)*
- **I9 [the offer's zone is the initiator's]** *(Carved from Y1 2026-08-22 — the arm's Given needs this family's Step 8 machinery, which Y1's row cannot cite.)* A §7.1 cross-owner offer resolves its time phrase in the **initiator's** board's declared home zone (`SPEC.md §1.2`'s goal-zone rule), while the counterparty's page displays the same instants in their own zone — one stored truth, two zone renderings, asserted side by side.

## W — Multi-day decomposition (SPEC §7)
*(Letter `W`, not `D` — this file cross-references the **harness** D-family, the floor, and two D-series would be unreadable.)*
- **W1 [per-day calls]** A four-day course resolves as four single-day `resolve` calls composed by an Order — no multi-day search exists to call. *(Situation C's flagship.)*
- **W2 [partial placement survives]** Day 3 declines; days 1–2 handles remain valid and independently committable. Nothing is rolled back, because nothing was written.
- **W3 [which days placed]** The result names, per day, a placement or a decline carrying its refusing constraint class — never a bare partial.
- **W4 [all-or-nothing is the caller's]** The same day set committed as one Order applies atomically: a day-4 failure leaves days 1–3 uncommitted. Committed individually, they stand independently. Both paths exist; the engine picks neither.
- **W5 [template expands to ordinary objects]** A four-day `KindTemplate` (SPEC §1.12) instantiates to four Commitments under one Order, sequenced by `depends_on` per `requires`. **No object type exists in the result that a hand-authored course would not produce** — asserted structurally. *(F7's resolution, made testable.)* This is *expansion*-equivalence (template → objects). Its **authoring**-equivalence twin — an agent-authored *bundle* ≡ a hand-authored one — is FR38's instance and lives in `../marketplace/SCENARIOS.md` I6, because the engine is deliberately not marketplace-aware (`INTERFACES.md`); FR38's "W5 (agent-authored ≡ hand-authored)" citation points back to the principle stated here.
- **W6 [relative offsets]** The same template expanded against two different anchor days produces correctly-dated session sets, with no stored absolute time anywhere in the template.
- **W7 [role binds at expansion]** A template naming a `board_role` ("a boat") binds to a concrete board at expansion; the template is unchanged and re-expands against a different fleet.
- **W8 [template edit is forward-only]** Editing a template leaves already-expanded courses untouched — mirror of M3's pattern-edit law.
- **W9 [a spanning consumption rides the course]** *(Situation C's "tagged once, theirs for the whole course"; added 2026-08-22.)* An Order combining per-day session commitments with each member's gear as **one commitment spanning the course days on one unit** — the Situation-B rental shape, no object type W5's law forbids — behaves as the story requires: no day re-draws from stock, the unit never re-enters availability mid-course, and cancelling through the Order path (W4) frees the whole span at once. Asserted structurally: per member, one consumption row spanning four days — never four daily draws.

## V — Travel envelope (SPEC §5)
- **V1 [ceiling]** A placement whose candidate set would need more than the fetch ceiling makes no further fetches: remaining legs are unknown and fail closed. The search declines; it never stalls.
- **V2 [dedup + batch]** Many candidate instants over the same place-pair and time-bucket produce **one** provider request; the scripted provider's call count is asserted.
- **V3 [cache hit beats fetch]** A second placement over a cached `(place_a, place_b, bucket)` makes no provider call and returns the identical duration.
- **V4 [the two declines are distinguishable]** A day that is genuinely full returns `no-feasible-placement`; a day the provider failed on returns `travel-unknown`. Asserted as different structured reasons — collapsing them fails this scenario.
- **V5 [a cold day warms, and says so]** *(added 2026-08-22 — SPEC §5's warm-then-re-offer: without it, every board's first location-aware placement declined by construction.)* A cold-cache Situation-D day whose candidate set exceeds the fetch ceiling: the decline's unknown legs carry `travel-unknown` with `detail.cause: ceiling` — distinguishable from a provider outage's `detail.cause: provider-failed`, V4's discipline one level down, with the closed reason set unwidened — the cut triples are resolved by the background warm-up, its completion fires the clock source, and the follow-up resolve **places** with no fresh provider calls beyond the warmed cache (V3's assertion closes the loop). A flat decline with no warming path fails this scenario.

- **V6 [a commit makes zero provider calls — §6.1]** *(added 2026-08-29 — §6.1's named hard law had no `§10` row, no Verify line and no scenario, while V2 already proved the assertion is cheap.)* *Given* a commit whose travel-computed `buffer` gap **misses** the cache, *then*: the scripted provider's call count is **zero** (V2's assertion pattern); the gap is `unknown` and the commit **fails closed** — the placement is refused, never accepted on an assumed gap; and **no cache fact is written** by the failed commit, asserted on the stored cache, which is the half that makes a failed commit side-effect-free. Assert the contrast **in the same run**: the identical placement through `calculate`/`resolve` **does** fetch, because §5's fresh-fetch rung belongs to the read path — a build that simply never fetches anywhere passes the first half and fails this one.

## N — The trigger registry (SPEC §1.15; the bridge at `INTERFACES.md §2.2`)
*(Family added 2026-08-22 — §1.15 existed with no scenario, no build home, and no stated bridge to the harness clock; V5's warm-up, the hold clocks, the horizon job, and the FD-53 age-out all ride it.)*
- **N1 [armed, fired, kind-routed — exhaustive over the union plus the internal branch]** Stored registrations covering all four harness kinds and the internal branch arm on the virtual clock and fire per `INTERFACES.md §2.2`'s mapping: an **engine-internal** registration (no harness `kind`) surfaces as the ordinary `clock` source with `registration_kind` **absent**; one carrying `reminder` surfaces as `clock` carrying `registration_kind: reminder`; one carrying `ask-age-out` surfaces as `clock` carrying `registration_kind: ask-age-out`; one carrying `escalation-deadline` surfaces as `clock` carrying `registration_kind: escalation-deadline`; and one carrying `offer-hold` surfaces as the **`hold-expiry`** source carrying `registration_kind: offer-hold` — each fixture asserts its emitted source AND its carried-or-absent discriminator, the firing carries the registered ref, and the trigger vocabulary widens by nothing (`../harness/SPEC.md §4` stays seven sources). The reminder fixture stores its booking revision, policy reference/revision and Grant reference under §1.15; these are storage/clock fields here, not evidence that activation or provider delivery ran. Exercise both stored escalation deadline roles under `../harness/SPEC.md §3.13`, including an in-app-only ladder fixture. Restart the clock adapter against the retained store before stepping: both original instants remain armed with the same identities. This proves persistent arming/emission, not harness rung advance or park.
- **N2 [re-register replaces]** Re-registering the same purpose with a new `fire_at` replaces the arming: exactly one firing, at the new instant, never two. With §1.15's reminder key, retain exactly one current registration per booking/owner policy, including after repeated same-revision writes. At Step 5, apply a successful move with registration replacement in the same commit; a refused/stale move preserves the prior arming. Activation stores both the live-confirmed Grant and policy link or neither, and replay of its write id cannot create a second registration. These transaction assertions need commit; Step 1 proves only storage/arming replacement. For the escalation key in §1.15, replace a rung registration, retain exactly one current rung, and assert the original total registration and instant survive replacement and adapter restart. This Step-1 control supplies stored transitions directly; actual creation/advance/answer atomicity remains B4/N3 at Step 5.
- **N3 [withdrawn or orphaned, inert]** A registration withdrawn through its `scheduler_handle` fires nothing; a firing that finds its registration gone (removed between arming and fire) makes **no business-state write, park, or ask; it appends only its own attributed no-op record**, identified by the firing registration and `SPEC.md §1.10`'s history — inert by law (`SPEC.md §1.15`: nothing evaluates against a registration). *(This bookkeeping-only arm closes at Step 5, when `commit` exists to be wrongly reached.)* At Step 5, inject cancelled-booking, withdrawn-policy, expired/revoked-Grant, superseded booking/policy revision and repeated-current-callback cases through the existing write path. A late old callback cannot claim a current occurrence, and concurrent fresh-id claims of the same occurrence permit at most one basis spend; an already-spent occurrence stays spent after a content-only revision. Booking/policy/grant changes and dependent registry writes are all-or-nothing, with no extra arming after failed/replayed changes. A roster edit refreshes one booking reminder rather than registering per participant. Assert only registry, attribution and basis-spend state here; actual dispatch belongs to harness D28, not this engine scenario. For `../harness/SPEC.md §3.13` at Step 5, commit escalation creation with its registrations, advance with rung replacement, and answer/closure with withdrawal: each bundle applies all or nothing and repeated writes never duplicate it. An early callback leaves the not-yet-due registration armed without consuming it. Reject stale registration ids, wrong escalation/rung identity and closed-state work under fresh ids; enforce the total cutoff and current claim in the same transaction. Exercise answer/advance and total/rung ties in both transaction orders, with retained terminal latches and no stale fresh-id work. Prove the no-business-state-write/no-op history disposition here; notification, floor and worker-restart ladder behavior remain harness D12–D14/D16.
- **N4 [the registration's `kind` is stored and returned unchanged — the storage/bridge row only]** A `TriggerRegistration` written with a closed harness `kind` (§1.15's optional member; the vocabulary's home is the harness) reads back with the **same** `kind`, and one written without it reads back without it — storage fidelity and nothing more: **the routing Green (a `hold-expiry` firing and a `clock` firing distinguishable at the point the loop routes them) is harness-owned and gated at harness R7** — the row C3.3 lands in the harness suite, cited by name rather than path until it exists — never by this row. Include escalation/rung/role identity from `../harness/SPEC.md §3.13`: two escalations on the same commitment stay distinct; both role variants round-trip; a rung role without rung, a total role with rung, missing escalation ref, unknown role, or metadata on another kind refuses. Historic other-kind rows read with escalation metadata absent; no guessed migration. These field controls do not certify the current TypeScript clock fixture as a stored registration decoder.

## O — Minimum occupancy (SPEC §3 `min-occupancy`)
- **O1 [early booking never blocked]** The first booking on a boat with `min-occupancy: 4` commits normally. Being below the minimum is never a reason to refuse a booking.
- **O2 [clock trigger, owner decision]** At `start − decide_by` with 2 of 4 seats confirmed, the engine **writes the `needs_human` park and a `PendingDecision` (SPEC §1.14)** and stops there; the harness's trigger loop reads it and the run **parks for a human**. The engine cancels nothing and notifies no customer on its own. *Assert the record is a `PendingDecision` carrying its three engine-named choices (run under minimum / cancel / extend the window), readable through `calculate` with no new seam verb, and that **no engine path authors or selects `chosen`** — a run in which the engine authors, selects, defaults, or times out into a choice fails this scenario; persisting a valid authenticated human-authored choice is required. A valid authenticated human answer arrives through the stored-object `diff`, and the engine persists `chosen { choice, by, at }`, clears the park, and replays once by write id; engine/LLM choice, off-menu choice, or forged actor/tenant/scope/`ShareGrant` fields refuse.* **Run identity/time control (SPEC §3):** run A occupies [10:00,11:00), `decide_by=1 hour`, `min=4 seats`; advance the virtual clock to 09:00 with only 2 confirmed seats for A. Put 4 confirmed seats on unrelated run B on the same board, first at [09:00,10:00), then in a separate fixture at [10:00,11:00) with sufficient board capacity. Neither contributes to A; A parks in both. Unconverted holds for A do not make confirmed seats. Assert the registration’s `subject_ref`, evaluated run interval and as-of instant in the read fixture, so a board-only sum cannot pass.
- **O3 [minimum met, silence]** At the trigger instant with 4 of 4 confirmed **for the registration’s subject run**, the scheduled evaluation executes but raises no park or decision and surfaces nothing. For run A [10:00,11:00) with a 09:00 decision and an otherwise idle board at 09:00, count A’s four confirmed seats over [10:00,11:00), not the zero board draw at 09:00 (SPEC §3). Add or remove unrelated same-board run B’s reservations and the result stays silent; reducing only A to two seats produces O2. This is recorded occupancy, not physical attendance.
- **O4 [unsailable clash]** `min-occupancy.min` greater than the board's `capacity.N` classifies **unsatisfiable** at `check_consistency` (SPEC §8 item 4) and is **refused at write even though both rules are the shop's own** — not offered on the override-with-reason path, because no override could make the boat sailable. The refusal names the fix. Discovered at authoring, never at sea.

## Y — Typed values (SPEC §2)
- **Y1 [instant resolution — FD-27; propose-then-verify per FD-57]** `typed_value` resolves a raw time phrase — arriving as `{phrase, proposed}`, the model's proposed structured value beside the raw words — to an `instant`/`interval` against a `type_spec` carrying `{zone, reference_instant, locale}` (§2's resolution-context rule), **by verification: the proposal re-rendered into the locale and zone must match the phrase's components, asserted as a round-trip** — a proposal that renders to a *different* Thursday, hour, or period is refused as a `typed_value` error, never accepted on plausibility: *"Thursday 3–4pm"* (`locale: en`) with zone `Asia/Bangkok` and a Tuesday reference yields that week's Thursday 15:00–16:00 as zone-resolved instants; the identical inputs yield the identical result on replay; the Thai phrasing **"บ่ายสามถึงบ่ายสี่วันพฤหัสนี้"** (`locale: th`) and the zh-TW phrasing **"這週四下午三點到四點"** (`locale: zh-TW`) of the same time resolve to the **same instants** *(operands printed 2026-08-21 — an equivalence asserted over unprinted inputs has nothing to code against)*; a phrase resolution cannot settle (*"sometime soon"*) returns a `typed_value` **error**, never a guess — and nothing in this path reads wall time. **And the two-zone case** *(2026-08-22, §1.2's goal-zone rule)*: a goal whose `consumes` spans two boards with different declared home zones resolves the phrase in the zone of the first-consumed board under the goal's declared ordering — assert that choice *(the §7.1 cross-owner arm is carved to I9, 2026-08-22 — its Given needs Step 8's share machinery; this row stays whole)*. *(The engine half of the thin-agent time law: the model passes the phrase raw, `../model/SPEC.md §5`.)* **Duration operand companion (SPEC §3, Matt S07/MT-33/MT-51/HR-19):** raw “30 minutes to 2 hours” retains both inclusive endpoints through typed storage/readback; step “15 minutes” and confirmed anchor “from 30 minutes” produce the same typed duration values on replay. Reject a zero/negative step, a seats-valued step, a lone step/anchor, and **30–125 minutes, step 15, anchor 30** or **30–120 minutes, step 15, anchor 5** as incompatible definitions. The harness must clarify rather than round endpoints, drop a step or guess an anchor. A valid no-step range remains readable and storable. This companion uses the shared typed-value boundary; it does not broaden FD-57’s instant proposal exemption.
- **Y2 [a literal cannot sit in a handle-typed position — §2, §4]** A write whose handle-typed field carries a raw literal (a display-facet string re-entered, a fabricated id) is a **type error** before any math runs — the structural half of thin-agent, asserted directly. *(I4 covers only the tenant-crossing case; the general law had no scenario.)*

## Z — Stub parity (the swap insurance)
- **Z1 [behavior parity]** For each harness stub behavior (`../harness/INTERFACES.md §5`: capacity check, latch check, canned handles), the real engine reproduces the *contract-visible* behavior exactly on the harness scenarios' inputs.
- **Z2 [the swap]** The full harness `SCENARIOS.md` suite runs green against the real engine with **zero harness changes** — the layer's definition of done. The harness suite **includes P1** (the compaction pass-through) **and P2** (the pending-decision round-trip), so both engine-originated paths are exercised end-to-end through the harness — Z2 cannot pass vacuously on either. *(FD-80: one list. This row is the full harness `[MUST]` suite, including P1 and P2 — not a tagged subset. Home `../PRD.md` §6.5.)*
- **Z3 [fault injection at the swap]** With the real engine deliberately unreachable (and again with a forced timeout), a seam call returns `unavailable`/`timeout` — never `{conflict}`, never a hang — and the subsequent retry through the same write id recovers without a duplicate (A4's law observed through the harness). *(A healthy engine can never emit these members, so harness K4's consuming half runs only against the scripted stub until this scenario exercises the real path.)*

---

**Coverage map (interview acceptance → scenarios):** no-double-book race → A1 · quota restored on cancel → Q4 · the duration floor → P6 · write-id idempotency (§6.6) → A4 · interval-widening re-check (§6.7) → A5 · latch invariant → B1–B3 · no-delete asserted → B5 · time resolution (FD-27) → Y1 · thin-agent structural half → Y2 · escalation stored/terminal-write-once (§1.13; harness §3.9) → B4 · diff-only rule writes → A3 · quota math → Q1 · buffer math → T4 · the buffer operand edit binds forward only (FD-79) → T5 · recurrence + single-copy edits → M1–M4 · DST edges → M5–M6 · materialization-capacity law → M7 · unknown-is-not-free (§1.2) → P5 · off-menu unstorable → G8 · structural coverage → G4 · unsatisfiable-outranks-authority → G5–G6 · **the class does not over-apply** → G7 · place-only + honest decline → P1–P2 · reshuffle (revisit) → X1–X7 · the owner-board fence (§1.7b) → S4 (+ S4b, the cross-tenant rung) · the party-rule × projection intersection → S5 · the FD-34 ghost fence → S6 · the trigger registry (§1.15) → N1–N3 · contention ordering (§7) → P8 · the mid-series rate ask (FD-75) → P9 · stub-swap → Z1–Z2 · seam fault injection → Z3 · Situation-D pins → T1/T2/T3, P3/P4, X2, X5 · Situation-C pins → A1 (the last seats), P3 (route rotation), B1 (the clocked offer lapses like any hold), P1–P2 + K1–K3 (the week's placements, its money record, and no engine-declared no-show), P7 (the fallback's two causes, distinguishable), Q5 (the operator's day ceiling), W9 (gear spans the course) · no-show-then-cancel (FD-30) → K1 (the carve) + K4 · the cross-owner money record (one record, both boards) → K5 · the conversation precondition (FR35) → R1 · the cross-owner share → I1–I9 (engine half of harness I1; I8 the accept-gate; I9 the offer's zone) · multi-day courses → W1–W4, course templates (F7) → W5–W8 · travel envelope → V1–V5 · boat minimum (F20) → O1–O4.
