# annnä Marketplace — BUILD (ordered plan)

*The marketplace **builds last** — after all four layers. Gated on: the app stub-swap complete (`../app/SCENARIOS.md` Z2), the harness suite green (specifically the elicitation family B1/B2 and the floor family D, which the install crossing rides), and the engine's rule menu + Shared projection green. The closed service exists in this repo only as the CI mock (`INTERFACES.md §1/§5`).*

> **FROZEN 2026-08-08 — Steps 0–5.** The whole package is specified and is not being built. The freeze's normative home, with its reasoning and resume condition, is [`README.md`](README.md); this line exists so the status report reads the same state a human reads here. Nothing below is deleted, weakened, or made optional, and gate-coverage still walks every scenario.

## Step 0 — Formats + the install door
- **Adoption while frozen** *(2026-08-31, F-27)*: this step's formats and validator are already adopted by external gates (harness G8; the takeout-fails-install walk) — the freeze holds this package's own steps, and those adoptions run against the frozen formats as they stand.
The two document schemas (skin pack, template bundle) and the install-door validator, including the two seed bundles ("Free Time Available", dive-center) as fixtures. These two seeds are **throwaway test fixtures, not production catalog content** (FR38, `../RULINGS.md`): they exist only to exercise the format's span; any shipped version is founder-built through the app, and no developer authors a bundle. **The artifacts this step consumes, each a readiness condition — the step is buildable when all four stand:** `SPEC.md` §1.1's and §1.2's **printed grammars** (the pack document with its palette sub-shapes and derivatives, the bundle wire grammar, and the blanked-operand sentinel the door reads); `../app/SPEC.md §4.1`'s **manifest file** with its per-type closed prop lists, each prop's closed structural value shape and portable/app-local classification, and its version; `../engine/SPEC.md §3`'s **closed rule menu**; and `../engine/SPEC.md §1.12`'s **`KindTemplate`**. Gate: **F1–F5**. Extend F3/F4 with the private-payload/public-projection separation and whole range/step/anchor carrier (`SPEC.md §§1.2/2`); private validation is adopted at harness/app gates, not a reordered marketplace build. **The validator also decides `SPEC.md §1.2`'s transferable subset** *(2026-09-12)*: the two schema-carrying fields (`domain`, each `shared_shapes[].form`) are walked to full depth for node `default?` values, value-bearing `props` members, reference-resolving `bind` targets and `file-slot` nodes, each refusing the document whole with the failing entry named by node path — the readiness condition being `../app/SPEC.md §4.1`'s per-type closed prop lists and structural value metadata, already listed above, since a value-bearing prop is only decidable against them. Extend **F1/F3** with those nested probes and **I3** with the whole-document refusal. **Extend the F family with the pack document's own value grammar** *(2026-09-12)*: an intentional `null` for `accent` or `accentDeep` is **accepted** and a **non-null** value outside `#rrggbb` on either member is **refused with the member named** (`SPEC.md §1.1`) — both arms through **the same shared pack schema the install door runs**, one schema serving the format and the door, never a pack-name exception and never a second handwritten validator. `../deployment/scripts/pack-shape.mjs` checks field presence on the shipped packs and is **not** evidence of value validity; the app-side door arm is `../app/SCENARIOS.md` S8 at `../app/BUILD.md` Step 1. **The manifest stamp's acceptance belongs to the same schema**: a missing, fractional, negative or non-numeric `manifest_version` is malformed here, and the newer-than-client comparison is I9's at Step 3 (`../app/SPEC.md §4.1`).

## Step 1 — The service mock
Canned catalog (the four shipped skins + one store-skin fixture + the two seed templates — the Step 0 **throwaway fixtures**, FR38, never shipped catalog defaults), the entitlement map **and the `entitlements.grant` / `catalog.publish` / `catalog.unpublish` verbs** (`INTERFACES.md §1`, 2026-08-21), test signer minting genuinely expiring URLs — the same mock CI uses forever. The transaction surface exists here, so the lane separation is proven here too: nothing the store does writes a board money record (E7) — and it is exercised against the **inert `transaction.begin` / `transaction.complete` stubs** `INTERFACES.md §1` now specifies, which is what makes E7 runnable at this step rather than post-alpha. Gate: **P2a, P5a, P8, E1, E4, E7**. **Build the mock's three-valued standing and the printed response variants here** *(2026-09-12)*: the entitlement map distinguishes never-granted from withdrawn (`INTERFACES.md §5`) and the canned catalog answers in `INTERFACES.md §1`'s good-and-standing variants, which is what lets E1 and E4 run over **whole response bodies** — `catalog.list`, `catalog.get` and the grant result, not the signing call alone — and what makes the cross-account signed-URL arm executable against the test signer. P2a's caller/credential arm runs here too: the adapter's call on an explicit owner act, refused unsigned, stale-credentialed and under another owner's credential (`INTERFACES.md §2`); the app-side surface is `../app/BUILD.md` Step 7. *(P1 moved to Step 3, 2026-08-21 — its Given opens "an installed copy" and installing does not exist until then; a Step-1 P1 was the dead-stub green `INTERFACES.md §1` legislates against. P2 was an absence test until FD-82; its verb half is now the presence test P2a, on the mock verb, and the umbrella P2 keeps its homes at Steps 3 and 4.)*

## Step 2 — Skins end-to-end
The store shelf in the gallery riser, the seam-granted entitlement + pack fetch into the app-managed device store, degradation path, and the two entitlement-withdrawal behaviors — deactivate on the next successful check, never on a failed one (`SPEC.md §4`, FR14). Gate: **I7 (the grant is real, the device store persists), E2, E5, E6, E8 (a stale pack degrades visibly), P5c (unpublish never reaches an entitled skin), D3 (skin half), I10 (owner-removal is a device event — 2026-08-31)**; re-run app **S5–S6** unchanged. *(P2 is a template-publish presence test — not re-gated on the skin shelf. Skins stay the admin pack pipeline, `SPEC.md` §0.)*

## Step 3 — Templates end-to-end
The browse riser, the install door wired to the real harness, the propose→confirm parameter walk **as a session (save/resume/abandon — `SPEC.md §3`)**, uninstall, and the installed copy's independence from the service (an install is a local fork, so an outage cannot reach it — `SPEC.md §3`). Gate: **P1 (fork isolation — moved from Step 1, now that installs exist), I1–I6, I8 (the dive-center-scale walk), I9 (a newer manifest version refuses whole), E3, E9 (a withdrawn template entitlement never reaches the fork), D3 (template half — including its named absences: no Shared row, no token digests), P2 (re-gated — the riser is the owner-publish surface), P2b (the install half), P5b (an unpublished template stops being installable), P6 (deleting a saved bundle), P7 (a re-publish lists the newest), P3 (save is not publish), P4 (no extract)**; re-run harness **B1/B2** unchanged — *zero new verbs is the exam*. Extend P3/P6/I1/I5 with the empty-account author/save/read/revise/use/publish walk, headless parity, wrong-account refusal, concurrent revision, lost-acknowledgement recovery, original/copy isolation and dependency-checked retirement (`SPEC.md §§2–3`). Reuse the app adapter established at app Steps 7/8; the service mock stays the catalog authority in this suite. Saved/installed private definitions and the published projection are checked independently; passing these future scenarios must not be inferred from the documentary source repair. **I9 runs the manifest comparator itself here** *(2026-09-12)*: the door reuses `../app/SPEC.md §4.1`'s numeric comparison over its nonnegative integer rather than implementing a second one, with the 10-versus-2 arm, the equal and older accepts, and the missing/fractional/negative/non-numeric malformed refusals. **E9 runs over the direct calls** at the same step: `catalog.get`, `entitlements.grant` and re-install each refused for withdrawn standing with no reason exported, the fork re-read unchanged, and a different eligible item and a second account still installing normally — driven from the mock's three-valued map built at Step 1. P3's and P4's zero-publish-call absences are asserted on the mock's recorded calls here, against the adapter established at `../app/BUILD.md` Step 7. **Wire the §6 lookup's own branches here** *(2026-09-12, FD-105)*: the install door and the propose→confirm walk are reached from a **real** `catalog.list`/`catalog.get` against the mock, and the mock drives a populated catalog, a **genuinely empty** one, a **failing** one and a **withdrawn/version-moved** candidate; assert an empty catalog and a failed call are distinguishable and that neither produces a fabricated listing or an install. The owner-declined and saved-resume arms run over the same fixtures against the account-document double. The result's delivery onto the owner turn is the app adapter's (`../app/BUILD.md` Step 7) and its consumption is `../harness/BUILD.md` Steps 4/5; this step owns the service-side branching only. **The door wired here enforces the transferable subset** (`SPEC.md §1.2`) on the real path: fixtures carrying a nested synthetic contact default, a prefilled-address prop, a resolved owner/board reference and a `file-slot` node each refuse the whole install with the failing entry named, while the neutral-structure and publisher-choice fixtures install and walk normally. Re-gate **I3** here against the real harness, and re-run **F1/F3** unchanged from Step 0 — one subset, two places it is read. The projection side of the same subset is proven at `../harness/BUILD.md` Step 4 (G8/G9) and `../engine/BUILD.md` Step 4 (S3), not here.

## Step 4 — Discovery (minimal)
Categories + tags at publish (through `catalog.publish` — the owner verb for templates, FD-82), featured shelves, the New shelf, the client-side text filter; `popularity` present in the format, unused by any surface. Gate: **D1–D7 (the agent-suggested entry and its four real-lookup outcomes plus the owner decline, FD-105 — D5; the viewing owner's catalog language, FR32 — D6; the text filter as the only finding aid, and no ranking consulted to reach a suggestion — D7), P2 (re-gated — listing copy and public catalog are the exact thing it requires)**.
The D5 service-mock walk begins with the existing owner utterance, asserts the typed `CatalogRequest` filters that the marketplace policy extractor derives, then asserts the returned suitable, unsuitable, empty and unavailable outcomes as the `normalize` context is assembled. A supplied result is a fixture input; production of the request and account-freshness report stays at the existing app/harness owners.

## Step 5 — Integration
The two seeds end-to-end on real layers. Gates: **Z1, Z2** *(one row until the 2026-08-22 release-tag split — same content, two seeds)*.

## Guardrails
- A new harness tool or seam verb has gone wrong — stop and flag (`INTERFACES.md` preamble).
- Any engine marketplace-awareness (field, flag, table) is a defect (`INTERFACES.md §4`).
- A CI call to the real service is a build error, not a flake (`INTERFACES.md §1`).
- Anything licensed in a public artifact fails E4 — treat as an incident, not a bug.
