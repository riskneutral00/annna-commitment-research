# annnä App — BUILD (ordered plan)

*Each step is gated by its scenarios (`SCENARIOS.md`) and, for visual steps, the design-law checklist (`DESIGN.md`). Substrates per `INTERFACES.md §2`: TanStack Start (TanStack Router on Vite) · Convex · Clerk · Astryx + glass theme · Resend · Cloudflare Workers. The final gate is the stub-swap — the app is done when the harness never noticed the change.*

## Design-law coverage — every section of `DESIGN.md`, accounted for

*Added 2026-08-09, and gated: `../deployment/scripts/design-law-coverage.mjs` refuses if a `DESIGN.md` section is named nowhere below. Three sections landed in one sitting and gated nothing before this existed, and the remaining UI/UX phases each add another — so the accounting is mechanical rather than remembered. **Being named here is not being reviewed well**; it only means no visual law is silently ungated. **And a step that builds or touches a surface a section governs re-runs that section's checklist rows at that step** *(generalized 2026-08-29 from the §Glass/§Islands row, which stated this discipline for itself while every other row read as though one step owned it for good)* — this table names where a section is **first** checked, not the only place it is.*

| section | where it is checked |
|---|---|
| §Canvas & photo · §Appearance · §Spacing · §Shapes · §Colour · §Board rendering | **Step 1** |
| §Typography | **Step 1** (ramp, tabular figures, self-hosted delivery) and **Step 5** (the 16px guest floor) |
| §Glass · §Islands & mobile | **Step 1**, then re-checked wherever a new surface appears |
| §Console · §Proposal cards | **Step 2** |
| §Risers and forms | **Step 3** |
| §Guest pages | **Step 5** |
| §Motion & iconography · §Accessibility baseline · §Standing rejections | **every visual step** — they bound all of them, so no single step owns them |
| §Design-system mechanics | **Step 0** (the pin, the layer order, the postinstall refusal) |
| §Supersessions | **no gate, and deliberately** — it records what this repo reversed in the prior build, not law a build satisfies |
| §Language | **no visual gate** — chrome posture is a scope line (English at v1); the guest-language law is gated at **G12** (Step 5) and the catalog half at `../marketplace/SCENARIOS.md` D6, not by looking at a surface. **The section's string-length MUST is gated too** *(named 2026-08-29 — the row named the language law and left this MUST silently un-gated)*: *"nothing in the token or catalog design may assume English string lengths"* is enforced by **G12's long-label fixture** — a long non-English label rendering un-clipped — at Step 5 |

## Step 0 — Scaffold
TanStack Start app + Convex + Clerk wired; Astryx installed **pinned, no postinstall approval** (`DESIGN.md` §Mechanics), CSS layer order and per-scope `color-scheme` set; glass materials file (the closed inventory) created; Cloudflare Workers deploy from day one. **Three substrate-spike carry-forwards land here** *(2026-08-22 — the spike verdict's two workaround findings and its path-hazard finding; the verdict file is working material, its content restated in full here)*: (1) dev-mode StyleX CSS on TanStack Start needs the plugin's documented dev links in the root route or `vite dev` renders unstyled while `vite build` is green; (2) the app must own a CSS asset or the extracted stylesheet is emitted and never linked — the gate is "the built HTML links a stylesheet containing a known StyleX rule", not "the build exited 0"; (3) the repo path contains `ä` — build tooling that round-trips `import.meta.url` through `.pathname` silently creates a percent-encoded sibling tree; use `fileURLToPath`. **Security arrival:** X2's clean-bundle check closes at this Step 0; the full X-family remains security Step 0's gate. Gate: a signed-in owner sees an empty canvas **on each platform `SPEC.md §9` declares — the web app and the native iPhone owner app**; a token route renders outside auth in an ordinary web browser, with no install and no account.

**The substrate check, printed here.** FD-11 ruled TanStack Start + Cloudflare; this step is where that ruling is *checked*, on the Convex precedent (`../engine/BUILD.md` Step 0). Print the table and the result, so a later reader re-derives the decision instead of inheriting it. The five criteria, fixed before either candidate is tried:

| # | Criterion | Passes when |
|---|---|---|
| 1 | **Convex live subscriptions reach the browser** — the board updates on commit without polling | a projection change repaints with no request from the app |
| 2 | **Clerk owner sessions, and guest token routes outside auth, in one codebase** | both render; the guest route never touches a session |
| 3 | **The Astryx pin installs and builds unmodified** — StyleX ≥ peer range, React ≥ 19 | the shipped four skins render; no fork, no patch, no version float |
| 4 | **No token route is served from a build-time snapshot** (`SPEC.md §5`) | the guard is asserted, not inherited from a default |
| 5 | **A per-change preview rung and a protected production rung exist** (`../deployment/SPEC.md §3`, R7, R9) | both stand up; production refuses anonymous access; lower rungs are unindexed |

**If any row fails when it is first built against, FD-11 reopens** — that is the whole point of printing it rather than asserting it. Until this table exists with a result in it, the ruling is a ruling and nothing more; no document may describe the substrate as ratified.

**The admission and code-challenge qualification, printed at this same step** *(2026-09-12 — criterion 2 stands up owner sessions and nothing said what admits a person to have one, while `../security/SPEC.md §2` now owns the invite-only door)*. Against **the pinned SDK**, demonstrate and print: that the restricted **invite-only** access mode accepts a valid invitation only with its matching verified email/account and a successful required code verification, producing exactly one principal, one finalized session and one binding outcome; that no invitation, an invalid, expired, revoked or already-redeemed ticket, a ticket used by another account, a wrong invited email, or a rejected wrong, expired or replayed code produces no new principal, session or binding and no draft disclosure; and that an interrupted acknowledgement or an already-completed signup, when recovered by the authorized **same principal**, returns its existing successful result with no duplicate account or second binding, without treating an invalid replayed code as new authentication. A **real additional email-code challenge can be required before the session is finalized** — the step `../user-stories/Situations/Situation-A/story-matt.md` S02 depicts, where the documented invitation path verifies the invited email automatically and **automatic verification alone does not satisfy a code step**. **If the pinned SDK cannot supply the separate code challenge, the outcome is a proposed amendment to S02's wording — from completing an email-code verification to completing verified-email account creation — put to the founder and the story's owners; it is not a silent rewrite and it is not a pass.** Until this runs, the provider evidence behind `../security/SPEC.md §2` is documentation and this result is owed. The invitation's lifetime and reuse policy are the operator's unresolved choice and are not a precondition of this step.

**The native shell's substrate is an open design task, decided and printed here** *(added 2026-09-12 with `SPEC.md §9`'s platform scope: §9 declares two platforms and names a substrate for neither the second nor its packaging, and `INTERFACES.md §2` admits no runtime dependency for it — so the choice is made where the first one is, not mid-build)*. Print the candidates considered, the choice, whether it reuses this codebase, and TanStack Start's own stability status as recorded above — the table's discipline, applied to the second platform, so a later reader re-derives the decision instead of inheriting it. The chosen substrate lands as its own row in `INTERFACES.md §2` in the same commit, and its egress and rung obligations are read from `../deployment/SPEC.md §3`, which names no provider. Until that print exists, no document may describe a native packaging or store route as ratified. **Nothing at this step approves push, offline operation or any new provider** (`SPEC.md §9`; §6 owns the push statement) — *the admission qualification printed above runs against the already-pinned auth substrate, which is not a new provider* — it chooses a substrate and proves no parity, which is Step 8's.

**What the substrate spike did and did not cover — the evidence, so nobody re-derives it from a gitignored `.tmp/` file** *(recorded 2026-08-22; spike of 2026-08-22 against `@stylexjs/stylex` 0.19.0, `@tanstack/react-start` 1.168.x, Chromium)*: it exercised **StyleX on TanStack Start** — client + SSR builds green, SSR handler 200, 24/24 behavioural assertions in-browser ×2 substrates, the twelve derived-contrast cells matching the ruled tables, and the load-bearing marketplace result: **installing a runtime pack added zero CSS rules and the production CSS asset did not change by one byte**, with the installed pack's custom-property set identical to a shipped skin's 14 names. It exercised **none of the five criteria above** (no Convex, no Clerk, no Astryx install, no token routes, no rungs) — "spike-verified" means the styling substrate, not this table. Astryx's evidence was narrower still until the install spike ran — **the 2026-08-22 rig result, printed and re-runnable** (`npm install @astryxdesign/core@<v> --ignore-scripts && ls node_modules/@astryxdesign/core/dist`, run 2026-08-22 against the live registry): **0.3.0 and 0.4.5 both install clean**; their peer ranges are **byte-identical** (React ≥19, `@stylexjs/stylex ^0.19.0`); the component census is 105 → 107 and the diff is **additive only** (`BottomSheet`, `Indicator` — 0.4.5 removes nothing), so the stranded-law risk the pin carried collapses: nothing the §4 catalog could bind at 0.3.0 is gone at 0.4.5. Direct components exist for the catalog's composition, action, and simple-field nodes (`Section`/`Stack`, `HStack`, `Text`, `Divider`, `TextInput`, `NumberInput`, `DateInput`, `TimeInput`, `DateTimeInput`, `Selector`, `MultiSelector`, `Switch`, `FileInput`, `Typeahead`, `Button`); the historical `FileInput` export is not an active v1 catalog binding (`SPEC.md §4.1`, security §4’s upload exclusion). **Seven node types have no single component — `duration`, `recurrence`, `buffer`, `threshold`, `ranked-list`, `place`, `signature`** — each is a local composite that itself becomes the manifest's one vetted component, a Step-4 finding recorded now so the manifest step arrives knowing it. **`scope-selector`'s disposition, recorded 2026-08-29 — the census's own reading, not a new rig result:** the printed census names a component for every other node and names none for it, and by the census's own criterion that makes it the **eighth composite** — a rule-target picker over board / commitment-kind / audience / party, composing from parts the census already named. It was neither bound nor listed, which left one node type with no stated home; it is listed here so the manifest step arrives knowing it, exactly as the seven do. **Pin verdict: stay at `0.3.0`** — the bump buys two components the catalog does not name. **Bound, stated:** this was an install-and-census rig, not a render — criterion 3's "the shipped four skins render" is still owed by this step. TanStack Start remains **Release Candidate by its own docs** (read 2026-08-22) — record its stability status, with date, in the table's result when this step runs. And two StyleX guards ride the skin steps *(the runtime var-handle contract is observed behaviour in a pre-1.0 package, not documented API)*: a build-time assertion that a skin token evaluates to a `var(--…)` string, and an install-door assertion that a candidate pack's derived custom-property name set equals the shipped set — both convert a silent marketplace-only break into a red build. Pin StyleX **exact, not caret**, until 1.0.

## Step 1 — Canvas + board
C13 uses correlated result/projection doubles from `../engine/INTERFACES.md §2a`: exercise unrelated pushes, older same-object acknowledgements, reversed arrival, coalesced reconnect, duplicate/refused/unknown results and display-only isolation under `SPEC.md §2`. Type/result propagation belongs to harness Step 2 and its loop composition to Step 5; real engine producer parity is owed at the existing commit/swap gates. Do not expand the Step-0 placeholder to claim this behavior.
The wallpaper canvas, the board on it, blocks from stored payloads (**events and tasks, one component**), date-row navigation, frontier-extension requests, the two-gravity split (75/25 default, owner-adjustable — the ratio is §7 display state) and its asymmetric scroll, the wake policy's four parts, mobile rack + bottom bar; **the console settings pane shell** *(re-cut 2026-08-22 — FD-39 relocated the island rows into the pane, and Step 1 gated C10 and both dials while the console was Step 2: the pane shell and its board-facing rows are built here, which is what C10's readback and the dials gate; the chat console the pane hangs off is Step 2)*; skin packs + palette tokens + **the derived veil** and **the per-skin urgency marks**, boring mode, **both opacity dials**, no-flash landing. **Check `corner-shape` support per engine here** and wire the circular fallback (`DESIGN.md` §Shapes) — it is a support question, not a design one, and it is cheapest to answer at the first surface that draws a corner. Gate: **C1, C6, C9, C10 (the wake policy rendered; the scope-menu readback in the settings pane — FD-70; its off-menu-utterance clause runs on a scripted refusal payload until Step 2 supplies the console — noted 2026-08-22), C11 (staleness shows itself — the expired attestation renders from the stored expiry alone), C12 (board mode packed-default; the hour-grid write is gated, its rendering fenced — FD-81; undated-on-clock residue unruled), C13 (the in-flight echo and its replacement), C14 (the frame is the skeleton), C15 (stale discarded; a quiet subscription shown), C16 (a conflicted bare draft renders marked), S1, S2, S4, S7 (guests unskinned — it runs on the **scaffold token route** until Step 5 supplies real guest pages, and Step 5 re-runs it; noted 2026-08-29 in C10's annotation style)** *(C13–C16 added 2026-08-29 with their rows)*, design-law checklist (**the appearance-and-tutorial rows added 2026-09-12 — the date row's visible Day/Week/Month grain and its selected occurrence, the active-filter indicator with its restore action, a packed block's own time mark at full strength, an unloaded span rendering unknown rather than bare photograph, and the role/category border and label measured beside the urgency marks at their own floor and ΔE separation (`DESIGN.md` §Board rendering, §Colour) — checked here with the sections they extend**, board laws, **§Islands & mobile's live content — the mobile bottom bar and rack, and the edge-empty law — not the retired island chrome** *(word re-cut 2026-08-29 alongside the §Spacing/§Shapes retirement annotations; FD-39 left nothing at the canvas edge to check)*, photo rules, breathing blocks, §Appearance), **the digit-width gate** *(2026-08-22, FD-44's fact-check landed early: Nunito Sans carries no `tnum` feature, but its digits measured uniform — so the check is a ~15-line `hmtx` parse of the shipped face asserting the ten digit advances are equal at each variable-axis extreme, failing the build; it survives a face change, a fallback change, and an upstream re-release, which a one-time eyeball would not — and the ä gate runs on the same binary at the same moment)*. *(The FD-42 precondition this step carried is discharged — FD-66, 2026-08-22, named the verb: `display_settings`, `SPEC.md §7`; the settings pane builds against it.)* *(2026-08-09: `+N` overflow and due chips are struck — internal scroll replaced the chip, and tasks are blocks. `DESIGN.md` §Board rendering.)*

## Step 2 — Console
C5’s Contacts controls use the same owner-input/render path for console and direct lookup (`../harness/INTERFACES.md §3.1`): selected filter/party/slot/revision, stale-selection discard and pending/saved/unknown/refused add results. Use scoped two-owner/guest response fixtures; Step 7 owns browse/readback and Step 8 real seam parity.
C3/C5/C17/C18 additionally gate `../harness/INTERFACES.md §3.1`'s submission controls, selected use/field/scope/ask stamping, stale proposal refusal, accepted-in-draft wording, stored-question/proposal reload and pending/unknown Save recovery. Use structured response fixtures; card readback is not a persistence or model proof.
The draggable card / pill, utterance in → seam → narrate out, proposal cards, surface stamping. Gate: **C3, C5, C7, C8** (the pending-decision card: engine-named choices as the action strip, nothing applied client-side, and nothing pre-selected), **C17** (the parked card, and re-send offered only where no commit point was reached), **C18** (cards re-derive after reload; chat history absent at v1, **and the blocked rule candidate rebuilds from its stored candidate with the verdict the seam returns now**) *(C17–C18 added 2026-08-29 with their rows)*, **C8's blocked-candidate arm** (stored and proposed rule side by side, the engine's blocking record, violated candidate constraint and named resolutions as the actions, a continue action reaching the same ref and re-emitting the retained candidate declaration whole, nothing app-authored and nothing applied client-side) **and C17's entry-point arm** (a discard offered over an unresolved outcome is refused and the actual result shown; `unavailable`/`timeout` renders as unresolved, never as an invitation to submit again), design-law checklist (**the appearance-and-tutorial rows added 2026-09-12 — the Conversation · Appearance two-tab swap control over the one settings pane and store, a proposal's OK / Edit rendered inside its own message beside Send at the input edge (`DESIGN.md` §Console), and the first-run send cue's flash threshold, reduced-motion static form and keyboard equivalent over an example that writes nothing (`DESIGN.md` §Accessibility baseline) — checked here with the sections they extend**, console laws, card anatomy — the base anatomy is settled law, while the five further card classes `DESIGN.md` §Proposal cards declares are *status: drafted at its home §Proposal cards*, built under review until struck, FD-91).

## Step 3 — Risers
C2/C4 re-run the shared form/console sequence in both directions, picker Save versus final Save, cross-selection delayed responses and abandonment disclosure for separately committed place/contact fixtures under `../harness/INTERFACES.md §3.1`. Assert unchanged saved domain state on unapplied discard; current revisions and stored outcomes come from the supplied seam, never app calculations.
Field/value propagation is gated by C2 under `SPEC.md §1` and `../harness/SPEC.md §3.3`: current row/revision targeting, explicit clear/restore and default scope, booking-only link changes, calculated read-only output and honest pending/refused dependency readback. Use supplied stored responses; full authoring, contact, pricing and compatibility behavior is not proved by these render controls.
The single overlay primitive with sink/rise motion; the commitment form riser first (creation form = detail form), then rules and settings *(the Plan riser is struck — 2026-08-09, tasks are board blocks)*. Gate: **C2, C4**, design-law checklist (overlay + whisper dim + motion restraint + `DESIGN.md` §Risers and forms). C4 also renders private-definition/use readback fixtures from `../marketplace/SPEC.md §2`: persisted revision versus unsaved/pending/unknown edit, immediate fresh use after Save, one definition-branch choice and a blocked candidate kept beside current settings. **The blocked candidate's riser readback is that pairing made explicit** *(2026-09-12; `SPEC.md §3`)*: render the stored rule and the proposed revision together with the supplied blocker members, the resumed candidate's retained values and already-answered definition branch, and a continue action reaching the same ref; the riser computes no blocker, no resolution and no clearance of its own, and the engine's compatibility gate and its apply-time recheck remain `../engine/BUILD.md` Steps 3/5. Adapter behavior is Step 7, not proven by these render fixtures.

## Step 4 — Catalog + `render_generative` *(split 2026-08-22 — "node→component mapping" scoped a mapping table, and the Step 0 census printed the truth: seven composite node types resolve to no single substrate component; a composite is a component to design and build, not a binding to declare)*
Re-run C3/C5's finite-answer and exact field/ask binding controls over existing catalog actions with U1/U5; keep Send outside proposal OK/Edit and preserve picker/final Save semantics (`../harness/INTERFACES.md §3.1`). No new node or prop is implied. Shared-draft interaction tests remain C2/C3/C4/C5/C17/C18, not an added rendering subsystem.
U1/U5 additionally gate `SPEC.md §4.1`’s stable id/bind validation against `../harness/SPEC.md §3.3`: valid current/historical bindings, required/default/type agreement, explicit removal effects and supplied dependency choices; reject unknown props and stale/retired or label/index targets without a write. Re-run over both bound and composite subsets below; no new catalog node is introduced.

**Step 4a — the meta-schema and the bindings (genuinely a mapping).** The manifest (vetted Astryx subset, named list, **and per-type closed prop lists** — `SPEC.md §4.1`, 2026-08-21), schema validation against the meta-schema, whole-schema rejected-render path, and the bindings for the node types the census found single-component homes for — restricted to the active list at `SPEC.md §4.1`. Its historical `file-slot`/`FileInput` binding and upload-dependent scenarios are deferred under `../security/SPEC.md §4`; reject that node whole, including a persisted or catalog-supplied schema. Gate: **U1–U5** over the bound subset.

**Step 4b — the composites, each its own build.** **The census's composite nodes, printed at Step 0** *(count-free since 2026-08-29 — `scope-selector`'s disposition moved the number, and a restated count here strands silently)*, each with: its own design-law review, its **typed-value round trip named** (the control's value must serialize into `../engine/SPEC.md §2`'s vocabulary and read back — `recurrence` into the closed pattern menu, `threshold` into comparator + typed operand, and so on), its validation, and — for `signature` (consent capture) — its security scenarios. Gate: **U1–U5 re-run over the full catalog**, plus the composites' named round trips.

**The closing gate for both halves — manifest ⇄ exports, both directions** *(owed here; recorded in `../deployment/SPEC.md §7a` until the manifest file exists)*: every catalog entry resolves to a real exported component carrying exactly its declared prop list, and no exported catalog component is absent from the manifest — which converts "unknown prop = rejected render" from an undecidable MUST into a build-time fact, and makes every future Astryx bump mechanically checkable (the marketplace's manifest-version compare rides this).

## Step 5 — Guest pages + tokens
G6 additionally exercises security §12's notices before guest collection/conversation/provider transfer, versioned required terms, independent purpose refusal/withdrawal and truthful request readback, paired with security S1–S4. No legal review or ops erasure is inferred from rendered controls.
G6/G7 additionally prove the successor input contract at `../harness/INTERFACES.md §§3.3/7.2`: structured fields and version-bound consent round-trip; excluded participant attestation/verification riders, upload refs and attachment bytes refuse without effects. Source-form approval cannot substitute for participant evidence or elevate provenance. G12 covers owner-approved ordinary translation drafts, retained approved formal language versions and zero-model stored rendering. Keep security §§4–6's consent, retention and quarantine requirements; BYO/import credential custody remains at security §3.1/T8 and the existing Step 6a connection gate.
Under SPEC §5, render the template-bound console beside the form/calendar through the existing harness carriers. G1/G3/G7/G9/G10/G12 additionally prove fenced model/wire data, no model on stored-page rendering, deliberate interactive submission versus no-normalize form triggers, retained fields, current terms/fresh confirmation, limits/parks and reader/coordinator refusal. Use scoped model/admission/projection doubles here; real qualified supply stays model BUILD Steps 1–4 and the existing swap. No new dependency order or provider is selected.
Token routes, month at-a-glance, day drill-in, published form rendering, submission → `on_form_return`, the bound token's **manage state (cancel/move, and the series' *end series* — `SPEC.md §5`, FD-47; where the page shows money or quota standing it renders the bearer's **own** — FD-54, G10's fence sentence)**, the guest-language rendering from the stored ranked set (FR15/FR32 — G12), dead-token page, consent capture; token law and admitted-data custody per `../security/BUILD.md` Steps 3–4, bounded by `../security/SPEC.md §4` (no owner/guest file intake or participant verification). Gate: **G1–G12**, plus the foreign gate **cited at its own home rather than restated as a list** *(re-cut 2026-08-29 — the list here had drifted from what security actually gates, and a copied list drifts again)*: `../security/BUILD.md` Step 4's printed gate — *"no public link goes live before T1–T7 are green"* — whose own text states which T-family rows it carries and which are re-homed elsewhere; **plus V1’s admitted-data custody and excluded-input controls** at `../security/SCENARIOS.md`, gated at `../security/BUILD.md` Step 3 with the rest of the vault suite; its participant-upload arm is deferred with the feature, not a guest-page deliverable. Read the carve-outs there, never from a copy here. G5/G8/G9 include the injected-clock **unused-pass** `issued_at + 24 hours` boundary, delayed/repeated cleanup, generic invalid responses and reusable published entry URL; first-use/hold/booking races and duplicate submits remain atomic controls at `../security/SPEC.md §3` and T7. G9 is the per-request rendering guard — the same row FD-11's substrate check owes a result (Step 0, criterion 4). These are documentary obligations here; application/provider execution remains owed until this gate runs. G4/G5/G10 additionally exercise security §3's request→booking binding, exact operation scopes, manual handoff/forwarding, independent new uses, contact edits versus revoke/reissue, failed/unknown mint recovery and retained cancelled-record reads. The real token door must refuse wrong-request and reader writes/replays while preserving authorized individual management and current arrangement actions under `../harness/SPEC.md §3.6`; the form and approved request-bound conversation share that admission. T2/T3/T5 retain server-side custody and race proof. No new step, swap order or live-provider waiver follows.

## Step 6 — Delivery
Owner-reminder extensions of D1–D3/D5–D6 build here under `SPEC.md §6` and `../harness/SPEC.md §3.3`: truthful draft/active/unsent readback, exact verified account recipient, actual provider success/failure/handoff/unknown-fate mapping, and account-authenticated booking navigation with wrong-owner/revoked controls. The D6 navigation Given uses existing owner auth, booking rendering and guest routes from Steps 0–5. Capture S14a/S14d's successful receipt and current lesson details only when those real integrations exist; an AppStub event is not received email. Exercise security R4's deferred stale-reminder and access controls at its existing delivery host. At Step 8 re-run harness D28 over the real app; full story participant/terms/guest-console composition requires its mapped contracts, not merely this provider seam.

Resend integration + token links, **the LINE Messaging API send and the WhatsApp utility-template send behind the identical seam shape (FD-61 + FD-74, `SPEC.md §6` — the first release's automated non-email channels: same delivery events, same stops; WeChat and Instagram sit out on §6's recorded hard points)**, hand-me-the-link composition, event recording, ask-once channel storage. Gate: **D1–D8** *(D7 — the LINE send — added 2026-08-22; D8 — the WhatsApp send — added 2026-08-23)*.

## Step 6a — Calendar import (inbound)
*Appended 2026-08-22 (FD-49): `SPEC.md §9`'s import was a v1-ruled capability (FR3/FR12/FR36, FD-37) with no build home in any layer — the `../harness/BUILD.md` Step-7 defect class verbatim, on the first thing both first-ring protagonists do. Steps keep their numbers; build after Step 6 (it composes the console (Step 2), the seam, and delivery's provider-hygiene patterns) and before Step 8's swap.*

The connect conversation surface; the `import_fetch` implementation (`../harness/INTERFACES.md §3.3`) with its three provider adapters — Google Calendar `calendar.readonly` (including the Google app-verification step the "sensitive" scope carries), Microsoft Graph `Calendars.Read`, ICS secret-feed; vault-resident credential custody (`../security/SPEC.md §3.1`); "Sync now"; the disconnected state. **The egress-allowlist rows for the three provider paths land with this step, each as its own floor act** (`../deployment/egress-allowlist.md`, the reserved note). Gate: **I1–I4**, security **T8** (held-credential custody, all three members), **Q3** (import quarantine).

## Step 7 — App-only views + starters
Run V2's privacy/settings companion for pre-account settings/Home/map notice timing, abandonment and verified choice association, version changes/withdrawal and account/guest request scope/results under SPEC §§7/8 and security §§4/12. O4 runs beside it on scripted admission fixtures: the five identical refusals, one draft per admitted invitation, exactly one principal and one binding under interruption and lost acknowledgement, and the empty board wearing the chosen skin. Settings readback covers each bound member's effective value; appearance edit/reset stays `SPEC.md §7`. Provider configuration, the pinned-SDK qualification (Step 0) and security M5's own enumeration are not proved by these fixtures. Account transaction implementation keeps its existing owner; actual erasure/provider/legal proof stays security Steps 3/7/8.
Implement V1’s Contacts/Students browse/readback through the harness alongside C5’s console selection, using `SPEC.md §7` and the scoped contact double in `INTERFACES.md §4`. Prove four identities, lost add acknowledgement, correction, distinct removal scopes, dependency explanations and independent contacts after parent discard. Contacts owns no directory store or direct subscription; engine Steps 1/4/5 retain storage/read/transaction proof and the existing swap retains integration. Audit/management stays read-only.
SOP library, audit read surface, the **effective-policy read view (V3 — FD-85's app half)**, default-availability UX, version-propagation UI; the **neutral authoring affordance** + saved locations *(the starter-template clause removed 2026-08-31 under FD-102, in the same commit as O2's re-cut)*; **the onboarding steps, their review summary and the verified-account binding** (`SPEC.md §8`; admission law `../security/SPEC.md §2`) build here beside them; the skins pop-out (fave four) + gallery riser + the install door, run against the marketplace CI mock and against its absence. Gate: **V1–V3, O2–O4, S3, S5, S6, S7, S8**, design-law checklist (§Appearance — this step builds its surfaces: the skins pop-out, the gallery riser and the store shelf, while the section's checklist slot is Step 1, so the preamble's re-run rule applies here) *(added 2026-08-29)*, **and the role/category presentation controls beside them under the same rule** *(added 2026-09-12)* — setting, changing and clearing a role's border and label, the resulting appearance shown before a consequential change is accepted, the border-collision choice put to the owner rather than defaulted, and the cleared style leaving its classification standing (`SPEC.md §7`, `DESIGN.md` §Colour) — and because `SPEC.md §9` declares two platforms, **the appearance surfaces and controls this pass adds at Steps 1, 2 and 7 are inside what Step 8's *appearance* parity item ranges over**, so neither declared platform passes on the other's evidence for them either. Implement the existing internal account-document adapter on the existing backend (`../marketplace/SPEC.md §3`), including authenticated headless owner-input handling and private definition/use persistence. V2 exercises save/read/revise/remove, other-device reload, wrong-account refusal, conditional revisions, lost-acknowledgement recovery and retirement/reference races with the stateful document double and the real internal store. Re-run C4 over real persistence results. **Build the marketplace-first lookup producer beside it** *(2026-09-12, FD-105; `SPEC.md §8`, `../marketplace/SPEC.md §§3/6`)*: the adapter performs the catalog call for an owner console turn and stamps the bounded `results`/`empty`/`unavailable` outcome on the existing `normalize` context (`INTERFACES.md §1`; `../harness/INTERFACES.md §2.1`) — no new seam verb, no filter or ranking authored here. O2's arm gates the rendering and recording: the actual returned candidate with its id and content version, a genuinely empty lookup, an unavailable one distinguished from it, the owner decline reaching the neutral affordance, and a saved selection resuming through the single document path without publish. Run it against the marketplace CI mock's suite-selectable populated/empty/unavailable catalog and against its absence; real service branching stays `../marketplace/BUILD.md` Steps 3/4 and the harness's consumption its own Steps 4/5. The catalog remains mocked/absent here; marketplace order is unchanged.

For the owner-turn lookup arm, Step 7 verifies the existing owner-input utterance produces the typed marketplace request before `normalize` context assembly, that the service mock outcome is stamped without app-authored filtering or ranking, and that the capability report's account snapshot is current, absent, stale or unavailable truthfully. O2 renders the actual choices and records choose/customize/decline; it never supplies a seed or invents a result. Retrieval branching remains marketplace Steps 3/4 and context consumption remains harness Steps 4/5.

## Step 8 — The stub-swap (final gate)
Re-run C13 with the real harness commit-result and engine projection producers, including lost result recovery and coalesced/reconnected associations; repeat C2/C3/C4/C5/C17/C18 with actual structured ask/document readback. Earlier scripted payload checks prove neither persistence nor full Matt story execution.
**Declared-platform parity, at the swap** *(added 2026-09-12, `SPEC.md §9`)*: run this exam on **each platform §9 declares — the web app and the native iPhone owner app** — and name what parity means, so neither passes on the other's evidence: **owner authentication**, **commitments**, **authoring, save and reload**, **appearance** (skins, boring mode, both dials) and **marketplace surfaces** behave the same on each. **Ordinary guest web access is proved outside all of it** — a token route rendering in an ordinary web browser with no install and no account, which is the guest contract (`SPEC.md §5`), not a platform feature. A native shell that merely renders the canvas is not parity. This step proves delivery no further than the roster already reaches: **no push channel, no offline operation and no new provider is proved or approved here** (`SPEC.md §6`, §9).
Point the harness at the real app. Run the **full harness suite including P1 and P2** *(corrected 2026-08-29 — the home names both and this line named one, which is exactly the tagged-subset reading FD-80 forbids; `../PRD.md §6.5` and Z2 both say "including P1 and P2")*. Green with `git diff harness/` empty, then run **Z1–Z3** (spy parity + the two story walkthroughs). The swap is the exam. The full run includes G6/G8’s private-definition/use controls over the real app adapter and C4/V2’s authenticated persistence/retry readbacks; no render-only spy establishes durable account storage. Provider calls remain mocked; this swap proves the app seam without constructing the closed marketplace service.

## Guardrails
- A step that needs a new seam verb has gone wrong — stop and flag (`INTERFACES.md §1`).
- A value computed in the app that someone could act on is a defect, not a shortcut (`SPEC.md §2/§10`).
- Any Astryx version bump re-runs the churn re-measure (`DESIGN.md` §Mechanics) before merging.
- Design-law checklist failures block the step like scenario failures — visual law is law.
- Appearance state on a seam payload or in an engine write is a defect (`SPEC.md §10`).
- No public link goes live before security BUILD Step 4 is green (`../security/SPEC.md §3`, the printed gate).
