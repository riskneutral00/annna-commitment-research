# annnä App — BUILD (ordered plan)

*Each step is gated by its scenarios (`SCENARIOS.md`) and, for visual steps, the design-law checklist (`DESIGN.md`). Substrates per `INTERFACES.md §2`: TanStack Start (TanStack Router on Vite) · Convex · Clerk · Astryx + glass theme · Resend · Cloudflare Workers. The final gate is the stub-swap — the app is done when the harness never noticed the change.*

## Design-law coverage — every section of `DESIGN.md`, accounted for

*Added 2026-08-09, and gated: `../deployment/scripts/design-law-coverage.mjs` refuses if a `DESIGN.md` section is named nowhere below. Three sections landed in one sitting and gated nothing before this existed, and the remaining UI/UX phases each add another — so the accounting is mechanical rather than remembered. **Being named here is not being reviewed well**; it only means no visual law is silently ungated. **And a step that builds or touches a surface a section governs re-runs that section's checklist rows at that step** *(generalized 2026-08-29 from the §Glass/§Islands row, which stated this discipline for itself while every other row read as though one step owned it for good)* — this table names where a section is **first** checked, not the only place it is.*

| section | where it is checked |
|---|---|
| §Canvas & photo · §Appearance · §Spacing · §Shapes · §Colour · §Board rendering | **Step 1** |
| §Typography | **Step 1** (ramp, tabular figures, self-hosted delivery) and **Step 5** (the 16px guest floor) |
| §Glass · §Islands & mobile | **Step 1**, then re-checked wherever a new surface appears |
| §Console · §Proposal cards | **Step 2** |
| §Risers and forms | **Step 3** |
| §Guest pages | **Step 5** |
| §Motion & iconography · §Accessibility baseline · §Standing rejections | **every visual step** — they bound all of them, so no single step owns them |
| §Design-system mechanics | **Step 0** (the pin, the layer order, the postinstall refusal) |
| §Supersessions | **no gate, and deliberately** — it records what this repo reversed in the prior build, not law a build satisfies |
| §Language | **no visual gate** — chrome posture is a scope line (English at v1); the guest-language law is gated at **G12** (Step 5) and the catalog half at `../marketplace/SCENARIOS.md` D6, not by looking at a surface. **The section's string-length MUST is gated too** *(named 2026-08-29 — the row named the language law and left this MUST silently un-gated)*: *"nothing in the token or catalog design may assume English string lengths"* is enforced by **G12's long-label fixture** — a long non-English label rendering un-clipped — at Step 5 |

## Step 0 — Scaffold
TanStack Start app + Convex + Clerk wired; Astryx installed **pinned, no postinstall approval** (`DESIGN.md` §Mechanics), CSS layer order and per-scope `color-scheme` set; glass materials file (the closed inventory) created; Cloudflare Workers deploy from day one. **Three substrate-spike carry-forwards land here** *(2026-08-22 — the spike verdict's two workaround findings and its path-hazard finding; the verdict file is working material, its content restated in full here)*: (1) dev-mode StyleX CSS on TanStack Start needs the plugin's documented dev links in the root route or `vite dev` renders unstyled while `vite build` is green; (2) the app must own a CSS asset or the extracted stylesheet is emitted and never linked — the gate is "the built HTML links a stylesheet containing a known StyleX rule", not "the build exited 0"; (3) the repo path contains `ä` — build tooling that round-trips `import.meta.url` through `.pathname` silently creates a percent-encoded sibling tree; use `fileURLToPath`. Gate: a signed-in owner sees an empty canvas; a token route renders outside auth.

**The substrate check, printed here.** FD-11 ruled TanStack Start + Cloudflare; this step is where that ruling is *checked*, on the Convex precedent (`../engine/BUILD.md` Step 0). Print the table and the result, so a later reader re-derives the decision instead of inheriting it. The five criteria, fixed before either candidate is tried:

| # | Criterion | Passes when |
|---|---|---|
| 1 | **Convex live subscriptions reach the browser** — the board updates on commit without polling | a projection change repaints with no request from the app |
| 2 | **Clerk owner sessions, and guest token routes outside auth, in one codebase** | both render; the guest route never touches a session |
| 3 | **The Astryx pin installs and builds unmodified** — StyleX ≥ peer range, React ≥ 19 | the shipped four skins render; no fork, no patch, no version float |
| 4 | **No token route is served from a build-time snapshot** (`SPEC.md §5`) | the guard is asserted, not inherited from a default |
| 5 | **A per-change preview rung and a protected production rung exist** (`../deployment/SPEC.md §3`, R7, R9) | both stand up; production refuses anonymous access; lower rungs are unindexed |

**If any row fails when it is first built against, FD-11 reopens** — that is the whole point of printing it rather than asserting it. Until this table exists with a result in it, the ruling is a ruling and nothing more; no document may describe the substrate as ratified.

**What the substrate spike did and did not cover — the evidence, so nobody re-derives it from a gitignored `.tmp/` file** *(recorded 2026-08-22; spike of 2026-08-22 against `@stylexjs/stylex` 0.19.0, `@tanstack/react-start` 1.168.x, Chromium)*: it exercised **StyleX on TanStack Start** — client + SSR builds green, SSR handler 200, 24/24 behavioural assertions in-browser ×2 substrates, the twelve derived-contrast cells matching the ruled tables, and the load-bearing marketplace result: **installing a runtime pack added zero CSS rules and the production CSS asset did not change by one byte**, with the installed pack's custom-property set identical to a shipped skin's 14 names. It exercised **none of the five criteria above** (no Convex, no Clerk, no Astryx install, no token routes, no rungs) — "spike-verified" means the styling substrate, not this table. Astryx's evidence was narrower still until the install spike ran — **the 2026-08-22 rig result, printed and re-runnable** (`npm install @astryxdesign/core@<v> --ignore-scripts && ls node_modules/@astryxdesign/core/dist`, run 2026-08-22 against the live registry): **0.3.0 and 0.4.5 both install clean**; their peer ranges are **byte-identical** (React ≥19, `@stylexjs/stylex ^0.19.0`); the component census is 105 → 107 and the diff is **additive only** (`BottomSheet`, `Indicator` — 0.4.5 removes nothing), so the stranded-law risk the pin carried collapses: nothing the §4 catalog could bind at 0.3.0 is gone at 0.4.5. Direct components exist for the catalog's composition, action, and simple-field nodes (`Section`/`Stack`, `HStack`, `Text`, `Divider`, `TextInput`, `NumberInput`, `DateInput`, `TimeInput`, `DateTimeInput`, `Selector`, `MultiSelector`, `Switch`, `FileInput`, `Typeahead`, `Button`); **seven node types have no single component — `duration`, `recurrence`, `buffer`, `threshold`, `ranked-list`, `place`, `signature`** — each is a local composite that itself becomes the manifest's one vetted component, a Step-4 finding recorded now so the manifest step arrives knowing it. **`scope-selector`'s disposition, recorded 2026-08-29 — the census's own reading, not a new rig result:** the printed census names a component for every other node and names none for it, and by the census's own criterion that makes it the **eighth composite** — a rule-target picker over board / commitment-kind / audience / party, composing from parts the census already named. It was neither bound nor listed, which left one node type with no stated home; it is listed here so the manifest step arrives knowing it, exactly as the seven do. **Pin verdict: stay at `0.3.0`** — the bump buys two components the catalog does not name. **Bound, stated:** this was an install-and-census rig, not a render — criterion 3's "the shipped four skins render" is still owed by this step. TanStack Start remains **Release Candidate by its own docs** (read 2026-08-22) — record its stability status, with date, in the table's result when this step runs. And two StyleX guards ride the skin steps *(the runtime var-handle contract is observed behaviour in a pre-1.0 package, not documented API)*: a build-time assertion that a skin token evaluates to a `var(--…)` string, and an install-door assertion that a candidate pack's derived custom-property name set equals the shipped set — both convert a silent marketplace-only break into a red build. Pin StyleX **exact, not caret**, until 1.0.

## Step 1 — Canvas + board
The wallpaper canvas, the board on it, blocks from stored payloads (**events and tasks, one component**), date-row navigation, frontier-extension requests, the two-gravity split (75/25 default, owner-adjustable — the ratio is §7 display state) and its asymmetric scroll, the wake policy's four parts, mobile rack + bottom bar; **the console settings pane shell** *(re-cut 2026-08-22 — FD-39 relocated the island rows into the pane, and Step 1 gated C10 and both dials while the console was Step 2: the pane shell and its board-facing rows are built here, which is what C10's readback and the dials gate; the chat console the pane hangs off is Step 2)*; skin packs + palette tokens + **the derived veil** and **the per-skin urgency marks**, boring mode, **both opacity dials**, no-flash landing. **Check `corner-shape` support per engine here** and wire the circular fallback (`DESIGN.md` §Shapes) — it is a support question, not a design one, and it is cheapest to answer at the first surface that draws a corner. Gate: **C1, C6, C9, C10 (the wake policy rendered; the scope-menu readback in the settings pane — FD-70; its off-menu-utterance clause runs on a scripted refusal payload until Step 2 supplies the console — noted 2026-08-22), C11 (staleness shows itself — the expired attestation renders from the stored expiry alone), C12 (board mode packed-default; the hour-grid write is gated, its rendering fenced — FD-81; undated-on-clock residue unruled), C13 (the in-flight echo and its replacement), C14 (the frame is the skeleton), C15 (stale discarded; a quiet subscription shown), C16 (a conflicted bare draft renders marked), S1, S2, S4, S7 (guests unskinned — it runs on the **scaffold token route** until Step 5 supplies real guest pages, and Step 5 re-runs it; noted 2026-08-29 in C10's annotation style)** *(C13–C16 added 2026-08-29 with their rows)*, design-law checklist (board laws, **§Islands & mobile's live content — the mobile bottom bar and rack, and the edge-empty law — not the retired island chrome** *(word re-cut 2026-08-29 alongside the §Spacing/§Shapes retirement annotations; FD-39 left nothing at the canvas edge to check)*, photo rules, breathing blocks, §Appearance), **the digit-width gate** *(2026-08-22, FD-44's fact-check landed early: Nunito Sans carries no `tnum` feature, but its digits measured uniform — so the check is a ~15-line `hmtx` parse of the shipped face asserting the ten digit advances are equal at each variable-axis extreme, failing the build; it survives a face change, a fallback change, and an upstream re-release, which a one-time eyeball would not — and the ä gate runs on the same binary at the same moment)*. *(The FD-42 precondition this step carried is discharged — FD-66, 2026-08-22, named the verb: `display_settings`, `SPEC.md §7`; the settings pane builds against it.)* *(2026-08-09: `+N` overflow and due chips are struck — internal scroll replaced the chip, and tasks are blocks. `DESIGN.md` §Board rendering.)*

## Step 2 — Console
The draggable card / pill, utterance in → seam → narrate out, proposal cards, surface stamping. Gate: **C3, C5, C7, C8** (the pending-decision card: engine-named choices as the action strip, nothing applied client-side, and nothing pre-selected), **C17** (the parked card, and re-send offered only where no commit point was reached), **C18** (cards re-derive after reload; chat history absent at v1) *(C17–C18 added 2026-08-29 with their rows)*, design-law checklist (console laws, card anatomy).

## Step 3 — Risers
The single overlay primitive with sink/rise motion; the commitment form riser first (creation form = detail form), then rules and settings *(the Plan riser is struck — 2026-08-09, tasks are board blocks)*. Gate: **C2, C4**, design-law checklist (overlay + whisper dim + motion restraint + `DESIGN.md` §Risers and forms).

## Step 4 — Catalog + `render_generative` *(split 2026-08-22 — "node→component mapping" scoped a mapping table, and the Step 0 census printed the truth: seven composite node types resolve to no single substrate component; a composite is a component to design and build, not a binding to declare)*

**Step 4a — the meta-schema and the bindings (genuinely a mapping).** The manifest (vetted Astryx subset, named list, **and per-type closed prop lists** — `SPEC.md §4.1`, 2026-08-21), schema validation against the meta-schema, whole-schema rejected-render path, and the bindings for the node types the census found single-component homes for — **including `file-slot`, whose security scenarios ride this half** *(re-homed here 2026-08-29: `file-slot` binds directly to `FileInput`, so the census excludes it from the composites and its guest-artifact → vault-stream scenarios were sitting on a step that does not build it)*. Gate: **U1–U5** over the bound subset.

**Step 4b — the composites, each its own build.** **The census's composite nodes, printed at Step 0** *(count-free since 2026-08-29 — `scope-selector`'s disposition moved the number, and a restated count here strands silently)*, each with: its own design-law review, its **typed-value round trip named** (the control's value must serialize into `../engine/SPEC.md §2`'s vocabulary and read back — `recurrence` into the closed pattern menu, `threshold` into comparator + typed operand, and so on), its validation, and — for `signature` (consent capture) — its security scenarios. Gate: **U1–U5 re-run over the full catalog**, plus the composites' named round trips.

**The closing gate for both halves — manifest ⇄ exports, both directions** *(owed here; recorded in `../deployment/SPEC.md §7a` until the manifest file exists)*: every catalog entry resolves to a real exported component carrying exactly its declared prop list, and no exported catalog component is absent from the manifest — which converts "unknown prop = rejected render" from an undecidable MUST into a build-time fact, and makes every future Astryx bump mechanically checkable (the marketplace's manifest-version compare rides this).

## Step 5 — Guest pages + tokens
Token routes, month at-a-glance, day drill-in, published form rendering, submission → `on_form_return`, the bound token's **manage state (cancel/move, and the series' *end series* — `SPEC.md §5`, FD-47; where the page shows money or quota standing it renders the bearer's **own** — FD-54, G10's fence sentence)**, the guest-language rendering from the stored ranked set (FR15/FR32 — G12), dead-token page, consent capture; token law + vault uploads per `../security/BUILD.md` Steps 3–4. Gate: **G1–G12**, plus the foreign gate **cited at its own home rather than restated as a list** *(re-cut 2026-08-29 — the list here had drifted from what security actually gates, and a copied list drifts again)*: `../security/BUILD.md` Step 4's printed gate — *"no public link goes live before T1–T6 are green"* — whose own text states which T-family rows it carries and which are re-homed elsewhere; **plus V1 for the vault half**, gated at `../security/BUILD.md` Step 3 with the rest of the vault suite. Read the carve-outs there, never from a copy here. G9 is the per-request rendering guard — the same row FD-11's substrate check owes a result (Step 0, criterion 4).

## Step 6 — Delivery
Resend integration + token links, **the LINE Messaging API send and the WhatsApp utility-template send behind the identical seam shape (FD-61 + FD-74, `SPEC.md §6` — the first release's automated non-email channels: same delivery events, same stops; WeChat and Instagram sit out on §6's recorded hard points)**, hand-me-the-link composition, event recording, ask-once channel storage. Gate: **D1–D8** *(D7 — the LINE send — added 2026-08-22; D8 — the WhatsApp send — added 2026-08-23)*.

## Step 6a — Calendar import (inbound)
*Appended 2026-08-22 (FD-49): `SPEC.md §9`'s import was a v1-ruled capability (FR3/FR12/FR36, FD-37) with no build home in any layer — the `../harness/BUILD.md` Step-7 defect class verbatim, on the first thing both first-ring protagonists do. Steps keep their numbers; build after Step 6 (it composes the console (Step 2), the seam, and delivery's provider-hygiene patterns) and before Step 8's swap.*

The connect conversation surface; the `import_fetch` implementation (`../harness/INTERFACES.md §3.3`) with its three provider adapters — Google Calendar `calendar.readonly` (including the Google app-verification step the "sensitive" scope carries), Microsoft Graph `Calendars.Read`, ICS secret-feed; vault-resident credential custody (`../security/SPEC.md §3.1`); "Sync now"; the disconnected state. **The egress-allowlist rows for the three provider paths land with this step, each as its own floor act** (`../deployment/egress-allowlist.md`, the reserved note). Gate: **I1–I4**, security **T8** (held-credential custody, all three members), **Q3** (import quarantine).

## Step 7 — App-only views + starters
SOP library, audit read surface, default-availability UX, version-propagation UI; starter templates + saved locations; the skins pop-out (fave four) + gallery riser, run against the marketplace CI mock and against its absence. Gate: **V1–V2, O2–O3, S3, S5, S6**, design-law checklist (§Appearance — this step builds its surfaces: the skins pop-out, the gallery riser and the store shelf, while the section's checklist slot is Step 1, so the preamble's re-run rule applies here) *(added 2026-08-29)*.

## Step 8 — The stub-swap (final gate)
Point the harness at the real app. Run the **full harness suite including P1 and P2** *(corrected 2026-08-29 — the home names both and this line named one, which is exactly the tagged-subset reading FD-80 forbids; `../PRD.md §6.5` and Z2 both say "including P1 and P2")*. Green with `git diff harness/` empty, then run **Z1–Z3** (spy parity + the two story walkthroughs). The swap is the exam.

## Guardrails
- A step that needs a new seam verb has gone wrong — stop and flag (`INTERFACES.md §1`).
- A value computed in the app that someone could act on is a defect, not a shortcut (`SPEC.md §2/§10`).
- Any Astryx version bump re-runs the churn re-measure (`DESIGN.md` §Mechanics) before merging.
- Design-law checklist failures block the step like scenario failures — visual law is law.
- Appearance state on a seam payload or in an engine write is a defect (`SPEC.md §10`).
- No public link goes live before security BUILD Step 4 is green (`../security/SPEC.md §3`, the printed gate).
