# annnä App — INTERFACES (the seams)

*Two seams: **upward** (satisfy the contract the harness already pinned — `../harness/INTERFACES.md §3`, verbatim) and **downward** (the substrates the app itself stands on). The upward seam is the layer's acceptance boundary: the stub-swap (`BUILD.md` final gate) proves the real app slots in with **zero harness changes**.*

---

## §1. Upward — the harness seam, satisfied

Every obligation of `../harness/INTERFACES.md §3`, mapped to where this package answers it:

| Harness obligation (§3.x) | App answer |
|---|---|
| §3.1 `render(surface: board \| commitment-page \| console, payload)` | `SPEC.md §1` (surface → canvas state, incl. the riser-IS-commitment-page mapping), `§2` (board), `§3` (console) |
| §3.2 `render_generative(schema)` — fixed catalog, LLM composes never invents | `SPEC.md §4` (catalog discipline, rejected-render poka-yoke, manifest) |
| §3.3 `publish(shared, recipients?)` — board-blind export, per-recipient tokens | `SPEC.md §5` (guest pages; blindness by construction) |
| §3.3 `send(form_payload, recipient)` — outward, crosses the floor | `SPEC.md §6` (email + hand-me-the-link; card-visible in §3) |
| §3.3 `on_form_return(reply) → Event` — trigger source, token attribution | `SPEC.md §5` (submission), `§6` (recording) |
| §3.3 `import_fetch(connection_ref)` — the owner-triggered inbound calendar read *(FD-49, 2026-08-22)* | `SPEC.md §9` (providers, console connect flow, never a background pull; custody `../security/SPEC.md §3.1`; gated at I1–I4) |
| §3.3 `display_settings(diff)` — the owner's display-only settings write *(FD-66, 2026-08-22 — FD-42's ratified mechanism, landed by the RQ-3 process exactly as `import_fetch` was)* | `SPEC.md §7` (the app-owned store and the closed member set; `internal` class, never engine truth, outside the parity enumeration both directions) |
| §3.4 App-only views, no harness logic | `SPEC.md §7` — **the four disowned views, read-only by construction**; appearance and the storefront surfaces join them there, and appearance is precisely the exception the phrase must not reach: it is the **one app-owned seam write**, `display_settings`; private documents use the internal account-document adapter at `../marketplace/SPEC.md §3` (FD-66) *(scoped 2026-08-29 — the row read as though the write path did not exist)* |

The shared Event union also carries engine-origin `offered` (`../harness/INTERFACES.md §3.3`, proposed amendment at §7.2); the app does not emit it. Its initial arrival belongs to the engine's creating commit and recipient inbox under `../harness/SPEC.md §4`, independently of the app's sale, decline and returned-form events.

**Events the app emits back** — all through existing seam shapes, zero new verbs: form returns (`on_form_return`), delivery results — the immediate outcomes returning on the `send` call itself, **and the out-of-band `complaint` / late `delivered-failed`, which the app emits as a fully correlated delivery-report event carrying `act_ref`, `party_ref`, `channel`, and `at` and fires the harness's sixth trigger source** (`../harness/INTERFACES.md §3.3`; row completed 2026-08-21 — the complaint previously had no inbound path in either direction of this seam) — owner confirmations/dismissals of surfaced cards (ordinary utterance/act inputs to the loop), the guest manage-state's cancel/move submissions (`SPEC.md §5`, riding `on_form_return`'s token-attributed shape), and `on_form_return`'s two other inbound riders — a vault attestation and a confirmation-time or open-time mint record (`../harness/INTERFACES.md §3.3` is the home; members added here 2026-08-22 so the two enumerations agree). Owner reminder `send` payloads carry the account-authenticated booking navigation from `SPEC.md §6`, with the owner recipient and destination already resolved under `../harness/SPEC.md §3.3`; no student token or contact lookup is substituted. Draft/active/unsent policy readbacks ride the existing render leg. The app also stamps each console utterance with the active canvas state — the **view-context** `{surface, visible_range?, selected_ref?, draft_ref?, draft_revision?, field_ref?, scope_ref?, ask_ref?}` — so the harness's standing frame carries the correct `surface` **and the deictic referent** ("push this back an hour" resolves against `selected_ref`) (`SPEC.md §1`; `../harness/INTERFACES.md §2.1`). This rides the existing `normalize` context contract, not a new call. For an unsaved edit, stamp the optional draft ref/revision and echo the field/picker change, particular proposal action or final Save through the same owner-input path (`../harness/INTERFACES.md §3.1`). Render that contract’s `draft` consistently in form and console, including a travel edit’s current request/source/freshness and accepted scope; discard a stale response for another revision. Saved-place reads arrive as authorized relevant-slice readback, and their accepted ordinary principal-attribute diffs remain separate from the occurrence Save (`SPEC.md §8`). No direct provider access, local fact store or new projection is added. The selected_ref may be the existing use ref, distinguished by document_context; field_ref/scope_ref/ask_ref retain the exact active row and proposal. Emit Send/finite-choice/OK/Edit/picker-Save/final-Save with §3.1's distinct semantics and existing submission identity. The existing owner-act result and its render readback retain write_id, submitted draft ref/revision and the engine's applied_ref/committed_revision unchanged; reconcile only with authorized projection applied_revisions under `SPEC.md §2`, including reversed arrival and coalesced recovery. Retain the immutable result independently when an accompanying stale render is discarded; never repaint from it. No UI success is inferred from an object match or store-version advance.

**Contacts inputs/results** follow `../harness/INTERFACES.md §3.1`: direct browse and console lookup carry the same owner-authorized PartyContact directory selection; render the supplied filter/page and party/slot/revision references, never fetch engine contacts directly. Echo those references with the current draft/ask correlation on edits; an old filter/selection response cannot overwrite the new selection. Render the add’s exact correlated applied result as saved only when acknowledged, separately from an unsaved parent. No board projection association is invented for private contact persistence. The ordinary send input still uses harness fire-time resolution; directory values are never converted into a send recipient by the app.

**Zero new seam verbs, zero harness changes — as ruled.** The contract above includes the two ruled amendments to the interview lock (`import_fetch`, FD-49, and `display_settings`, FD-66 — each landed by RQ-3's process, a spec change with its own ruling). If building ever demands a verb beyond the contract as ruled, stop and flag — that contradicts the lock in `../.specs/deep-interview-app.md`.

## §2. Downward — the substrates

**This section is the normative home of the app's non-harness runtime dependencies** *(2026-08-22 — the runtime graph's three ruled edges beyond `app → harness` lived in three files with no single home, and `README.md`'s build-wave pointer is a different fact)*: the engine display-projection subscription (read-only, `../engine/SPEC.md §0`'s carve + `../engine/INTERFACES.md §2a`), the direct guest-upload stream to the vault (`../security/INTERFACES.md §2`), the marketplace closed service (catalog, entitlements, signed URLs), and — since FD-49 (2026-08-22) — the **calendar-provider reads** (Google Calendar API `calendar.readonly`, Microsoft Graph `Calendars.Read`, owner-supplied ICS URLs), reached **only** inside an `import_fetch` call (§1): owner-triggered, read-only by scope or format, credentials vault-resident (`../security/SPEC.md §3.1`), with the egress-allowlist rows landing as their own floor acts when the importer module exists (`BUILD.md` Step 6a; `../deployment/egress-allowlist.md`) — plus the substrate bindings below. Nothing else outside the harness may be called at runtime. The account-document adapter (`../marketplace/SPEC.md §3`) is internal app persistence on the existing backend, not an additional external runtime dependency. It owns the authenticated save/read/revise/remove producer for both saved definitions and installed forks, including headless owner-input handling, revision comparison and lost-acknowledgement recovery. Its results join existing draft/readback and owner context carriers; it adds no `AppSeam` or engine verb.

Named here (and in `BUILD.md`/`DESIGN.md` mechanics) only — `SPEC.md` stays substrate-neutral. Each is a binding the spec constrains but doesn't depend on by name:

- **TanStack Start** (TanStack Router on Vite) — one codebase: the owner canvas (behind auth) + the guest token routes (outside auth). Mobile-first responsive; no native shells v1. **Ruled under FD-11**, replacing Next.js; **the printed five-criterion check it owes is at `BUILD.md` Step 0** — the home, on the Convex precedent (`../engine/BUILD.md` Step 0). *The ruling did not exempt the substrate from the check: if a criterion fails when it is first built against, the ruling reopens.* The requirement this row encodes — one codebase serving an authenticated canvas and unauthenticated token routes — is what `SPEC.md` constrains, and it is framework-independent.
- **Convex** — the reactive backend, **ratified under FR7 at `../engine/BUILD.md` Step 0** (which holds the printed five-criterion check — the home; a substrate change re-runs it there, not here): live subscriptions are what make the board update in real time when a booking lands. What the app subscribes to is **engine-published display projections only** (`../engine/SPEC.md` §0 sole-client carve) — never raw engine truth. The store may end up shared; the readable set never is.
- **Clerk** — owner sessions only. Guests never authenticate: the capability token in the URL is the whole credential (`SPEC.md §5`).
- **Astryx + glass theme** — the component substrate of the fixed catalog (`SPEC.md §4`) and the design-system mechanics in `DESIGN.md`. Strict adoption per the 2026-07-25 ruling; glass rides as a theme. **The pin** *(founder-approved; verified against the npm registry 2026-08-06)*: **`@astryxdesign/core@0.3.0`** with **`@astryxdesign/cli@0.3.0`** (the CLI tracks core exactly) and the theme package pinned to the same version — theme packages peer-pin core **exactly**, not as a range, so all three move together. `@stylexjs/stylex` is a **peer dependency** (`^0.19.0` at core 0.3.0 — re-check per version: earlier releases shipped it as a regular dependency at a different range); React ≥ 19. Cadence, honestly: ~weekly releases with a breaking change in most minors, mitigated by first-party `astryx upgrade` codemods — which is exactly why the churn re-measure law (`BUILD.md`) exists and why the pin never floats. **The escape hatch, written while it is cheap** *(2026-08-22 — the corpus's largest body of ratified visual law stood on this package with no stated exit; the Postgres-beside-Convex discipline, applied here)*: the catalog's meta-schema and manifest are substrate-neutral — **the manifest's node→component binding column is the only Astryx-aware artifact in the corpus**, and the design tokens are StyleX-owned, never Astryx-owned. If the package dies or a breaking release strands the pin, the named fallback is **Radix primitives under the same StyleX tokens**: re-bind the manifest column, keep every schema, scenario, token and design law. The 2026-08-22 install census (`BUILD.md` Step 0) bounds the blast radius — seven catalog nodes are local composites under either substrate.
- **The send substrates** *(re-cut 2026-08-31, F-30 — this list closed runtime deps at email while `SPEC.md §6`'s roster carries three channels)*: **Resend** (the v1 email channel), the **LINE** messaging API, and the **WhatsApp** business API — one row per `SPEC.md §6` roster channel, each swappable, each mocked in the suite; a roster change lands here in the same commit (`SPEC.md §6` is the roster's home). The seam shape (send-or-hand-over + recorded result) is one contract over all three — a WhatsApp or LINE send returns the same recorded outcome the email row doesn't change per provider.
- **Cloudflare** (Workers) — hosting. Pure BUILD concern; ruled under FD-11 with TanStack Start. The rung law it has to satisfy is `../deployment/SPEC.md §3`, which names no provider.
- **Marketplace service** (seam spec: `../marketplace/INTERFACES.md`) — the catalog / entitlement / signed-URL source for store skins and templates. The app consumes typed catalog documents and renders them; it never talks payment. **Absent or unreachable → the shipped four skins + Plain, and every installed template keeps working** (installed = local forked data). Mocked in CI, always.
- **The vault** (seam spec: `../security/INTERFACES.md §2`) — the encrypted object store for guest artifacts (passports, medical documents, waivers). Guest uploads stream to it directly; the app serves artifacts only via logged, basis-carrying reads. Mocked in CI (in-memory vault, virtual clock).

## §3. What the app OWNS (and what it must never absorb)

- **Owns:** rendering every surface; the canvas state machine (board/risers/console — islands retired as chrome, FD-39: their controls live on in the console's settings pane); the catalog manifest and the schema→component mapping; guest token routes; delivery transport + recording; **the app-only views `SPEC.md §7` enumerates** *(count-free since 2026-08-29 — the enumeration grew past the four the harness disowns, gaining appearance and the storefront surfaces, and a hardcoded count here goes stale silently)*; starter templates and saved-locations surfaces.
- **Never absorbs:** the floor and grants decisions (harness); elicitation, narration, judgment (harness/model); any deterministic math — availability, gaps, quota, placement (engine); scheduling truth (engine). Private account documents and appearance remain app-owned under `SPEC.md §7`; this does not let the app compute rule validity. If the app is computing a value someone could act on, that's a defect.

## §4. Stub strategy — from spies to the real thing

The harness suite runs against app stubs that are **record-and-return spies** (`../harness/INTERFACES.md §5`): they assert the payload and reversibility class, and simulate `on_form_return`. Swap-parity means the real app preserves exactly the **contract-visible** behavior the spies asserted:

- `render`/`render_generative`: the real app accepts every payload the spies accepted and rejects what they rejected (unknown node types) — the visual output is new, the contract behavior identical.
- `publish`: real token URLs replace canned artifacts; blindness and attribution properties hold on real responses.
- `send`/`on_form_return`: real email/hand-over replaces the spy's simulation; returns still fire the loop with token attribution.

The app's own `SCENARIOS.md` runs headless where possible (payload/wire assertions) and drives the rendered canvas only where the scenario is about the canvas itself. The internal account-document adapter uses an account/revision-aware document-store double with controllable concurrent writes, dependency insertion and dropped acknowledgements. V2 checks the real app controller against that double without canvas rendering; the same authenticated path and store contract are exercised on the existing backend at BUILD Steps 7/8. A render spy is not proof of persistence. C5/V1 also use a scoped PartyContact read double with two owners, a guest credential, existing/new identities, same-name strangers and controllable lost/stale responses under `../harness/SPEC.md §3.12`; real engine keyed read/write and reference/replay behavior remain engine BUILD Steps 1/4/5, with app parity at its existing swap.

The **marketplace service stub** is a canned catalog + entitlement map (`../marketplace/INTERFACES.md §5`); appearance scenarios S6/S7 run against it both present and absent. **The send-substrate stubs cover the whole `SPEC.md §6` roster** *(2026-08-31, F-30)*: the email, LINE and WhatsApp substrates are each a scripted send-or-hand-over spy returning the recorded outcome — three mocks, one seam shape, so D8's template-honest send and the hand-over path run without any live provider.
